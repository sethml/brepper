# Refactoring Fitting Methods into Separate Files

- **Date:** 2026-03-09 18:27 – 18:51
- **Session ID:** 98370245-caf8-4eda-a268-ada7a1e19316
- **Model:** Claude Opus 4.6
- **Turns:** 1
- **Tool calls:** 74
- **Thinking blocks:** 5
- **Input words:** 36,820
- **Output words:** 16,175
- **Prompt tokens (last round):** 74,951
- **API rounds:** 67
- **Total elapsed:** 23m31s

## Table of Contents

1. [iterate by Refactor cylindrical fitting and spherical fitting and planar fitting into their own s...](#user-1) (Claude Opus 4.6)

---

<a id="user-1"></a>

## User (1)

> iterate by Refactor cylindrical fitting and spherical fitting and planar fitting into their own source files.

*2026-03-09 18:28*

### Assistant — Claude Opus 4.6

> 💭 The user wants me to iterate, which means I need to follow the instructions in AGENTS.md. Let me start by reading the required files: LEARNINGS.md, README.md, and DEVELOPMENT_PLAN.md. The specific task is to refactor cylindrical fitting, spherical fitting, and planar fitting into their own source files.
> 
> Let me first load the hashline tools since AGENTS.md says to use them.

<details>
<summary>Reading all lines of <a href="../LEARNINGS.md">LEARNINGS.md</a></summary>

````
## opencascade-sys API Patterns

The Rust bindings mirror OCCT C++ closely but with automatic name transformations:

### Module and Type Naming
- `BRepBuilderAPI_MakeEdge` → `b_rep_builder_api::MakeEdge`
- `BRep_Tool` → `b_rep::Tool`
- `gp_Pnt` → `gp::Pnt`
- `TopExp_Explorer` → `top_exp::Explorer`
- `GeomAPI_ProjectPointOnSurf` → `geom_api::ProjectPointOnSurf`
- `STEPControl_Reader` → `step_control::Reader`
- `RWStl` → `rw_stl` (free functions)

### Constructors
Named `::new_<compressed_param_types>()`. For example:
```rust
gp::Pnt::new_real3(x, y, z)           // gp_Pnt(double, double, double)
gp::Dir::new_real3(dx, dy, dz)
top_exp::Explorer::new_shape_shapeenum2(shape, from_type, avoid_type)
geom_api::ProjectPointOnSurf::new_pnt_handlegeomsurface_extalgo(pt, surf, algo)
```

### Ownership
- All objects are returned as `OwnedPtr<T>` which auto-derefs via `Deref`/`DerefMut`
- `OwnedPtr<T>` is essentially `Box<T>` for OCCT heap objects

### Handles (OCCT `Handle<T>`)
- `Type::to_handle(owned_ptr)` consumes `OwnedPtr<T>` → `OwnedPtr<HandleT>`
- `.get()` dereferences a handle to access the underlying object
- Upcasting: `.to_handle_surface()`, `.to_handle_curve()`, etc.
- Downcasting: `.downcast_to_plane()` returns `Option<OwnedPtr<HandleDerived>>`

### Enums
Typed Rust enums, not raw integers:
```rust
top_abs::ShapeEnum::Face
extrema::ExtAlgo::Grad
```

### Shape Downcasting
`topo_ds::face_shape` is a safe function, but it panics if the shape is not a Face. Always guard with `shape_type()`:
```rust
if shape_ref.shape_type() == top_abs::ShapeEnum::Face {
    let face = topo_ds::face_shape(shape_ref);  // &topo_ds::Shape → &topo_ds::Face
    ...
}
```

### Static Methods → Free Functions
OCCT utility classes (e.g., `BRep_Tool`) become module-level free functions or associated functions on the type.

## Common API Recipes

### Read an STL file → vertex positions
```rust
use opencascade_sys::{rw_stl, message};
let progress = message::ProgressRange::new();
let tri_handle = rw_stl::read_file_charptr_progressrange_2(path, &progress);
let tri = tri_handle.get();  // &Poly_Triangulation
for i in 1..=tri.nb_nodes() {
    let pt = tri.node(i);  // OwnedPtr<gp::Pnt>
    let (x, y, z) = (pt.x(), pt.y(), pt.z());
}
```

### Read a STEP file → TopoDS_Shape
```rust
use opencascade_sys::{step_control, message};
let mut reader = step_control::Reader::new();
reader.read_file_charptr(path);
reader.transfer_roots(&message::ProgressRange::new());
let shape = reader.one_shape();  // OwnedPtr<topo_ds::Shape>
```

### Iterate faces of a shape
```rust
use opencascade_sys::{top_exp, top_abs, topo_ds};
let mut explorer = top_exp::Explorer::new_shape_shapeenum2(
    &shape, top_abs::ShapeEnum::Face, top_abs::ShapeEnum::Shape,
);
while explorer.more() {
    let face = topo_ds::face_shape(explorer.value());
    // ... use face
    explorer.next();
}
```

### Project a point onto a surface (compute distance)
```rust
use opencascade_sys::{b_rep, geom_api, extrema};
let surface = b_rep::Tool::surface_face(face);  // OwnedPtr<HandleGeomSurface>
let projector = geom_api::ProjectPointOnSurf::new_pnt_handlegeomsurface_extalgo(
    &point, &surface, extrema::ExtAlgo::Grad,
);
if projector.is_done() && projector.nb_points() > 0 {
    let dist = projector.lower_distance();
}
```

### Poly_Triangulation index convention
All node/triangle indices are **1-based** (OCCT convention).


### Bounded vs unbounded distance: BRepExtrema vs GeomAPI_ProjectPointOnSurf
`GeomAPI_ProjectPointOnSurf` projects onto the *infinite* underlying `Geom_Surface`, not the bounded face. Use `BRepExtrema_DistShapeShape` (via `b_rep_extrema::DistShapeShape`) instead to compute distance to bounded `TopoDS_Face` shapes. Create a `BRepBuilderAPI_MakeVertex` from the point, then compute `DistShapeShape` between the vertex shape and the target shape. This correctly reports nonzero distance for points that lie on an infinite surface extension but outside the bounded face.

### Stage 2.1 planar hypothes
... (truncated, 33967 chars)
````
</details>

<details>
<summary>Reading all lines of <a href="../README.md">README.md</a></summary>

````
# Brepper - STL to STEP Converter

Convert triangulated STL meshes from CAD exports into parametric STEP files with fitted analytic and freeform surfaces.

## Usage

```
brepper - Convert STL mesh to STEP with fitted surfaces

USAGE:
    brepper [OPTIONS] <input.stl> [-o <output.step>]

REQUIRED:
    <input.stl>              Input STL file (binary or ASCII)

GENERAL OPTIONS:
    -o <step>, --output=<step>  Output STEP file
    --stl-units=<units>      Units used by the STL file (mm, cm, m, in, ft, um; default: mm)
    --step-units=<units>     Units to use in exported STEP file (default: mm)
    --compare=<step>         STEP file to compare to at each step
    --vertex-tolerance=<value>  Fitting tolerance in STL units (default: 1e-5)
    --surface-tolerance=<value> Surface-to-face offset tolerance in STL units (default: 0.4)
    --angular-tolerance=<deg>   Max dihedral angle between adjacent triangles on the same surface (default: 17.5)
    -v, --verbose[=LEVEL]    Verbosity level: 1 (or bare -v) = summaries, -vv or --verbose=2 = per-face details, -vvv or --verbose=3 = full BFS trace
    -q, --quiet              Suppress non-error output
    --debug                  Enable debug output and intermediate files
    --stage=<stage>          Stop after stage, e.g. 2.2 (default: 4.1). --stage=2 stops after all of stage2
    --viz-stages=<stages>    Open interactive 3D visualization at specified stages (e.g. 2.1,2.2,2.3)
```

### Examples

```bash
# Full conversion
./target/debug/brepper input.stl -o output.step

# Just load and validate mesh (stage 1.2)
./target/debug/brepper input.stl --stage 1.2 -v

# Compare mesh against reference STEP at each stage
./target/debug/brepper input.stl --compare reference.step --stage 1 -v
```

See [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) for full documentation.

### Tolerance Settings

#### Surface Tolerance

This is how far the surface of a mesh triangle is allowed to deviate from the surface it is approximating.

Onshape calls this "Chordal tolerance (mm)", and the preset values are:
- Coarse: 0.24 mm
- Medium: 0.12 mm
- Fine: 0.06 mm

Fusion 360 calls this "Surface Deviation", and the preset values seem to vary based on model size:
- Low: 1x
- Medium: 2.5x
- High: 8x

#### Angular Tolerance

This is the maximum angle between adjacent triangle faces that approximate a smooth surface.

Onshape calls this "Angular deviation (deg)", and the present values are:
- Coarse: 12.5 deg
- Medium: 6.25 deg
- Fine: 2.5 deg

Fusion 360 calls this "Normal Deviation", and the preset values are:
- Low: 30.0
- Medium: 15.0
- High: 10.0
In theory "Normal Deviation" should be the maximum angular error of the face relative to the surface normal, but in practice it seems to result in the given maximum angular deviation.

### Generating Test Models

Some tests use models generated via [CodeCAD](https://codecad.xyz/). The generated STL/STEP files are committed to the repo, so CodeCAD is not required to run tests.

To add or modify these models:
1. Install CodeCAD
2. Edit files in `tests/ccad/parts/` — see `tests/ccad/README.md` for primitive conventions and placement rules
3. Run `./tests/ccad/generate_models.sh`

**Important:** `box(w,d,h)` places a corner at the origin, not centered. Use `center_xyz()` to center. Each part file includes analytical surface area and volume in its header comments.


## Viewer

`viewer` is an interactive 3D viewer for STL and STEP files.

```
USAGE:
    viewer [OPTIONS] <file>...

ARGUMENTS:
    <file>...   One or more STL or STEP files to view (detected by extension)

OPTIONS:
    --deflection <value>          Linear deflection for STEP tessellation (default: 0.1)
    --angular-deflection <value>  Angular deflection for STEP tessellation in radians (default: 0.5)
```

Multiple files can be loaded simultaneously; each is rendered in a distinct color.

### Examples

```bash
# View a single STEP file
./target/release/viewer part.step

# View a STEP and an STL side by side
./target/release/v
... (truncated, 9724 chars)
````
</details>

<details>
<summary>Reading all lines of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
# STL to STEP Conversion Utility - Development Plan

## Project Overview

**brepper** (B-Rep from Mesh) - A utility to convert triangulated STL meshes from CAD exports into parametric STEP files with fitted analytic and freeform surfaces.

## Assumptions

- Assumes the vertices of the mesh lie precisely on the CAD surfaces, modulo numerical error/stability.
- Assumes transitions between shapes always have a seam.
- Assumes manifold mesh (each edge shared by exactly 2 faces).
- Assumes consistent face orientation (all normals point outward).

Fails noisily if any of the assumptions are violated. Possible future work: offer a repair mode.

## Dependencies

| Library | Purpose | Version |
|---------|---------|---------|
| OpenCASCADE (OCCT) | STL import, B-Rep modeling, surface fitting, STEP export | latest |

Use the opencascade binding in ../opencascade-rs/crates/opencascade-sys, including porting instructions in PORTING.md and details on the FFI mapping in ../opencascade-rs/crates/opencascade-binding-generator/README.md. It's a reasonably complete binding of the C++ API of the opencascade library which resides in ../opencascade-rs/crates/occt-sys/OCCT, with source code in src/ and documentation in dox/.

### Required OCCT Functionality

This section enumerates the OCCT classes and functions needed, for evaluating Rust binding coverage.

**Stage 1: Mesh Input**
- `RWStl` / `RWStl_Reader` — Read binary/ASCII STL files
- `gp_Pnt` — 3D point representation
- `gp_Dir` / `gp_Vec` — Direction and vector types

**Stage 2: Surface Fitting**
- `gp_Pln` — Plane geometry (normal + distance representation)
- `gp_Cylinder` / `gp_Ax3` — Cylindrical surface geometry (axis + radius)
- `gp_Sphere` — Spherical surface geometry (center + radius)
- `gp_Cone` — Conical surface geometry (axis + half-angle)
- `gp_Torus` — Toroidal surface geometry (for fillets)

**Stage 3: Surface Reconstruction**

*Geometry (infinite surfaces and curves):*
- `Geom_Plane`, `Geom_CylindricalSurface`, `Geom_SphericalSurface`, `Geom_ConicalSurface`, `Geom_ToroidalSurface` — Analytic surface objects
- `Geom_BSplineSurface` — NURBS/B-spline surfaces
- `Geom_Line`, `Geom_Circle`, `Geom_Ellipse` — Analytic curve types
- `Geom_BSplineCurve` — Freeform curves
- `Geom2d_Curve`, `Geom2d_Line`, `Geom2d_Circle`, `Geom2d_BSplineCurve` — 2D curves for pcurves

*Surface fitting:*
- `GeomAPI_PointsToBSplineSurface` — Fit B-spline surface to points

*Surface-surface intersection:*
- `GeomAPI_IntSS` — Compute intersection curves between two surfaces

*Point-curve projection:*
- `GeomAPI_ProjectPointOnCurve` — Project point onto curve (for trimming)
- `ShapeAnalysis_Curve` — Curve analysis utilities

*Topology (B-Rep construction):*
- `TopoDS_Vertex`, `TopoDS_Edge`, `TopoDS_Wire`, `TopoDS_Face`, `TopoDS_Shell`, `TopoDS_Solid`, `TopoDS_Compound` — Topological entities
- `BRep_Builder` — Low-level topology construction
- `BRepBuilderAPI_MakeVertex` — Create vertices
- `BRepBuilderAPI_MakeEdge` — Create edges from curves (with optional pcurves)
- `BRepBuilderAPI_MakeWire` — Assemble edges into wires
- `BRepBuilderAPI_MakeFace` — Create faces from surface + bounding wires
- `BRepBuilderAPI_Sewing` — Stitch faces into shells
- `BRepBuilderAPI_MakeSolid` — Create solids from shells

*Topology exploration:*
- `TopExp_Explorer` — Iterate over sub-shapes
- `TopExp::MapShapes` — Build maps of topology
- `BRep_Tool` — Extract geometry from topology (IsSeam, SameParameter, etc.)

*Shape healing and fixing:*
- `ShapeFix_Shape` — General shape repair
- `ShapeFix_Face` — Face-specific repairs
- `ShapeFix_Wire` — Wire repairs, including `FixEdgeCurves` for pcurve computation
- `ShapeFix_Shell` — Shell orientation fixing
- `ShapeFix_Solid` — Solid creation/repair, `SolidFromShell`

*Shape analysis and validation:*
- `BRepCheck_Analyzer` — Validate B-Rep topology/geometry
- `ShapeAnalysis_Shell` — Shell analysis (e.g., `CheckOrientedShells`)
- `GProp_GProps` / `BRepGProp` — Compute volume, area, center o
... (truncated, 87984 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../LEARNINGS.md">LEARNINGS.md</a></summary>

````
1:uf|## opencascade-sys API Patterns
2:dd|
3:lz|The Rust bindings mirror OCCT C++ closely but with automatic name transformations:
4:dd|
5:hx|### Module and Type Naming
6:fs|- `BRepBuilderAPI_MakeEdge` → `b_rep_builder_api::MakeEdge`
7:pg|- `BRep_Tool` → `b_rep::Tool`
8:un|- `gp_Pnt` → `gp::Pnt`
9:dq|- `TopExp_Explorer` → `top_exp::Explorer`
10:tl|- `GeomAPI_ProjectPointOnSurf` → `geom_api::ProjectPointOnSurf`
11:fj|- `STEPControl_Reader` → `step_control::Reader`
12:qf|- `RWStl` → `rw_stl` (free functions)
13:dd|
14:jh|### Constructors
15:me|Named `::new_<compressed_param_types>()`. For example:
16:fj|```rust
17:gp|gp::Pnt::new_real3(x, y, z)           // gp_Pnt(double, double, double)
18:vv|gp::Dir::new_real3(dx, dy, dz)
19:nc|top_exp::Explorer::new_shape_shapeenum2(shape, from_type, avoid_type)
20:wj|geom_api::ProjectPointOnSurf::new_pnt_handlegeomsurface_extalgo(pt, surf, algo)
21:pi|```
22:dd|
23:ds|### Ownership
24:cw|- All objects are returned as `OwnedPtr<T>` which auto-derefs via `Deref`/`DerefMut`
25:dk|- `OwnedPtr<T>` is essentially `Box<T>` for OCCT heap objects
26:dd|
27:bo|### Handles (OCCT `Handle<T>`)
28:xj|- `Type::to_handle(owned_ptr)` consumes `OwnedPtr<T>` → `OwnedPtr<HandleT>`
29:rh|- `.get()` dereferences a handle to access the underlying object
30:sv|- Upcasting: `.to_handle_surface()`, `.to_handle_curve()`, etc.
31:mo|- Downcasting: `.downcast_to_plane()` returns `Option<OwnedPtr<HandleDerived>>`
32:dd|
33:uk|### Enums
34:bv|Typed Rust enums, not raw integers:
35:fj|```rust
36:wl|top_abs::ShapeEnum::Face
37:xr|extrema::ExtAlgo::Grad
38:pi|```
39:dd|
40:oc|### Shape Downcasting
41:pu|`topo_ds::face_shape` is a safe function, but it panics if the shape is not a Face. Always guard with `shape_type()`:
42:fj|```rust
43:qu|if shape_ref.shape_type() == top_abs::ShapeEnum::Face {
44:nm|    let face = topo_ds::face_shape(shape_ref);  // &topo_ds::Shape → &topo_ds::Face
45:bf|    ...
46:io|}
47:pi|```
48:dd|
49:ej|### Static Methods → Free Functions
50:qz|OCCT utility classes (e.g., `BRep_Tool`) become module-level free functions or associated functions on the type.
51:dd|
52:va|## Common API Recipes
53:dd|
54:tt|### Read an STL file → vertex positions
55:fj|```rust
56:xj|use opencascade_sys::{rw_stl, message};
57:nr|let progress = message::ProgressRange::new();
58:ci|let tri_handle = rw_stl::read_file_charptr_progressrange_2(path, &progress);
59:kc|let tri = tri_handle.get();  // &Poly_Triangulation
60:xd|for i in 1..=tri.nb_nodes() {
61:rn|    let pt = tri.node(i);  // OwnedPtr<gp::Pnt>
62:xp|    let (x, y, z) = (pt.x(), pt.y(), pt.z());
63:io|}
64:pi|```
65:dd|
66:zd|### Read a STEP file → TopoDS_Shape
67:fj|```rust
68:sg|use opencascade_sys::{step_control, message};
69:dl|let mut reader = step_control::Reader::new();
70:bz|reader.read_file_charptr(path);
71:zy|reader.transfer_roots(&message::ProgressRange::new());
72:fa|let shape = reader.one_shape();  // OwnedPtr<topo_ds::Shape>
73:pi|```
74:dd|
75:br|### Iterate faces of a shape
76:fj|```rust
77:ic|use opencascade_sys::{top_exp, top_abs, topo_ds};
78:zu|let mut explorer = top_exp::Explorer::new_shape_shapeenum2(
79:cu|    &shape, top_abs::ShapeEnum::Face, top_abs::ShapeEnum::Shape,
80:dk|);
81:qh|while explorer.more() {
82:mj|    let face = topo_ds::face_shape(explorer.value());
83:rn|    // ... use face
84:zk|    explorer.next();
85:io|}
86:pi|```
87:dd|
88:zd|### Project a point onto a surface (compute distance)
89:fj|```rust
90:ci|use opencascade_sys::{b_rep, geom_api, extrema};
91:iw|let surface = b_rep::Tool::surface_face(face);  // OwnedPtr<HandleGeomSurface>
92:qv|let projector = geom_api::ProjectPointOnSurf::new_pnt_handlegeomsurface_extalgo(
93:gt|    &point, &surface, extrema::ExtAlgo::Grad,
94:dk|);
95:wl|if projector.is_done() && projector.nb_points() > 0 {
96:wb|    let dist = projector.lower_distance();
97:io|}
98:pi|```
99:dd|
100:vv|### Poly_Triangulation index convention
101:ij|All node/triangle indices are **1-based** (OCCT convention).
10
... (truncated, 35861 chars)
````
</details>

<details>
<summary>Reading all lines of <a href="../README.md">README.md</a></summary>

````
1:di|# Brepper - STL to STEP Converter
2:dd|
3:mu|Convert triangulated STL meshes from CAD exports into parametric STEP files with fitted analytic and freeform surfaces.
4:dd|
5:sb|## Usage
6:dd|
7:pi|```
8:ku|brepper - Convert STL mesh to STEP with fitted surfaces
9:dd|
10:yo|USAGE:
11:jx|    brepper [OPTIONS] <input.stl> [-o <output.step>]
12:dd|
13:wp|REQUIRED:
14:pj|    <input.stl>              Input STL file (binary or ASCII)
15:dd|
16:zh|GENERAL OPTIONS:
17:gw|    -o <step>, --output=<step>  Output STEP file
18:cp|    --stl-units=<units>      Units used by the STL file (mm, cm, m, in, ft, um; default: mm)
19:ix|    --step-units=<units>     Units to use in exported STEP file (default: mm)
20:si|    --compare=<step>         STEP file to compare to at each step
21:cs|    --vertex-tolerance=<value>  Fitting tolerance in STL units (default: 1e-5)
22:oj|    --surface-tolerance=<value> Surface-to-face offset tolerance in STL units (default: 0.4)
23:nz|    --angular-tolerance=<deg>   Max dihedral angle between adjacent triangles on the same surface (default: 17.5)
24:ok|    -v, --verbose[=LEVEL]    Verbosity level: 1 (or bare -v) = summaries, -vv or --verbose=2 = per-face details, -vvv or --verbose=3 = full BFS trace
25:te|    -q, --quiet              Suppress non-error output
26:un|    --debug                  Enable debug output and intermediate files
27:ba|    --stage=<stage>          Stop after stage, e.g. 2.2 (default: 4.1). --stage=2 stops after all of stage2
28:gb|    --viz-stages=<stages>    Open interactive 3D visualization at specified stages (e.g. 2.1,2.2,2.3)
29:pi|```
30:dd|
31:nu|### Examples
32:dd|
33:hc|```bash
34:lr|# Full conversion
35:ae|./target/debug/brepper input.stl -o output.step
36:dd|
37:ga|# Just load and validate mesh (stage 1.2)
38:nu|./target/debug/brepper input.stl --stage 1.2 -v
39:dd|
40:cw|# Compare mesh against reference STEP at each stage
41:py|./target/debug/brepper input.stl --compare reference.step --stage 1 -v
42:pi|```
43:dd|
44:bm|See [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) for full documentation.
45:dd|
46:mf|### Tolerance Settings
47:dd|
48:ti|#### Surface Tolerance
49:dd|
50:as|This is how far the surface of a mesh triangle is allowed to deviate from the surface it is approximating.
51:dd|
52:om|Onshape calls this "Chordal tolerance (mm)", and the preset values are:
53:jp|- Coarse: 0.24 mm
54:ie|- Medium: 0.12 mm
55:ct|- Fine: 0.06 mm
56:dd|
57:fl|Fusion 360 calls this "Surface Deviation", and the preset values seem to vary based on model size:
58:bp|- Low: 1x
59:cd|- Medium: 2.5x
60:kh|- High: 8x
61:dd|
62:ul|#### Angular Tolerance
63:dd|
64:sl|This is the maximum angle between adjacent triangle faces that approximate a smooth surface.
65:dd|
66:jb|Onshape calls this "Angular deviation (deg)", and the present values are:
67:hd|- Coarse: 12.5 deg
68:ym|- Medium: 6.25 deg
69:vb|- Fine: 2.5 deg
70:dd|
71:gt|Fusion 360 calls this "Normal Deviation", and the preset values are:
72:nz|- Low: 30.0
73:pr|- Medium: 15.0
74:lx|- High: 10.0
75:aj|In theory "Normal Deviation" should be the maximum angular error of the face relative to the surface normal, but in practice it seems to result in the given maximum angular deviation.
76:dd|
77:yo|### Generating Test Models
78:dd|
79:rq|Some tests use models generated via [CodeCAD](https://codecad.xyz/). The generated STL/STEP files are committed to the repo, so CodeCAD is not required to run tests.
80:dd|
81:hm|To add or modify these models:
82:ah|1. Install CodeCAD
83:zh|2. Edit files in `tests/ccad/parts/` — see `tests/ccad/README.md` for primitive conventions and placement rules
84:vb|3. Run `./tests/ccad/generate_models.sh`
85:dd|
86:qi|**Important:** `box(w,d,h)` places a corner at the origin, not centered. Use `center_xyz()` to center. Each part file includes analytical surface area and volume in its header comments.
87:dd|
88:dd|
89:ll|## Viewer
90:dd|
91:rt|`viewer` is an interactive 3D viewer for STL and STEP files.
92:dd|
93:pi|```
94:yo|U
... (truncated, 11030 chars)
````
</details>

> 💭 Now let me read the DEVELOPMENT_PLAN.md to understand the current state and find the next task.

Now let me read the `DEVELOPMENT_PLAN.md` to find the current state:

<details>
<summary>Reading lines 1-100 of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
1:jm|# STL to STEP Conversion Utility - Development Plan
2:dd|
3:pq|## Project Overview
4:dd|
5:le|**brepper** (B-Rep from Mesh) - A utility to convert triangulated STL meshes from CAD exports into parametric STEP files with fitted analytic and freeform surfaces.
6:dd|
7:tw|## Assumptions
8:dd|
9:iq|- Assumes the vertices of the mesh lie precisely on the CAD surfaces, modulo numerical error/stability.
10:pt|- Assumes transitions between shapes always have a seam.
11:xo|- Assumes manifold mesh (each edge shared by exactly 2 faces).
12:yx|- Assumes consistent face orientation (all normals point outward).
13:dd|
14:ak|Fails noisily if any of the assumptions are violated. Possible future work: offer a repair mode.
15:dd|
16:wt|## Dependencies
17:dd|
18:aq|| Library | Purpose | Version |
19:ue||---------|---------|---------|
20:jp|| OpenCASCADE (OCCT) | STL import, B-Rep modeling, surface fitting, STEP export | latest |
21:dd|
22:lu|Use the opencascade binding in ../opencascade-rs/crates/opencascade-sys, including porting instructions in PORTING.md and details on the FFI mapping in ../opencascade-rs/crates/opencascade-binding-generator/README.md. It's a reasonably complete binding of the C++ API of the opencascade library which resides in ../opencascade-rs/crates/occt-sys/OCCT, with source code in src/ and documentation in dox/.
23:dd|
24:td|### Required OCCT Functionality
25:dd|
26:wu|This section enumerates the OCCT classes and functions needed, for evaluating Rust binding coverage.
27:dd|
28:xd|**Stage 1: Mesh Input**
29:cd|- `RWStl` / `RWStl_Reader` — Read binary/ASCII STL files
30:mh|- `gp_Pnt` — 3D point representation
31:ao|- `gp_Dir` / `gp_Vec` — Direction and vector types
32:dd|
33:zm|**Stage 2: Surface Fitting**
34:vo|- `gp_Pln` — Plane geometry (normal + distance representation)
35:hl|- `gp_Cylinder` / `gp_Ax3` — Cylindrical surface geometry (axis + radius)
36:aj|- `gp_Sphere` — Spherical surface geometry (center + radius)
37:hs|- `gp_Cone` — Conical surface geometry (axis + half-angle)
38:ny|- `gp_Torus` — Toroidal surface geometry (for fillets)
39:dd|
40:xd|**Stage 3: Surface Reconstruction**
41:dd|
42:jo|*Geometry (infinite surfaces and curves):*
43:nk|- `Geom_Plane`, `Geom_CylindricalSurface`, `Geom_SphericalSurface`, `Geom_ConicalSurface`, `Geom_ToroidalSurface` — Analytic surface objects
44:ka|- `Geom_BSplineSurface` — NURBS/B-spline surfaces
45:kc|- `Geom_Line`, `Geom_Circle`, `Geom_Ellipse` — Analytic curve types
46:fl|- `Geom_BSplineCurve` — Freeform curves
47:us|- `Geom2d_Curve`, `Geom2d_Line`, `Geom2d_Circle`, `Geom2d_BSplineCurve` — 2D curves for pcurves
48:dd|
49:jr|*Surface fitting:*
50:hg|- `GeomAPI_PointsToBSplineSurface` — Fit B-spline surface to points
51:dd|
52:xb|*Surface-surface intersection:*
53:gz|- `GeomAPI_IntSS` — Compute intersection curves between two surfaces
54:dd|
55:kk|*Point-curve projection:*
56:vt|- `GeomAPI_ProjectPointOnCurve` — Project point onto curve (for trimming)
57:sl|- `ShapeAnalysis_Curve` — Curve analysis utilities
58:dd|
59:at|*Topology (B-Rep construction):*
60:fl|- `TopoDS_Vertex`, `TopoDS_Edge`, `TopoDS_Wire`, `TopoDS_Face`, `TopoDS_Shell`, `TopoDS_Solid`, `TopoDS_Compound` — Topological entities
61:ia|- `BRep_Builder` — Low-level topology construction
62:wm|- `BRepBuilderAPI_MakeVertex` — Create vertices
63:vm|- `BRepBuilderAPI_MakeEdge` — Create edges from curves (with optional pcurves)
64:qm|- `BRepBuilderAPI_MakeWire` — Assemble edges into wires
65:sx|- `BRepBuilderAPI_MakeFace` — Create faces from surface + bounding wires
66:ml|- `BRepBuilderAPI_Sewing` — Stitch faces into shells
67:ft|- `BRepBuilderAPI_MakeSolid` — Create solids from shells
68:dd|
69:pc|*Topology exploration:*
70:dh|- `TopExp_Explorer` — Iterate over sub-shapes
71:fj|- `TopExp::MapShapes` — Build maps of topology
72:ep|- `BRep_Tool` — Extract geometry from topology (IsSeam, SameParameter, etc.)
73:dd|
74:al|*Shape healing and fixing:*
75:im|- `ShapeFix_Shape` — General shape repair
76:st|- `ShapeFix
... (truncated, 5500 chars)
```
</details>

<details>
<summary>Reading lines 100-200 of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
100:xw|*Comment: Indices into vectors are generally the right choice for this domain—they're stable under serialization, easily debuggable, and avoid lifetime complexity. Pointers/references become problematic when hypotheses are deleted or vectors reallocate. Consider using "generation counters" or a slot-map pattern if you need to detect stale indices.*
101:dd|
102:sz|## Architecture
103:dd|
104:co|### Stage 1: Mesh Input & Preprocessing
105:dd|
106:qm|#### 1.1 Read STL File
107:tb|Read the input STL file and generate an in-memory representation of the triangle mesh, with fields for future stages to traverse the mesh and fit shapes to sets of mesh faces. Weld vertices by position (with tolerance) to build connectivity.
108:ef|- **Library**: OCCT `RWStl::ReadFile` (static function, via `rw_stl::read_file_charptr_progressrange_2` in the Rust bindings)
109:ej|- **Input**: Binary or ASCII STL file
110:nj|- **Output**: `ConnectedMesh`, storing:
111:wa|    - `vertices: Vec<MeshVertex>` — Mesh vertices with double-precision 3D coordinates (`x`, `y`, `z: f64`).
112:hf|    - `faces: Vec<MeshFace>` — Mesh faces with:
113:fg|        - `vertex_count: u8` — 3 for triangles, 4 for quads. Always 3 after initial STL input; stage 1.3 may merge coplanar triangle pairs into quads.
114:ua|        - `vertex_indices: [usize; 4]` — Indices of vertices, ordered by right-hand rule.
115:bj|        - `neighbors: [i32; 4]` — Index of the mesh face across each edge, or -1 if none. Filled in stage 1.2.
116:ss|        - `normal: Option<[f64; 3]>` — Mesh face normal, computed from vertices in stage 1.2. `None` for degenerate faces. (Changed from `gp_Dir` in original plan — native Rust arrays avoid OCCT binding complexity and keep the mesh data structure self-contained.)
117:dx|        - `planar_hypothesis: i32` — Index of active planar hypothesis, or `NO_HYPOTHESIS` (-1) if none, or `UNDEDUCED_PLANAR_HYPOTHESIS` (-2) if not yet deduced.
118:rf|        - `cylindrical_hypothesis: i32` — Index of active cylindrical hypothesis, or `NO_HYPOTHESIS` (-1) if none, or `UNDEDUCED_CYLINDRICAL_HYPOTHESIS` (-2) if not yet deduced.
119:va|        - `spherical_hypothesis: i32` — Index of active spherical hypothesis, or `NO_HYPOTHESIS` (-1) if none, or `UNDEDUCED` (-2) if not yet deduced.
120:mr|    - `stats: MeshValidationStats` — Statistics described in stage 1.2.
121:dd|
122:eb|Note: hypotheses vectors (planar, cylindrical, spherical) are stored in `Stage2Output`, not in `ConnectedMesh`. The mesh stores only per-face hypothesis indices. (Changed from original plan which listed hypothesis vectors as part of `ConnectedMesh` — separating them into the stage 2 output keeps each stage's output self-contained and avoids coupling the mesh representation to later stages.)
123:dd|
124:vo|For now, each face can belong to a single hypothesis of each type. Hopefully that's sufficient since hypotheses should nearly exactly match the vertices, but it's possible that in the future we may need to keep a set of candidate hypothesis indices per type, then select the best one in stage 2.6.
125:dd|
126:gk|**Vertex welding** uses a spatial hash grid: each vertex is bucketed by integer coordinates `(round(x/tol), round(y/tol), round(z/tol))`. To find matches, adjacent cells (3×3×3 neighborhood) are checked for distance ≤ tolerance. This gives O(1) average lookup. The weld tolerance is derived from `--vertex-tolerance` as `min(1e-9, vertex_tolerance * 0.01)`, so welding is much tighter than the surface-fitting tolerance.
127:dd|
128:wg|#### 1.2 Mesh Validation
129:ws|Traverse the faces of the mesh, collecting statistics, validating the geometry, and populating normals and neighbors.
130:nj|- Collect stats into `MeshValidationStats` and optionally print: `mesh_faces`, `mesh_vertices`, `mesh_edges_open` (edges with only 1 incident face), `mesh_edges_non_manifold` (edges with >2 incident faces), `mesh_edges_inconsistent_orientation`, `mesh_faces_degenerate`, `connected_shells`, `solids`, `voids
... (truncated, 14274 chars)
```
</details>

<details>
<summary>Reading lines 200-400 of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
200:ot|    - Single-face hypotheses are kept (not deleted). They represent faces on curved surfaces that will be assigned to cylindrical/spherical hypotheses in later stages.
201:dd|
202:nl|Plane fitting uses **area-weighted normal averaging**: each face's unit normal is weighted by the face's triangle area, then the weighted sum is normalized. The plane distance is the mean of `normal · vertex` over all vertices in the hypothesis. This gives larger faces more influence on the plane orientation, which is more robust than unweighted averaging.
203:dd|
204:wc|- With the `--compare` flag:
205:pp|  - For each hypothesis, compute the centroid of each member face.
206:jo|  - Project each centroid onto the fitted plane (perpendicular projection).
207:xg|  - Measure the distance from the projected point to the nearest surface in the reference STEP file using `BRepExtrema_DistShapeShape`.
208:op|  - If any projected centroid exceeds `--surface-tolerance`, report an error.
209:ci|  - (Changed from original plan: the plan described projecting face *edges* onto the hypothesis and checking the enclosed surface region. The centroid-projection approach is simpler and sufficient for validation — it confirms the fitted plane aligns with a real STEP surface at representative interior points. Edge projection would be needed for boundary accuracy validation, which is a stage 3 concern.)
210:dd|
211:fp|*Comment on 2.1: ~~The DFS vs BFS question is worth considering: DFS can get "trapped" in a narrow corridor and accumulate drift before exploring the main region. BFS explores more uniformly. However, the re-fitting step partially mitigates this.~~ Resolved: BFS was chosen for the implementation. ~~A bigger issue: once you re-fit the plane, previously accepted faces might no longer fit the new plane! Consider a final validation pass that removes faces whose vertices exceed the tolerance after the final fit.~~ Resolved: the re-fit step checks all vertices (existing + new) before accepting, and a final re-fit with error metrics is computed at the end. ~~Also: the "vertices in right-hand-rule order" for the hypothesis is unclear—planar regions aren't simply connected in general (they can have holes), so you'll need a more complex boundary representation.~~ Resolved: vertices are stored as an unordered set; boundary representation is deferred to stage 3.*
212:dd|
213:da|#### 2.2 Deduce cylindrical hypotheses
214:ev|Fit cylindrical hypotheses to connected sets of faces that lie on a common cylinder. A cylindrical hypothesis consists of:
215:cw|- `axis_origin: [f64; 3]` — A point on the cylinder axis.
216:ez|- `axis_direction: [f64; 3]` — Unit direction vector along the axis.
217:fv|- `radius: f64` — Radius of the cylinder (always positive).
218:dk|- `convex: bool` — Whether face normals point away from the axis (convex, like the outside of a pipe) or toward it (concave, like the inside of a hole).
219:lp|- `faces: Vec<usize>` — Mesh face indices that fit this hypothesis.
220:ua|- `vertices: Vec<usize>` — Mesh vertex indices on this cylinder.
221:zz|- `error_max: f64` — Maximum absolute distance from any vertex to the cylinder surface (i.e. max of `| ||v - axis_closest|| - radius |`).
222:ov|- `error_abs_sum: f64` — Sum of absolute vertex-to-surface distances.
223:dd|
224:mt|The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from triples of nearby faces (since a single triangle is always planar and cannot determine curvature, and two faces provide minimal angular constraint on the cylinder axis). A key challenge is that **any two non-coplanar faces define some cylinder** — the cross product of their normals gives an axis, and a circle fit always produces a radius. The previous algorithm took the first valid seed and committed to it, which meant a bogus 3-face cylinder from an unfortunate seed pair could permanently consume faces that should belong to a correct 20-face cylinder seeded from a different pair. The fix is multi-seed evalu
... (truncated, 31989 chars)
```
</details>

<details>
<summary>Reading lines 400-600 of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
400:dd|
401:ne|*Comment: This is a good idea for capturing linear extrusions and sweeps. A ruled surface is defined by two boundary curves with linear interpolation between them. For extrusions, one "curve" is a point (the surface degenerates to a generalized cylinder). Detection: look for parallel mesh edges that share the same direction. Fitting: project to a plane perpendicular to the ruling direction and fit a 2D curve. Watch out for twisted ruled surfaces (rulings aren't parallel)—these are harder to detect and fit.*
402:dd|
403:we|#### 2.5 Deduce NURBS hypotheses
404:ib|TODO: for groups of adjacent faces which are covered by one- or two-face planar hypotheses and not cylindrical or spherical hypotheses, try to fit a NURBS or b-spline surface to the vertices.
405:dd|
406:wv|*Comment: NURBS fitting is complex and requires careful consideration: (1) Parameterization: you need to assign (u,v) parameters to each mesh vertex before fitting. Common approaches: conformal mapping, Floater's mean value coordinates, or discrete harmonic mapping. (2) Degree and knot selection: start with bicubic (degree 3×3); knot placement can use chord-length parameterization or be optimized. (3) Regularization: without it, the surface may oscillate. Consider smoothness penalties. (4) An alternative worth considering: use OCCT's `GeomAPI_PointsToBSplineSurface` which handles much of this automatically. (5) For "freeform" regions that are nearly planar, a plane with small tolerance may be preferable to a NURBS that overfits noise.*
407:dd|
408:ur|#### 2.6 Select surfaces to use for reconstruction
409:cf|Assign each mesh face to exactly one "selected surface" for reconstruction. Since stages 2.1–2.3 produce hypotheses that may overlap (a face can belong to one planar, one cylindrical, and one spherical hypothesis simultaneously), surface selection must resolve these overlaps.
410:dd|
411:ca|**Greedy area-based selection:**
412:dd|
413:im|The algorithm selects hypotheses greedily by total mesh face area, largest first. This naturally resolves conflicts: real surface hypotheses cover many faces with large total area, while bogus hypotheses (e.g., a near-degenerate r=139mm sphere from 3 nearly-coplanar faces) cover minimal area and lose their faces to correctly-fitted surfaces.
414:dd|
415:mv|**Algorithm:**
416:ed|1. Compute the geometric area of each mesh face.
417:fb|2. Build a candidate list of all hypotheses (planar with face count > 1, cylindrical, spherical). Each candidate tracks its set of member faces and their total area.
418:ez|3. **Greedy loop**: while unassigned faces remain:
419:vg|   a. For each candidate, compute the total area of its still-unassigned faces.
420:xz|   b. Select the candidate with the largest remaining area.
421:jg|   c. Assign all its still-unassigned faces to it as a selected surface.
422:lt|   d. Remove those faces from all other candidates' pools.
423:hz|4. Assign any remaining unassigned faces to their single-face planar hypothesis (fallback for faces on surfaces not yet fitted: conical, toroidal, freeform). Stages 2.4–2.5 will replace some of these with ruled or NURBS surfaces.
424:vj|5. Build a `Vec<SelectedSurface>`, one per selected hypothesis, containing:
425:ru|   - `surface_type: SurfaceType` — enum: Planar, Cylindrical, Spherical (later: Ruled, BSpline).
426:mu|   - `hypothesis_index: usize` — index into the appropriate hypothesis vector in Stage2Output.
427:ll|   - `faces: Vec<usize>` — mesh face indices assigned to this surface.
428:oq|   - `vertices: Vec<usize>` — mesh vertex indices on this surface (union of face vertices).
429:is|6. Validate: every face is assigned to exactly one selected surface.
430:it|7. Print a summary: count of selected surfaces by type, total faces covered.
431:dd|
432:zt|**Why this works better than a fixed type-priority rule:** A fixed priority (e.g., spherical > cylindrical > multi-face planar) fails when a bogus hypothesis of a high-priority type (3-face r=139mm sphere) compet
... (truncated, 23691 chars)
```
</details>

<details>
<summary>Reading lines 600-750 of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
600:qx|  - `Solid`, `Compound`, and `Compsolid` sub-shapes are explored recursively for `Shell` children.
601:bq|- Apply `ShapeFix_Shell::Perform()` (which calls `ShapeFix_Face::Perform()` on each face to re-add pcurves that sewing may have discarded, and `FixFaceOrientation` for edge consistency). Face orientation is NOT post-processed — `FixFaceOrientation` handles edge consistency and `SolidFromShell` (stage 3.6) handles global orientation via `PerformInfinitePoint`. Investigation showed that flipping individual face orientations breaks edge consistency and causes volume computation errors.
602:jm|- Validate with `ShapeAnalysis_Shell::CheckOrientedShells` and report inconsistencies.
603:oy|- Report sewing statistics: free edges, multiple edges, contiguous edges.
604:dd|
605:wc|- With the `--compare` flag:
606:wu|    - Count shells in the reference STEP file by exploring for `Shell` sub-shapes.
607:gy|    - Report an error if the sewing produced free edges (edges not shared by any pair of faces), as this indicates incomplete stitching.
608:pf|    - Report shell count comparison and orientation check results.
609:dd|
610:nr|#### 3.6 Construct solids
611:ub|Convert closed shells into `TopoDS_Solid` objects.
612:dd|
613:mv|**Algorithm:**
614:rb|- For each shell, use `ShapeFix_Solid::SolidFromShell` which handles orientation automatically.
615:fo|- Validate with `BRepCheck_Analyzer`. Log warning if validation fails (may indicate upstream face issues from stage 3.4).
616:yf|- Compute and report volume using `BRepGProp::VolumeProperties`.
617:ln|- Multi-shell solids (outer shell containing voids) not yet implemented — currently each shell produces one solid.
618:dd|
619:wc|- With the `--compare` flag:
620:cy|    - Count solids in the reference STEP file.
621:bo|    - Compute volumes of both constructed and reference solids. Match by sorted volume. Report warning if relative volume difference exceeds 1%. All current test models achieve volume agreement within ~1e-7 relative difference.
622:wx|    - Compute `BRepExtrema_DistShapeShape` between each constructed solid and the reference STEP shape. Report error if distance exceeds `--surface-tolerance`. This is the tightest geometric accuracy check.
623:dd|
624:ea|---
625:dd|
626:vd|### Stage 4: Output
627:dd|
628:tv|#### 4.1 Output objects
629:ra|Write constructed solid(s) to a STEP file.
630:dd|
631:py|**Algorithm (implemented):**
632:ce|- Build a `topo_ds::Compound` containing all solids from stage 3.6.
633:ks|- Set STEP metadata: AP214 schema, product name "brepper" via `Interface_Static::set_c_val`.
634:pv|- Create a `STEPControl_Writer`, transfer the compound with `StepModelType::Asis` mode to preserve exact geometry.
635:qy|- Write the output file. Report error if transfer or write returns non-Done status.
636:wc|- With the `--compare` flag:
637:bc|    - Re-read the written STEP file via `STEPControl_Reader` to validate the round-trip.
638:lv|    - Compare volumes of the output shape vs reference shape using `BRepGProp::VolumeProperties`. Warn on >1% relative difference.
639:ln|    - Compute maximum distance between output and reference using `BRepExtrema_DistShapeShape`. Hard error if exceeds `--surface-tolerance`.
640:dd|
641:ea|---
642:dd|
643:yk|## Source Code Organization
644:dd|
645:od|Each stage will be represented by a function which takes a ref to a configuration data structure and consumes an input data structure, and returns an output data structure (or error). That data structure is passed to the next stage:
646:dd|
647:li|- main:
648:vs|  - let config = parse flags, load --compare STEP file
649:ql|  - out1 = stage1(config).unwrap()
650:lb|  - if config.stage < 2: exit
651:ct|  - out2 = stage2(congig, out1).unwrap()
652:wx|  - if config.stage < 3: exit
653:wx|  - out3 = stage3(config, out2).unwrap()
654:zi|  - if config.stage < 4: exit
655:qs|  - stage4(config, out3).unwrap()  # writes output file
656:yb|- stageN:
657:us|  - execute whatever portions of stage1 are al
... (truncated, 16422 chars)
```
</details>

<details>
<summary>Reading lines 750-786 of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
750:ob||| Dome Hemisphere (onshape) | Fine tessellation | 1 sphere + 1 plane | ✗ Stage 3.5 (sewing failure) |
751:kk||| Pill coarse (onshape) | Capsule (coarse) | 1 cylinder + 2 spheres | ✓ Stage 4.1 (STEP output) |
752:mp||| Pill fine (onshape) | Capsule (fine) | 1 cylinder + 2 spheres | ✓ Stage 4.1 (STEP output) |
753:ss||| Plate with Hole (onshape) | 128 triangles | 6 planes + 1 cylinder | ✓ Stage 4.1 (STEP output) |
754:zw||| Plate with Hole low (fusion) | Low tessellation | 6 planes + 1 cylinder | ✓ Stage 4.1 (STEP output) |
755:ov||| Plate with Hole med (fusion) | Medium tessellation | 6 planes + 1 cylinder | ✓ Stage 4.1 (STEP output) |
756:qp||| Plate with Hole high (fusion) | High tessellation | 6 planes + 1 cylinder | ✗ Stage 3.1 (coplanarity tolerance) |
757:xa||| Part Rounded Cube coarse (onshape) | 820 triangles | 6 planes + 4 cylinders | ✓ Stage 4.1 (STEP output) |
758:wu||| Part Rounded Cube fine (onshape) | 4260 triangles | 6 planes + 4 cylinders | ✓ Stage 4.1 (STEP output) |
759:pi||| Rounded Cube coarse (onshape) | 820 triangles | 6 planes + 12 cylinders + 8 spheres | ✗ Stage 3.1 |
760:hc||| Rounded Cube medium (onshape) | 3548 triangles | 6 planes + 12 cylinders + 8 spheres | ✗ Stage 3.1 |
761:jc||| Rounded Cube fine (onshape) | 21466 triangles | 6 planes + 12 cylinders + 8 spheres | ✗ Stage 3.1 |
762:sy||| Pipe Elbow (onshape) | Fine tessellation | Cylinders + torus | ✗ Stage 3.1 (missing torus surface type) |
763:mf|| Cone | Tessellated cone | 1 cone + 1 plane | ✗ Stage 3.1 (missing cone surface type) |
764:re|| Fillet | Blended edge | Planes + fillet surface | |
765:sq|| Complex part | Real CAD export | Matching topology | |
766:dd|
767:ea|---
768:dd|
769:zi|## Performance Targets
770:dd|
771:vn|| Mesh Size | Target Time |
772:tk||-----------|-------------|
773:ai|| < 10K triangles | < 5 seconds |
774:xk|| 10K - 100K triangles | < 30 seconds |
775:lt|| 100K - 1M triangles | < 5 minutes |
776:dd|
777:ea|---
778:dd|
779:hd|## Future Enhancements
780:dd|
781:ey|- [ ] Support for OBJ, PLY input formats
782:de|- [ ] IGES export option
783:bs|- [ ] Machine learning for surface type classification
784:bu|- [ ] Hole detection and filling
785:vw|- [ ] Feature recognition (holes, pockets, bosses)
786:dd|
```
</details>

> 💭 Now I have a good understanding of the project. The user wants me to refactor by extracting cylindrical fitting, spherical fitting, and planar fitting into their own source files. Let me first look at the current stage2.rs to understand its structure.

Now let me understand the current structure of stage2.rs:

**Terminal:**
```sh
wc -l /Users/sethml/src/brepper/src/stage2.rs
```
```
    3805 /Users/sethml/src/brepper/src/stage2.rs
```

**Todo list:**
- 🔄 Analyze stage2.rs structure
- ⬜ Create planar fitting module
- ⬜ Create cylindrical fitting module
- ⬜ Create spherical fitting module
- ⬜ Update stage2.rs to use modules
- ⬜ Update lib.rs module declarations
- ⬜ Build and fix errors
- ⬜ Run tests
- ⬜ Run clippy
- ⬜ Update docs and commit

Let me analyze the structure of stage2.rs to understand where to split:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n '^pub\|^fn\|^pub fn\|^pub(crate) fn\|^pub struct\|^pub enum\|^struct\|^enum\|^impl\|^const\|^pub const\|^pub(crate) const\|^// Stage\|^/// Stage\|^// ==\|^//-\|^use ' src/stage2.rs | head -200
```
<details>
<summary>Output (81 lines)</summary>

```
10:use crate::config::Config;
11:use crate::stage1::{self, ConnectedMesh, MeshFace, MeshVertex, UNDEDUCED_PLANAR_HYPOTHESIS, UNDEDUCED_CYLINDRICAL_HYPOTHESIS, UNDEDUCED_SPHERICAL_HYPOTHESIS, NO_HYPOTHESIS};
12:use crate::viz::{self, VizAction, VizSender};
13:use levenberg_marquardt::{LeastSquaresProblem, LevenbergMarquardt};
14:use nalgebra::{Const, Dyn, OMatrix, OVector, Owned};
15:use opencascade_sys::gp;
16:use std::collections::{HashSet, VecDeque};
17:use std::error::Error;
18:use std::fmt::{Display, Formatter};
32:const MIN_CROSS_THRESHOLD: f64 = 0.01;
38:const REFIT_SKIP_MULTIPLIER: f64 = 2.0;
44:const MIN_CYLINDER_FACES: usize = 3;
50:const MIN_SPHERE_FACES: usize = 4;
56:const MAX_SPHERE_RADIUS_FACTOR: f64 = 1000.0;
62:const MIN_SPHERE_EIGENVALUE_RATIO: f64 = 0.01;
70:pub struct PlanarHypothesis {
89:pub struct CylindricalHypothesis {
112:pub struct SphericalHypothesis {
136:pub enum SelectedSurface {
145:// Stage 2 output
150:pub struct Stage2Output {
169:pub enum Stage2Error {
174:impl Display for Stage2Error {
183:impl Error for Stage2Error {}
185:impl From<Stage2CompareError> for Stage2Error {
192:pub struct Stage2CompareError {
199:impl Display for Stage2CompareError {
209:impl Error for Stage2CompareError {}
212:// Stage 2.1: Planar hypothesis deduction
216:fn vertex_to_plane_distance(v: &MeshVertex, normal: &[f64; 3], distance: f64) -> f64 {
221:fn face_area(face: &MeshFace, vertices: &[MeshVertex]) -> f64 {
250:fn fit_plane(
283:fn all_vertices_within_tolerance(
303:fn viz_face_centroid(face_idx: usize, mesh: &ConnectedMesh) -> [f32; 3] {
319:fn viz_face_normal(face_idx: usize, mesh: &ConnectedMesh) -> Option<[f32; 3]> {
329:fn viz_bfs_step(
377:fn viz_bfs_seed(
412:fn viz_custom(
435:fn centered_cylinder_overlay(
486:fn deduce_planar_hypotheses(
754:// Stage 2.1: Compare check
762:fn compare_planar_hypotheses(
814:// Stage 2.2: Cylindrical hypothesis deduction
818:fn face_centroid(face: &MeshFace, vertices: &[MeshVertex]) -> [f64; 3] {
835:fn vertex_to_cylinder_distance(
856:fn normalize3(v: &mut [f64; 3]) -> f64 {
867:fn cross3(a: &[f64; 3], b: &[f64; 3]) -> [f64; 3] {
876:fn dot3(a: &[f64; 3], b: &[f64; 3]) -> f64 {
885:fn eigenvalues_3x3_symmetric(m: &[f64; 6]) -> [f64; 3] {
916:fn eigenvector_for_eigenvalue(m: &[f64; 6], lambda: f64) -> [f64; 3] {
950:fn smallest_eigenvector_3x3(m: &[f64; 6]) -> [f64; 3] {
959:fn build_normal_covariance(
982:fn fit_circle_for_axis(
1088:struct CylinderLMProblem {
1096:impl CylinderLMProblem {
1139:impl LeastSquaresProblem<f64, Dyn, Const<6>> for CylinderLMProblem {
1182:fn fit_cylinder(
1221:fn all_vertices_within_cylinder_tolerance(
1239:fn determine_convexity(
1263:fn perpendicular_basis(d: &[f64; 3]) -> ([f64; 3], [f64; 3]) {
1287:fn angular_coverage_valid(
1351:struct CylinderTrialResult {
1369:fn run_cylinder_trial_bfs(
1856:fn is_pairwise_qualified(
1888:fn deduce_cylindrical_hypotheses(
2074:fn compare_cylindrical_hypothe
... (truncated, 3737 chars total)
```
</details>

Good. Now let me read the key sections to understand exact boundaries. Let me read imports, constants, struct definitions, and the lib.rs:

<details>
<summary>Reading lines 1-215 of <a href="../src/stage2.rs#L1-L216">stage2.rs</a></summary>

```
//! Stage 2: Surface Fitting
//!
//! 2.1: Deduce planar hypotheses
//! 2.2: Deduce cylindrical hypotheses
//! 2.3: Deduce spherical hypotheses
//! 2.4: Deduce ruled surface hypotheses
//! 2.5: Deduce NURBS hypotheses
//! 2.6: Select surfaces for reconstruction

use crate::config::Config;
use crate::stage1::{self, ConnectedMesh, MeshFace, MeshVertex, UNDEDUCED_PLANAR_HYPOTHESIS, UNDEDUCED_CYLINDRICAL_HYPOTHESIS, UNDEDUCED_SPHERICAL_HYPOTHESIS, NO_HYPOTHESIS};
use crate::viz::{self, VizAction, VizSender};
use levenberg_marquardt::{LeastSquaresProblem, LevenbergMarquardt};
use nalgebra::{Const, Dyn, OMatrix, OVector, Owned};
use opencascade_sys::gp;
use std::collections::{HashSet, VecDeque};
use std::error::Error;
use std::fmt::{Display, Formatter};


// ---------------------------------------------------------------------------
// Constants
// ---------------------------------------------------------------------------

/// Lower bound on cross product magnitude |n₁ × n₂| for a valid cylinder/sphere
/// seed pair. Since |n₁ × n₂| = sin(θ) for unit normals, this corresponds to a
/// minimum dihedral angle of arcsin(0.01) ≈ 0.57° (~0.6°). Seed pairs closer
/// than this are too nearly parallel for numerically stable axis estimation
/// (cylinders) or sphere fitting (spheres). Together with `angular_tol` (the
/// upper bound, typically 17.5°), this defines the valid seed-pair window
/// [~0.6°, angular_tol].
const MIN_CROSS_THRESHOLD: f64 = 0.01;

/// Fast-reject multiplier for BFS vertex-distance checks. When a candidate face
/// has a vertex farther than `REFIT_SKIP_MULTIPLIER × vertex_tol` from the
/// current fitted surface, the distance is too large for a re-fit to absorb and
/// the face is rejected immediately without attempting re-fitting.
const REFIT_SKIP_MULTIPLIER: f64 = 2.0;

/// Minimum number of mesh faces required to accept a cylindrical hypothesis.
/// Any real CAD tessellation of a cylinder produces at least 3 facets around
/// the circumference; this rejects spurious 2-face fits (e.g. adjacent torus
/// facets that locally approximate a cylinder).
const MIN_CYLINDER_FACES: usize = 3;

/// Minimum number of mesh faces required to accept a spherical hypothesis.
/// Sphere fitting has 4 degrees of freedom (cx, cy, cz, r), so at least 4
/// non-degenerate faces are needed for a well-determined fit. This also rejects
/// spurious fits from small patches that are consistent with many surface types.
const MIN_SPHERE_FACES: usize = 4;

/// Maximum sphere radius as a multiple of the mesh bounding-box diagonal.
/// With solid-angle coverage validation and surface-tolerance validation during
/// BFS, this no longer needs to be tight — those checks prevent pathological
/// growth. A large value serves as a numerical guardrail against degenerate fits.
const MAX_SPHERE_RADIUS_FACTOR: f64 = 1000.0;

/// Minimum eigenvalue ratio for solid-angle coverage validation.
/// Centroid-to-center direction vectors of a genuine spherical hypothesis span
/// 3D, so all eigenvalues of their covariance matrix are substantial.
/// Fillet-strip growth produces nearly coplanar directions (λ₃ ≈ 0).
const MIN_SPHERE_EIGENVALUE_RATIO: f64 = 0.01;

// ---------------------------------------------------------------------------
// Hypothesis data structures
// ---------------------------------------------------------------------------

/// A planar surface hypothesis fitted to a set of mesh faces.
#[derive(Debug, Clone)]
pub struct PlanarHypothesis {
    /// Unit normal vector pointing outward from the shell/solid.
    pub normal: [f64; 3],
    /// Signed distance from origin to plane along the normal.
    pub distance: f64,
    /// Set of mesh face indices that fit this hypothesis.
    pub faces: Vec<usize>,
    /// Set of mesh vertex indices on this plane.
    pub vertices: Vec<usize>,
    /// Maximum (positive) distance from any vertex to the plane.
    pub error_max: f64,
    /// Minimum (most negative) distance from any vertex to the plane.
    pu
... (truncated, 8718 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../src/lib.rs">lib.rs</a></summary>

```
// brepper - B-Rep from Mesh

pub mod config;
pub mod stage1;
pub mod stage2;
pub mod stage3;
pub mod stage4;
pub mod viz;

```
</details>

<details>
<summary>Reading all lines of <a href="../src/stage2.rs">stage2.rs</a></summary>

```
1:ej|//! Stage 2: Surface Fitting
2:at|//!
3:zx|//! 2.1: Deduce planar hypotheses
4:wj|//! 2.2: Deduce cylindrical hypotheses
5:kf|//! 2.3: Deduce spherical hypotheses
6:hk|//! 2.4: Deduce ruled surface hypotheses
7:jg|//! 2.5: Deduce NURBS hypotheses
8:pr|//! 2.6: Select surfaces for reconstruction
9:dd|
10:ka|use crate::config::Config;
11:wu|use crate::stage1::{self, ConnectedMesh, MeshFace, MeshVertex, UNDEDUCED_PLANAR_HYPOTHESIS, UNDEDUCED_CYLINDRICAL_HYPOTHESIS, UNDEDUCED_SPHERICAL_HYPOTHESIS, NO_HYPOTHESIS};
12:ex|use crate::viz::{self, VizAction, VizSender};
13:gb|use levenberg_marquardt::{LeastSquaresProblem, LevenbergMarquardt};
14:kl|use nalgebra::{Const, Dyn, OMatrix, OVector, Owned};
15:cw|use opencascade_sys::gp;
16:nk|use std::collections::{HashSet, VecDeque};
17:iz|use std::error::Error;
18:zv|use std::fmt::{Display, Formatter};
19:dd|
20:dd|
21:sx|// ---------------------------------------------------------------------------
22:mz|// Constants
23:sx|// ---------------------------------------------------------------------------
24:dd|
25:py|/// Lower bound on cross product magnitude |n₁ × n₂| for a valid cylinder/sphere
26:zj|/// seed pair. Since |n₁ × n₂| = sin(θ) for unit normals, this corresponds to a
27:ov|/// minimum dihedral angle of arcsin(0.01) ≈ 0.57° (~0.6°). Seed pairs closer
28:ww|/// than this are too nearly parallel for numerically stable axis estimation
29:lj|/// (cylinders) or sphere fitting (spheres). Together with `angular_tol` (the
30:cl|/// upper bound, typically 17.5°), this defines the valid seed-pair window
31:uv|/// [~0.6°, angular_tol].
32:kw|const MIN_CROSS_THRESHOLD: f64 = 0.01;
33:dd|
34:jv|/// Fast-reject multiplier for BFS vertex-distance checks. When a candidate face
35:gr|/// has a vertex farther than `REFIT_SKIP_MULTIPLIER × vertex_tol` from the
36:xl|/// current fitted surface, the distance is too large for a re-fit to absorb and
37:qs|/// the face is rejected immediately without attempting re-fitting.
38:ci|const REFIT_SKIP_MULTIPLIER: f64 = 2.0;
39:dd|
40:sj|/// Minimum number of mesh faces required to accept a cylindrical hypothesis.
41:cq|/// Any real CAD tessellation of a cylinder produces at least 3 facets around
42:ag|/// the circumference; this rejects spurious 2-face fits (e.g. adjacent torus
43:bn|/// facets that locally approximate a cylinder).
44:xn|const MIN_CYLINDER_FACES: usize = 3;
45:dd|
46:nj|/// Minimum number of mesh faces required to accept a spherical hypothesis.
47:rg|/// Sphere fitting has 4 degrees of freedom (cx, cy, cz, r), so at least 4
48:jh|/// non-degenerate faces are needed for a well-determined fit. This also rejects
49:vq|/// spurious fits from small patches that are consistent with many surface types.
50:fe|const MIN_SPHERE_FACES: usize = 4;
51:dd|
52:iv|/// Maximum sphere radius as a multiple of the mesh bounding-box diagonal.
53:uc|/// With solid-angle coverage validation and surface-tolerance validation during
54:jp|/// BFS, this no longer needs to be tight — those checks prevent pathological
55:jl|/// growth. A large value serves as a numerical guardrail against degenerate fits.
56:se|const MAX_SPHERE_RADIUS_FACTOR: f64 = 1000.0;
57:dd|
58:xo|/// Minimum eigenvalue ratio for solid-angle coverage validation.
59:rd|/// Centroid-to-center direction vectors of a genuine spherical hypothesis span
60:rg|/// 3D, so all eigenvalues of their covariance matrix are substantial.
61:wl|/// Fillet-strip growth produces nearly coplanar directions (λ₃ ≈ 0).
62:ji|const MIN_SPHERE_EIGENVALUE_RATIO: f64 = 0.01;
63:dd|
64:sx|// ---------------------------------------------------------------------------
65:vg|// Hypothesis data structures
66:sx|// ---------------------------------------------------------------------------
67:dd|
68:qw|/// A planar surface hypothesis fitted to a set of mesh faces.
69:uk|#[derive(Debug, Clone)]
70:ep|pub struct PlanarHypothesis {
71:zt|    /// Unit normal vector pointing outward from the shell/solid.
72:pd|    pub normal: [f64; 
... (truncated, 10115 chars)
```
</details>

Now let me look at what functions belong to each section and their exact boundaries:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n '^// Stage\|^// ===\|^// ---\|^fn \|^pub fn \|^pub(crate) fn \|^struct \|^pub struct \|^impl \|^const \|^pub const ' src/stage2.rs
```
<details>
<summary>Output (94 lines)</summary>

```
21:// ---------------------------------------------------------------------------
23:// ---------------------------------------------------------------------------
32:const MIN_CROSS_THRESHOLD: f64 = 0.01;
38:const REFIT_SKIP_MULTIPLIER: f64 = 2.0;
44:const MIN_CYLINDER_FACES: usize = 3;
50:const MIN_SPHERE_FACES: usize = 4;
56:const MAX_SPHERE_RADIUS_FACTOR: f64 = 1000.0;
62:const MIN_SPHERE_EIGENVALUE_RATIO: f64 = 0.01;
64:// ---------------------------------------------------------------------------
66:// ---------------------------------------------------------------------------
70:pub struct PlanarHypothesis {
89:pub struct CylindricalHypothesis {
112:pub struct SphericalHypothesis {
144:// ---------------------------------------------------------------------------
145:// Stage 2 output
146:// ---------------------------------------------------------------------------
150:pub struct Stage2Output {
164:// ---------------------------------------------------------------------------
166:// ---------------------------------------------------------------------------
174:impl Display for Stage2Error {
183:impl Error for Stage2Error {}
185:impl From<Stage2CompareError> for Stage2Error {
192:pub struct Stage2CompareError {
199:impl Display for Stage2CompareError {
209:impl Error for Stage2CompareError {}
211:// ---------------------------------------------------------------------------
212:// Stage 2.1: Planar hypothesis deduction
213:// ---------------------------------------------------------------------------
216:fn vertex_to_plane_distance(v: &MeshVertex, normal: &[f64; 3], distance: f64) -> f64 {
221:fn face_area(face: &MeshFace, vertices: &[MeshVertex]) -> f64 {
250:fn fit_plane(
283:fn all_vertices_within_tolerance(
298:// ---------------------------------------------------------------------------
300:// ---------------------------------------------------------------------------
303:fn viz_face_centroid(face_idx: usize, mesh: &ConnectedMesh) -> [f32; 3] {
319:fn viz_face_normal(face_idx: usize, mesh: &ConnectedMesh) -> Option<[f32; 3]> {
329:fn viz_bfs_step(
377:fn viz_bfs_seed(
412:fn viz_custom(
435:fn centered_cylinder_overlay(
486:fn deduce_planar_hypotheses(
753:// ---------------------------------------------------------------------------
754:// Stage 2.1: Compare check
755:// ---------------------------------------------------------------------------
762:fn compare_planar_hypotheses(
813:// ---------------------------------------------------------------------------
814:// Stage 2.2: Cylindrical hypothesis deduction
815:// ---------------------------------------------------------------------------
818:fn face_centroid(face: &MeshFace, vertices: &[MeshVertex]) -> [f64; 3] {
835:fn vertex_to_cylinder_distance(
856:fn normalize3(v: &mut [f64; 3]) -> f64 {
867:fn cross3(a: &[f64; 3], b: &[f64; 3]) -> [f64; 3] {
876:fn dot3(a: &[f64; 3], b: &[f64; 3]) -> f64 {
885:fn eigenvalues_3x3_symmetr
... (truncated, 5166 chars total)
```
</details>

Now let me read the last section and the helper functions section to understand dependencies:

<details>
<summary>Reading lines 3268-3805 of <a href="../src/stage2.rs#L3268-L3806">stage2.rs</a></summary>

```
// Stage 2 entry point
// ---------------------------------------------------------------------------

/// Run stage 2: fit surface hypotheses to mesh faces and select surfaces.
pub fn stage2(config: &Config, mut mesh: ConnectedMesh, viz: Option<&crate::viz::VizSender>) -> Result<Stage2Output, Stage2Error> {
    // Stage 2.1: Deduce planar hypotheses
    let viz_21 = if config.viz_active(2, 1) { viz } else { None };
    let (mut planar_hypotheses, planar_quit) = deduce_planar_hypotheses(&mut mesh, config.vertex_tolerance_mm, config.verbosity, viz_21);

    if !config.quiet {
        let multi_face_count = planar_hypotheses.iter().filter(|h| h.faces.len() > 1).count();
        let single_face_count = planar_hypotheses.len() - multi_face_count;
        let covered_faces: usize = planar_hypotheses.iter().map(|h| h.faces.len()).sum();
        eprintln!(
            "Stage 2.1: Deduced {} planar hypotheses ({} multi-face, {} single-face) covering {} of {} mesh faces",
            planar_hypotheses.len(),
            multi_face_count,
            single_face_count,
            covered_faces,
            mesh.faces.len(),
        );
        if config.verbose {
            for (i, h) in planar_hypotheses.iter().enumerate() {
                if h.faces.len() > 1 {
                    eprintln!(
                        "  Plane {}: {} faces, {} vertices, normal=[{:.4}, {:.4}, {:.4}], d={:.4}, err_max={:.2e}, err_min={:.2e}",
                        i, h.faces.len(), h.vertices.len(),
                        h.normal[0], h.normal[1], h.normal[2], h.distance,
                        h.error_max, h.error_min,
                    );
                }
            }
        }
    }

    // Compare against STEP file if --compare was specified
    if config.compare_shape.is_some() {
        compare_planar_hypotheses(&planar_hypotheses, &mesh, config)?;
        if !config.quiet {
            eprintln!("  Compare: all planar hypothesis centroids within tolerance");
        }
    }

    if planar_quit {
        return Ok(Stage2Output {
            mesh,
            planar_hypotheses,
            cylindrical_hypotheses: Vec::new(),
            spherical_hypotheses: Vec::new(),
            selected_surfaces: Vec::new(),
        });
    }



    if !config.stage.at_least(2, 2) {
        return Ok(Stage2Output {
            mesh,
            planar_hypotheses,
            cylindrical_hypotheses: Vec::new(),
            spherical_hypotheses: Vec::new(),
            selected_surfaces: Vec::new(),
        });
    }

    // Stage 2.2: Deduce cylindrical hypotheses
    let viz_22 = if config.viz_active(2, 2) { viz } else { None };
    let (mut cylindrical_hypotheses, cylindrical_quit) = deduce_cylindrical_hypotheses(
        &mut mesh, config.vertex_tolerance_mm,
        config.surface_tolerance_mm, config.angular_tolerance_rad,
        config.verbosity,
        viz_22,
    );

    if !config.quiet {
        let covered_faces: usize = cylindrical_hypotheses.iter().map(|h| h.faces.len()).sum();
        let convex_count = cylindrical_hypotheses.iter().filter(|h| h.convex).count();
        let concave_count = cylindrical_hypotheses.len() - convex_count;
        eprintln!(
            "Stage 2.2: Deduced {} cylindrical hypotheses ({} convex, {} concave) covering {} faces",
            cylindrical_hypotheses.len(),
            convex_count,
            concave_count,
            covered_faces,
        );
        if config.verbose {
            for (i, h) in cylindrical_hypotheses.iter().enumerate() {
                eprintln!(
                    "  Cylinder {}: {} faces, {} vertices, r={:.4}, {}, \
axis_origin=[{:.4}, {:.4}, {:.4}], axis_dir=[{:.4}, {:.4}, {:.4}], \
vtx_err_max={:.2e}, cen_err_max={:.2e}",
                    i, h.faces.len(), h.vertices.len(), h.radius,
                    if h.convex { "convex" } else { "concave" },
                    h.axis_origin[0], h.axis_origin[1], h.axis_origin[2],
                    h.axis_direction[0], h.axis_direc
... (truncated, 21156 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../src/stage2.rs">stage2.rs</a></summary>

```
3268:jn|// Stage 2 entry point
3269:sx|// ---------------------------------------------------------------------------
3270:dd|
3271:at|/// Run stage 2: fit surface hypotheses to mesh faces and select surfaces.
3272:ul|pub fn stage2(config: &Config, mut mesh: ConnectedMesh, viz: Option<&crate::viz::VizSender>) -> Result<Stage2Output, Stage2Error> {
3273:ig|    // Stage 2.1: Deduce planar hypotheses
3274:ke|    let viz_21 = if config.viz_active(2, 1) { viz } else { None };
3275:rb|    let (mut planar_hypotheses, planar_quit) = deduce_planar_hypotheses(&mut mesh, config.vertex_tolerance_mm, config.verbosity, viz_21);
3276:dd|
3277:mc|    if !config.quiet {
3278:tu|        let multi_face_count = planar_hypotheses.iter().filter(|h| h.faces.len() > 1).count();
3279:ja|        let single_face_count = planar_hypotheses.len() - multi_face_count;
3280:eg|        let covered_faces: usize = planar_hypotheses.iter().map(|h| h.faces.len()).sum();
3281:ih|        eprintln!(
3282:bt|            "Stage 2.1: Deduced {} planar hypotheses ({} multi-face, {} single-face) covering {} of {} mesh faces",
3283:wt|            planar_hypotheses.len(),
3284:kr|            multi_face_count,
3285:vl|            single_face_count,
3286:un|            covered_faces,
3287:ih|            mesh.faces.len(),
3288:nw|        );
3289:vj|        if config.verbose {
3290:nl|            for (i, h) in planar_hypotheses.iter().enumerate() {
3291:sn|                if h.faces.len() > 1 {
3292:mg|                    eprintln!(
3293:by|                        "  Plane {}: {} faces, {} vertices, normal=[{:.4}, {:.4}, {:.4}], d={:.4}, err_max={:.2e}, err_min={:.2e}",
3294:ru|                        i, h.faces.len(), h.vertices.len(),
3295:cq|                        h.normal[0], h.normal[1], h.normal[2], h.distance,
3296:jz|                        h.error_max, h.error_min,
3297:hi|                    );
3298:gl|                }
3299:uo|            }
3300:cy|        }
3301:ax|    }
3302:dd|
3303:mk|    // Compare against STEP file if --compare was specified
3304:gi|    if config.compare_shape.is_some() {
3305:vm|        compare_planar_hypotheses(&planar_hypotheses, &mesh, config)?;
3306:ag|        if !config.quiet {
3307:ze|            eprintln!("  Compare: all planar hypothesis centroids within tolerance");
3308:cy|        }
3309:ax|    }
3310:dd|
3311:vc|    if planar_quit {
3312:bu|        return Ok(Stage2Output {
3313:gq|            mesh,
3314:so|            planar_hypotheses,
3315:nj|            cylindrical_hypotheses: Vec::new(),
3316:sw|            spherical_hypotheses: Vec::new(),
3317:ye|            selected_surfaces: Vec::new(),
3318:ie|        });
3319:ax|    }
3320:dd|
3321:dd|
3322:dd|
3323:un|    if !config.stage.at_least(2, 2) {
3324:bu|        return Ok(Stage2Output {
3325:gq|            mesh,
3326:so|            planar_hypotheses,
3327:nj|            cylindrical_hypotheses: Vec::new(),
3328:sw|            spherical_hypotheses: Vec::new(),
3329:ye|            selected_surfaces: Vec::new(),
3330:ie|        });
3331:ax|    }
3332:dd|
3333:fe|    // Stage 2.2: Deduce cylindrical hypotheses
3334:wc|    let viz_22 = if config.viz_active(2, 2) { viz } else { None };
3335:bh|    let (mut cylindrical_hypotheses, cylindrical_quit) = deduce_cylindrical_hypotheses(
3336:vr|        &mut mesh, config.vertex_tolerance_mm,
3337:np|        config.surface_tolerance_mm, config.angular_tolerance_rad,
3338:ao|        config.verbosity,
3339:fh|        viz_22,
3340:pd|    );
3341:dd|
3342:mc|    if !config.quiet {
3343:ei|        let covered_faces: usize = cylindrical_hypotheses.iter().map(|h| h.faces.len()).sum();
3344:dn|        let convex_count = cylindrical_hypotheses.iter().filter(|h| h.convex).count();
3345:ui|        let concave_count = cylindrical_hypotheses.len() - convex_count;
3346:ih|        eprintln!(
3347:xt|            "Stage 2.2: Deduced {} cylindrical hypotheses ({} convex, {} concave) covering {} faces",
3348:ak|            cylindrical_hypotheses.len(),
3349:sd|  
... (truncated, 25460 chars)
```
</details>

Now I have a thorough understanding of the structure. Let me also check what functions the viz section and shared helpers use:

<details>
<summary>Reading lines 296-498 of <a href="../src/stage2.rs#L296-L499">stage2.rs</a></summary>

```
}

// ---------------------------------------------------------------------------
// BFS visualization helper
// ---------------------------------------------------------------------------

/// Compute the centroid of a mesh face (as f32 for viz).
fn viz_face_centroid(face_idx: usize, mesh: &ConnectedMesh) -> [f32; 3] {
    let face = &mesh.faces[face_idx];
    let vc = face.vertex_count as usize;
    let mut cx = 0.0f64;
    let mut cy = 0.0f64;
    let mut cz = 0.0f64;
    for i in 0..vc {
        let v = &mesh.vertices[face.vertex_indices[i]];
        cx += v.x;
        cy += v.y;
        cz += v.z;
    }
    [cx as f32 / vc as f32, cy as f32 / vc as f32, cz as f32 / vc as f32]
}

/// Return the normal of a mesh face as f32 (for viz camera orientation).
fn viz_face_normal(face_idx: usize, mesh: &ConnectedMesh) -> Option<[f32; 3]> {
    mesh.faces[face_idx].normal.map(|n| [n[0] as f32, n[1] as f32, n[2] as f32])
}

/// Send a BFS step visualization overlay and return the user's action.
/// `seed_faces`: faces shown in green (seed), `hyp_faces`: faces shown in blue
/// (hypothesis so far), `explored_face`: face shown in yellow (being evaluated),
/// `status`: text shown in the status bar.
/// Returns None if viz is inactive, otherwise the user's VizAction.
#[allow(clippy::too_many_arguments)]
fn viz_bfs_step(
    viz: Option<&VizSender>,
    seed_faces: &[usize],
    hyp_faces: &[usize],
    explored_face: usize,
    status: &str,
    cylinders: Vec<viz::CylinderOverlay>,
    spheres: Vec<viz::SphereOverlay>,
    background_faces: &[usize],
    mesh: &ConnectedMesh,
) -> Option<VizAction> {
    let viz_sender = viz?;
    let mut overlay = viz::VizOverlay::new();
    overlay.status_text = status.to_string();
    // Background (already-hypothesized) faces in gray
    if !background_faces.is_empty() {
        overlay.face_highlights.push(viz::FaceHighlight {
            face_indices: background_faces.to_vec(),
            color: [0.5, 0.5, 0.5, 1.0],
        });
    }
    // Seed faces in green
    if !seed_faces.is_empty() {
        overlay.face_highlights.push(viz::FaceHighlight {
            face_indices: seed_faces.to_vec(),
            color: [0.0, 0.8, 0.0, 1.0],
        });
    }
    // Hypothesis faces (accepted so far) in blue
    if !hyp_faces.is_empty() {
        overlay.face_highlights.push(viz::FaceHighlight {
            face_indices: hyp_faces.to_vec(),
            color: [0.2, 0.4, 1.0, 1.0],
        });
    }
    // Face being explored in yellow
    overlay.face_highlights.push(viz::FaceHighlight {
        face_indices: vec![explored_face],
        color: [1.0, 0.9, 0.0, 1.0],
    });
    overlay.cylinders = cylinders;
    overlay.spheres = spheres;
    overlay.focus_point = Some(viz_face_centroid(explored_face, mesh));
    overlay.focus_normal = viz_face_normal(explored_face, mesh);
    Some(viz_sender.show_and_wait(overlay))
}

/// Send a BFS seed visualization (before expansion starts).
fn viz_bfs_seed(
    viz: Option<&VizSender>,
    seed_faces: &[usize],
    status: &str,
    cylinders: Vec<viz::CylinderOverlay>,
    spheres: Vec<viz::SphereOverlay>,
    background_faces: &[usize],
    mesh: &ConnectedMesh,
) -> Option<VizAction> {
    let viz_sender = viz?;
    let mut overlay = viz::VizOverlay::new();
    overlay.status_text = status.to_string();
    // Background faces in gray
    if !background_faces.is_empty() {
        overlay.face_highlights.push(viz::FaceHighlight {
            face_indices: background_faces.to_vec(),
            color: [0.5, 0.5, 0.5, 1.0],
        });
    }
    overlay.face_highlights.push(viz::FaceHighlight {
        face_indices: seed_faces.to_vec(),
        color: [0.0, 0.8, 0.0, 1.0],
    });
    overlay.cylinders = cylinders;
    overlay.spheres = spheres;
    // Focus on first seed face
    if !seed_faces.is_empty() {
        overlay.focus_point = Some(viz_face_centroid(seed_faces[0], mesh));
        overlay.focus_normal = viz_face_normal(seed_faces[0], mesh);
    }
    Som
... (truncated, 7093 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../src/stage2.rs">stage2.rs</a></summary>

```
296:io|}
297:dd|
298:sx|// ---------------------------------------------------------------------------
299:ux|// BFS visualization helper
300:sx|// ---------------------------------------------------------------------------
301:dd|
302:su|/// Compute the centroid of a mesh face (as f32 for viz).
303:xs|fn viz_face_centroid(face_idx: usize, mesh: &ConnectedMesh) -> [f32; 3] {
304:pc|    let face = &mesh.faces[face_idx];
305:or|    let vc = face.vertex_count as usize;
306:vo|    let mut cx = 0.0f64;
307:ov|    let mut cy = 0.0f64;
308:xp|    let mut cz = 0.0f64;
309:wm|    for i in 0..vc {
310:qz|        let v = &mesh.vertices[face.vertex_indices[i]];
311:dn|        cx += v.x;
312:xx|        cy += v.y;
313:zb|        cz += v.z;
314:ax|    }
315:fs|    [cx as f32 / vc as f32, cy as f32 / vc as f32, cz as f32 / vc as f32]
316:io|}
317:dd|
318:ry|/// Return the normal of a mesh face as f32 (for viz camera orientation).
319:fe|fn viz_face_normal(face_idx: usize, mesh: &ConnectedMesh) -> Option<[f32; 3]> {
320:hq|    mesh.faces[face_idx].normal.map(|n| [n[0] as f32, n[1] as f32, n[2] as f32])
321:io|}
322:dd|
323:ld|/// Send a BFS step visualization overlay and return the user's action.
324:fa|/// `seed_faces`: faces shown in green (seed), `hyp_faces`: faces shown in blue
325:dl|/// (hypothesis so far), `explored_face`: face shown in yellow (being evaluated),
326:hm|/// `status`: text shown in the status bar.
327:pe|/// Returns None if viz is inactive, otherwise the user's VizAction.
328:wr|#[allow(clippy::too_many_arguments)]
329:ve|fn viz_bfs_step(
330:pl|    viz: Option<&VizSender>,
331:xn|    seed_faces: &[usize],
332:bb|    hyp_faces: &[usize],
333:gd|    explored_face: usize,
334:od|    status: &str,
335:fn|    cylinders: Vec<viz::CylinderOverlay>,
336:zt|    spheres: Vec<viz::SphereOverlay>,
337:aw|    background_faces: &[usize],
338:or|    mesh: &ConnectedMesh,
339:oe|) -> Option<VizAction> {
340:ff|    let viz_sender = viz?;
341:pf|    let mut overlay = viz::VizOverlay::new();
342:vw|    overlay.status_text = status.to_string();
343:lv|    // Background (already-hypothesized) faces in gray
344:wu|    if !background_faces.is_empty() {
345:dt|        overlay.face_highlights.push(viz::FaceHighlight {
346:wk|            face_indices: background_faces.to_vec(),
347:mv|            color: [0.5, 0.5, 0.5, 1.0],
348:ie|        });
349:ax|    }
350:yb|    // Seed faces in green
351:pd|    if !seed_faces.is_empty() {
352:dt|        overlay.face_highlights.push(viz::FaceHighlight {
353:hb|            face_indices: seed_faces.to_vec(),
354:bk|            color: [0.0, 0.8, 0.0, 1.0],
355:ie|        });
356:ax|    }
357:jc|    // Hypothesis faces (accepted so far) in blue
358:tn|    if !hyp_faces.is_empty() {
359:dt|        overlay.face_highlights.push(viz::FaceHighlight {
360:hr|            face_indices: hyp_faces.to_vec(),
361:en|            color: [0.2, 0.4, 1.0, 1.0],
362:ie|        });
363:ax|    }
364:pd|    // Face being explored in yellow
365:vk|    overlay.face_highlights.push(viz::FaceHighlight {
366:cy|        face_indices: vec![explored_face],
367:by|        color: [1.0, 0.9, 0.0, 1.0],
368:ew|    });
369:hi|    overlay.cylinders = cylinders;
370:pr|    overlay.spheres = spheres;
371:ec|    overlay.focus_point = Some(viz_face_centroid(explored_face, mesh));
372:pw|    overlay.focus_normal = viz_face_normal(explored_face, mesh);
373:pr|    Some(viz_sender.show_and_wait(overlay))
374:io|}
375:dd|
376:lk|/// Send a BFS seed visualization (before expansion starts).
377:ao|fn viz_bfs_seed(
378:pl|    viz: Option<&VizSender>,
379:xn|    seed_faces: &[usize],
380:od|    status: &str,
381:fn|    cylinders: Vec<viz::CylinderOverlay>,
382:zt|    spheres: Vec<viz::SphereOverlay>,
383:aw|    background_faces: &[usize],
384:or|    mesh: &ConnectedMesh,
385:oe|) -> Option<VizAction> {
386:ff|    let viz_sender = viz?;
387:pf|    let mut overlay = viz::VizOverlay::new();
388:vw|    overlay.status_text = status.to_string();
389:pa|    // Backg
... (truncated, 8514 chars)
```
</details>

**Todo list:**
- ✅ Analyze stage2.rs structure
- 🔄 Create planar fitting module
- ⬜ Create cylindrical fitting module
- ⬜ Create spherical fitting module
- ⬜ Update stage2.rs to use modules
- ⬜ Update lib.rs module declarations
- ⬜ Build and fix errors
- ⬜ Run tests
- ⬜ Run clippy
- ⬜ Update docs and commit

Now I have a thorough understanding. Let me plan the refactoring:

**Shared utilities** (stay in stage2.rs or go into a common module):
- `face_area` (lines 221-248) - used by planar, cylindrical, spherical, and selection
- `face_centroid` (lines 818-833) - used by cylindrical, spherical, and selection 
- `vertex_to_plane_distance` (lines 216-219) - used by planar  
- `fit_plane` (lines 250-281) - used by planar
- `all_vertices_within_tolerance` (lines 283-296) - used by planar
- Viz helpers (lines 298-479) - used by planar, cylindrical, spherical
- Math helpers: `normalize3`, `cross3`, `dot3`, `eigenvalues_3x3_symmetric`, `eigenvector_for_eigenvalue`, `smallest_eigenvector_3x3`, `perpendicular_basis` - used by cylindrical and spherical

Let me read the exact code sections I need now. Let me read the full file in chunks:

<details>
<summary>Reading lines 486-750 of <a href="../src/stage2.rs#L486-L751">stage2.rs</a></summary>

```
fn deduce_planar_hypotheses(
    mesh: &mut ConnectedMesh,
    vertex_tol: f64,
    verbosity: u8,
    viz: Option<&VizSender>,
) -> (Vec<PlanarHypothesis>, bool) {
    let num_faces = mesh.faces.len();
    let mut hypotheses: Vec<PlanarHypothesis> = Vec::new();
    let mut user_quit = false;

    for face in &mut mesh.faces {
        face.planar_hypothesis = UNDEDUCED_PLANAR_HYPOTHESIS;
    }

    for fi in 0..num_faces {
        if mesh.faces[fi].planar_hypothesis != UNDEDUCED_PLANAR_HYPOTHESIS {
            continue;
        }

        let hi = hypotheses.len() as i32;
        let seed_normal = mesh.faces[fi].normal.unwrap();

        // Compute initial plane from seed face: normal from face, distance
        // as average projection of seed face vertices.
        let vc = mesh.faces[fi].vertex_count as usize;
        let mut sum_d = 0.0_f64;
        let mut vertex_set = HashSet::new();
        for vi_idx in 0..vc {
            let vi = mesh.faces[fi].vertex_indices[vi_idx];
            vertex_set.insert(vi);
            let v = &mesh.vertices[vi];
            sum_d += seed_normal[0] * v.x + seed_normal[1] * v.y + seed_normal[2] * v.z;
        }
        let mut current_normal = seed_normal;
        let mut current_distance = sum_d / vc as f64;
        let mut face_list: Vec<usize> = vec![fi];

        mesh.faces[fi].planar_hypothesis = hi;

        // Level-3 trace: print seed face info
        if verbosity >= 3 {
            eprintln!(
                "[BFS-plane hi={}] Seed fi={}: normal=[{:.4},{:.4},{:.4}], d={:.4}",
                hi, fi,
                current_normal[0], current_normal[1], current_normal[2],
                current_distance,
            );
            for vi_idx in 0..vc {
                let vi = mesh.faces[fi].vertex_indices[vi_idx];
                let v = &mesh.vertices[vi];
                eprintln!("  seed vertex vi={}: [{:.4},{:.4},{:.4}]", vi, v.x, v.y, v.z);
            }
        }

        // Collect background faces for viz: faces assigned to multi-face planar hypotheses
        let bg_faces: Vec<usize> = if viz.is_some() {
            (0..num_faces).filter(|&f| {
                let ph = mesh.faces[f].planar_hypothesis;
                ph >= 0 && hypotheses.get(ph as usize).is_some_and(|h| h.faces.len() > 1)
            }).collect()
        } else { Vec::new() };

        // Viz: show seed face
        let mut skip_viz = false;
        if let Some(action) = viz_bfs_seed(
            viz,
            &[fi],
            &format!("BFS-plane hi={hi}: seed fi={fi} [space=step, shift+space=skip]"),
            Vec::new(), Vec::new(), &bg_faces, mesh,
        ) {
            match action {
                VizAction::Quit => { user_quit = true; return (hypotheses, user_quit); }
                VizAction::NextSeed => { skip_viz = true; }
                VizAction::NextStep => {}
            }
        }

        // BFS expansion
        let mut queue = VecDeque::new();
        queue.push_back(fi);

        while let Some(current_fi) = queue.pop_front() {
            let vertex_count = mesh.faces[current_fi].vertex_count as usize;
            let neighbors = mesh.faces[current_fi].neighbors;

            for &ni in &neighbors[..vertex_count] {
                if ni < 0 {
                    continue;
                }
                let ni = ni as usize;

                if mesh.faces[ni].planar_hypothesis != UNDEDUCED_PLANAR_HYPOTHESIS {
                    if verbosity >= 4 {
                        eprintln!("  [BFS-plane] fi={ni}: already assigned hyp={} → skip", mesh.faces[ni].planar_hypothesis);
                    }
                    continue;
                }
                // Vertex distance check
                let nvc = mesh.faces[ni].vertex_count as usize;
                let nvi = mesh.faces[ni].vertex_indices;
                let mut all_ok = true;
                let mut any_far = false;
                let mut vtx_err_max = 0.0_f64;
                for &vi in &nvi[..nvc] {
    
... (truncated, 10637 chars)
```
</details>

<details>
<summary>Reading lines 751-816 of <a href="../src/stage2.rs#L751-L817">stage2.rs</a></summary>

```
}

// ---------------------------------------------------------------------------
// Stage 2.1: Compare check
// ---------------------------------------------------------------------------

/// Validate fitted planar hypotheses against a reference STEP shape.
///
/// For each hypothesis, projects face centroids onto the fitted plane and
/// checks that those projected points are within surface_tolerance of the
/// reference STEP surface.
fn compare_planar_hypotheses(
    hypotheses: &[PlanarHypothesis],
    mesh: &ConnectedMesh,
    config: &Config,
) -> Result<(), Stage2CompareError> {
    let compare_shape = config.compare_shape.as_ref().unwrap();

    for (hi, hyp) in hypotheses.iter().enumerate() {
        let mut max_dist = 0.0_f64;

        for &fi in &hyp.faces {
            let face = &mesh.faces[fi];
            let n = face.vertex_count as usize;
            let mut cx = 0.0_f64;
            let mut cy = 0.0_f64;
            let mut cz = 0.0_f64;
            for vi_idx in 0..n {
                let v = &mesh.vertices[face.vertex_indices[vi_idx]];
                cx += v.x;
                cy += v.y;
                cz += v.z;
            }
            let inv_n = 1.0 / n as f64;
            cx *= inv_n;
            cy *= inv_n;
            cz *= inv_n;

            // Project centroid onto the fitted plane
            let dist_to_plane = hyp.normal[0] * cx + hyp.normal[1] * cy + hyp.normal[2] * cz
                - hyp.distance;
            let px = cx - dist_to_plane * hyp.normal[0];
            let py = cy - dist_to_plane * hyp.normal[1];
            let pz = cz - dist_to_plane * hyp.normal[2];

            let pt = gp::Pnt::new_real3(px, py, pz);
            let d = stage1::min_distance_to_shape(&pt, compare_shape);
            max_dist = max_dist.max(d);
        }

        if max_dist > config.surface_tolerance_mm {
            return Err(Stage2CompareError {
                hypothesis_type: "planar",
                hypothesis_index: hi,
                max_distance: max_dist,
                tolerance: config.surface_tolerance_mm,
            });
        }
    }

    Ok(())
}
// ---------------------------------------------------------------------------
// Stage 2.2: Cylindrical hypothesis deduction
// ---------------------------------------------------------------------------

```
</details>

Now let me check exactly which functions the spherical code uses that overlap with cylindrical code:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n 'face_area\|face_centroid\|vertex_to_plane\|fit_plane\|all_vertices_within_tolerance\|normalize3\|cross3\|dot3\|eigenvalue\|eigenvector\|smallest_eigen\|perpendicular_basis\|build_normal_covariance\|viz_face_centroid\|viz_face_normal\|viz_bfs_step\|viz_bfs_seed\|viz_custom\|centered_cylinder\|REFIT_SKIP\|MIN_CROSS\|bounding_box_diagonal' src/stage2.rs | grep -v '^\s*//' | head -100
```
<details>
<summary>Output (100 lines)</summary>

```
32:const MIN_CROSS_THRESHOLD: f64 = 0.01;
35:/// has a vertex farther than `REFIT_SKIP_MULTIPLIER × vertex_tol` from the
38:const REFIT_SKIP_MULTIPLIER: f64 = 2.0;
58:/// Minimum eigenvalue ratio for solid-angle coverage validation.
60:/// 3D, so all eigenvalues of their covariance matrix are substantial.
216:fn vertex_to_plane_distance(v: &MeshVertex, normal: &[f64; 3], distance: f64) -> f64 {
221:fn face_area(face: &MeshFace, vertices: &[MeshVertex]) -> f64 {
250:fn fit_plane(
263:        let area = face_area(face, vertices);
283:fn all_vertices_within_tolerance(
291:        if vertex_to_plane_distance(&vertices[vi], normal, distance).abs() > tolerance {
303:fn viz_face_centroid(face_idx: usize, mesh: &ConnectedMesh) -> [f32; 3] {
319:fn viz_face_normal(face_idx: usize, mesh: &ConnectedMesh) -> Option<[f32; 3]> {
329:fn viz_bfs_step(
371:    overlay.focus_point = Some(viz_face_centroid(explored_face, mesh));
372:    overlay.focus_normal = viz_face_normal(explored_face, mesh);
377:fn viz_bfs_seed(
404:        overlay.focus_point = Some(viz_face_centroid(seed_faces[0], mesh));
405:        overlay.focus_normal = viz_face_normal(seed_faces[0], mesh);
412:fn viz_custom(
435:fn centered_cylinder_overlay(
459:        let c = face_centroid(&mesh.faces[fi], &mesh.vertices);
461:        let t = dot3(&d, &direction);
550:        if let Some(action) = viz_bfs_seed(
590:                    let d = vertex_to_plane_distance(
599:                        if abs_d > REFIT_SKIP_MULTIPLIER * vertex_tol {
610:                            "  [BFS-plane] from fi={} try ni={}: vtx_err_max={:.2e} > REFIT_SKIP*tol={:.2e} → REJECT(too far)",
611:                            current_fi, ni, vtx_err_max, REFIT_SKIP_MULTIPLIER * vertex_tol,
626:                    let (new_normal, new_distance) = fit_plane(
634:                    if !all_vertices_within_tolerance(
678:                    if let Some(action) = viz_bfs_step(
695:            fit_plane(&face_list, &vertex_set, &mesh.faces, &mesh.vertices);
702:            let d = vertex_to_plane_distance(&mesh.vertices[vi], &final_normal, final_distance);
727:            if let Some(action) = viz_custom(
732:                Some(viz_face_centroid(fi, mesh)),
733:                viz_face_normal(fi, mesh),
818:fn face_centroid(face: &MeshFace, vertices: &[MeshVertex]) -> [f64; 3] {
856:fn normalize3(v: &mut [f64; 3]) -> f64 {
867:fn cross3(a: &[f64; 3], b: &[f64; 3]) -> [f64; 3] {
876:fn dot3(a: &[f64; 3], b: &[f64; 3]) -> f64 {
880:/// Compute the smallest eigenvector of a 3x3 symmetric matrix.
882:/// Returns the eigenvector corresponding to the smallest eigenvalue.
883:/// Compute eigenvalues of a 3x3 symmetric matrix stored as upper triangle
884:/// [m00, m01, m02, m11, m12, m22]. Returns eigenvalues in unspecified order.
885:fn eigenvalues_3x3_symmetric(m: &[f64; 6]) -> [f64; 3] {
914:/// Compute the eigenvector of a 3x3 symmetric matrix for a given eigenvalue.
916:fn eigenvector_
... (truncated, 6357 chars total)
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n 'face_area\|face_centroid\|vertex_to_plane\|fit_plane\|all_vertices_within_tolerance\|normalize3\|cross3\|dot3\|eigenvalue\|eigenvector\|smallest_eigen\|perpendicular_basis\|build_normal_covariance\|viz_face_centroid\|viz_face_normal\|viz_bfs_step\|viz_bfs_seed\|viz_custom\|centered_cylinder\|REFIT_SKIP\|MIN_CROSS\|bounding_box_diagonal' src/stage2.rs | grep -v '^\s*//' | awk -F: '{print $1}' | sort -n | uniq -c | awk '$1 > 1 || $2 > 2100' | head -50
```
<details>
<summary>Output (38 lines)</summary>

```
   1 2137
   1 2278
   1 2285
   1 2306
   1 2312
   1 2316
   1 2333
   1 2334
   1 2488
   1 2504
   1 2557
   1 2579
   1 2644
   1 2682
   1 2691
   1 2692
   1 2699
   1 2704
   1 2707
   1 2708
   1 2763
   1 2811
   1 2872
   1 2882
   1 2883
   1 2902
   1 2922
   1 2932
   1 2933
   1 2977
   1 3039
   1 3040
   1 3088
   1 3182
   1 3204
   1 3369
   1 3429
   1 3464
```
</details>

Good. Let me also check which functions are used in the spherical section by grepping the relevant calls:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && sed -n '2133,3020p' src/stage2.rs | grep -o 'face_area\|face_centroid\|vertex_to_plane\|fit_plane\|all_vertices_within_tolerance\|normalize3\|cross3\|dot3\|eigenvalue\|eigenvector\|smallest_eigen\|perpendicular_basis\|build_normal_covariance\|viz_face_centroid\|viz_face_normal\|viz_bfs_step\|viz_bfs_seed\|viz_custom\|centered_cylinder\|REFIT_SKIP\|MIN_CROSS\|bounding_box_diagonal\|MIN_SPHERE\|MAX_SPHERE\|face_area' | sort | uniq -c | sort -rn
```
<details>
<summary>Output (13 lines)</summary>

```
   8 face_centroid
   6 REFIT_SKIP
   4 MIN_SPHERE
   3 dot3
   2 viz_face_normal
   2 viz_face_centroid
   2 viz_custom
   2 eigenvalue
   1 viz_bfs_step
   1 viz_bfs_seed
   1 normalize3
   1 face_area
   1 bounding_box_diagonal
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && sed -n '3020,3270p' src/stage2.rs | grep -o 'face_area\|face_centroid\|vertex_to_plane\|fit_plane\|all_vertices_within_tolerance\|normalize3\|cross3\|dot3\|eigenvalue\|eigenvector\|smallest_eigen\|perpendicular_basis\|build_normal_covariance\|viz_face_centroid\|viz_face_normal\|viz_bfs_step\|viz_bfs_seed\|viz_custom\|centered_cylinder\|REFIT_SKIP\|MIN_CROSS\|bounding_box_diagonal\|MIN_SPHERE\|MAX_SPHERE\|face_area' | sort | uniq -c | sort -rn
```
```
   3 face_area
   1 face_centroid
   1 dot3
```

Now I have a clear picture. Here's the plan:

**Shared utilities** that multiple modules need:
- `face_area`, `face_centroid`, `normalize3`, `cross3`, `dot3` - used across planar, cylindrical, spherical
- Now I have a clear picture. Here's the plan:

**Shared utilities** that multiple modules need:
- `face_area`, `face_centroid`, `normalize3`, `cross3`, `dot3` - used across planar, cylindrical, spherical
- `eigenvalues_3x3_symmetric`, `eigenvector_for_eigenvalue`, `smallest_eigenvector_3x3` - used by cylindrical and spherical  
- `perpendicular_basis`, `build_normal_covariance` - used by cylindrical (and indirectly spherical)
- `REFIT_SKIP_MULTIPLIER`, `MIN_CROSS_THRESHOLD` - used across multiple
- Viz helpers (`viz_face_centroid`, `viz_face_normal`, `viz_bfs_step`, `viz_bfs_seed`, `viz_custom`, `centered_cylinder_overlay`) - used across all three

My approach: Create a `stage2/` module directory with:
- `stage2/mod.rs` - public types, shared utilities, entry point, stage 2.6 selection
- `stage2/planar.rs` - planar fitting
- `stage2/cylindrical.rs` - cylindrical fitting  
- `stage2/spherical.rs` - spherical fitting

Let me now read all the code I need to put into each file:

<details>
<summary>Reading lines 815-2132 of <a href="../src/stage2.rs#L815-L2133">stage2.rs</a></summary>

```
// ---------------------------------------------------------------------------

/// Compute the face centroid for a mesh face.
fn face_centroid(face: &MeshFace, vertices: &[MeshVertex]) -> [f64; 3] {
    let n = face.vertex_count as usize;
    let mut cx = 0.0_f64;
    let mut cy = 0.0_f64;
    let mut cz = 0.0_f64;
    for vi_idx in 0..n {
        let v = &vertices[face.vertex_indices[vi_idx]];
        cx += v.x;
        cy += v.y;
        cz += v.z;
    }
    let inv_n = 1.0 / n as f64;
    [cx * inv_n, cy * inv_n, cz * inv_n]
}

/// Compute the distance from a vertex to a cylinder surface.
/// Returns the signed distance: positive if outside the cylinder, negative if inside.
fn vertex_to_cylinder_distance(
    v: &MeshVertex,
    axis_origin: &[f64; 3],
    axis_direction: &[f64; 3],
    radius: f64,
) -> f64 {
    // Vector from axis origin to vertex
    let dx = v.x - axis_origin[0];
    let dy = v.y - axis_origin[1];
    let dz = v.z - axis_origin[2];
    // Project onto axis
    let t = dx * axis_direction[0] + dy * axis_direction[1] + dz * axis_direction[2];
    // Radial vector (perpendicular to axis)
    let rx = dx - t * axis_direction[0];
    let ry = dy - t * axis_direction[1];
    let rz = dz - t * axis_direction[2];
    let radial_dist = (rx * rx + ry * ry + rz * rz).sqrt();
    radial_dist - radius
}

/// Normalize a 3D vector in place, return its length.
fn normalize3(v: &mut [f64; 3]) -> f64 {
    let len = (v[0] * v[0] + v[1] * v[1] + v[2] * v[2]).sqrt();
    if len > 0.0 {
        v[0] /= len;
        v[1] /= len;
        v[2] /= len;
    }
    len
}

/// Cross product of two 3D vectors.
fn cross3(a: &[f64; 3], b: &[f64; 3]) -> [f64; 3] {
    [
        a[1] * b[2] - a[2] * b[1],
        a[2] * b[0] - a[0] * b[2],
        a[0] * b[1] - a[1] * b[0],
    ]
}

/// Dot product of two 3D vectors.
fn dot3(a: &[f64; 3], b: &[f64; 3]) -> f64 {
    a[0] * b[0] + a[1] * b[1] + a[2] * b[2]
}

/// Compute the smallest eigenvector of a 3x3 symmetric matrix.
/// The matrix is stored as [m00, m01, m02, m11, m12, m22] (upper triangle).
/// Returns the eigenvector corresponding to the smallest eigenvalue.
/// Compute eigenvalues of a 3x3 symmetric matrix stored as upper triangle
/// [m00, m01, m02, m11, m12, m22]. Returns eigenvalues in unspecified order.
fn eigenvalues_3x3_symmetric(m: &[f64; 6]) -> [f64; 3] {
    let a00 = m[0]; let a01 = m[1]; let a02 = m[2];
    let a11 = m[3]; let a12 = m[4]; let a22 = m[5];

    let c2 = a00 + a11 + a22;
    let c1 = a00 * a11 - a01 * a01 + a00 * a22 - a02 * a02 + a11 * a22 - a12 * a12;
    let c0 = a00 * (a11 * a22 - a12 * a12)
            - a01 * (a01 * a22 - a12 * a02)
            + a02 * (a01 * a12 - a11 * a02);

    let p = c1 - c2 * c2 / 3.0;
    let q = -2.0 * c2 * c2 * c2 / 27.0 + c1 * c2 / 3.0 - c0;

    if p.abs() < 1e-30 {
        let ev = c2 / 3.0;
        return [ev, ev, ev];
    }

    let neg_p_3 = (-p / 3.0).max(0.0);
    let r = neg_p_3.sqrt();
    let cos_arg = (-q / (2.0 * neg_p_3 * r)).clamp(-1.0, 1.0);
    let theta = cos_arg.acos() / 3.0;
    let shift = c2 / 3.0;
    let two_pi_3 = 2.0 * std::f64::consts::PI / 3.0;
    [2.0 * r * theta.cos() + shift,
     2.0 * r * (theta - two_pi_3).cos() + shift,
     2.0 * r * (theta - 2.0 * two_pi_3).cos() + shift]
}

/// Compute the eigenvector of a 3x3 symmetric matrix for a given eigenvalue.
/// Uses cross products of (M - λI) rows to find the null space.
fn eigenvector_for_eigenvalue(m: &[f64; 6], lambda: f64) -> [f64; 3] {
    let b00 = m[0] - lambda;
    let b11 = m[3] - lambda;
    let b22 = m[5] - lambda;

    let row0 = [b00, m[1], m[2]];
    let row1 = [m[1], b11, m[4]];
    let row2 = [m[2], m[4], b22];

    let candidates = [
        cross3(&row0, &row1),
        cross3(&row0, &row2),
        cross3(&row1, &row2),
    ];

    let mut best_idx = 0;
    let mut best_len_sq = 0.0;
    for (i, c) in candidates.iter().enumerate() {
        let len_sq = c[0] * c[0] + c[1] * c[1] + c[2] * c[2];
        if len_sq > best_l
... (truncated, 50083 chars)
```
</details>

<details>
<summary>Reading lines 2133-3020 of <a href="../src/stage2.rs#L2133-L3021">stage2.rs</a></summary>

```
// Stage 2.3: Spherical hypothesis deduction
// ---------------------------------------------------------------------------

/// Compute the bounding box diagonal of a mesh.
fn bounding_box_diagonal(vertices: &[MeshVertex]) -> f64 {
    if vertices.is_empty() {
        return 0.0;
    }
    let mut min = [f64::INFINITY; 3];
    let mut max = [f64::NEG_INFINITY; 3];
    for v in vertices {
        min[0] = min[0].min(v.x);
        min[1] = min[1].min(v.y);
        min[2] = min[2].min(v.z);
        max[0] = max[0].max(v.x);
        max[1] = max[1].max(v.y);
        max[2] = max[2].max(v.z);
    }
    let dx = max[0] - min[0];
    let dy = max[1] - min[1];
    let dz = max[2] - min[2];
    (dx * dx + dy * dy + dz * dz).sqrt()
}

/// Compute the distance from a vertex to a sphere surface.
/// Returns signed distance: positive if outside the sphere, negative if inside.
fn vertex_to_sphere_distance(
    v: &MeshVertex,
    center: &[f64; 3],
    radius: f64,
) -> f64 {
    let dx = v.x - center[0];
    let dy = v.y - center[1];
    let dz = v.z - center[2];
    (dx * dx + dy * dy + dz * dz).sqrt() - radius
}

/// Fit sphere parameters (center, radius) from a set of vertices.
/// Uses algebraic least-squares: |v - c|² = r², expanded linearly.
/// Returns (center, radius) or None if degenerate.
#[allow(clippy::needless_range_loop)]
fn fit_sphere(
    vertex_set: &HashSet<usize>,
    vertices: &[MeshVertex],
) -> Option<([f64; 3], f64)> {
    let n = vertex_set.len();
    if n < 4 {
        return None;
    }

    // Solve: 2*vx*cx + 2*vy*cy + 2*vz*cz + k = vx² + vy² + vz²
    // where k = r² - |c|²
    // Normal equations: A^T A x = A^T b
    // A = [[2*vx, 2*vy, 2*vz, 1], ...], b = [vx²+vy²+vz², ...]
    let mut ata = [[0.0_f64; 4]; 4];
    let mut atb = [0.0_f64; 4];

    for &vi in vertex_set {
        let v = &vertices[vi];
        let row = [2.0 * v.x, 2.0 * v.y, 2.0 * v.z, 1.0];
        let b_i = v.x * v.x + v.y * v.y + v.z * v.z;

        for i in 0..4 {
            for j in 0..4 {
                ata[i][j] += row[i] * row[j];
            }
            atb[i] += row[i] * b_i;
        }
    }

    // Solve 4x4 system using Gaussian elimination with partial pivoting
    let mut aug = [[0.0_f64; 5]; 4];
    for i in 0..4 {
        for j in 0..4 {
            aug[i][j] = ata[i][j];
        }
        aug[i][4] = atb[i];
    }

    for col in 0..4 {
        // Find pivot
        let mut max_row = col;
        let mut max_val = aug[col][col].abs();
        for row in (col + 1)..4 {
            if aug[row][col].abs() > max_val {
                max_val = aug[row][col].abs();
                max_row = row;
            }
        }
        if max_val < 1e-30 {
            return None; // Singular
        }
        if max_row != col {
            aug.swap(col, max_row);
        }
        let pivot = aug[col][col];
        for j in col..5 {
            aug[col][j] /= pivot;
        }
        for row in 0..4 {
            if row == col {
                continue;
            }
            let factor = aug[row][col];
            for j in col..5 {
                aug[row][j] -= factor * aug[col][j];
            }
        }
    }

    let cx = aug[0][4];
    let cy = aug[1][4];
    let cz = aug[2][4];
    let k = aug[3][4];

    let r_sq = k + cx * cx + cy * cy + cz * cz;
    if r_sq <= 0.0 {
        return None;
    }

    Some(([cx, cy, cz], r_sq.sqrt()))
}

/// Check if all vertices are within tolerance of a sphere surface.
fn all_vertices_within_sphere_tolerance(
    vertex_set: &HashSet<usize>,
    center: &[f64; 3],
    radius: f64,
    tolerance: f64,
    vertices: &[MeshVertex],
) -> bool {
    for &vi in vertex_set {
        if vertex_to_sphere_distance(&vertices[vi], center, radius).abs() > tolerance {
            return false;
        }
    }
    true
}

/// Determine convexity for a sphere: does the face normal point away from the center?
fn determine_sphere_convexity(
    face: &MeshFace,
    vertices: &[MeshVertex],
    center: &[
... (truncated, 33709 chars)
```
</details>

<details>
<summary>Reading lines 3018-3270 of <a href="../src/stage2.rs#L3018-L3271">stage2.rs</a></summary>

```
// ---------------------------------------------------------------------------
// Stage 2.6: Select surfaces for reconstruction
// ---------------------------------------------------------------------------

/// Greedy area-based surface selection.
///
/// Selects hypotheses greedily by total mesh face area (largest first).
/// This naturally resolves conflicts: real surface hypotheses cover many faces
/// with large total area, while bogus hypotheses cover minimal area and lose.
///
/// After selection, updates hypothesis face/vertex lists to reflect only the
/// faces actually assigned to each selected surface.
fn select_surfaces(
    mesh: &ConnectedMesh,
    planar_hypotheses: &mut [PlanarHypothesis],
    cylindrical_hypotheses: &mut [CylindricalHypothesis],
    spherical_hypotheses: &mut [SphericalHypothesis],
) -> Vec<SelectedSurface> {
    let num_faces = mesh.faces.len();

    // Step 1: Compute geometric area of each mesh face.
    let face_areas: Vec<f64> = (0..num_faces)
        .map(|fi| face_area(&mesh.faces[fi], &mesh.vertices))
        .collect();

    // Step 2: Build candidate list from multi-face hypotheses.
    // Each candidate tracks its hypothesis type/index and original face set.
    struct Candidate {
        surface_type: u8, // 0=planar, 1=cylindrical, 2=spherical
        hypothesis_index: usize,
        faces: Vec<usize>,
    }

    let mut candidates: Vec<Candidate> = Vec::new();

    for (i, hyp) in planar_hypotheses.iter().enumerate() {
        if hyp.faces.len() > 1 {
            candidates.push(Candidate {
                surface_type: 0,
                hypothesis_index: i,
                faces: hyp.faces.clone(),
            });
        }
    }
    for (i, hyp) in cylindrical_hypotheses.iter().enumerate() {
        candidates.push(Candidate {
            surface_type: 1,
            hypothesis_index: i,
            faces: hyp.faces.clone(),
        });
    }
    for (i, hyp) in spherical_hypotheses.iter().enumerate() {
        candidates.push(Candidate {
            surface_type: 2,
            hypothesis_index: i,
            faces: hyp.faces.clone(),
        });
    }

    // Step 3: Greedy loop — select candidate with largest remaining area.
    let mut assigned = vec![false; num_faces];
    let mut selected: Vec<SelectedSurface> = Vec::new();

    loop {
        let mut best_ci = None;
        let mut best_area = 0.0_f64;

        for (ci, cand) in candidates.iter().enumerate() {
            let area: f64 = cand.faces.iter()
                .filter(|fi| !assigned[**fi])
                .map(|fi| face_areas[*fi])
                .sum();
            if area > best_area {
                best_area = area;
                best_ci = Some(ci);
            }
        }

        let Some(ci) = best_ci else { break };

        // Collect still-unassigned faces for the winning candidate.
        let sel_faces: Vec<usize> = candidates[ci].faces.iter()
            .filter(|fi| !assigned[**fi])
            .copied()
            .collect();

        for &fi in &sel_faces {
            assigned[fi] = true;
        }

        // Build vertex set for the selected faces.
        let sel_vertices: Vec<usize> = {
            let mut vset = HashSet::new();
            for &fi in &sel_faces {
                let f = &mesh.faces[fi];
                for vi in 0..f.vertex_count as usize {
                    vset.insert(f.vertex_indices[vi]);
                }
            }
            let mut v: Vec<usize> = vset.into_iter().collect();
            v.sort_unstable();
            v
        };

        // Update hypothesis face/vertex lists to reflect only the assigned faces,
        // and create the SelectedSurface entry.
        let selected_surface = match candidates[ci].surface_type {
            0 => {
                let idx = candidates[ci].hypothesis_index;
                planar_hypotheses[idx].faces = sel_faces;
                planar_hypotheses[idx].vertices = sel_vertices;
                SelectedSurfa
... (truncated, 9791 chars)
```
</details>

Now let me read the raw file content for the sections I need to extract. First, the full planar section:

Read [stage2.rs](../src/stage2.rs#211-211), lines 211 to 812

Now let me read the cylindrical section:

Read [stage2.rs](../src/stage2.rs#813-813), lines 813 to 1350

Now I have enough context. This is a large refactor. Let me create all the files. The approach will be:

1. Convert `src/stage2.rs` into `src/stage2/mod.rs` (keeping shared code, types, entry point, selection)
2. Create `src/stage2/planar.rs` (planar-specific code)
3. Create `src/stage2/cylindrical.rs` (cylindrical-specific code)
4. Create `src/stage2/spherical.rs` (spherical-specific code)

Let me first rename the file to the module directory structure:

**Terminal:**
```sh
cd /Users/sethml/src/brepper/src && mkdir stage2_new && cp stage2.rs stage2_backup.rs
```

Now let me read the full cylindrical section to copy it:

Read [stage2.rs](../src/stage2.rs#1351-1351), lines 1351 to 2132

**Todo list:**
- ✅ Read LEARNINGS.md, README.md, DEVELOPMENT_PLAN.md
- ✅ Analyze stage2.rs structure
- ✅ Map cross-cutting dependencies
- 🔄 Read full stage2.rs code
- ⬜ Create stage2/mod.rs with shared code
- ⬜ Create stage2/planar.rs
- ⬜ Create stage2/cylindrical.rs
- ⬜ Create stage2/spherical.rs
- ⬜ Build and fix errors
- ⬜ Run tests and clippy
- ⬜ Update docs and commit

> 💭 Let me read the remaining sections of stage2.rs that I need to create the module files. I need to read:
> 1. Lines 1-296 (imports, constants, types, planar helpers)
> 2. Lines 298-479 (shared viz helpers)
> 3. Lines 486-812 (planar section + cylindrical start)
> 4. Lines 813-1350 (cylindrical section first half - already referenced from summary)
> 5. Lines 2133-3020 (spherical section)
> 6. Lines 3019-3805 (selection, entry point, tests)
> 
> Let me read all of these in parallel.

Let me read the remaining sections of `src/stage2.rs` that I need:

Read [stage2.rs](../src/stage2.rs#1-1), lines 1 to 500

Read [stage2.rs](../src/stage2.rs#500-500), lines 500 to 1000

Read [stage2.rs](../src/stage2.rs#1000-1000), lines 1000 to 1360

Read [stage2.rs](../src/stage2.rs#2132-2132), lines 2132 to 3805

> 💭 Now I have the complete picture of the file. Let me also check the current state of things - the conversation summary says a backup was created and a stage2_new directory exists. Let me verify.

Now I have the full picture. Let me check the current state of the working directory:

**Terminal:**
```sh
cd /Users/sethml/src/brepper/src && ls stage2* 2>/dev/null && ls -d stage2_new 2>/dev/null; echo "---"; ls lib.rs
```
<details>
<summary>Output (6 lines)</summary>

```
stage2_backup.rsstage2.rs

stage2_new:
stage2_new
---
lib.rs
```
</details>

**Todo list:**
- ✅ Read LEARNINGS.md, README.md, DEVELOPMENT_PLAN.md
- ✅ Analyze stage2.rs structure
- ✅ Map cross-cutting dependencies
- ✅ Read full stage2.rs code
- 🔄 Create stage2/mod.rs with shared code
- ⬜ Create stage2/planar.rs
- ⬜ Create stage2/cylindrical.rs
- ⬜ Create stage2/spherical.rs
- ⬜ Build and fix errors
- ⬜ Run tests and clippy
- ⬜ Update docs and commit

Now I need to remove the empty `stage2_new` dir, remove the old `src/stage2.rs`, create a  directory, and populate it with the four module files. Let me start by restructuring:

**Terminal:**
```sh
cd /Users/sethml/src/brepper/src && rm -rf stage2_new && mkdir -p stage2
```
```
(base) whitelily:src sethml$
```

Now let me create all four files. I'll start with `mod.rs` which contains the shared code, types, constants, selection logic, entry point, and tests:

Using "Create File"

Using "Create File"

Using "Create File"

Now create the main `mod.rs` with shared code, types, selection logic, entry point, and tests:

Using "Create File"

**Todo list:**
- ✅ Create stage2/planar.rs
- ✅ Create stage2/cylindrical.rs
- ✅ Create stage2/spherical.rs
- ✅ Create stage2/mod.rs
- 🔄 Remove old stage2.rs
- ⬜ Build and fix errors
- ⬜ Run tests
- ⬜ Run clippy
- ⬜ Update docs and commit

Now remove the old `src/stage2.rs` so Rust resolves  to the directory module:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && mv src/stage2.rs src/stage2_old.rs
```
```
(base) whitelily:brepper sethml$
```

**Todo list:**
- ✅ Create stage2/planar.rs
- ✅ Create stage2/cylindrical.rs
- ✅ Create stage2/spherical.rs
- ✅ Create stage2/mod.rs
- ✅ Remove old stage2.rs
- 🔄 Build and fix errors
- ⬜ Run tests
- ⬜ Run clippy
- ⬜ Update docs and commit

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo build 2>&1
```

It compiles. Let me fix those 3 unused import warnings:

Replacing 3 lines with 3 lines in [planar.rs](../src/stage2/planar.rs), [spherical.rs](../src/stage2/spherical.rs), [mod.rs](../src/stage2/mod.rs)

```

```

```

**Terminal:**
```sh
time cargo build 2>&1
```
```
   Compiling brepper v0.1.0 (/Users/sethml/src/brepper)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 2.42s
real  2.4s
```

Clean build. Now run tests:

**Todo list:**
- ✅ Create stage2/planar.rs
- ✅ Create stage2/cylindrical.rs
- ✅ Create stage2/spherical.rs
- ✅ Create stage2/mod.rs
- ✅ Remove old stage2.rs
- ✅ Build and fix errors
- 🔄 Run tests
- ⬜ Run clippy
- ⬜ Update docs and commit

**Terminal:**
```sh
time cargo test 2>&1
```

**Terminal:**
```sh
cargo test 2>&1 | grep -E '(test result|FAILED|failures)'
```

All 316 tests pass. Now clippy:

**Todo list:**
- ✅ Create stage2/planar.rs
- ✅ Create stage2/cylindrical.rs
- ✅ Create stage2/spherical.rs
- ✅ Create stage2/mod.rs
- ✅ Remove old stage2.rs
- ✅ Build and fix errors
- ✅ Run tests
- 🔄 Run clippy
- ⬜ Update docs and commit

**Terminal:**
```sh
time cargo clippy --all-targets 2>&1
```
```
    Checking brepper v0.1.0 (/Users/sethml/src/brepper)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 2.52s
real  2.6s
```

Zero clippy warnings. Now let me read the docs to update, and then commit.

**Todo list:**
- ✅ Create stage2/planar.rs
- ✅ Create stage2/cylindrical.rs
- ✅ Create stage2/spherical.rs
- ✅ Create stage2/mod.rs
- ✅ Remove old stage2.rs
- ✅ Build and fix errors
- ✅ Run tests
- ✅ Run clippy
- 🔄 Update docs and commit

Read [LEARNINGS.md](../LEARNINGS.md#1-1), lines 1 to 100

Read [README.md](../README.md#1-1), lines 1 to 50

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#1-1), lines 1 to 100

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#100-100), lines 100 to 260

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#260-260), lines 260 to 640

The `DEVELOPMENT_PLAN.md` mentions the project layout with `src/stage2.rs` — I should update it. Let me also check the README for any mention of the file structure:

<details>
<summary>Searched for regex <code>stage2\.rs</code> (<code>**/*.md</code>), 20 results</summary>

- [2026-03-02_00-10_log.md:1137](../agent-logs/2026-03-02_00-10_log.md#L1137)
- [2026-03-02_00-10_log.md:1139](../agent-logs/2026-03-02_00-10_log.md#L1139)
- [2026-03-02_00-10_log.md:1142](../agent-logs/2026-03-02_00-10_log.md#L1142)
- [2026-03-08_21-49_log.md:160](../agent-logs/2026-03-08_21-49_log.md#L160)
- [2026-03-08_21-49_log.md:194](../agent-logs/2026-03-08_21-49_log.md#L194)
- [2026-03-08_21-49_log.md:201](../agent-logs/2026-03-08_21-49_log.md#L201)
- [2026-03-08_21-49_log.md:212](../agent-logs/2026-03-08_21-49_log.md#L212)
- [2026-03-08_21-49_log.md:214](../agent-logs/2026-03-08_21-49_log.md#L214)
- [2026-03-08_21-49_log.md:215](../agent-logs/2026-03-08_21-49_log.md#L215)
- [2026-03-08_21-49_log.md:216](../agent-logs/2026-03-08_21-49_log.md#L216)
- [2026-03-08_21-49_log.md:217](../agent-logs/2026-03-08_21-49_log.md#L217)
- [2026-03-08_21-49_log.md:218](../agent-logs/2026-03-08_21-49_log.md#L218)
- [2026-03-08_21-49_log.md:219](../agent-logs/2026-03-08_21-49_log.md#L219)
</details>

`src/stage2.rs` references are only in agent logs, not in `README.md` or DEVELOPMENT_PLAN.md. Let me check AGENTS.md:

Searched for regex `stage2\.rs` (`**/AGENTS.md`), no results

Good — no references to `src/stage2.rs` in the docs that need updating. Now let me read back `LEARNINGS.md` to check if anything needs updating:

Read [LEARNINGS.md](../LEARNINGS.md#100-100), lines 100 to 150

Read [LEARNINGS.md](../LEARNINGS.md#150-150), lines 150 to 250

`LEARNINGS.md` doesn't need changes for this refactoring. Now let me clean up the backup files and commit:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && rm src/stage2_old.rs src/stage2_backup.rs
```
```
(base) whitelily:brepper sethml$
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git add -A && git status
```
<details>
<summary>Output (11 lines)</summary>

```
On branch main
Your branch is ahead of 'origin/main' by 46 commits.
  (use "git push" to publish your local commits)

Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
deleted:    src/stage2.rs
new file:   src/stage2/cylindrical.rs
new file:   src/stage2/mod.rs
new file:   src/stage2/planar.rs
new file:   src/stage2/spherical.rs
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git commit -F - <<'EOF'
Refactor stage2 into separate modules for planar, cylindrical, and spherical fitting

Split the monolithic 3805-line src/stage2.rs into a directory module:
- src/stage2/mod.rs: shared types, constants, math utilities, viz helpers,
  selection logic, entry point, and tests
- src/stage2/planar.rs: planar hypothesis deduction and comparison (~320 lines)
- src/stage2/cylindrical.rs: cylindrical hypothesis deduction and comparison (~780 lines)
- src/stage2/spherical.rs: spherical hypothesis deduction and comparison (~620 lines)

Shared code (face_area, face_centroid, normalize3, cross3, dot3, eigenvalue
functions, perpendicular_basis, build_normal_covariance, viz helpers) stays
in mod.rs and is accessed via pub(super) visibility. No functional changes -
all 316 tests pass, zero clippy warnings.

---
Generated with assistance from Claude Opus 4 (Anthropic, 2025)

User prompt(s):
- "iterate by Refactor cylindrical fitting and spherical fitting and planar fitting into their own source files."
EOF
```
<details>
<summary>Output (7 lines)</summary>

```
[main cb3a691] Refactor stage2 into separate modules for planar, cylindrical, and spherical fitting
 5 files changed, 3830 insertions(+), 3806 deletions(-)
 delete mode 100644 src/stage2.rs
 create mode 100644 src/stage2/cylindrical.rs
 create mode 100644 src/stage2/mod.rs
 create mode 100644 src/stage2/planar.rs
 create mode 100644 src/stage2/spherical.rs
```
</details>

**Todo list:**
- ✅ Create stage2/planar.rs
- ✅ Create stage2/cylindrical.rs
- ✅ Create stage2/spherical.rs
- ✅ Create stage2/mod.rs
- ✅ Remove old stage2.rs
- ✅ Build and fix errors
- ✅ Run tests
- ✅ Run clippy
- ✅ Update docs and commit

Now export the chat log:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && ( ../vscode-export-chat-log/export-chat-log.py --wait && git add agent-logs/ && git commit --amend --no-edit ) &
```
```
(base) whitelily:brepper sethml$
```

Done. Here's what I accomplished:

**Refactored `src/stage2.rs` (3805 lines) into a directory module `src/stage2/`:**

- `src/stage2/planar.rs` (~320 lines) — planar hypothesis deduction and comparison
- `src/stage2/cylindrical.rs` (~780 lines) — cylindrical hypothesis deduction and comparison  
- `src/stage2/spherical.rs` (~620 lines) — spherical hypothesis deduction and comparison
- `src/stage2/mod.rs` — shared types, constants, math utilities, viz helpers, selection logic, entry point, and tests

No functional changes. All 316 tests pass, zero clippy warnings.

---

*A file so tall it scraped the sky,*
*Three thousand lines stacked high and dry.*
*We split the planes, the spheres, the tubes,*
*Into their own respective cubes—*
*Now each fits neatly, organized and spry!*

*2026-03-09 18:51 · 1411s · Words: 36,820 in · 16,175 out · 74,951 ctx · 67 rounds*

---
