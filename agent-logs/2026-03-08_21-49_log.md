# BFS Algorithm Revision and Code Review

- **Date:** 2026-03-08 21:49 – 22:30
- **Session ID:** 38c9190f-ea9b-4513-8d81-0c5033b4d22b
- **Model:** Claude Opus 4.6
- **Turns:** 1
- **Tool calls:** 31
- **Thinking blocks:** 3
- **Input words:** 5,749
- **Output words:** 4,379
- **Prompt tokens (last round):** 77,648
- **API rounds:** 25
- **Total elapsed:** 6m34s

## Table of Contents

1. [Iterate: I&#x27;ve rewritten the algorithm of stage 2.2 to try to ensure that BFS is seeding from 3 fa...](#user-1) (Claude Opus 4.6)

---

<a id="user-1"></a>

## User (1)

> Iterate: I&#x27;ve rewritten the algorithm of stage 2.2 to try to ensure that BFS is seeding from 3 faces, and try to ensure that BFS is exploring in all directions, and applying validity criteria consistently. Look at the diff of the HEAD commit to see the changes. Think about whether this is likely to improve cylinder deduction. Rewrite any sections that seem incorrect or incomplete. Then commit. Don&#x27;t change any code.

*2026-03-08 22:23*

### Assistant — Claude Opus 4.6

> 💭 The user wants me to:
> 1. Read LEARNINGS.md, README.md, DEVELOPMENT_PLAN.md
> 2. Look at the diff of the HEAD commit to see the changes to the algorithm description
> 3. Think about whether the rewritten algorithm is likely to improve cylinder deduction
> 4. Rewrite any sections that seem incorrect or incomplete
> 5. Update DEVELOPMENT_PLAN.md
> 6. Commit changes
> 
> Let me start by reading the relevant files and the diff.

**Todo list:**
- 🔄 Read LEARNINGS.md, README.md
- ⬜ View HEAD commit diff
- ⬜ Analyze algorithm correctness
- ⬜ Fix incorrect/incomplete sections
- ⬜ Commit changes

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git diff HEAD~1 HEAD -- DEVELOPMENT_PLAN.md | head -500
```
<details>
<summary>Output (71 lines)</summary>

```
diff --git a/DEVELOPMENT_PLAN.md b/DEVELOPMENT_PLAN.md
index 0cc4a9c..0ee2428 100644
--- a/DEVELOPMENT_PLAN.md
+++ b/DEVELOPMENT_PLAN.md
@@ -223,29 +223,44 @@ Fit cylindrical hypotheses to connected sets of faces that lie on a common cylin
 
 The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from pairs of adjacent faces (since a single triangle is always planar and cannot determine curvature). A key challenge is that **any two non-coplanar faces define some cylinder** — the cross product of their normals gives an axis, and a circle fit always produces a radius. The previous algorithm took the first valid seed and committed to it, which meant a bogus 3-face cylinder from an unfortunate seed pair could permanently consume faces that should belong to a correct 20-face cylinder seeded from a different pair. The fix is multi-seed evaluation with angular coverage validation:
 
+**Pairwise qualification:**
+Two faces fi and ni are pairwise qualified to participate in a cylindrical hypothesis if:
+- fi and ni are neighbors.
+- fi and ni are unassigned (`cylindrical_hypothesis == -2`).
+- fi and ni have a sufficiently different normal (cross product magnitude exceeds `MIN_CROSS_THRESHOLD`, e.g. 0.01).
+- The dihedral angle between fi and ni does not exceed `--angular-tolerance` (default 17.5°).
+
+**Cylinder fit qualification:**
+A face or set of faces is cylinder fit qualified if:
+- All vertices are within `--vertex-tolerance` of the cylinder.
+- All face centroids are within `--surface-tolerance` of the cylinder.
+- **Convexity check**: compute the vector from the axis to the face's centroid. Check that the face's normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.
+
 **Multi-seed evaluation:**
 - Initialize all `cylindrical_hypothesis` to `UNDEDUCED` (-2).
 - For each face fi where `cylindrical_hypothesis == -2`:
-    - Collect **all** valid seed partners: for each neighbor ni of fi, check that ni is also unassigned (`cylindrical_hypothesis == -2`), has a sufficiently different normal (cross product magnitude exceeds `MIN_CROSS_THRESHOLD`, e.g. 0.01), and the dihedral angle between fi and ni does not exceed `--angular-tolerance` (default 17.5°). Additionally, the cylinder fit from the seed pair must succeed and all seed vertices must be within `--vertex-tolerance`. Collect all neighbors that pass these checks.
-    - If no valid seed partner found: set `cylindrical_hypothesis = -1`, skip. (This face will be considered for spherical or other hypotheses in later stages.)
-    - For each valid seed partner, run a **trial BFS** (see BFS expansion below) using temporary data structures (a local face list and vertex set — do not mutate `mesh.faces[].cylindrical_hypothesis` during trials). Each trial produces a candidate hypothesis with a face count and fitted p
... (truncated, 12439 chars total)
```
</details>

<details>
<summary>Tool output (run_in_terminal)</summary>

```
Note: The tool simplified the command to ` git diff HEAD~1 HEAD -- DEVELOPMENT_PLAN.md | head -500`, and this is the output of running that command instead:
diff --git a/DEVELOPMENT_PLAN.md b/DEVELOPMENT_PLAN.md
index 0cc4a9c..0ee2428 100644
--- a/DEVELOPMENT_PLAN.md
+++ b/DEVELOPMENT_PLAN.md
@@ -223,29 +223,44 @@ Fit cylindrical hypotheses to connected sets of faces that
 lie on a common cylin                                                           
 The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from p
airs of adjacent faces (since a single triangle is always planar and cannot determine curvature). A key challenge is that **any two non-coplanar faces define some cylinder** — the cross product of their normals gives an axis, and a circle fit always produces a radius. The previous algorithm took the first valid seed and committed to it, which meant a bogus 3-face cylinder from an unfortunate seed pair could permanently consume faces that should belong to a correct 20-face cylinder seeded from a different pair. The fix is multi-seed evaluation with angular coverage validation:                                                           
+**Pairwise qualification:**
+Two faces fi and ni are pairwise qualified to participate in a cylindrical hypo
thesis if:                                                                      +- fi and ni are neighbors.
+- fi and ni are unassigned (`cylindrical_hypothesis == -2`).
+- fi and ni have a sufficiently different normal (cross product magnitude excee
ds `MIN_CROSS_THRESHOLD`, e.g. 0.01).                                           +- The dihedral angle between fi and ni does not exceed `--angular-tolerance` (d
efault 17.5°).                                                                  +
+**Cylinder fit qualification:**
+A face or set of faces is cylinder fit qualified if:
+- All vertices are within `--vertex-tolerance` of the cylinder.
+- All face centroids are within `--surface-tolerance` of the cylinder.
+- **Convexity check**: compute the vector from the axis to the face's centroid.
 Check that the face's normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.               +
 **Multi-seed evaluation:**
 - Initialize all `cylindrical_hypothesis` to `UNDEDUCED` (-2).
 - For each face fi where `cylindrical_hypothesis == -2`:
-    - Collect **all** valid seed partners: for each neighbor ni of fi, check th
at ni is also unassigned (`cylindrical_hypothesis == -2`), has a sufficiently different normal (cross product magnitude exceeds `MIN_CROSS_THRESHOLD`, e.g. 0.01), and the dihedral angle between fi and ni does not exceed `--angular-tolerance` (default 17.5°). Additionally, the cylinder fit from the seed pair must succeed and all seed vertices must be within `--vertex-tolerance`. Collect all neighbors that pass these checks.                                                      -    - If no valid seed partner found: set `cylindrical_hypothesis = -1`, skip. 
(This face will be considered for spherical or other hypotheses in later stages.)                                                                               -    - For each valid seed partner, run a **trial BFS** (see BFS expansion below
) using temporary data structures (a local face list and vertex set — do not mutate `mesh.faces[].cylindrical_hypothesis` during trials). Each trial produces a candidate hypothesis with a face count and fitted parameters. After each trial completes (and passes centroid validation + minimum face count), compare against the **current best candidate** for this starting face; keep whichever has more faces. This avoids storing all trial results.                                    -    - **Early termination** of redundant trials: during a trial BFS, if a best 
candidate already exists, check whether the trial is rediscovering the same
... (truncated, 14327 chars)
```
</details>

Read [LEARNINGS.md](../LEARNINGS.md#1-1), lines 1 to 100

Read [LEARNINGS.md](../LEARNINGS.md#101-101), lines 101 to 200

**Todo list:**
- ✅ Read LEARNINGS.md, README.md
- 🔄 View HEAD commit diff
- ⬜ Analyze algorithm correctness
- ⬜ Fix incorrect/incomplete sections
- ⬜ Commit changes

Now let me read the full current state of section 2.2 in `DEVELOPMENT_PLAN.md` to analyze it carefully:

<details>
<summary>Reading lines 213-320 of <a href="../DEVELOPMENT_PLAN.md#L213-L321">DEVELOPMENT_PLAN.md</a></summary>

```
#### 2.2 Deduce cylindrical hypotheses
Fit cylindrical hypotheses to connected sets of faces that lie on a common cylinder. A cylindrical hypothesis consists of:
- `axis_origin: [f64; 3]` — A point on the cylinder axis.
- `axis_direction: [f64; 3]` — Unit direction vector along the axis.
- `radius: f64` — Radius of the cylinder (always positive).
- `convex: bool` — Whether face normals point away from the axis (convex, like the outside of a pipe) or toward it (concave, like the inside of a hole).
- `faces: Vec<usize>` — Mesh face indices that fit this hypothesis.
- `vertices: Vec<usize>` — Mesh vertex indices on this cylinder.
- `error_max: f64` — Maximum absolute distance from any vertex to the cylinder surface (i.e. max of `| ||v - axis_closest|| - radius |`).
- `error_abs_sum: f64` — Sum of absolute vertex-to-surface distances.

The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from pairs of adjacent faces (since a single triangle is always planar and cannot determine curvature). A key challenge is that **any two non-coplanar faces define some cylinder** — the cross product of their normals gives an axis, and a circle fit always produces a radius. The previous algorithm took the first valid seed and committed to it, which meant a bogus 3-face cylinder from an unfortunate seed pair could permanently consume faces that should belong to a correct 20-face cylinder seeded from a different pair. The fix is multi-seed evaluation with angular coverage validation:

**Pairwise qualification:**
Two faces fi and ni are pairwise qualified to participate in a cylindrical hypothesis if:
- fi and ni are neighbors.
- fi and ni are unassigned (`cylindrical_hypothesis == -2`).
- fi and ni have a sufficiently different normal (cross product magnitude exceeds `MIN_CROSS_THRESHOLD`, e.g. 0.01).
- The dihedral angle between fi and ni does not exceed `--angular-tolerance` (default 17.5°).

**Cylinder fit qualification:**
A face or set of faces is cylinder fit qualified if:
- All vertices are within `--vertex-tolerance` of the cylinder.
- All face centroids are within `--surface-tolerance` of the cylinder.
- **Convexity check**: compute the vector from the axis to the face's centroid. Check that the face's normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.

**Multi-seed evaluation:**
- Initialize all `cylindrical_hypothesis` to `UNDEDUCED` (-2).
- For each face fi where `cylindrical_hypothesis == -2`:
    - For each neighbor n1 of fi where n1 is pairwise qualified with fi:
        - For each neighbor n2 of n1 where n2 is not a neighbor of fi and n2 is pairwise qualified with n1:
            - Produce a cylinder fit to the set of vertices belonging to fi, n1, and n2.
            - If faces fi, n1, and n2 are cylinder fit qualified, accept this set of faces as a valid seed and perform BFS expansion on them using temporary data structures (a local face list and vertex set — do not mutate `mesh.faces[].cylindrical_hypothesis` during trials). The initial BFS queue should contain fi, n1, and n2, so we explore all of their neighbors. Each trial produces a candidate hypothesis with a face count and fitted parameters. After each trial completes with acceptance, compare against the **current best candidate** for this starting face; keep whichever has more area covered. This avoids storing all trial results.
- Accept the **best candidate** as a cylindrical hypothesis: create a cylindrical hypothesis, and for every face in the hypothesis, assign the face's cylindrical hypothesis field.

Future work (not currently implemented - in the future we'll evaluate whether these optimizations help):
    - **Early termination** of redundant trials: during a trial BFS, if a best candidate already exists, check whether the trial is rediscovering the same cylinder. After the trial adds a face, compare 
... (truncated, 15320 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
213:da|#### 2.2 Deduce cylindrical hypotheses
214:ev|Fit cylindrical hypotheses to connected sets of faces that lie on a common cylinder. A cylindrical hypothesis consists of:
215:cw|- `axis_origin: [f64; 3]` — A point on the cylinder axis.
216:ez|- `axis_direction: [f64; 3]` — Unit direction vector along the axis.
217:fv|- `radius: f64` — Radius of the cylinder (always positive).
218:dk|- `convex: bool` — Whether face normals point away from the axis (convex, like the outside of a pipe) or toward it (concave, like the inside of a hole).
219:lp|- `faces: Vec<usize>` — Mesh face indices that fit this hypothesis.
220:ua|- `vertices: Vec<usize>` — Mesh vertex indices on this cylinder.
221:zz|- `error_max: f64` — Maximum absolute distance from any vertex to the cylinder surface (i.e. max of `| ||v - axis_closest|| - radius |`).
222:ov|- `error_abs_sum: f64` — Sum of absolute vertex-to-surface distances.
223:dd|
224:rh|The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from pairs of adjacent faces (since a single triangle is always planar and cannot determine curvature). A key challenge is that **any two non-coplanar faces define some cylinder** — the cross product of their normals gives an axis, and a circle fit always produces a radius. The previous algorithm took the first valid seed and committed to it, which meant a bogus 3-face cylinder from an unfortunate seed pair could permanently consume faces that should belong to a correct 20-face cylinder seeded from a different pair. The fix is multi-seed evaluation with angular coverage validation:
225:dd|
226:ie|**Pairwise qualification:**
227:yv|Two faces fi and ni are pairwise qualified to participate in a cylindrical hypothesis if:
228:iv|- fi and ni are neighbors.
229:zo|- fi and ni are unassigned (`cylindrical_hypothesis == -2`).
230:kn|- fi and ni have a sufficiently different normal (cross product magnitude exceeds `MIN_CROSS_THRESHOLD`, e.g. 0.01).
231:fq|- The dihedral angle between fi and ni does not exceed `--angular-tolerance` (default 17.5°).
232:dd|
233:hs|**Cylinder fit qualification:**
234:hx|A face or set of faces is cylinder fit qualified if:
235:zc|- All vertices are within `--vertex-tolerance` of the cylinder.
236:dj|- All face centroids are within `--surface-tolerance` of the cylinder.
237:ia|- **Convexity check**: compute the vector from the axis to the face's centroid. Check that the face's normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.
238:dd|
239:mw|**Multi-seed evaluation:**
240:wf|- Initialize all `cylindrical_hypothesis` to `UNDEDUCED` (-2).
241:xw|- For each face fi where `cylindrical_hypothesis == -2`:
242:ea|    - For each neighbor n1 of fi where n1 is pairwise qualified with fi:
243:tv|        - For each neighbor n2 of n1 where n2 is not a neighbor of fi and n2 is pairwise qualified with n1:
244:dh|            - Produce a cylinder fit to the set of vertices belonging to fi, n1, and n2.
245:ta|            - If faces fi, n1, and n2 are cylinder fit qualified, accept this set of faces as a valid seed and perform BFS expansion on them using temporary data structures (a local face list and vertex set — do not mutate `mesh.faces[].cylindrical_hypothesis` during trials). The initial BFS queue should contain fi, n1, and n2, so we explore all of their neighbors. Each trial produces a candidate hypothesis with a face count and fitted parameters. After each trial completes with acceptance, compare against the **current best candidate** for this starting face; keep whichever has more area covered. This avoids storing all trial results.
246:vk|- Accept the **best candidate** as a cylindrical hypothesis: create a cylindrical hypothesis, and for every face in the hypothesis, assign the face's cylindrical hypothesis field.
247:dd|
248:wi|Future work (not currently implemented - in the future 
... (truncated, 16076 chars)
```
</details>

**Todo list:**
- ✅ Read LEARNINGS.md, README.md
- ✅ View HEAD commit diff
- 🔄 Analyze algorithm correctness
- ⬜ Fix incorrect/incomplete sections
- ⬜ Commit changes

> 💭 Now let me analyze the rewritten algorithm carefully. Let me also look at the current code to understand the existing implementation and the problem being solved.
> 
> Let me read the current stage2.rs code to understand what's actually implemented:

Let me also look at the actual implementation to understand the current behavior and the fragmentation problem:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git log --oneline -5
```
<details>
<summary>Output (5 lines)</summary>

```
197eb77 (HEAD -> main) Manual re-write of stage 2.2 cylindrical hypothesis generation.
63afa3d Fix --viz-stages to only pause at requested stages
3166ce8 Refactor viz from post-stage to per-BFS-step visualization
d6f7807 Fix --viz-stages panic: use value_delimiter instead of Vec-returning parser
f3df476 Add interactive debugging visualization (--viz-stages)
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git log HEAD -1 --format='%B' | head -30
```
```
Manual re-write of stage 2.2 cylindrical hypothesis generation.
```

Now let me look at the current code to understand what's implemented vs. what the plan says:

<details>
<summary>Analyze cylinder deduction code (Claude Opus 4.6)</summary>

**Prompt:**
> Search the file /Users/sethml/src/brepper/src/stage2.rs for the cylinder deduction function. I need to understand:
> 
> 1. How seeding works currently - is it 2-face or 3-face seeds? Look for functions like `deduce_cylindrical_hypotheses` or similar.
> 2. How BFS expansion works - what checks are applied to neighbor faces during BFS?
> 3. The pairwise qualification checks - what's currently checked for seed pairs?
> 4. How multi-seed evaluation works - does it try all seeds and pick the best?
> 
> Read the relevant sections of stage2.rs (likely 500+ lines) and give me a detailed summary of:
> - The seed selection logic (which faces seed, how many faces per seed)
> - The BFS expansion logic (what checks: vertex distance, centroid, convexity, angular tolerance, pairwise qualification)
> - Whether early termination is implemented
> - Whether angular coverage validation is implemented
> - The comparison logic (best candidate selection: faces vs area)
> - Any discrepancies from the DEVELOPMENT_PLAN.md description
> 
> Be thorough - read the entire cylinder deduction section of the code.

<details>
<summary>Searched for regex <code>fn.*cyl|cylinder|deduce</code> (<code>**/src/stage2.rs</code>), 20 results</summary>

- [stage2.rs:3](../src/stage2.rs#L3)
- [stage2.rs:4](../src/stage2.rs#L4)
- [stage2.rs:5](../src/stage2.rs#L5)
- [stage2.rs:6](../src/stage2.rs#L6)
- [stage2.rs:7](../src/stage2.rs#L7)
- [stage2.rs:11](../src/stage2.rs#L11)
- [stage2.rs:23](../src/stage2.rs#L23)
- [stage2.rs:27](../src/stage2.rs#L27)
- [stage2.rs:39](../src/stage2.rs#L39)
- [stage2.rs:41](../src/stage2.rs#L41)
- [stage2.rs:42](../src/stage2.rs#L42)
- [stage2.rs:88](../src/stage2.rs#L88)
- [stage2.rs:90](../src/stage2.rs#L90)
- [stage2.rs:92](../src/stage2.rs#L92)
- [stage2.rs:98](../src/stage2.rs#L98)
- [stage2.rs:100](../src/stage2.rs#L100)
- [stage2.rs:102](../src/stage2.rs#L102)
- [stage2.rs:151](../src/stage2.rs#L151)
</details>

**Terminal:**
```sh
wc -l /Users/sethml/src/brepper/src/stage2.rs
```
```
    3302 /Users/sethml/src/brepper/src/stage2.rs
```

<details>
<summary>Searched for regex <code>^(pub )?fn |^/// </code> (<code>**/src/stage2.rs</code>), 100 results</summary>

- [stage2.rs:23](../src/stage2.rs#L23)
- [stage2.rs:24](../src/stage2.rs#L24)
- [stage2.rs:25](../src/stage2.rs#L25)
- [stage2.rs:26](../src/stage2.rs#L26)
- [stage2.rs:27](../src/stage2.rs#L27)
- [stage2.rs:28](../src/stage2.rs#L28)
- [stage2.rs:29](../src/stage2.rs#L29)
- [stage2.rs:32](../src/stage2.rs#L32)
- [stage2.rs:33](../src/stage2.rs#L33)
- [stage2.rs:34](../src/stage2.rs#L34)
- [stage2.rs:35](../src/stage2.rs#L35)
- [stage2.rs:38](../src/stage2.rs#L38)
- [stage2.rs:39](../src/stage2.rs#L39)
- [stage2.rs:40](../src/stage2.rs#L40)
- [stage2.rs:41](../src/stage2.rs#L41)
- [stage2.rs:44](../src/stage2.rs#L44)
- [stage2.rs:45](../src/stage2.rs#L45)
- [stage2.rs:46](../src/stage2.rs#L46)
- [stage2.rs:47](../src/stage2.rs#L47)
- [stage2.rs:50](../src/stage2.rs#L50)
- [stage2.rs:51](../src/stage2.rs#L51)
- [stage2.rs:52](../src/stage2.rs#L52)
- [stage2.rs:53](../src/stage2.rs#L53)
- [stage2.rs:56](../src/stage2.rs#L56)
- [stage2.rs:57](../src/stage2.rs#L57)
- [stage2.rs:58](../src/stage2.rs#L58)
- [stage2.rs:59](../src/stage2.rs#L59)
- [stage2.rs:66](../src/stage2.rs#L66)
- [stage2.rs:85](../src/stage2.rs#L85)
- [stage2.rs:108](../src/stage2.rs#L108)
- [stage2.rs:132](../src/stage2.rs#L132)
- [stage2.rs:146](../src/stage2.rs#L146)
- [stage2.rs:213](../src/stage2.rs#L213)
- [stage2.rs:214](../src/stage2.rs#L214)
- [stage2.rs:218](../src/stage2.rs#L218)
- [stage2.rs:219](../src/stage2.rs#L219)
- [stage2.rs:246](../src/stage2.rs#L246)
- [stage2.rs:247](../src/stage2.rs#L247)
- [stage2.rs:248](../src/stage2.rs#L248)
- [stage2.rs:280](../src/stage2.rs#L280)
- [stage2.rs:281](../src/stage2.rs#L281)
- [stage2.rs:300](../src/stage2.rs#L300)
- [stage2.rs:301](../src/stage2.rs#L301)
- [stage2.rs:302](../src/stage2.rs#L302)
- [stage2.rs:303](../src/stage2.rs#L303)
- [stage2.rs:304](../src/stage2.rs#L304)
- [stage2.rs:305](../src/stage2.rs#L305)
- [stage2.rs:341](../src/stage2.rs#L341)
- [stage2.rs:342](../src/stage2.rs#L342)
- [stage2.rs:361](../src/stage2.rs#L361)
- [stage2.rs:363](../src/stage2.rs#L363)
- [stage2.rs:364](../src/stage2.rs#L364)
- [stage2.rs:365](../src/stage2.rs#L365)
- [stage2.rs:366](../src/stage2.rs#L366)
- [stage2.rs:595](../src/stage2.rs#L595)
- [stage2.rs:597](../src/stage2.rs#L597)
- [stage2.rs:598](../src/stage2.rs#L598)
- [stage2.rs:599](../src/stage2.rs#L599)
- [stage2.rs:600](../src/stage2.rs#L600)
- [stage2.rs:655](../src/stage2.rs#L655)
- [stage2.rs:656](../src/stage2.rs#L656)
- [stage2.rs:671](../src/stage2.rs#L671)
- [stage2.rs:672](../src/stage2.rs#L672)
- [stage2.rs:673](../src/stage2.rs#L673)
- [stage2.rs:693](../src/stage2.rs#L693)
- [stage2.rs:694](../src/stage2.rs#L694)
- [stage2.rs:704](../src/stage2.rs#L704)
- [stage2.rs:705](../src/stage2.rs#L705)
- [stage2.rs:713](../src/stage2.rs#L713)
- [stage2.rs:714](../src/stage2.rs#L714)
- [stage2.rs:718](../src/stage2.rs#L718)
- [stage2.rs:719](../src/stage2.rs#L719)
- [stage2.rs:720](../src/stage2.rs#L720)
- [stage2.rs:721](../src/stage2.rs#L721)
- [stage2.rs:803](../src/stage2.rs#L803)
- [stage2.rs:804](../src/stage2.rs#L804)
- [stage2.rs:805](../src/stage2.rs#L805)
- [stage2.rs:825](../src/stage2.rs#L825)
- [stage2.rs:826](../src/stage2.rs#L826)
- [stage2.rs:827](../src/stage2.rs#L827)
- [stage2.rs:954](../src/stage2.rs#L954)
- [stage2.rs:955](../src/stage2.rs#L955)
- [stage2.rs:972](../src/stage2.rs#L972)
- [stage2.rs:973](../src/stage2.rs#L973)
- [stage2.rs:996](../src/stage2.rs#L996)
- [stage2.rs:997](../src/stage2.rs#L997)
- [stage2.rs:1012](../src/stage2.rs#L1012)
- [stage2.rs:1013](../src/stage2.rs#L1013)
- [stage2.rs:1014](../src/stage2.rs#L1014)
- [stage2.rs:1016](../src/stage2.rs#L1016)
- [stage2.rs:1017](../src/stage2.rs#L1017)
- [stage2.rs:1018](../src/stage2.rs#L1018)
- [stage2.rs:1019](../src/stage2.rs#L1019)
- [stage2.rs:1020](../src/stage2.rs#L1020)
- [stage2.rs:1021](../src/stage2.rs#L1021)
- [stage2.rs:1084](../src/stage2.rs#L1084)
- [stage2.rs:1097](../src/stage2.rs#L1097)
- [stage2.rs:1098](../src/stage2.rs#L1098)
- [stage2.rs:1099](../src/stage2.rs#L1099)
- [stage2.rs:1100](../src/stage2.rs#L1100)
</details>

Read [stage2.rs](../src/stage2.rs#671-671), lines 671 to 1000

Read [stage2.rs](../src/stage2.rs#1000-1000), lines 1000 to 1400

Read [stage2.rs](../src/stage2.rs#1400-1400), lines 1400 to 1700

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#213-213), lines 213 to 305

Now I have the complete picture. Here's the detailed summary:

---

## Cylinder Deduction Analysis — `deduce_cylindrical_hypotheses` in [src/stage2.rs](src/stage2.rs)

### 1. Seed Selection Logic — **2-face seeds** (not 3-face)

The code uses **2-face seeds**, not 3-face as described in DEVELOPMENT_PLAN.md.

In `deduce_cylindrical_hypotheses` (~line 1570):
- Iterates each face `fi` where `cylindrical_hypothesis == UNDEDUCED`.
- For each **direct neighbor** `ni` of `fi`:
  - Checks `ni` is also `UNDEDUCED`.
  - Cross product magnitude of `fi_normal × ni_normal` must exceed `MIN_CROSS_THRESHOLD` (0.01, ~0.6°).
  - Dihedral angle must be within `angular_tol`.
  - If qualified, `ni` is added to `seed_partners`.
- Each seed pair `(fi, ni)` is tested via `run_cylinder_trial_bfs`.

**DISCREPANCY from DEVELOPMENT_PLAN.md**: The plan describes 3-face seeds (fi → n1 → n2 where n2 is a neighbor of n1 but NOT a neighbor of fi). The code only does 2-face seeds.

### 2. BFS Expansion Logic — `run_cylinder_trial_bfs` (~line 1102)

Starting from the 2-face seed, the BFS applies these checks to each neighbor `cni`:

1. **Already assigned check**: Skip if `cylindrical_hypothesis != UNDEDUCED` or already claimed by this trial.

2. **Convexity check**: Computes whether `cni`'s normal points away from (convex) or toward (concave) the axis. Rejects if it doesn't match the seed's convexity.

3. **Angular tolerance (pairwise)**: Checks dihedral angle between `cni` and each **trial-claimed neighbor** of `cni`. Rejects if any pairwise angle exceeds `angular_tol`. This is the "defense-in-depth" check the plan describes.

4. **Vertex distance check**: For each vertex of `cni`, computes distance to current cylinder. Tracks `vtx_err_max`. If any vertex exceeds `REFIT_SKIP_MULTIPLIER × vertex_tol` (i.e. 2× vertex_tol), fast-rejects without refit.

5. **Centroid distance check**: Computes centroid-to-cylinder distance. Fast-rejects if it exceeds `REFIT_SKIP_MULTIPLIER × surface_tol`.

6. **Refit logic**: If vertices or centroid are outside tolerance but not beyond the skip multiplier:
   - Refits cylinder from all current faces + candidate face.
   - Checks ALL vertices in the refitted set are within `vertex_tol`.
   - Checks ALL face centroids are within `surface_tol`.
   - If refit passes, accepts and updates current cylinder params.

7. **Accept**: Adds face to `trial_claimed`, `face_list`, adds its vertices, pushes onto BFS queue.

### 3. Pairwise Qualification Checks

For **seed pairs**: Cross product magnitude > 0.01 AND dihedral angle < `angular_tol`.

During **BFS expansion**: The angular tolerance check is against ALL trial-claimed neighbors of the candidate face (not just the BFS parent). This matches the plan's "defense-in-depth" description.

**DISCREPANCY**: The plan says "If ni is not pairwise qualified with all of its neighbor faces which are assigned in this trial already, skip." The code checks angular tolerance against trial-claimed neighbors but does NOT re-check the cross product minimum threshold (`MIN_CROSS_THRESHOLD`) during BFS - it only checks it for seed pair qualification. This means a face could be accepted via BFS with very similar normal to an already-claimed neighbor (nearly zero cross product), as long as the dihedral angle is small.

### 4. Multi-seed Evaluation

**Yes, it tries all seeds and picks the best.**

- For each face `fi`, collects all valid seed partners into `seed_partners: Vec<usize>`.
- Runs `run_cylinder_trial_bfs` for each partner.
- Keeps the best candidate by **face count** (`trial.faces.len() > b.faces.len()`).

**DISCREPANCY from DEVELOPMENT_PLAN.md**: The plan says "keep whichever has more **area** covered." The code compares by **face count**, not total area.

### 5. Early Termination — **Implemented**

After each BFS queue pop (at the end of the outer `while` loop, ~line 1449):
- If the trial has 3+ faces and a best candidate exists:
  - Checks axis alignment (`|dot| > 1 - 1e-6`), radius within `vertex_tol`, axis-to-axis perpendicular distance within `vertex_tol`.
  - AND checks that every face so far is already in the best candidate's face set.
  - If all conditions met → abandons trial as redundant.

**Note**: The plan marks early termination as "Future work (not currently implemented)" but it IS implemented. The plan is stale.

### 6. Angular Coverage Validation — **Implemented**

After selecting the best candidate (~line 1672):
- Calls `angular_coverage_valid()` which:
  - Projects face centroids to angular coordinate θ around the axis.
  - Sorts θ values, computes all gaps including wraparound.
  - Requires `second_largest_gap ≤ span / 3` (where `span = 2π - largest_gap`).
- If fails, marks `fi` as `NO_HYPOTHESIS` and continues.

**Note**: The plan also marks this as "Future work" but it IS implemented.

### 7. Summary of Discrepancies from DEVELOPMENT_PLAN.md

| Aspect | DEVELOPMENT_PLAN says | Code does |
|---|---|---|
| **Seed size** | 3-face (fi → n1 → n2) | **2-face** (fi, neighbor) |
| **Best candidate comparison** | Most **area** | Most **face count** |
| **Early termination** | "Future work, not implemented" | **Implemented** |
| **Angular coverage** | "Future work, not implemented" | **Implemented** |
| **Computational cost** | Says "UPDATE ME" | Unchanged |
| **Cross product check during BFS** | "pairwise qualified with all assigned neighbors" (implies full check) | Only checks angular tolerance, not MIN_CROSS_THRESHOLD |

### 8. Constants

- `MIN_CROSS_THRESHOLD = 0.01` (~0.6° between normals)
- `REFIT_SKIP_MULTIPLIER = 2.0` (fast-reject at 2× tolerance)
- `MIN_CYLINDER_FACES = 3` (minimum to accept a hypothesis)

</details>

> 💭 Excellent analysis. Now I have a clear picture:
> 
> The DEVELOPMENT_PLAN.md was rewritten to describe a new 3-face seeding algorithm, but the code still uses 2-face seeds. The plan was rewritten to say "future work" for early termination and angular coverage, but both are already implemented in the code. There are several discrepancies.
> 
> Let me think about whether the 3-face seeding algorithm described in the plan is actually better than the current 2-face approach, and then fix the sections that are incorrect or incomplete.
> 
> **Analysis of the 3-face seed approach:**
> 
> The plan says: for each face fi, for each neighbor n1 qualified with fi, for each neighbor n2 of n1 where n2 is NOT a neighbor of fi and n2 IS pairwise qualified with n1: seed from {fi, n1, n2}.
>  
> Now I'm examining the constraints in this approach. The requirement that n2 isn't adjacent to fi is meant to maximize angular diversity for the initial cylinder fit by selecting faces that are further apart. On a typical quad-strip cylinder mesh, this should work fine since n2 would be two steps away from fi. However, I'm wondering if this constraint might be too restrictive in some cases, and whether the pairwise qualification check between n1 and n2 is sufficient or if we're missing something about how these faces actually relate to each other geometrically.
> 
> The main benefit of using three non-collinear faces is that it gives a much better initial axis estimate and circle fit with points spread around the circumference, which should filter out spurious hypotheses early. But I'm concerned the non-neighbor requirement for n2 could be overly limiting in practice.
> 
> In a typical cylinder mesh, fi has four neighbors, and n1 (one step away) has neighbors that include fi plus faces further around the cylinder. A face two steps away from fi wouldn't be a neighbor of fi, so that works as n2. However, in a very fine mesh, n2 being only two steps away provides minimal additional angular diversity compared to n1, though it still gives the three angular samples needed for the initial circle fit.
> 
> The 3-face seed approach requires more trials—roughly 6 per face compared to 3 for the 2-face approach—but each trial starts with better geometric constraints. This should reduce false cylinder fits by requiring stronger evidence before the BFS expands. The trade-off is worth it since the angular diversity from three faces at different positions creates a more constrained initial fit.
> 
> I'm noticing the plan text marks early termination and angular coverage as future work, but those are already implemented in the code, so that's misleading.
> 
> Looking at the AGENTS.md guidelines, the plan should match the actual implementation. But the user seems to be writing a prospective algorithm change—describing what they want the code to do next, not documenting what's currently there. The user's instruction is to evaluate whether the 3-face seeding improves cylinder deduction and fix any incorrect or incomplete sections, which suggests they're iterating on the algorithm design itself.
> 
> Now I'm analyzing the algorithm description for correctness. The pairwise qualification definition seems incomplete—it specifies that both faces must be unassigned, but doesn't fully explain how this constraint works with the BFS seeding strategy they're proposing. I think the real issue is that "pairwise qualified" means something different in the BFS context—it's just checking angular compatibility, not the full set of conditions that apply during seeding. The plan should either clarify that only the geometric checks (cross product and dihedral angle) apply during BFS expansion, or avoid using "pairwise qualified" terminology altogether in that context to prevent confusion. instead explicitly state what's checked.
> 
> Wait, re-reading the BFS expansion: "If ni is not pairwise qualified with all of its neighbor faces which are assigned in this trial already, skip."
> 
> The pairwise qualification requires:
> - fi and ni are neighbors. ✓ (ni is a mesh neighbor of each assigned neighbor)
> - fi and ni are unassigned. ✗ (assigned neighbors are logically "assigned" in the trial, though mesh-level still -2)
> - Cross product > MIN_CROSS_THRESHOLD. (This is a real check)
> - Dihedral angle ≤ angular_tolerance. (This is a real check)
> 
> So point (2) is problematic. The current code only checks dihedral angle during BFS (not the "unassigned" check or the cross product minimum). The plan's use of "pairwise qualified" here is misleading because it implies all 4 conditions, but only 2 are relevant in BFS.
> 
> Actually, the cross-product check shouldn't apply during BFS expansion at all. On a finely tessellated cylinder, adjacent faces naturally have nearly parallel normals, so requiring cross product > MIN_CROSS_THRESHOLD would incorrectly reject valid faces. The cross-product check only makes sense for seed pair selection, where we need angular diversity to determine the cylinder axis. So the plan should say "check that the dihedral angle between ni and each assigned neighbor does not exceed angular_tolerance" instead of using the misleading "pairwise qualified" language.
> 
> The seed traversal pattern (fi → n1 → n2 where n2 is not a neighbor of fi) is sound because it ensures circumferential diversity. In a quad-strip cylinder mesh, n1 is adjacent to fi in the same row, and n2 is adjacent to n1 but not fi—meaning n2 is either one more step around the cylinder or one step axially plus one step around. Either way, the three faces span different regions of the cylinder surface, giving good coverage for fitting.
> 
> However, there's a potential issue: on very coarse meshes where each face's neighbors are also neighbors of each other, a valid n2 might not exist for a given fi/n1 pair. The plan doesn't account for this fallback scenario, which could be problematic for meshes with minimal circumferential resolution.
> 
> Let me trace through some concrete examples to see if this is actually a problem. On a 6-quad cylinder, face 0 has neighbors 1 and 5 around the circumference. If n1 = face 1, then n1's neighbors include face 2, which isn't a neighbor of face 0, so face 2 works as n2. But with only 4 quads around the circumference, the situation gets tighter and I need to check whether valid n2 candidates still exist.
> 
> The real issue emerges with 3 quads: face 0 neighbors faces 1 and 2, and n1 = face 1 has neighbors 0 and 2. Face 2 is a neighbor of both n1 and face 0, so there's no valid n2 at all. This suggests that very low-resolution cylinders might not support the required face configuration.
> 
> When fi and n1 are axially adjacent instead (sharing an edge along the cylinder axis), n1's neighbors include fi plus faces in different columns. If the mesh is a regular quad grid, those left/right neighbors of n1 won't be neighbors of fi, making them valid n2 candidates and providing the circumferential diversity needed.
> 
> But there's a subtlety I need to think through here. pairwise qualification also requires assignment status. In the seed context, n2 must be pairwise qualified with n1. "n2 is pairwise qualified with n1" means n2 and n1 are neighbors, both unassigned, cross product > MIN_CROSS_THRESHOLD, and dihedral angle ≤ angular_tol. If n1 and n2 are in the same column of a cylinder quad strip (axially adjacent), their normals are identical, so cross product = 0, failing the MIN_CROSS_THRESHOLD check. This means n2 can only be circumferentially adjacent to n1.
> 
> So the valid seed patterns are like: fi and n1 share a circumferential edge (different columns, same row), then n2 is circumferentially adjacent to n1 (two columns away from fi). This gives good angular diversity. On a cylinder, faces in the same column have identical normals with zero cross product, so they can't be pairwise qualified — seeds must come from circumferentially adjacent faces, with n2 positioned two steps around from fi. This ensures seeds span at least three circumferential positions, giving a reliable axis and center estimate.
> 
> Now I'm wondering what happens when no valid (fi, n1, n2) triple exists, since the plan doesn't specify this case. no valid seed partner was found, the face was marked as `NO_HYPOTHESIS`. The new plan text doesn't mention this case. Should the face be skipped (remain UNDEDUCED) or marked NO_HYPOTHESIS?
> 
> If we just skip (leave UNDEDUCED), then the face could potentially be absorbed by a later cylinder's BFS. But if no cylinder ever absorbs it, it remains UNDEDUCED forever. The outer iteration "For each face fi where `cylindrical_hypothesis == -2`" would revisit it, but if no valid triple exists, it stays -2. At the end, all remaining -2 faces should probably be set to -1 (NO_HYPOTHESIS).
> 
> Actually, looking at the existing code more carefully... after the main loop over faces, there's probably a cleanup step. Let me check.
> 
> Now I'm thinking about the pairwise qualification check — the plan requires n2 to be pairwise qualified with n1, but fi and n2 aren't neighbors so the angular tolerance doesn't apply between them. Since all three must pass cylinder fit qualification though, any major geometric inconsistency would get caught there anyway. For the best candidate selection, the plan mentions "more area covered" while the code uses face count — area is more meaningful for correctness, but face count is simpler and usually correlates well enough for an initial implementation.
> 
> The BFS expansion section is where things get tricky. It says "pairwise qualified" but during BFS we're checking compatibility with already-accepted faces. The unassigned requirement from pairwise qualification is partially redundant with the check that skips trial-claimed faces, and the cross product check becomes problematic during expansion since we don't have a consistent reference normal. I need to rethink what the actual BFS check should be — for each candidate and its mesh neighbors that are already assigned to the current hypothesis, we need to verify they're compatible without relying on the cross product logic.
> 
> The computational cost section is marked for update with the 3-face seed approach, and the cylinder fitting section needs clarification on whether we're using the full eigenvector computation for 3-face seeds or if we can still simplify with a cross product of two faces. I'm realizing the user's intent here—they're treating this plan as a specification for upcoming code changes rather than a description of what's currently implemented. So marking early termination and angular coverage as future work makes sense if they want to implement the 3-face seeding first, then add those optimizations in a second pass. The user said not to change any code, which confirms they're rewriting the plan as a blueprint for what comes next, not documenting the present state.
> 
> Now I need to analyze whether this 3-face seeding approach will actually improve cylinder deduction. Looking at the key issues: the BFS expansion step is using "pairwise qualified" too broadly when it should only check the dihedral angle, the algorithm needs to handle the case where no valid 3-face triple exists even if 2-face partners are available, and I should remove the placeholder text and properly describe the computational cost of the new approach.
> 
> There's also an important constraint to clarify — requiring that n2 isn't a neighbor of fi ensures angular diversity in the seed, preventing the three faces from clustering too tightly together. But I'm questioning whether this constraint is actually necessary or if it's too restrictive, since a valid 3-face seed could theoretically exist where all three faces are mutually adjacent if their normals are sufficiently different.
> 
> The real trade-off is that non-adjacency gives better angular coverage across the cylinder's circumference, whereas allowing adjacent faces risks clustering them too close together — similar to what a 2-face seed already provides. For typical CAD meshes where individual faces span small angles, the non-adjacency requirement seems reasonable, though it might miss some valid seeds on coarser meshes where faces cover larger angular ranges.
> 
> For pairwise qualification between n1 and n2, both should be unassigned since we're seeding new cylinders, not extending existing ones. The convexity check happens after fitting the cylinder to the 3-face seed — if the seed faces disagree on convexity, the seed gets rejected, which is already implicit in the cylinder fit qualification step.
> 
> Now I'm identifying the specific issues to address. The BFS expansion is incorrectly applying the full pairwise qualification criteria when it should only check the dihedral angle constraint — the unassigned requirement and cross product threshold don't apply during BFS since adjacent faces on a fine mesh naturally have nearly parallel normals. I need to clarify this distinction and also update the area comparison logic to be consistent across the plan and the code comments.

**Todo list:**
- ✅ Read LEARNINGS.md, README.md
- ✅ View HEAD commit diff
- ✅ Analyze algorithm correctness
- 🔄 Fix incorrect/incomplete sections
- ⬜ Commit changes

Good analysis. I've identified several issues in the rewritten algorithm description. Let me now read the exact regions I need to edit:

<details>
<summary>Reading lines 224-305 of <a href="../DEVELOPMENT_PLAN.md#L224-L306">DEVELOPMENT_PLAN.md</a></summary>

```
The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from pairs of adjacent faces (since a single triangle is always planar and cannot determine curvature). A key challenge is that **any two non-coplanar faces define some cylinder** — the cross product of their normals gives an axis, and a circle fit always produces a radius. The previous algorithm took the first valid seed and committed to it, which meant a bogus 3-face cylinder from an unfortunate seed pair could permanently consume faces that should belong to a correct 20-face cylinder seeded from a different pair. The fix is multi-seed evaluation with angular coverage validation:

**Pairwise qualification:**
Two faces fi and ni are pairwise qualified to participate in a cylindrical hypothesis if:
- fi and ni are neighbors.
- fi and ni are unassigned (`cylindrical_hypothesis == -2`).
- fi and ni have a sufficiently different normal (cross product magnitude exceeds `MIN_CROSS_THRESHOLD`, e.g. 0.01).
- The dihedral angle between fi and ni does not exceed `--angular-tolerance` (default 17.5°).

**Cylinder fit qualification:**
A face or set of faces is cylinder fit qualified if:
- All vertices are within `--vertex-tolerance` of the cylinder.
- All face centroids are within `--surface-tolerance` of the cylinder.
- **Convexity check**: compute the vector from the axis to the face's centroid. Check that the face's normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.

**Multi-seed evaluation:**
- Initialize all `cylindrical_hypothesis` to `UNDEDUCED` (-2).
- For each face fi where `cylindrical_hypothesis == -2`:
    - For each neighbor n1 of fi where n1 is pairwise qualified with fi:
        - For each neighbor n2 of n1 where n2 is not a neighbor of fi and n2 is pairwise qualified with n1:
            - Produce a cylinder fit to the set of vertices belonging to fi, n1, and n2.
            - If faces fi, n1, and n2 are cylinder fit qualified, accept this set of faces as a valid seed and perform BFS expansion on them using temporary data structures (a local face list and vertex set — do not mutate `mesh.faces[].cylindrical_hypothesis` during trials). The initial BFS queue should contain fi, n1, and n2, so we explore all of their neighbors. Each trial produces a candidate hypothesis with a face count and fitted parameters. After each trial completes with acceptance, compare against the **current best candidate** for this starting face; keep whichever has more area covered. This avoids storing all trial results.
- Accept the **best candidate** as a cylindrical hypothesis: create a cylindrical hypothesis, and for every face in the hypothesis, assign the face's cylindrical hypothesis field.

Future work (not currently implemented - in the future we'll evaluate whether these optimizations help):
    - **Early termination** of redundant trials: during a trial BFS, if a best candidate already exists, check whether the trial is rediscovering the same cylinder. After the trial adds a face, compare its fitted cylinder parameters against the best candidate's: axis directions nearly parallel (`|a_trial · a_best| > 1 - 1e-6`), radii within `--vertex-tolerance`, and axis-to-axis distance within `--vertex-tolerance`. If parameters match AND every face accepted so far is already in the best candidate's face set, abandon the trial — it can only produce a subset of the existing best. This is the dominant optimization: on a 20-face fillet cylinder with ~3 valid seeds per face, most of the ~60 trials terminate after 2–3 faces instead of exploring all 20.
    - Apply **angular coverage validation** (see below) to the best candidate. If it fails, discard and try the next face.
    - If it passes, **commit** the hypothesis: write assignments to `mesh.faces[].cylindrical_hypothesis` for all member faces, and append to the hypotheses vector.
... (truncated, 13127 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
224:rh|The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from pairs of adjacent faces (since a single triangle is always planar and cannot determine curvature). A key challenge is that **any two non-coplanar faces define some cylinder** — the cross product of their normals gives an axis, and a circle fit always produces a radius. The previous algorithm took the first valid seed and committed to it, which meant a bogus 3-face cylinder from an unfortunate seed pair could permanently consume faces that should belong to a correct 20-face cylinder seeded from a different pair. The fix is multi-seed evaluation with angular coverage validation:
225:dd|
226:ie|**Pairwise qualification:**
227:yv|Two faces fi and ni are pairwise qualified to participate in a cylindrical hypothesis if:
228:iv|- fi and ni are neighbors.
229:zo|- fi and ni are unassigned (`cylindrical_hypothesis == -2`).
230:kn|- fi and ni have a sufficiently different normal (cross product magnitude exceeds `MIN_CROSS_THRESHOLD`, e.g. 0.01).
231:fq|- The dihedral angle between fi and ni does not exceed `--angular-tolerance` (default 17.5°).
232:dd|
233:hs|**Cylinder fit qualification:**
234:hx|A face or set of faces is cylinder fit qualified if:
235:zc|- All vertices are within `--vertex-tolerance` of the cylinder.
236:dj|- All face centroids are within `--surface-tolerance` of the cylinder.
237:ia|- **Convexity check**: compute the vector from the axis to the face's centroid. Check that the face's normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.
238:dd|
239:mw|**Multi-seed evaluation:**
240:wf|- Initialize all `cylindrical_hypothesis` to `UNDEDUCED` (-2).
241:xw|- For each face fi where `cylindrical_hypothesis == -2`:
242:ea|    - For each neighbor n1 of fi where n1 is pairwise qualified with fi:
243:tv|        - For each neighbor n2 of n1 where n2 is not a neighbor of fi and n2 is pairwise qualified with n1:
244:dh|            - Produce a cylinder fit to the set of vertices belonging to fi, n1, and n2.
245:ta|            - If faces fi, n1, and n2 are cylinder fit qualified, accept this set of faces as a valid seed and perform BFS expansion on them using temporary data structures (a local face list and vertex set — do not mutate `mesh.faces[].cylindrical_hypothesis` during trials). The initial BFS queue should contain fi, n1, and n2, so we explore all of their neighbors. Each trial produces a candidate hypothesis with a face count and fitted parameters. After each trial completes with acceptance, compare against the **current best candidate** for this starting face; keep whichever has more area covered. This avoids storing all trial results.
246:vk|- Accept the **best candidate** as a cylindrical hypothesis: create a cylindrical hypothesis, and for every face in the hypothesis, assign the face's cylindrical hypothesis field.
247:dd|
248:wi|Future work (not currently implemented - in the future we'll evaluate whether these optimizations help):
249:iz|    - **Early termination** of redundant trials: during a trial BFS, if a best candidate already exists, check whether the trial is rediscovering the same cylinder. After the trial adds a face, compare its fitted cylinder parameters against the best candidate's: axis directions nearly parallel (`|a_trial · a_best| > 1 - 1e-6`), radii within `--vertex-tolerance`, and axis-to-axis distance within `--vertex-tolerance`. If parameters match AND every face accepted so far is already in the best candidate's face set, abandon the trial — it can only produce a subset of the existing best. This is the dominant optimization: on a 20-face fillet cylinder with ~3 valid seeds per face, most of the ~60 trials terminate after 2–3 faces instead of exploring all 20.
250:zk|    - Apply **angular coverage validation** (see below) to the best candidate. If it fails, discar
... (truncated, 13701 chars)
```
</details>

Now I have a thorough picture. Here are the issues I've identified and will fix:

1. **BFS expansion incorrectly uses "pairwise qualified"** — during BFS, only the dihedral angle check is relevant. The "unassigned" check doesn't apply (trial-claimed faces are conceptually assigned), and the cross product minimum is inappropriate (adjacent faces on a finely tessellated cylinder legitimately have near-parallel normals). Need to rewrite that bullet.

2. **No fallback when no valid 3-face triple exists** — the plan doesn't say what happens for faces that can't form any (fi, n1, n2) triple.

3. **"UPDATE ME" in computational cost** — needs rewriting for 3-face seeding without early termination.

4. **Cylinder fitting mentions "two-face seed" simplification** — needs update for 3-face seed.

5. **"Near-parallel seed normals" subtlety** references "two seed normals" — outdated for 3-face seeds.

6. **Intro paragraph still says "seeded from pairs of adjacent faces"** — needs update for 3-face approach.Now let me apply the fixes:

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L224-L225">DEVELOPMENT_PLAN.md -1+1</a>, <a href="../DEVELOPMENT_PLAN.md#L239-L249">-8+10</a>, <a href="../DEVELOPMENT_PLAN.md#L255-L259">-4+4</a>, <a href="../DEVELOPMENT_PLAN.md#L282-L283">-1+1</a>, <a href="../DEVELOPMENT_PLAN.md#L288-L289">-1+1</a>, <a href="../DEVELOPMENT_PLAN.md#L295-L296">-1+1</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "224:rh",
      "content": "The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from triples of nearby faces (since a single triangle is always planar and cannot determine curvature, and two faces provide minimal angular constraint on the cylinder axis). A key challenge is that **any two non-coplanar faces define some cylinder** — the cross product of their normals gives an axis, and a circle fit always produces a radius. The previous algorithm took the first valid seed and committed to it, which meant a bogus 3-face cylinder from an unfortunate seed pair could permanently consume faces that should belong to a correct 20-face cylinder seeded from a different pair. The fix is multi-seed evaluation with 3-face seeds that span more of the cylinder's circumference:"
    },
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "239:mw,240:wf,241:xw,242:ea,243:tv,244:dh,245:ta,246:vk",
      "content": "**Multi-seed evaluation:**\n- Initialize all `cylindrical_hypothesis` to `UNDEDUCED` (-2).\n- For each face fi where `cylindrical_hypothesis == -2`:\n    - For each neighbor n1 of fi where n1 is pairwise qualified with fi:\n        - For each neighbor n2 of n1 where n2 is not a neighbor of fi and n2 is pairwise qualified with n1:\n            - Produce a cylinder fit to the set of vertices belonging to fi, n1, and n2.\n            - If faces fi, n1, and n2 are cylinder fit qualified, accept this set of faces as a valid seed and perform BFS expansion on them using temporary data structures (a local face list and vertex set — do not mutate `mesh.faces[].cylindrical_hypothesis` during trials). The initial BFS queue should contain fi, n1, and n2, so we explore all of their neighbors. Each trial produces a candidate hypothesis with a face count and fitted parameters. After each trial completes with acceptance, compare against the **current best candidate** for this starting face; keep whichever has more area covered. This avoids storing all trial results.\n    - If no valid (fi, n1, n2) triple was found, skip fi (leave as `UNDEDUCED` — it may be absorbed by a later face's BFS, or will remain unassigned at the end).\n- Accept the **best candidate** as a cylindrical hypothesis: create a cylindrical hypothesis, and for every face in the hypothesis, assign the face's cylindrical hypothesis field.\n- After all faces have been processed, set any remaining `UNDEDUCED` faces to `NO_HYPOTHESIS`."
    },
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "253:qu,254:bg,255:jj,256:za",
      "content": "**BFS expansion** (run once per trial seed):\n- Repeatedly pop a face fi from the queue and examine each neighbor ni:\n    - If ni is already assigned a cylindrical hypothesis (in the mesh) or claimed by this trial, skip.\n    - **Angular tolerance check**: for ea
... (truncated, 6340 chars)
```
**Output:**
```
{
  "applied": 6,
  "failed": 0,
  "files": [
    {
      "file": "DEVELOPMENT_PLAN.md",
      "startLine": 224,
      "endLine": 295
    }
  ]
}
```
</details>

Now let me update the `LEARNINGS.md` to reflect the planned changes:

<details>
<summary>Reading lines 138-160 of <a href="../LEARNINGS.md#L138-L161">LEARNINGS.md</a></summary>

```
- Angular coverage validation: after selecting the best trial, checks that face centroids span at least 3 angular clusters around the cylinder circumference. Computes angular coordinate θ for each face centroid, finds the largest gap (empty arc), and requires the second-largest gap ≤ span/3. Prevents bogus hypotheses from faces clustered on one side.
- Centroid validation after BFS: each face centroid must be within surface_tolerance of the fitted cylinder. This rejects spurious fits from perpendicular planar faces that algebraically fit a cylinder.
- Minimum 3-face requirement: cylinder hypotheses must contain at least 3 faces. Any real cylindrical surface from a CAD tessellation will have at least 3 facets around its circumference. This eliminates spurious 2-face cylinder fits from locally adjacent faces on tori, cones, and spheres (e.g., cone went from 29 to 0 spurious fits, pipe_elbow from 252 to 51).
- Angular tolerance check: both seed pairs and BFS expansion candidates must have dihedral angle ≤ `--angular-tolerance` (default 17.5°). This prevents fitting cylinders to faces that meet at sharp angles (e.g., cube faces at 90°). CAD tessellators enforce a maximum angular tolerance between adjacent triangles on the same surface.
- Extracting cylinder parameters from STEP: `b_rep_adaptor::Surface::cylinder()` → `gp::Cylinder`, then `.radius()`, `.axis().direction()`, `.axis().location()`. Useful for test verification.

### ccad box() coordinate system
- `box(w, h, d)` places the box in the positive octant: (0,0,0) to (w,h,d). It does NOT center at origin.
- To center a box, use `translate(box(w,h,d), -w/2, -h/2, -d/2)`.
- `cylinder(diameter, height)` is vertical (Z-axis), centered at the XY origin, extends from z=0 to z=height (NOT centered vertically). To make through-holes, translate cylinders vertically so they extend past both sides of the target body.
- `sphere(diameter)` is centered at the origin. To make hemispherical pockets or domes, use `intersection()` or `difference()` with a box.

### Stage 2.3 spherical hypothesis algorithm
- Algebraic least-squares sphere fitting: expand |v-c|²=r² linearly into 4 unknowns (cx, cy, cz, k=r²-|c|²). Normal equations AᵀAx = Aᵀb solved via 4×4 Gaussian elimination with partial pivoting.
- Vertex-neighborhood seeding: for each mesh vertex, collect surrounding non-excluded faces (≥3), fit sphere from all their vertices. Naturally provides angular diversity for the 4-DOF sphere fit. Superior to pair-based seeding which failed on finely tessellated spheres.
- Multi-face planar faces excluded from seeding but absorb-able by BFS, same as cylindrical.
- Solid-angle coverage validation: area-weighted 3×3 covariance matrix of centroid-to-center unit direction vectors. Eigenvalues via depressed cubic (same as cylinder axis). Require λ_min/λ_max ≥ MIN_SPHERE_EIGENVALUE_RATIO (0.01). Rejects strip-like hypotheses from cylinder fillet growth.
- MAX_SPHERE_RADIUS_FACTOR = 1000.0 (was 10.0). With solid-angle coverage + surface-tolerance in BFS, radius limit only needs to prevent numerical precision issues.
- Centroid validation during BFS (not just post-BFS): each face centroid checked against surface_tolerance with 2x hard skip threshold.
- Minimum 4-face requirement: a sphere needs at least 4 non-coplanar points for a unique fit.
- All faces are candidates for BFS expansion (including those with cylindrical hypotheses and multi-face planar hypotheses). Only seeding excludes multi-face planar. Stage 2.6 resolves overlapping hypotheses.
- Torus faces locally fit spheres (e.g., pipe_elbow produces 36 sphere hypotheses). This is expected — stage 2.6 with torus hypothesis support will resolve it.
- Concave spheres (bowls/pockets) have normals pointing toward center — same convexity logic as cylinders.
```
</details>

<details>
<summary>Reading lines 128-143 of <a href="../LEARNINGS.md#L128-L144">LEARNINGS.md</a></summary>

```
- Solution: stage 1.3 merges coplanar triangle pairs back into quads, so curved-surface facets become single faces with single-face planar hypotheses.
- This restores the clean "single-face = curved surface candidate" criterion for stages 2.2 and 2.3.
- Sphere poles are an exception: triangles in the polar fan pattern don't have coplanar neighbors and correctly remain as triangles.

### Stage 2.2 cylindrical hypothesis algorithm
- Two-step fitting: (1) axis direction from smallest eigenvector of area-weighted normal covariance matrix M = Σ wᵢ nᵢ nᵢᵀ, (2) axis position and radius via 2D algebraic circle fit after projecting vertices perpendicular to axis.
- **Critical: center the 2D projected coordinates before circle fitting.** Without centering, the normal equations matrix becomes catastrophically ill-conditioned when absolute vertex coordinates are large relative to the arc span (e.g., vertices at (15, 12.5) spanning 2.5° of a r=2mm cylinder). The sums-of-squares terms (sx2 ≈ 1350) dwarf the signal (radius ≈ 2), causing Cramer's rule to produce wildly wrong radii (e.g., 139mm instead of 2mm). Fix: subtract mean_x/mean_y before fitting, add back when converting 2D center to 3D. This is the standard numerical remedy for polynomial least-squares fitting.
- 3×3 symmetric eigenvalue computation uses depressed cubic (trigonometric solution with cos(θ/3)), plus null-space eigenvector via cross products of (M - λI) rows.
- Multi-seed evaluation: for each starting face, collects ALL valid seed partners (not just the first), runs a trial BFS for each using temporary data (no mesh mutation during trials), keeps the best trial (most faces). Early termination abandons trials that rediscover the same cylinder as the current best.
- BFS seeds from pairs of adjacent faces with non-parallel normals (cross product > MIN_CROSS_THRESHOLD=0.01). No restrictions on multi-face planar faces — they can be both seed faces and seed partners. Angular coverage validation and other checks prevent bogus large-radius cylinders.
- Angular coverage validation: after selecting the best trial, checks that face centroids span at least 3 angular clusters around the cylinder circumference. Computes angular coordinate θ for each face centroid, finds the largest gap (empty arc), and requires the second-largest gap ≤ span/3. Prevents bogus hypotheses from faces clustered on one side.
- Centroid validation after BFS: each face centroid must be within surface_tolerance of the fitted cylinder. This rejects spurious fits from perpendicular planar faces that algebraically fit a cylinder.
- Minimum 3-face requirement: cylinder hypotheses must contain at least 3 faces. Any real cylindrical surface from a CAD tessellation will have at least 3 facets around its circumference. This eliminates spurious 2-face cylinder fits from locally adjacent faces on tori, cones, and spheres (e.g., cone went from 29 to 0 spurious fits, pipe_elbow from 252 to 51).
- Angular tolerance check: both seed pairs and BFS expansion candidates must have dihedral angle ≤ `--angular-tolerance` (default 17.5°). This prevents fitting cylinders to faces that meet at sharp angles (e.g., cube faces at 90°). CAD tessellators enforce a maximum angular tolerance between adjacent triangles on the same surface.
- Extracting cylinder parameters from STEP: `b_rep_adaptor::Surface::cylinder()` → `gp::Cylinder`, then `.radius()`, `.axis().direction()`, `.axis().location()`. Useful for test verification.

```
</details>

The `LEARNINGS.md` describes the current implementation, which is correct. Since we're not changing code, the LEARNINGS should stay as-is — they describe what the code actually does. The plan describes the target. Let me re-verify the full updated section:

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#213-213), lines 213 to 310

The section looks good. Let me verify there are no other problems. Let me also check that the implementation phases section has the right status for stage 2.2:

<details>
<summary>Searched for regex <code>2\.2.*[Cc]ylind</code> (<code>**/DEVELOPMENT_PLAN.md</code>), 5 results</summary>

- [DEVELOPMENT_PLAN.md:213](../DEVELOPMENT_PLAN.md#L213)
- [DEVELOPMENT_PLAN.md:320](../DEVELOPMENT_PLAN.md#L320)
- [DEVELOPMENT_PLAN.md:340](../DEVELOPMENT_PLAN.md#L340)
- [DEVELOPMENT_PLAN.md:679](../DEVELOPMENT_PLAN.md#L679)
- [DEVELOPMENT_PLAN.md:685](../DEVELOPMENT_PLAN.md#L685)
</details>

Good — stage 2.2 is already marked as implemented. Now let me commit the changes:

**Todo list:**
- ✅ Read LEARNINGS.md, README.md
- ✅ View HEAD commit diff
- ✅ Analyze algorithm correctness
- ✅ Fix incorrect/incomplete sections
- 🔄 Commit changes

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git add -A && git commit -F - <<'EOF'
Review and fix stage 2.2 algorithm description for 3-face seeding

Reviewed the rewritten stage 2.2 cylindrical hypothesis algorithm in
DEVELOPMENT_PLAN.md. Fixed several issues:

1. BFS expansion incorrectly used "pairwise qualified" term — during BFS,
   only the dihedral angle check is applicable. The cross product minimum
   is a seeding criterion only (adjacent cylinder faces legitimately have
   near-parallel normals). The "unassigned" check doesn't apply to
   trial-claimed faces. Rewrote to explicitly state the angular tolerance
   check and explain why full pairwise qualification doesn't apply.

2. Added missing fallback behavior: when no valid (fi, n1, n2) triple
   exists, fi is left as UNDEDUCED (may be absorbed by later BFS). After
   all faces processed, remaining UNDEDUCED faces are set to NO_HYPOTHESIS.

3. Updated computational cost section (was "UPDATE ME") — described the
   K×L trial structure for 3-face seeding with typical values.

4. Updated cylinder fitting to reference 3-face seed (full eigenvector
   computation, not the 2-face cross-product shortcut).

5. Rewrote "Near-parallel seed normals" subtlety for 3-face seeds —
   explains that fi↔n1 and n1↔n2 each need cross product > threshold,
   but fi↔n2 need not differ (same circumferential position is fine).

6. Updated intro paragraph from "pairs of adjacent faces" to "triples
   of nearby faces".

Analysis: The 3-face seeding approach should improve cylinder deduction by
providing better initial axis and radius estimates (3 angular samples vs 2),
reducing bogus seed fits. The n2-not-neighbor-of-fi constraint ensures seeds
span at least 2 mesh edges of circumferential arc, giving meaningful angular
diversity. This works on typical CAD tessellations where cylinder quad strips
have sufficient connectivity for fi→n1→n2 chains.

---
Generated with assistance from Claude Opus 4 (Anthropic, 2025)

User prompt(s):
- "Iterate: I've rewritten the algorithm of stage 2.2 to try to ensure that
  BFS is seeding from 3 faces, and try to ensure that BFS is exploring in
  all directions, and applying validity criteria consistently. Look at the
  diff of the HEAD commit to see the changes. Think about whether this is
  likely to improve cylinder deduction. Rewrite any sections that seem
  incorrect or incomplete. Then commit. Don't change any code."
EOF
```
<details>
<summary>Output (5 lines)</summary>

```
Warning: commit message did not conform to UTF-8.
You may want to amend it after fixing the message, or set the config
variable i18n.commitEncoding to the encoding your project uses.
[main 5a7d3c8] Review and fix stage 2.2 algorithm description for 3-face seeding
 1 file changed, 7 insertions(+), 5 deletions(-)
```
</details>

Now export the chat log:

**Terminal:**
```sh
( ../vscode-export-chat-log/export-chat-log.py --wait && git add agent-logs/ && git commit --amend --no-edit ) &
```
```
(base) whitelily:brepper sethml$
```

**Todo list:**
- ✅ Read LEARNINGS.md, README.md
- ✅ View HEAD commit diff
- ✅ Analyze algorithm correctness
- ✅ Fix incorrect/incomplete sections
- ✅ Commit changes

**Summary of what I did:**

Reviewed the rewritten stage 2.2 algorithm for 3-face cylinder seeding and fixed 6 issues:

1. **BFS "pairwise qualified" was wrong** — the full pairwise qualification (cross product minimum + unassigned check) should NOT be applied during BFS expansion. Adjacent faces on finely tessellated cylinders have near-parallel normals (tiny cross product), which is perfectly valid. Rewrote to explicitly state only the dihedral angle check applies during BFS.

2. **Missing fallback** — added what happens when no valid (fi, n1, n2) triple exists (leave UNDEDUCED, cleanup at end).

3. **"UPDATE ME" computational cost** — rewrote for the K×L trial structure of 3-face seeding.

4. **Cylinder fitting reference** — updated from 2-face cross-product shortcut to 3-face eigenvector computation.

5. **Near-parallel normals subtlety** — rewrote for 3-face seeds (fi↔n2 can have identical normals; angular diversity comes from n1 at a different circumferential position).

6. **Intro paragraph** — updated from "pairs" to "triples".

**Assessment**: The 3-face seeding approach should improve cylinder deduction. By requiring seeds to span 2+ mesh edges of circumferential arc (the n2-not-neighbor-of-fi constraint), initial cylinder fits have better angular diversity — 3 samples around the circumference instead of 2. This should reduce bogus fits from locally adjacent face pairs that happen to define a cylinder but don't represent real cylindrical surfaces.

---

*A coder named Seth had a plan,*
*To seed cylinders three-face, not two, man.*
*"The spec had a flaw —*
*BFS pairwise was raw!"*
*Now the algorithm's clean as it ran.*

*2026-03-08 22:30 · 394s · Words: 5,749 in · 4,379 out · 77,648 ctx · 25 rounds*

---
