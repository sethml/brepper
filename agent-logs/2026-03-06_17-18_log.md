# Updating algorithm description and committing changes

- **Date:** 2026-03-06 17:18 – 17:43
- **Session ID:** e090335e-fd6b-4170-9203-78b9ceae571b
- **Model:** Claude Opus 4.6
- **Turns:** 1
- **Tool calls:** 6
- **Thinking blocks:** 1
- **Input words:** 524
- **Output words:** 966
- **Prompt tokens (last round):** 38,229
- **API rounds:** 7
- **Total elapsed:** 56s

## Table of Contents

1. [commit changes. then Think about the TODOs in this algorithm, and whether to update the algorithm...](#user-1) (Claude Opus 4.6)

---

<a id="user-1"></a>

## User (1)

> commit changes. then Think about the TODOs in this algorithm, and whether to update the algorithm description based on them. If you make changes, commit. Don&#x27;t edit code, just pseudo-code.

*2026-03-06 17:42*

### Assistant — Claude Opus 4.6

> 💭 The user wants me to:
> 1. Commit existing changes
> 2. Think about the TODOs in the selected text (section 2.2 of DEVELOPMENT_PLAN.md) and decide whether to update the algorithm description
> 3. If I make changes, commit again
> 
> Let me start by committing any existing changes, then analyze the TODOs.
> 
> First, let me load the tools I need for git operations.

Read changed files in the active git repository

Let me first commit the existing changes, then analyze the TODOs.

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git add -A && git diff --cached --stat
```
```
 DEVELOPMENT_PLAN.md | 12 +++++-------
 1 file changed, 5 insertions(+), 7 deletions(-)
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git commit -F - <<'EOF'
Update cylindrical hypothesis algorithm description in DEVELOPMENT_PLAN.md

- Simplified opening sentence for section 2.2 (removed redundant detail
  already covered in multi-seed evaluation)
- Moved centroid validation from post-BFS step into BFS re-fit check
  (matching actual implementation)
- Added TODO about minimum face count usefulness
- Added TODO about applying angular coverage during hypothesis growth
- Removed resolved comment block (all items addressed in code)
- Added new task for revisiting stage 2.2 implementation

---
Generated with assistance from Claude Opus 4 (Anthropic, 2025)

User prompt(s):
- "commit changes"
EOF
```
```
[main dfa3099] Update cylindrical hypothesis algorithm description in DEVELOPMENT_PLAN.md
 1 file changed, 5 insertions(+), 7 deletions(-)
```

Now let me analyze the two TODOs in the selected text. Let me re-read the relevant sections for full context.

<details>
<summary>Reading lines 244-260 of <a href="../DEVELOPMENT_PLAN.md#L244-L261">DEVELOPMENT_PLAN.md</a></summary>

```
- After BFS completes: final re-fit from all accumulated faces and vertices. Compute error metrics.
- **Minimum face count**: Require at least 3 faces in a cylindrical hypothesis. Any real cylindrical surface from a CAD tessellation will produce at least 3 facets around its circumference. This eliminates spurious 2-face cylinder fits that arise from locally adjacent faces on non-cylindrical curved surfaces (tori, cones, spheres). Combined with centroid validation, this significantly reduces false positives. TODO: is this helpful any more?

**Angular coverage validation** (applied to the best trial for each starting face):

The problem: any two non-coplanar faces fit *some* cylinder. A bogus seed that doesn't span the cylinder's circumference can grow into a small hypothesis via BFS, consuming faces that should belong to a correct hypothesis from a better seed. The angular coverage check ensures the hypothesis has genuine circumferential support — faces distributed around the cylinder, not clustered on one side.

Algorithm:
1. For each face in the hypothesis, compute the centroid and project it onto the cylinder's angular coordinate θ. Given the cylinder's axis direction `a`, axis origin `o`, and orthogonal basis vectors `u`, `w` perpendicular to `a`: for centroid `c`, compute `radial = c - o - ((c - o) · a) * a`, then `θ = atan2(radial · w, radial · u)`.
2. Sort the θ values (one per face).
3. Compute all N gaps between consecutive sorted θ values, **including the wraparound gap**: `gap_wrap = θ[0] + 2π - θ[N-1]`.
4. The **largest gap** is the "empty arc" — the angular region not covered by any face. The **span** is `2π - largest_gap`.
5. Among the remaining N-1 gaps, the **second-largest gap** must be `≤ span / 3`.

This ensures the hypothesis has at least 3 distinct angular clusters of faces around the cylinder. A bogus cylinder where all faces are side-by-side in a narrow arc will fail: e.g., 5 faces clustered in a 30° arc have span ≈ 30°, but the second-largest internal gap (~7°) may pass. However, such a cluster's centroid validation and vertex tolerance checks should already reject it. The angular coverage check primarily catches the case where a seed grabs faces from 2 sides of a non-cylindrical surface (e.g., two planar faces that happen to fit a cylinder) and BFS adds a few more nearby faces, resulting in a hypothesis with faces clustered in 1–2 angular positions rather than distributed around the circumference. TODO: should we be applying this criterion as we're growing the hypothesis?

*Note: For partial cylinders (arcs less than 360°), this check still works — it requires 3+ angular clusters within whatever arc is covered, which is the minimum for a reliable cylinder fit. A 90° cylindrical arc with 6 evenly-spaced faces has internal gaps of ~18° and span of ~90°, so second_largest_gap (18°) ≤ span/3 (30°) — passes correctly.*
```
</details>

Now let me think about these two TODOs:

**TODO 1 (line 245): "is this [minimum face count of 3] helpful any more?"**

The minimum face count of 3 was introduced to eliminate spurious 2-face cylinder fits. But now the algorithm has multi-seed evaluation with angular coverage validation. Angular coverage requires at least 3 angular clusters — which inherently requires at least 3 faces. So the minimum face count check is redundant with angular coverage validation. However, it's a cheap O(1) check that short-circuits before the more expensive angular coverage computation. It's worth keeping as a fast-path rejection, but the rationale should be updated.

**TODO 2 (line 258): "should we be applying this criterion as we're growing the hypothesis?"**

Applying angular coverage during BFS growth would be problematic: early in BFS, you naturally have few faces clustered together (BFS grows outward from a seed), so the check would fail prematurely. You'd need to wait until some minimum face count before applying it, but then you're just deferring it. The post-BFS check is the right place — the purpose is to validate the final hypothesis, not to guide growth. BFS naturally grows by adjacency, and the vertex/centroid/convexity checks during growth are the correct constraints for per-face acceptance. Angular coverage is a global structural validation.

Let me resolve both TODOs in the document.

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L245-L246">DEVELOPMENT_PLAN.md -1+1</a>, <a href="../DEVELOPMENT_PLAN.md#L258-L261">-1+3</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "245:tw",
      "content": "- **Minimum face count**: Require at least 3 faces in a cylindrical hypothesis. This is technically redundant with angular coverage validation (which inherently requires 3+ angular clusters, hence 3+ faces), but serves as a cheap O(1) early rejection before the more expensive angular coverage computation."
    },
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "258:pb",
      "content": "This ensures the hypothesis has at least 3 distinct angular clusters of faces around the cylinder. A bogus cylinder where all faces are side-by-side in a narrow arc will fail: e.g., 5 faces clustered in a 30° arc have span ≈ 30°, but the second-largest internal gap (~7°) may pass. However, such a cluster's centroid validation and vertex tolerance checks should already reject it. The angular coverage check primarily catches the case where a seed grabs faces from 2 sides of a non-cylindrical surface (e.g., two planar faces that happen to fit a cylinder) and BFS adds a few more nearby faces, resulting in a hypothesis with faces clustered in 1–2 angular positions rather than distributed around the circumference.\n\n*Note on applying angular coverage during BFS growth: this was considered but is not useful. Early in BFS, faces are naturally clustered near the seed, so the check would fail prematurely. The vertex distance, convexity, and angular tolerance checks are the correct per-face acceptance criteria during growth. Angular coverage is a global structural validation — it answers \"does this completed hypothesis look like a real cylinder?\" rather than \"should this next face be added?\"*"
    }
  ]
}
```
**Output:**
```
{
  "applied": 2,
  "failed": 0,
  "files": [
    {
      "file": "DEVELOPMENT_PLAN.md",
      "startLine": 245,
      "endLine": 260
    }
  ]
}
```
</details>

Now let me commit this change.

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git add DEVELOPMENT_PLAN.md && git commit -F - <<'EOF'
Resolve two TODOs in cylindrical hypothesis algorithm description

1. Minimum face count (was: "is this helpful any more?"): Kept the check
   but updated rationale — it's redundant with angular coverage validation
   but serves as a cheap O(1) early rejection before the more expensive
   angular coverage computation.

2. Angular coverage during growth (was: "should we apply this during BFS?"):
   Resolved as "no" — early in BFS, faces are naturally clustered near the
   seed so the check would fail prematurely. Angular coverage is a global
   structural validation for completed hypotheses, not a per-face acceptance
   criterion.

---
Generated with assistance from Claude Opus 4 (Anthropic, 2025)

User prompt(s):
- "Think about the TODOs in this algorithm, and whether to update the
   algorithm description based on them. If you make changes, commit.
   Don't edit code, just pseudo-code."
  (referring to the selected text in section 2.2 of DEVELOPMENT_PLAN.md,
  which contained two TODOs about minimum face count and angular coverage
  during BFS growth)
EOF
```
```
[main 986d53d] Resolve two TODOs in cylindrical hypothesis algorithm description
 1 file changed, 4 insertions(+), 2 deletions(-)
```

Resolved both TODOs:

1. **Minimum face count** ("is this helpful any more?"): Yes, kept it. It's technically redundant with angular coverage (which inherently needs 3+ faces), but it's a cheap O(1) short-circuit before the more expensive angular coverage computation. Updated the rationale to say this.

2. **Angular coverage during BFS growth** ("should we apply this during growth?"): No. Early in BFS, faces are clustered near the seed, so the check would fail prematurely. The per-face checks (vertex distance, convexity, angular tolerance) are the right growth constraints. Angular coverage is a global structural validation for completed hypotheses. Added an explanatory note.

*2026-03-06 17:43 · 56s · Words: 524 in · 966 out · 38,229 ctx · 7 rounds*

---
