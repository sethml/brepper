# Reworking Brepper for Enhanced Surface Detection

- **Date:** 2026-03-11 15:41 – 16:34
- **Session ID:** b8089dae-7af7-4adc-ba38-a7dbaf3600d5
- **Model:** Claude Opus 4.6
- **Turns:** 1
- **Tool calls:** 75
- **Thinking blocks:** 6
- **Input words:** 20,148
- **Output words:** 12,389
- **Prompt tokens (last round):** 67,157
- **API rounds:** 73
- **Total elapsed:** 16m41s

## Table of Contents

2. [Iterate: read papers/chatgpt-fitting-conversation.md, and think about ways to rework brepper to d...](#user-2) (Claude Opus 4.6)

---

**1 user prompt rolled back**

---

<a id="user-2"></a>

## User (2)

> Iterate: read papers/chatgpt-fitting-conversation.md, and think about ways to rework brepper to detect all of the surfaces described in the conversation and to use the clever approaches described to detect surfaces. Rewrite the development plan with a reworked pipeline and an implementation plan to incrementally rework the current codebase to match the new pipeline design, with implementation steps that are a reasonable chunk of work, leave the pipeline functional afterward, and are testable. Include adding new test cases, including synthesizing new models using ccad to as necessary to test new features.

*2026-03-11 16:17*

### Assistant — Claude Opus 4.6

> 💭 The user wants me to:
> 1. Read the papers/chatgpt-fitting-conversation.md file
> 2. Read LEARNINGS.md, README.md, and DEVELOPMENT_PLAN.md (per AGENTS.md instructions)
> 3. Think about ways to rework brepper based on the conversation
> 4. Rewrite the development plan with a reworked pipeline and incremental implementation plan
> 
> Let me start by reading the key files.

**Todo list:**
- 🔄 Read papers/chatgpt conversation
- ⬜ Read LEARNINGS.md and README.md
- ⬜ Read current DEVELOPMENT_PLAN.md
- ⬜ Read current source code structure
- ⬜ Design reworked pipeline
- ⬜ Rewrite DEVELOPMENT_PLAN.md
- ⬜ Commit changes

**Terminal:**
```sh
wc -l /Users/sethml/src/brepper/papers/chatgpt-3d-fitting-conversation.md
```
```
    5270 /Users/sethml/src/brepper/papers/chatgpt-3d-fitting-conversation.md
```

<details>
<summary>Reading lines 1-200 of <a href="../papers/chatgpt-3d-fitting-conversation.md#L1-L201">chatgpt-3d-fitting-conversation.md</a></summary>

````
You: 

Find me an algorithm to fit a 3d cylinder to a set of 3d points minimizing sum of error squared.

ChatGPT: 

The standard formulation is a **nonlinear least-squares fit of a cylinder surface**. A cylinder can be parameterized by:

-   axis direction **a** (unit vector)
    
-   a point on the axis **q**
    
-   radius **r**
    

For each point $p_i$, the distance to the cylinder surface is:

$$
d_i = \left\| (p_i - q) \times a \right\| - r
$$

because $\|(p_i-q)\times a\|$ is the perpendicular distance from the point to the cylinder axis. [rdrr.io](https://rdrr.io/github/tiagodc/TreeLS/man/sgt.irls.cylinder.html?utm_source=chatgpt.com)

You then minimize

$$
E = \sum_i d_i^2
$$

This is a **6-parameter nonlinear least-squares problem**:

-   2 DOF for axis direction (unit vector constraint)
    
-   3 DOF for axis point
    
-   1 DOF for radius
    

---

# Practical algorithm (works well in practice)

## 1\. Estimate cylinder axis (good initialization)

Compute the covariance of the points:

$$
C = \frac{1}{N}\sum (p_i-\bar p)(p_i-\bar p)^T
$$

Let $v_1,v_2,v_3$ be eigenvectors.

For cylindrical point clouds:

-   **axis ≈ eigenvector with largest eigenvalue**
    

because the data spreads most along the axis.

```
center = mean(points)  
C = covariance(points)  
axis = eigenvector\_largest(C)  
axis = normalize(axis)
```

---

## 2\. Project points to plane perpendicular to axis

Choose an orthonormal basis $u,v$ spanning the plane perpendicular to **axis**.

For each point:

$$
x_i = (p_i - center)\cdot u
$$
 
$$
y_i = (p_i - center)\cdot v
$$

Now the problem becomes **2D circle fitting**.

---

## 3\. Fit circle in that plane

Use a linear least-squares circle fit (Taubin or Pratt).

Solve:

$$
x^2 + y^2 + Ax + By + C = 0
$$

Center:

$$
(x_c,y_c)=(-A/2,-B/2)
$$

Radius:

$$
r = \sqrt{x_c^2 + y_c^2 - C}
$$

Axis point:

$$
q = center + x_c u + y_c v
$$

---

## 4\. Refine with nonlinear least squares

Use **Levenberg–Marquardt** or **Nelder–Mead** to minimize

$$
E(a,q,r)=\sum_i \left(\|(p_i-q)\times a\|-r\right)^2
$$

The optimization variables:

```
axis (2 parameters)  
axis\_point (3 parameters)  
radius (1 parameter)
```

Libraries like **Ceres**, **Eigen LM**, or **scipy.optimize** work well.

---

# Pseudocode

```
C++

axis = PCA\_axis(points)  
center = mean(points)  
  
(u,v) = orthonormal\_basis(axis)  
  
for p in points:  
    x = dot(p\-center, u)  
    y = dot(p\-center, v)  
    circle\_points.append((x,y))  
  
(cx,cy,r) = least\_squares\_circle(circle\_points)  
  
q = center + cx\*u + cy\*v  
  
optimize axis,q,r to minimize:  
    sum((norm(cross(p\-q,axis))\-r)^2)
```

---

# Robust variant (for real scans)

If your points contain outliers:

1.  **RANSAC cylinder fit**
    
2.  Then **least-squares refinement**
    

This is common in point-cloud processing.

---

# If you're implementing this in C++ (given your OCCT work)

The most stable pipeline is:

1.  PCA axis estimate
    
2.  Pratt circle fit
    
3.  Levenberg–Marquardt refinement
    

This converges extremely reliably.

---

💡 If you'd like, I can also show you a **very compact closed-form method (Eberly / David Eberly algorithm)** that avoids iterative optimization for most of the solve. It's one of the nicest cylinder fits and popular in geometry kernels.

You: 

Is there a rust library that implements this? Or OCCT function?

ChatGPT: 

Short answer: **no widely-used Rust crate directly implements cylinder fitting yet**, and **OCCT also does not provide a built-in “fit cylinder to points” function**. You generally have to implement it yourself or use a point-cloud library.

Below is the practical landscape.
````
</details>

Let me read the full conversation file and the other required files in parallel.

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#200-200), lines 200 to 600

Read [LEARNINGS.md](../LEARNINGS.md#1-1), lines 1 to 200

Read [README.md](../README.md#1-1), lines 1 to 200

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#600-600), lines 600 to 1200

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#1200-1200), lines 1200 to 1800

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#1800-1800), lines 1800 to 2400

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#2400-2400), lines 2400 to 3000

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#3000-3000), lines 3000 to 3600

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#3600-3600), lines 3600 to 4200

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#4200-4200), lines 4200 to 4800

Read [chatgpt-3d-fitting-conversation.md](../papers/chatgpt-3d-fitting-conversation.md#4800-4800), lines 4800 to 5270

**Todo list:**
- ✅ Read papers/chatgpt conversation
- 🔄 Read LEARNINGS.md and README.md
- ⬜ Read current DEVELOPMENT_PLAN.md
- ⬜ Read current source code structure
- ⬜ Design reworked pipeline
- ⬜ Rewrite DEVELOPMENT_PLAN.md
- ⬜ Commit changes

Now let me read the current `DEVELOPMENT_PLAN.md` architecture sections more carefully, and understand the current source code structure.  architecture sections more carefully, and understand the current source code structure.

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#1-1), lines 1 to 400

Read [LEARNINGS.md](../LEARNINGS.md#200-200), lines 200 to 250

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#400-400), lines 400 to 573

**Todo list:**
- ✅ Read papers/chatgpt conversation
- ✅ Read LEARNINGS.md and README.md
- ✅ Read current DEVELOPMENT_PLAN.md
- 🔄 Read current source code structure
- ⬜ Design reworked pipeline
- ⬜ Rewrite DEVELOPMENT_PLAN.md
- ⬜ Commit changes

Let me quickly check the current source file sizes and the test model structure to understand what's already implemented.

**Terminal:**
```sh
cd /Users/sethml/src/brepper && wc -l src/*.rs src/stage2/*.rs src/bin/*.rs && echo "---" && ls tests/ccad/parts/*.lua 2>/dev/null | head -30 && echo "---" && ls tests/onshape/*.stl tests/fusion/*.stl tests/manual/*.stl 2>/dev/null | head -30
```
<details>
<summary>Output (52 lines)</summary>

```
     399 src/config.rs
      67 src/lib.rs
    1064 src/stage1.rs
    4523 src/stage3.rs
     182 src/stage4.rs
    1793 src/viz.rs
    1187 src/stage2/cylindrical.rs
    1402 src/stage2/mod.rs
     442 src/stage2/planar.rs
     893 src/stage2/spherical.rs
     128 src/bin/brepper.rs
     347 src/bin/step_measure_tolerance.rs
     327 src/bin/viewer.rs
   12754 total
---
tests/ccad/parts/ball_on_cylinder.lua
tests/ccad/parts/block_corner_hole.lua
tests/ccad/parts/block_with_hole.lua
tests/ccad/parts/cube.lua
tests/ccad/parts/hemisphere.lua
tests/ccad/parts/pipe.lua
tests/ccad/parts/simple_cylinder.lua
tests/ccad/parts/simple_sphere.lua
tests/ccad/parts/spherical_pocket.lua
tests/ccad/parts/staircase.lua
tests/ccad/parts/stepped_cylinder.lua
tests/ccad/parts/t_shape.lua
tests/ccad/parts/two_holes.lua
tests/ccad/parts/wedge.lua
---
tests/fusion/plate_with_hole_100x50_high.stl
tests/fusion/plate_with_hole_100x50_low.stl
tests/fusion/plate_with_hole_100x50_medium.stl
tests/manual/cube.stl
tests/onshape/chamfered_cube_10_c1_medium.ascii.stl
tests/onshape/chamfered_cube_10_c1_medium.stl
tests/onshape/cone_15x20_medium.stl
tests/onshape/cylinder_10_r2_fine.stl
tests/onshape/cylinder_10x30_medium.stl
tests/onshape/dome_hemisphere_20_fine.stl
tests/onshape/l_bracket_simple_medium.stl
tests/onshape/part_rounded_cube_10_r2_coarse.stl
tests/onshape/part_rounded_cube_10_r2_fine.stl
tests/onshape/pill_coarse.stl
tests/onshape/pill_fine.stl
tests/onshape/pipe_elbow_10_fine.stl
tests/onshape/plate_with_hole_100x50_coarse.stl
tests/onshape/rounded_cube_10_r2_coarse.stl
tests/onshape/rounded_cube_10_r2_fine.stl
tests/onshape/rounded_cube_10_r2_medium.stl
tests/onshape/sphere_25_fine.stl
tests/onshape/stepped_block_coarse.stl
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo test -- --list 2>&1 | grep "test$" | wc -l && echo "---" && cargo test -- --list 2>&1 | grep -c "stage[1234]"
```
```
     325
---
203
```

**Todo list:**
- ✅ Read papers/chatgpt conversation
- ✅ Read LEARNINGS.md and README.md
- ✅ Read current DEVELOPMENT_PLAN.md
- ✅ Read current source code structure
- 🔄 Design reworked pipeline
- ⬜ Rewrite DEVELOPMENT_PLAN.md
- ⬜ Commit changes

> 💭 Now I have a thorough understanding of:
> 
> 1. The conversation document - it covers:
>    - Cylinder fitting (PCA + 2D circle fit + LM refinement)
>    - Torus/toroid fitting (PCA of normals → axis, normal intersection for minor radius, tube center points → circle fit for major circle)
>    - Curvature-based torus detection (Rusinkiewicz curvature estimator, k1 constant = fillet radius)
>    - Normal cone + medial axis fillet detection (constant radius → spine curve detection)
>    - Unified quadric fitting (SVD on 10-coeff quadric → classify via eigenvalues)
>    - Normal-based primitive fitting across spheres, cylinders, and cones
>    - Gaussian map classification using normal covariance eigenvalues
>    - Cone fitting via PCA axis with apex search or quadric extraction
>    - Unified surface-of-revolution fitting that reduces to profile curve analysis in the (h,r) plane
>    - Complete CAD reconstruction pipeline architecture
> 
> 2. The current codebase - approximately 12,
> 
> 700 lines of Rust with 325 tests covering planes, cylinders, and spheres, with a fully functional STL-to-STEP pipeline that's missing cone and torus support.
> 
> Now I'm thinking about how to redesign the pipeline incrementally, incorporating the unified surface-of-revolution approach so each step stays testable and functional. The key is to detect revolution surface candidates, estimate the axis, project into the (h,r) plane, then fit different profile curves—horizontal lines for cylinders, sloped lines for cones, centered circles for spheres, and offset circles for tori.
> 
> I should also layer in the Gaussian map classification using normal covariance eigenvalues as a fast pre-filter, quadric fitting via SVD for initial detection, the medial axis method for robust torus detection on fillets, and cone fitting through either PCA with apex search or quadric extraction.
> 
> Now I'm weighing two restructuring approaches: either add cone and torus as separate stages (2.4 and 2.5) to keep things simple, or unify everything post-planar detection into a surface-of-revolution framework that classifies candidates by normal covariance, estimates the axis, and fits all profile types simultaneously in the reduced (h,r) plane to pick the best fit.
> 
> For an incremental rework, keeping planar detection as-is makes sense, then replacing the current cylinder and sphere stages with a unified surface-of-revolution detector that handles all four shapes while preserving the BFS region growing strategy but improving the seeding through axis estimation and profile classification. Though I should double-check what the user actually asked for to make sure I'm addressing the right scope.
> 
> Looking at the key techniques mentioned throughout—normal covariance for axis estimation, Gaussian map classification via eigenvalues, surface-of-revolution reduction, quadric fitting, medial axis methods for torus, curvature analysis, and unified nonlinear refinement—I can design a reworked stage 2 that introduces these incrementally. Starting with planar detection unchanged, then adding a surface classification step using normal covariance to distinguish between different surface types before applying specialized solvers.
> 
> For non-planar regions, I'll use anchor-based region growing and estimate the axis from the smallest eigenvector of the normal covariance matrix, then reduce the geometry to height-radius coordinates where I can fit different profile curves—constant radius for cylinders, linear for cones, circular for spheres, and offset circles for tori.
> 
> Now I'm reconsidering the approach—the current BFS method handles cylinders and spheres well, but I'm missing cone and torus detection. Rather than trying to unify all surface-of-revolution fitting upfront (which gets complicated with region validation), I should focus on the pragmatic gaps: adding cone detection, torus detection via the medial axis trick, and keeping quadric fitting as a fast classifier or alternative seeding strategy.
> 
> For the reworked pipeline, I want to extend incrementally to support cones and tori while staying testable. The first phase would add cone support by implementing PCA-based axis estimation with apex search and LM refinement, then apply BFS region growing similar to the cylinder approach, and finally add the cone surface creation to the geometry stage.
> 
> Then I'd handle cone tangent edges and validate with test models. Phase 2 follows the same pattern for tori using medial axis techniques to find the tube center, with BFS growing and surface creation, which should unlock some of the currently failing models. Phase 3 would unify the cylinder, cone, sphere, and torus fitting into a single surface-of-revolution framework once the individual implementations are solid.
> 
> After that, I'm thinking about using normal covariance eigenvalues to pre-classify surface types before seeding, then trying quadric fitting as a fast initialization step for each region before LM refinement. For anything that doesn't fit an analytic surface, NURBS would be the fallback using OCCT's point-to-spline conversion.
> 
> But actually, I'm wondering if there's a more fundamental rework needed here—maybe I should reconsider the whole approach rather than just adding features incrementally.
> 
> The key insight is that surface-of-revolution reduction is incredibly powerful: instead of separate algorithms for each type, I can estimate the axis, convert to (h,r) coordinates, and try fitting different curves—a horizontal line for cylinders, a sloped line for cones, circles for spheres, offset circles for tori. The medial axis estimation from normals could work alongside this too: if the k points (computed as p + r*n for each point) form a line, it's a cylinder; a circle means torus; a point means sphere. This dual approach would both detect the surface type and estimate parameters simultaneously.
> 
> I'm also considering using the eigenvalue pattern of the normal covariance matrix as a fast pre-filter to classify surface types before running expensive fitting routines. This could streamline the whole detection pipeline.
> 
> For the reworked Stage 2 architecture, I'd keep the planar hypothesis deduction as-is, then replace the old separate stages with a unified non-planar surface hypothesis deduction. The algorithm would seed from each unassigned face by collecting a local neighborhood of roughly 20-30 faces, computing the normal covariance eigenvalues to classify what type of surface it might be, and skipping any that look flat since those are already handled by the planar stage.
> 
> Once I identify a promising seed, I'd use the smallest eigenvector from the covariance as an initial axis estimate, then reduce the neighborhood to height-radius coordinates and test all the profile types—horizontal line for cylinders, sloped line for cones, circles for spheres, offset circles for tori—picking the best fit by residual error. From there I'd grow the region outward using BFS while checking vertex distances to the fitted surface, refine everything with Levenberg-Marquardt optimization, and validate based on angular or solid-angle coverage depending on the surface type.
> 
> I'm realizing the current implementation already handles cylinders and spheres well with its seeded BFS approach, so rather than risk breaking things by rewriting everything as a unified SOR framework, I should incrementally add cone and torus detection as new stages using the algorithms from our discussion, then consider refactoring into unified SOR later as an optimization. For the immediate next step, I'd focus on cone support—creating test models, defining a cone hypothesis structure, and implementing the fitting logic with apex search. M (from the conversation)
> 4. Implement cone BFS region growing (analogous to cylinder)
> 5. Add cone to stage 2.6 surface selection
> 6. Add `Geom_ConicalSurface` creation in stage 3.1
> 7. Handle cone edges in stage 3.3 (cone-plane, cone-cylinder intersections)
> 8. Handle cone faces in stage 3.4
> 9. Test: cone models should pass full pipeline
> 
> **Phase B: Torus support**
> 1. Create ccad test models (filleted edge block, full torus, pipe elbow arc)
> 2. Add `ToroidalHypothesis` data structure
> 3. Implement torus fitting using the medial axis / tube center method: estimate the minor radius from normal-line intersections, compute tube centers, fit a 3D circle to those centers to extract the major circle parameters, then refine with Levenberg-Marquardt optimization
> 4. Implement torus BFS region growing
> 5. Add torus to stage 2.6 surface selection
> 6. Add `Geom_ToroidalSurface` creation in stage 3.1
> 7. Handle torus edges in stage 3.3
> 8. Handle torus faces in stage 3.4
> 9. Test: fillet models and pipe_elbow should pass
> 
> **Phase C: Unified SOR fitting (optional refactor)**
> 1. Refactor: after initial axis estimation, try all SOR profiles
> 2. This replaces per-surface-type fitting with a single unified solver
> 3. May improve accuracy on marginal cases
> 
> **Phase D: Gaussian map pre-classification**
> 1. Add surface type classification from normal covariance eigenvalues
> 2. Use as a fast filter to skip unlikely surface types during seeding
> 
> **Phase E: NURBS fallback**
> 1. For remaining single-face planar hypotheses, try NURBS fitting
> 2. Use OCCT's GeomAPI_PointsToBSplineSurface
> 
> Now I need to structure the development plan document itself. The user wants me to rewrite DEVELOPMENT_PLAN.md to incorporate these phases while preserving existing content and laying out an incremental implementation strategy. I should keep the overview and assumptions, then reorganize the pipeline description and add a clear implementation roadmap showing how to incrementally refactor the codebase to match the new design.
> 
> For Stage 2, I'm expanding it to handle conical and toroidal surfaces with new detection methods, while keeping the existing planar, cylindrical, and spherical approaches intact. The key additions are the cone detection using PCA with apex search and Levenberg-Marquardt fitting, the torus detection via medial axis analysis, and then extending the selection and refitting stages to work with these new surface types.
> 
> For Stage 3, I need to add the corresponding geometry creation and processing steps for cones and toruses—creating the surfaces themselves, detecting tangency conditions, computing edge curves, and generating the final faces. I'm also adding a NURBS fallback option for cases where the parametric surfaces don't fit well enough.
> 
> The key insight is that the user wants me to use the specific detection techniques from our conversation rather than generic approaches. For cones, that means leveraging quadric fitting with SVD to extract the apex, axis, and angle directly. For toruses, I should use the medial axis method where I estimate the tube radius from normal intersections, compute the medial axis points, and fit a circle to them. After region growing, I'll reduce each surface to its (h, r) parameters and try multiple profile shapes as a refinement step, guided by the normal covariance eigenvalue patterns to seed and validate the fitting.
> 
> The challenge is figuring out how to integrate this elegant unified pipeline—where I classify surfaces by normal covariance, estimate axes, reduce to (h, r), and fit profiles—with the existing BFS region growing approach.
> 
> I think the cleanest solution is to keep BFS as the growth mechanism but use the unified SOR fitting as the backend. Instead of the current flow where I seed with a triple and fit a cylinder, then seed with a neighborhood and fit a sphere, I'd seed a neighborhood, classify it via normal covariance, estimate the axis, reduce to (h, r), try all profile shapes, pick the best fit, and then grow the region with that surface type.
> 
> For seeding, I'll stick with separate strategies for each surface type since cylinders, spheres, cones, and tori all need different geometric seeds, but I can unify the fitting and validation logic so everything goes through the same classification and profile-matching pipeline rather than separate passes for each type.
> 
> Now I need to decide whether to do a full rewrite of the development plan or just update sections. Since the user asked for a reworked pipeline and implementation plan, I think a complete rewrite makes sense while preserving the solid implementation details already documented. I'll keep the early stages as-is, completely rework the architecture for the new unified approach, and replace the implementation phases with an incremental plan that reflects the new pipeline structure.
> 
> For the architecture, I'm keeping Stage 1 intact, redesigning Stage 2 to handle all surface types through the new pipeline, updating Stage 3 accordingly, and leaving Stage 4 mostly unchanged. Then I'll mark completed work and add new phases for cone support, torus support, the SOR fitting refactor, Gaussian map classification, and NURBS fallback.
> 
> I'm starting with cone support and need to be concrete about the implementation steps—creating test models for a simple cone and a block with a conical bore, then exporting those...
> 
> Implementing cone fitting through quadric SVD classification to extract the apex, axis, and angle parameters, then adding cone region growing with BFS seeding from face triples and vertex-to-cone distance validation, followed by integrating cones into the greedy surface selection and Stage 3 geometry creation.
> 
> For Stage 3, I need to handle cone edge curves from plane and cylinder intersections, manage UV bounds for full revolutions and wire-based partial faces, then move through sewing and solid construction which should work without modification.
> 
> Now shifting to torus support—I'll create test models including a filleted block and quarter torus section using ccad's fillet operations, then build out the toroidal hypothesis data structure.
> 
> For torus fitting, I'm using the medial axis approach: computing tube centers from face normals and minor radius estimates, fitting a 3D circle through those centers to find the major radius and axis, then refining with Levenberg-Marquardt optimization.
> 
> I'll implement region growing via BFS seeded from edges showing normal rotation, add toroidal candidates to the surface selection stage, and finally create the geometric torus surface for stage 3.
> 
> 1 with tangency detection for cylinder-torus and plane-torus interactions, plus handling for torus edge curves and faces.
> 
> For the unified SOR fitting framework, I'm extracting the axis estimation logic (using normal covariance eigenvectors) into a shared function that works across cylinder, cone, and torus, then implementing a profile reduction step that converts vertices to (h,r) coordinates along the axis and fits multiple profile types—constant for cylinders, linear for cones, circular for spheres and tori—selecting the best fit by residual error.
> 
> During BFS growth, I'll use this unified SOR classification to validate surfaces as they expand from the initial seed. I'm also planning a Gaussian map pre-classification phase that examines normal covariance eigenvalues to distinguish between planar, spherical, cylindrical, conical, and toroidal surfaces, which will help guide the seeding strategy and skip unlikely surface types. Finally, I'll add a NURBS fallback for freeform surfaces that don't fit the standard SOR profiles.
> 
> Now I'm thinking through how to restructure the development plan itself. The user wants me to rewrite it, but I need to preserve all the currently-implemented algorithm descriptions while integrating these new phases. I should keep stages 1.1-1.3 and 2.1-2.3 as they are, add new sections for cone and torus detection plus the Gaussian map classification approach, and update the test cases table with entries for these new surface types.
> 
> I'm also considering what test models to generate using the available ccad functions—cones with variable diameters, fillets that create toroidal surfaces, and pipe elbows for more complex geometry.
> 
> Now I'm thinking through the core algorithmic improvements from the conversation: a quadric fitting approach that classifies surfaces in linear time using eigenvalue patterns of the coefficient matrix, and a surface-of-revolution reduction strategy that estimates the axis and fits profile curves to handle cylinders and other rotationally symmetric shapes.
> 
> For toruses specifically, I'd use the medial axis method where the locus of centers traced by the surface normals reveals the underlying geometry—a line indicates a cylinder, a circle suggests a torus, and a point means a sphere. The minor radius comes from analyzing how normal lines intersect between adjacent faces.
> 
> I'm also considering curvature signatures as a complementary detection layer, since principal curvatures have distinct patterns for each surface type: planes have near-zero curvature, spheres have equal constant curvature, cylinders have one constant and one near-zero, and toruses show constant curvature in one direction with variation in the other.
> 
> The question now is how to integrate these methods into the existing pipeline—whether to replace the current per-type BFS with a unified classification scheme or layer them as refinements. I'm consolidating the separate curved surface stages into one unified detector that uses normal covariance to classify the surface type, estimates the axis of revolution, and then applies the appropriate fitting technique for that specific geometry. Phase 1 through 6 lay out an incremental path, but I'm realizing the current architecture has a fundamental issue—stages 2.2 and 2.3 produce different hypothesis types with their own specific fields, so a unified SOR framework would need to handle polymorphic outputs based on what gets classified. The better approach is to have a single detection phase that generates various hypothesis types, then let each one proceed through its own validation pipeline.
> 
> Now I'm thinking through how to structure the development plan itself: I should describe the target architecture we're aiming for, document what's already implemented, then lay out concrete incremental steps to bridge the gap. I'll be thorough about the architecture but practical about the phases, and I should also check what test models we have available for cones and tori to inform the implementation order. I'm mapping out the specific architectural changes needed—keeping the foundational stages intact while inserting new stages for cone and torus detection, then adding a unified surface-of-revolution refactoring step and NURBS fallback for edge cases.
> 
> For the implementation timeline, I'm estimating each extension as a discrete chunk: cone hypothesis and integration would take a couple days each, torus fitting and integration slightly longer due to complexity, then the unified SOR refactor and NURBS support as follow-up phases. Each piece should be testable and leave the pipeline functional.
> 
> Now I'm laying out the phased approach with conical surfaces first—creating test models, implementing the hypothesis deduction for axis and apex fitting, extending surface selection, adding the geometry creation, handling intersections with other surface types, and validating through the full pipeline. Toroidal surfaces follow a similar structure but with more involved fitting logic for the medial surface parameters.
> 
> Beyond those, I'm thinking about extracting the common patterns across all these surface-of-revolution types into a shared framework so cylinder, sphere, cone, and torus can reuse the same axis estimation and profile fitting logic, with a fallback mechanism to try alternative profiles when the initial classification is uncertain. Then there's the longer-term work on freeform NURBS surfaces. - [ ] Implement NURBS fitting using GeomAPI_PointsToBSplineSurface
> - [ ] Integrate with stage 3
> 
> For the conical hypothesis deduction, I'm working through two complementary approaches. The first uses quadric matrix decomposition—building a feature matrix from point coordinates, extracting the smallest singular vector as coefficients, then deriving the apex and axis from eigenvalue analysis. The second approach applies PCA to establish the axis direction, projects points into cylindrical coordinates, and fits a linear profile to extract the cone angle and apex position, followed by Levenberg-Marquardt refinement. For region growing, I'm seeding from face triples with varying normals that maintain a constant angle to the axis, then expanding through vertex-to-cone adjacency.
> 
> The validation checks include radial distance tolerance, angular coverage, and a minimum face count threshold. I'm realizing the most practical path forward is actually simpler—leveraging the existing cylinder fitting pipeline since both shapes share the same axis estimation method. The key distinction is that cone radius varies linearly along the axis, so I can fit a line to the (height, radius) pairs, derive the cone angle from the slope, and position the apex accordingly, with optional Levenberg-Marquardt refinement to polish the fit.
> 
> Now moving into stage 2.5 to handle toroidal hypothesis deduction following the established conversation framework.
> 
> For toroidal detection, I'm using a medial axis approach: seed from mesh edges where adjacent face normals show significant rotation (indicating curvature typical of fillets), estimate the minor radius by finding closest approaches between normal lines from adjacent faces and taking the median, then compute tube center points by offsetting each vertex along its normal by that radius. The spine geometry classification uses PCA on these centers—if the first two eigenvalues are similar and much larger than the third, that indicates a circular major axis characteristic of a torus.
> 
> Once I've identified the torus geometry, I'll grow the region using vertex-to-torus distance with angular and convexity checks, then refine all seven parameters with Levenberg-Marquardt optimization.
> 
> Now I'm ready to implement this. Rather than rewriting the entire file, I'll update the Stage 2 architecture sections to incorporate cone and torus detection alongside the existing sphere and cylinder logic.
> 
> Let me review the Implementation Phases and test table from the document to make sure I'm not missing anything critical before I start writing.
> 
> Looking at the Stage 2 Extensions section, I can see the current plan has ruled surfaces at 2.4 and NURBS at 2.5, but the new approach will shift to cone at 2.4, torus at 2.5, and defer NURBS surfaces to a later stage. I need to carefully rewrite these sections to reflect the updated prioritization while keeping the test case structure intact. I'm finishing up the list of changes, then reconsidering the section numbering scheme to make sure the new conical and toroidal sections fit logically with the existing planar, cylindrical, and spherical hypotheses before I start writing the updated file. Now I'm mapping out the specific edits needed across the document. I'll replace the existing surface hypothesis sections with detailed algorithms for cones and tori, add a new NURBS fallback section, and then extend stages 3.1 through 3.4 to handle these additional surface types. Let me start writing out the new content for each section. 2.4 Deduce conical hypotheses
> Fit conical hypotheses to connected sets of faces that lie on a common cone. A conical hypothesis consists of:
> - `apex: [f64; 3]` — Apex (tip) of the cone.
> - `axis_direction: [f64; 3]` — Unit direction vector along the axis (from apex outward).
> - `half_angle: f64` — Half-angle of the cone (in radians). The angle between the axis and any surface line.
> - `convex: bool` — Whether face normals point away from the axis (convex, like the outside of a cone) or toward it (concave, like the inside of a conical hole).
> - `faces: Vec<usize>` — Mesh face indices that fit this hypothesis.
> - `vertices: Vec<usize>` — Mesh vertex indices on this cone.
> - `error_max: f64` — Maximum absolute distance from any vertex to the cone surface.
> - `error_abs_sum: f64` — Sum of absolute vertex-to-surface distances.
> 
> I'm using quadric fitting to initialize the cone parameters, then refining them with Levenberg-Marquardt optimization. The cone equation relates each point to the apex, axis direction, and half-angle, and I compute the signed distance from vertices to the cone surface as the residual for optimization.
> 
> For initialization, I construct a quadric feature matrix from the vertex coordinates, perform SVD to extract the quadric coefficients from the smallest singular vector, then convert those coefficients into the standard quadric matrix form.
> 
> To extract the cone parameters, I find the apex by solving where the gradient of the quadric is zero, identify the axis as the eigenvector corresponding to the distinct eigenvalue (since a cone's quadric has two equal eigenvalues and one different), and compute the half-angle from the eigenvalue ratio using the relationship between the cone's geometric parameters and the quadric's spectral decomposition.
> 
> Then I validate that the eigenvalue pattern actually matches a cone—two eigenvalues should be approximately equal while one stands out—to rule out cylinders or spheres.
> 
> When the quadric fit is unreliable due to sparse or noisy data, I switch to a profile-based approach: estimate the axis from the covariance of surface normals, then project vertices onto height and radial coordinates to fit the cone's $(h, r)$ profile directly.
> 
> After fitting both shapes, I compare their residuals to decide whether a cone is genuinely better than a cylinder, since cones with angles near 90° or 0° are effectively cylinders or planes anyway. Then I seed region growing from face triples to expand the cone surface.
> 
> During expansion, I validate each face by checking if its vertices stay within tolerance of the cone surface, ensuring centroids are close enough, verifying normal direction matches the expected convexity, and confirming dihedral angles between neighbors don't exceed the angular tolerance. I also validate angular coverage the same way as cylinders and require at least 6 faces since a cone has 6 degrees of freedom.
> 
> Finally, I refine the cone parameters using Levenberg-Marquardt optimization...
> 
> Defining toroidal hypothesis structure... The algorithm fits the torus by finding the medial axis through normal-line intersections between adjacent faces to estimate the minor radius, then refining the major radius and axis orientation from the resulting geometry. pairs with nearly parallel normals ($|n_1 \cdot n_2| > 0.95$).
> - The midpoint of closest approach approximates a tube center; distance from midpoint to each point approximates r.
> - Collect many estimates, take the median for robustness.
> 
> Step 2: Compute tube center points.
> - For each vertex with outward normal: $k_i = p_i + r \cdot n_i$ (for convex torus) or $k_i = p_i - r \cdot n_i$ (for concave torus).
> - These points should lie on the **major circle** (a circle of radius R centered at C in the plane perpendicular to a).
> 
> Step 3: Classify spine geometry (detect degeneracies).
> - Compute PCA of the k_i points. Eigenvalue pattern tells me what shape I'm dealing with: two large equal eigenvalues with a much smaller third means I have a torus, one dominant eigenvalue suggests a cylinder, and three near-zero eigenvalues indicate a sphere.
> 
> Step 4: Fit 3D circle to tube center points.
> - The smallest eigenvector from PCA gives me the torus axis direction, and I project the center points into the plane perpendicular to this axis using an orthonormal basis.
> - Then I perform a standard 2D circle fit...
> 
> Now I'm refining the fit using Levenberg-Marquardt optimization over seven parameters: the center coordinates, two angles for axis orientation, the major radius, and the minor radius. The residual measures how well each point's radial and axial distances match the expected torus geometry.
> 
> For seeding, I'm focusing on edges where adjacent mesh faces have nearly parallel normals, using the midpoint of closest approach as an initial tube center estimate and taking the median of multiple such estimates for robustness. After the region-growing phase, I need to re-fit the torus parameters on the marginal faces that were added during growth, similar to how I handle cylinder and sphere refinement. Then I'll validate the final result by checking that we have at least 7 faces (matching the torus's degrees of freedom) and confirming the faces span multiple angular positions around the minor circle rather than clustering in one region.
> 
> Now I'm mapping out the specific sections of the documentation that need updating: the ruled surface and NURBS hypothesis sections need to become cone and torus hypotheses respectively, I need to add a new NURBS fallback stage, and the surface creation, tangency, edge handling, and face creation stages all need cone and torus variants. I'll also need to update the implementation phases section with the new strategy and expand the test cases table to include cone and torus scenarios, plus add an algorithm reference section at the end.
> 
> This is a substantial rewrite, so I'm going to batch all the changes together and apply them carefully to make sure the text matches exactly where it needs to.

Now I have a thorough understanding of both the conversation's techniques and the current codebase. Let me read the remaining sections I need to modify.

Let me save the key information I need for the rewrite to session memory before context is compressed.

Created memory file [pipeline_redesign.md](/Users/sethml/Library/Application Support/Code - Insiders/User/workspaceStorage/ab9eb2372b28f9face595b837dd375f1/GitHub.copilot-chat/memory-tool/memories/YjgwODlkYWUtN2FmNy00YWRjLWJhMzgtYTdkYmFmMzYwMGQ1/pipeline_redesign.md)

Let me save additional context I need before it's cleared.

Created memory file [edit_anchors.md](/Users/sethml/Library/Application Support/Code - Insiders/User/workspaceStorage/ab9eb2372b28f9face595b837dd375f1/GitHub.copilot-chat/memory-tool/memories/YjgwODlkYWUtN2FmNy00YWRjLWJhMzgtYTdkYmFmMzYwMGQ1/edit_anchors.md)

Good, I already saved all the context I need to session memory. Let me now load the tool I need for editing and start working on the development plan.

Good, I have the hashline tools loaded. Let me make sure my session notes are complete, then proceed with reading and editing the file.

**Todo list:**
- ✅ Read papers/chatgpt conversation
- ✅ Read LEARNINGS.md and README.md
- ✅ Read current DEVELOPMENT_PLAN.md
- ✅ Read current source code structure
- 🔄 Design reworked pipeline
- ⬜ Rewrite DEVELOPMENT_PLAN.md
- ⬜ Commit changes

I've already saved the critical information to session memory. Let me verify my notes are complete before context is compressed.

Read memory

Good, my session memory files are saved. Let me now proceed to read and edit the `DEVELOPMENT_PLAN.md` file. Let me first read the file sections around the areas I need to modify.

Good, I have the hashline tools loaded. My session memory files contain all the information I need. Let me now read the sections of `DEVELOPMENT_PLAN.md` I need to edit.

<details>
<summary>Reading lines 395-420 of <a href="../DEVELOPMENT_PLAN.md#L395-L421">DEVELOPMENT_PLAN.md</a></summary>

```

*Comment: Spheres are 4 DOF (center + radius). Minimum 4 non-coplanar points for a unique fit. For "negative curvature" (concave spherical patch), track normal orientation relative to center rather than using negative radius. ~~Key challenge: partial spherical patches are hard to distinguish from cylinders or even planes if the patch is small relative to the radius. Consider requiring a minimum angular extent or using curvature analysis to disambiguate.~~ Addressed: solid-angle coverage validation prevents 1D strip growth (cylinder fillets), surface-tolerance validation in BFS prevents growth along non-spherical surfaces, and max_sphere_radius prevents degenerate large-radius fits on flat faces. Also consider toroidal surfaces (donuts, fillets)—they're common in CAD and combine characteristics of cylinders and spheres. Note: torus faces locally fit spheres, producing sphere hypotheses on torus surfaces (e.g., pipe_elbow). Stage 2.6 will need torus hypothesis support to resolve this.*

#### 2.4 Deduce ruled surface hypotheses
TODO Optional. Find mesh which is coplanar on one axis, and model as an extruded curve surface/ruled surface.

*Comment: This is a good idea for capturing linear extrusions and sweeps. A ruled surface is defined by two boundary curves with linear interpolation between them. For extrusions, one "curve" is a point (the surface degenerates to a generalized cylinder). Detection: look for parallel mesh edges that share the same direction. Fitting: project to a plane perpendicular to the ruling direction and fit a 2D curve. Watch out for twisted ruled surfaces (rulings aren't parallel)—these are harder to detect and fit.*

#### 2.5 Deduce NURBS hypotheses
TODO: for groups of adjacent faces which are covered by one- or two-face planar hypotheses and not cylindrical or spherical hypotheses, try to fit a NURBS or b-spline surface to the vertices.

*Comment: NURBS fitting is complex and requires careful consideration: (1) Parameterization: you need to assign (u,v) parameters to each mesh vertex before fitting. Common approaches: conformal mapping, Floater's mean value coordinates, or discrete harmonic mapping. (2) Degree and knot selection: start with bicubic (degree 3×3); knot placement can use chord-length parameterization or be optimized. (3) Regularization: without it, the surface may oscillate. Consider smoothness penalties. (4) An alternative worth considering: use OCCT's `GeomAPI_PointsToBSplineSurface` which handles much of this automatically. (5) For "freeform" regions that are nearly planar, a plane with small tolerance may be preferable to a NURBS that overfits noise.*

#### 2.6 Select surfaces to use for reconstruction
Assign each mesh face to exactly one "selected surface" for reconstruction. Since stages 2.1–2.3 produce hypotheses that may overlap (a face can belong to one planar, one cylindrical, and one spherical hypothesis simultaneously), surface selection must resolve these overlaps.

**Greedy area-based selection:**

The algorithm selects hypotheses greedily by total mesh face area, largest first. This naturally resolves conflicts: real surface hypotheses cover many faces with large total area, while bogus hypotheses (e.g., a near-degenerate r=139mm sphere from 3 nearly-coplanar faces) cover minimal area and lose their faces to correctly-fitted surfaces.

**Algorithm:**
1. Compute the geometric area of each mesh face.
2. Build a candidate list of all hypotheses (planar with face count > 1, cylindrical, spherical). Each candidate tracks its set of member faces and their total area.
3. **Greedy loop**: while unassigned faces remain:
   a. For each candidate, compute the total area of its still-unassigned faces.
   b. Select the candidate with the largest remaining area.
```
</details>

<details>
<summary>Reading lines 420-470 of <a href="../DEVELOPMENT_PLAN.md#L420-L471">DEVELOPMENT_PLAN.md</a></summary>

```
   b. Select the candidate with the largest remaining area.
   c. Assign all its still-unassigned faces to it as a selected surface.
   d. Remove those faces from all other candidates' pools.
4. Assign any remaining unassigned faces to their single-face planar hypothesis (fallback for faces on surfaces not yet fitted: conical, toroidal, freeform). Stages 2.4–2.5 will replace some of these with ruled or NURBS surfaces.
5. Build a `Vec<SelectedSurface>`, one per selected hypothesis, containing:
   - `surface_type: SurfaceType` — enum: Planar, Cylindrical, Spherical (later: Ruled, BSpline).
   - `hypothesis_index: usize` — index into the appropriate hypothesis vector in Stage2Output.
   - `faces: Vec<usize>` — mesh face indices assigned to this surface.
   - `vertices: Vec<usize>` — mesh vertex indices on this surface (union of face vertices).
6. Validate: every face is assigned to exactly one selected surface.
7. Print a summary: count of selected surfaces by type, total faces covered.

**Why this works better than a fixed type-priority rule:** A fixed priority (e.g., spherical > cylindrical > multi-face planar) fails when a bogus hypothesis of a high-priority type (3-face r=139mm sphere) competes with a correct hypothesis of a lower-priority type (35-face r=2mm cylinder). Area-based greedy selection naturally picks the correct hypothesis because it covers vastly more area.

**Nerfed tests:**
- `onshape_pipe_elbow_stage26`: Runs without `--compare` because torus surfaces can't be fitted with current primitives (planes, cylinders, spheres). This is a missing surface type issue, not a selection algorithm issue — greedy selection alone won't fix it.

- With the `--compare` flag, for each selected surface:
    - Compute face centroids and project them onto the selected surface.
    - Measure distance from each projected centroid to the nearest surface in the reference STEP file.
    - For all hypotheses, report an error if distance exceeds `--surface-tolerance`.

#### 2.7 Surface refitting (optional)

After stage 2.6 selects surfaces, some border faces may have been absorbed by a hypothesis that fits within tolerance but isn't the best fit. This step refines assignments and re-fits surfaces to improve accuracy.

**Algorithm:**
1. For each mesh face, check whether it belongs to more than one hypothesis (i.e., it has valid assignments in multiple hypothesis types — planar, cylindrical, spherical).
2. For each such face, compute the vertex-to-surface distance for each candidate surface (using the selected surfaces from 2.6, not the raw hypotheses).
3. Reassign the face to the selected surface with the smallest maximum vertex distance.
4. After all reassignments, re-fit each affected surface from its updated face/vertex set to remove any bias introduced by the extra vertices.

This step is expected to be most useful at boundaries between surfaces of different types (e.g., where a cylinder meets a plane), where BFS may have greedily absorbed a border face that technically fits within tolerance but belongs more naturally to the adjacent surface. It may be unnecessary if the greedy area-based selection in 2.6 already produces clean boundaries — implement only if stage 3 reconstruction reveals boundary accuracy problems.

---

### Stage 3: Surface Reconstruction

Build OCCT B-Rep topology (faces, edges, vertices) from the selected surfaces. This is the most complex stage — surface intersections in 3D are numerically delicate, and small perturbations can cause large changes in intersection curves or cause them to disappear.

**Core data structures:**

A vector of `FaceDescriptor` structs, one per selected surface:
- `selected_surface_idx: usize` — Index into the selected surfaces from stage 2.6.
- `surface: OwnedPtr<HandleGeomSurface>` — Infinite OCCT surface, type-erased (created in 3.1).
- `adjacent_faces: Vec<usize>` — Indices of adjacent FaceDescriptors, ordered topologically around this face's boundary. A face may appear multiple times 
... (truncated, 4479 chars)
```
</details>

<details>
<summary>Reading lines 470-560 of <a href="../DEVELOPMENT_PLAN.md#L470-L561">DEVELOPMENT_PLAN.md</a></summary>

```
A vector of `ReconEdge` structs:
- `face_indices: [usize; 2]` — Indices of the two adjacent FaceDescriptors.
- `vertex_indices: [usize; 2]` — Indices of the two BRepVertices at each end. For closed-loop edges (no vertices), both are `usize::MAX`.
- `curve_3d: Option<OwnedPtr<HandleGeomCurve>>` — 3D intersection curve, trimmed to vertex endpoints (populated in 3.3).
- (TODO) `pcurves: [Handle<Geom2d_Curve>; 2]` — 2D parametric curve on each adjacent face's surface.
- `tangent: bool` — Whether the adjacent surfaces are tangent along this edge (detected in 3.2).
- `mesh_boundary_vertices: Vec<usize>` — Mesh vertex indices along this boundary, ordered along the boundary.

A vector of `BRepVertex` structs:
- `point: [f64; 3]` — 3D position (currently raw mesh vertex position; future: project onto adjacent surfaces and average).
- `adjacent_faces: Vec<usize>` — Indices of FaceDescriptors meeting at this vertex, in topological order.
- `adjacent_edges: Vec<usize>` — Indices of ReconEdges meeting at this vertex, in topological order.
- (TODO) `uv_coords: Vec<(f64, f64)>` — UV coordinates of this vertex on each adjacent face's surface.
- (TODO) `tolerance: f64` — Maximum deviation from any adjacent surface.

#### 3.1 Create OCCT surface objects and build adjacency graph

**Surface creation** (`create_occt_surface`):
- For each selected surface, create the corresponding infinite OCCT surface:
    - Planar → `geom::Plane::new_pnt_dir` with `gp::Pnt` at centroid and `gp::Dir` along normal. Wrapped via `to_handle().to_handle_surface()`.
    - Cylindrical → `geom::CylindricalSurface::new_ax3_real` with `gp::Ax3` from axis origin/direction and radius. Wrapped via `to_handle().to_handle_surface()`.
    - Spherical → `geom::SphericalSurface::new_ax3_real` with `gp::Ax3` from center and Z-axis direction. Wrapped via `to_handle().to_handle_surface()`.
    - (Future: BSpline → `Geom_BSplineSurface` via `GeomAPI_PointsToBSplineSurface`, etc.)
- For consistency with OCCT conventions: planes use gp_Ax3 with Z as normal; cylinders have Z along axis; spheres have poles at Z extremes. This affects pcurve computation in 3.3.

**Adjacency graph construction:**
1. Build face-to-surface map: for each mesh face, record which selected surface it belongs to (`build_face_to_surface_map`).
2. Collect boundary edges: walk all mesh edges; when an edge separates faces belonging to different surfaces, record it as an `UndirectedEdge` keyed by `SurfacePair` (`collect_boundary_edges`).
3. Chain boundary edges: for each surface pair, chain connected boundary edges into ordered vertex sequences (`chain_boundary_edges`). Each chain becomes a `ReconEdge` with the mesh boundary vertices in order.
4. Find corner vertices: mesh vertices where 3+ selected surfaces meet become `BRepVertex` entries (`find_corner_vertices`). Their 3D position is taken from the mesh vertex.
5. Assign vertex indices to edges: for each `ReconEdge`, check if its first/last mesh boundary vertex is a corner vertex and set `vertex_indices` accordingly (or `usize::MAX` for closed-loop edges).
6. Build face descriptors: for each selected surface, create a `FaceDescriptor` with the OCCT surface handle. Walk the boundary edges, ordering them topologically around the face boundary (`walk_edges_around_face`). This uses a rotation-order algorithm: at each vertex, pick the next edge that rotates counter-clockwise around the face. This populates `adjacent_faces`, `edge_indices`, and `vertex_indices` for each face.
7. Build vertex adjacency: for each `BRepVertex`, walk the edges meeting at that vertex in topological order, recording `adjacent_faces` and `adjacent_edges`.
8. Validate topology: check Euler's formula V-E+F=2 for genus-0 models.

- With the `--compare` flag:
    - For each `ReconEdge`, sample the mesh boundary vertices along the edge. For each sample vertex, compute the distance to the nearest edge/vertex in the reference STEP file using `BRepExtrema_DistShapeShape`. Report an error if any sample e
... (truncated, 10122 chars)
```
</details>

<details>
<summary>Reading lines 559-650 of <a href="../DEVELOPMENT_PLAN.md#L559-L651">DEVELOPMENT_PLAN.md</a></summary>

```

- With the `--compare` flag:
    - For each ReconEdge with a computed `curve_3d`, sample points along the trimmed curve (e.g., 10 evenly-spaced parameter values between start and end). For each sample point, compute the distance to the nearest edge in the reference STEP file using `BRepExtrema_DistShapeShape`. Report an error if any sample exceeds `--vertex-tolerance`.
    - Additionally, verify that the curve endpoints coincide with BRepVertex positions within `--vertex-tolerance`.
    - This validates that the computed intersection curves not only connect the right vertices but also follow the correct geometric path between them.

#### 3.4 Create OCCT faces
For each ReconFace, construct a `TopoDS_Face` from the surface and bounding wires.

**Algorithm (as implemented):**

*Stage 1: Create TopoDS_Edge for each ReconEdge.*
- For each `ReconEdge`, create a `BRepBuilderAPI_MakeEdge` using the trimmed 3D curve and shared `TopoDS_Vertex` endpoints from stage 3.3. For periodic curves (circles from cylinder/sphere intersections), vertex order is matched to the curve parameterization direction: V1 corresponds to `first_parameter()` and V2 to `last_parameter()`. This is critical because `MakeEdge` strips `TrimmedCurve` wrappers and uses `ElCLib::AdjustPeriodic` on the base periodic curve, which always selects the forward (CCW) arc from V1 to V2.
- **Adaptive vertex tolerance**: Before each `MakeEdge`, the distance between each vertex position and its curve endpoint is computed. The OCCT vertex tolerance is updated to at least that distance (with 1% margin), accommodating imprecisely-fitted surface intersection curves that don't pass exactly through mesh vertex positions. This replaces the earlier fixed `vertex_tolerance_mm` approach.

*Stage 2: Create OCCT faces.*
- **Planar faces** use wire-based construction:
  - Group the face's edges into wire loops based on `BRepVertex` connectivity. Edges sharing a vertex index are grouped together; closed-loop edges (vertex_indices both `usize::MAX`) each form a separate wire.
  - Build `BRepBuilderAPI_MakeWire` for each group by sequentially adding `TopoDS_Edge` objects.
  - Identify the outer wire as the group with the most edges (heuristic).
  - Create `BRepBuilderAPI_MakeFace` with the surface handle and outer wire, then add inner wires as holes.

- **Cylindrical and spherical faces** use three approaches:
  - **Full revolution** (all boundary edges are closed loops): UV-bounds construction via `MakeFace::new_handlegeomsurface_real5(surface, umin, umax, vmin, vmax, tol)`. UV bounds are computed from edge projections using a circular gap algorithm that handles periodicity. This automatically creates seam edges for proper B-Rep topology.
  - **Spherical faces with closed-loop edges near poles** (e.g., pill/capsule hemispheres or dome hemispheres cut by a meridional plane): The boundary circles may pass through or near the sphere's UV singularities (poles at V=±π/2 where U is undefined). When any boundary circle passes within 45° of either pole (detected via `ProjectPointOnCurve` + chord-length formula), the sphere surface is recreated with its Z-axis aligned to a reorientation axis. The axis is determined from: (a) the adjacent cylinder's axis direction for tangent edges, or (b) the boundary circle's plane normal for non-tangent edges (computed from 3 sampled points on the curve). This is safe because for all-closed-loop boundaries, the reorientation axis is unique. The hemisphere is selected by projecting a mesh face centroid onto the oriented surface and checking the V-sign. UV-bounds [0,2π] × [0,π/2] or [0,2π] × [-π/2,0] then work correctly since the boundary circle becomes an iso-V curve.
  - **Spherical faces with pole vertices** (e.g., rounded cube corners where cylinders meet at a sphere pole): When a spherical face's UV bounds include V=±π/2, vertex U values at the pole are undefined (singularity). The `compute_uv_bounds_from_edges` function excludes pole U values (within 0.01 radian
... (truncated, 10211 chars)
```
</details>

Now let me read the Implementation Phases section and the rest of the file:

<details>
<summary>Reading lines 650-810 of <a href="../DEVELOPMENT_PLAN.md#L650-L811">DEVELOPMENT_PLAN.md</a></summary>

```
Each stage will be represented by a function which takes a ref to a configuration data structure and consumes an input data structure, and returns an output data structure (or error). That data structure is passed to the next stage:

- main:
  - let config = parse flags, load --compare STEP file
  - out1 = stage1(config).unwrap()
  - if config.stage < 2: exit
  - out2 = stage2(congig, out1).unwrap()
  - if config.stage < 3: exit
  - out3 = stage3(config, out2).unwrap()
  - if config.stage < 4: exit
  - stage4(config, out3).unwrap()  # writes output file
- stageN:
  - execute whatever portions of stage1 are allowed by config.stage

Each stage should have a source file stageN.rs, with a definition of that stage's output data structure at the top, the stageN() function, and then whatever other functions are required to implement the stage's functionality.

---

## Implementation Phases

### Foundation
- [x] Project setup (crates, cargo.toml, dependencies)
- [x] Test utility: read an STL and a STEP, compute maximum distance between STL vertices and STEP surfaces, and print it out. Create a script in scripts/ to apply it to all of the stl/step file pairs under tests/ and print out a table of maximum distances.
- [x] Program skeleton: implement main program, flag parsing, and create stub source files for each stage. Create stage output data structures. For portions of the data structures which are unclear at this point, stub them out with comments. Take existing stage 1.1 and stage 1.2 implementations in mesh.rs and reformulate them into stage1.rs and appropriate output data structures.

### Stage 1 Mesh Input
- [x] Stage 1.1: Read STL file into `ConnectedMesh`, including welded vertices and per-face placeholder fields for neighbors, normals, and hypotheses.
- [x] Stage 1.2: Mesh validation pass to compute face normals, edge neighbors, manifold stats, connected shells, and orientation consistency checks.
- [x] Stage 1.3: Coplanar triangle fusion — merge adjacent coplanar triangle pairs into quads. This simplifies surface fitting by ensuring curved-surface facets (cylinder/sphere quad strips) become single faces rather than 2-face planar hypothesis groups.
- [x] Implement tests that all stl/step file pairs in tests/ pass consistency checks and pass when fed to brepper with the --compare flag. Also invent a few file pairs in tests/bad that will fail with the --compare flag by editing the location of one or more vertices to fail surface closeness tests. Also invent some bad cases that fail the mesh validation tests in various ways. Ensure that these bad cases fail in the correct way.

### Stage 2: Surface Fitting - planes, cylinders, and spheres, oh my!
- [x] Understand stage 2.1 and the existing test models under tests/. Imagine additional test shapes which will be challenging for stage 2.1. Create these test shapes in tests/ccad/ - see tests/ccad/README.md.
- [x] Implement stage 2.1. Make sure --compare passes for all test shapes composed only of planar surfaces.
- [x] Understand stage 2.2 and the existing test models under tests/. Imagine additional test shapes which will be challenging for stage 2.2. Create these test shapes in tests/ccad/ - see tests/ccad/README.md.
- [x] Implement stage 2.2: Deduce cylindrical hypotheses.
- [x] Understand stage 2.3 and the existing test models under tests/. Imagine additional test shapes which will be challenging for stage 2.3. Create these test shapes in tests/ccad/ - see tests/ccad/README.md.
- [x] Implement stage 2.3: Deduce spherical hypotheses.
- [x] Implement stage 2.6: Select surfaces for reconstruction using per-face priority rule.
- [x] Implement the --angular-tolerance flag for cylindrical and spherical surface hypothesis generation.
- [x] Revisit stage 2.6: Replace per-face priority rule with greedy area-based selection.
- [x] Revisit stage 2.2: Update cylindrical hypothesis to match updated algorithm description from commits 2ac6d4f onward. Test that deduced cylinders on test models m
... (truncated, 14309 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
650:od|Each stage will be represented by a function which takes a ref to a configuration data structure and consumes an input data structure, and returns an output data structure (or error). That data structure is passed to the next stage:
651:dd|
652:li|- main:
653:vs|  - let config = parse flags, load --compare STEP file
654:ql|  - out1 = stage1(config).unwrap()
655:lb|  - if config.stage < 2: exit
656:ct|  - out2 = stage2(congig, out1).unwrap()
657:wx|  - if config.stage < 3: exit
658:wx|  - out3 = stage3(config, out2).unwrap()
659:zi|  - if config.stage < 4: exit
660:qs|  - stage4(config, out3).unwrap()  # writes output file
661:yb|- stageN:
662:us|  - execute whatever portions of stage1 are allowed by config.stage
663:dd|
664:xh|Each stage should have a source file stageN.rs, with a definition of that stage's output data structure at the top, the stageN() function, and then whatever other functions are required to implement the stage's functionality.
665:dd|
666:ea|---
667:dd|
668:bj|## Implementation Phases
669:dd|
670:pl|### Foundation
671:ww|- [x] Project setup (crates, cargo.toml, dependencies)
672:fu|- [x] Test utility: read an STL and a STEP, compute maximum distance between STL vertices and STEP surfaces, and print it out. Create a script in scripts/ to apply it to all of the stl/step file pairs under tests/ and print out a table of maximum distances.
673:st|- [x] Program skeleton: implement main program, flag parsing, and create stub source files for each stage. Create stage output data structures. For portions of the data structures which are unclear at this point, stub them out with comments. Take existing stage 1.1 and stage 1.2 implementations in mesh.rs and reformulate them into stage1.rs and appropriate output data structures.
674:dd|
675:au|### Stage 1 Mesh Input
676:vh|- [x] Stage 1.1: Read STL file into `ConnectedMesh`, including welded vertices and per-face placeholder fields for neighbors, normals, and hypotheses.
677:wv|- [x] Stage 1.2: Mesh validation pass to compute face normals, edge neighbors, manifold stats, connected shells, and orientation consistency checks.
678:og|- [x] Stage 1.3: Coplanar triangle fusion — merge adjacent coplanar triangle pairs into quads. This simplifies surface fitting by ensuring curved-surface facets (cylinder/sphere quad strips) become single faces rather than 2-face planar hypothesis groups.
679:ix|- [x] Implement tests that all stl/step file pairs in tests/ pass consistency checks and pass when fed to brepper with the --compare flag. Also invent a few file pairs in tests/bad that will fail with the --compare flag by editing the location of one or more vertices to fail surface closeness tests. Also invent some bad cases that fail the mesh validation tests in various ways. Ensure that these bad cases fail in the correct way.
680:dd|
681:yt|### Stage 2: Surface Fitting - planes, cylinders, and spheres, oh my!
682:um|- [x] Understand stage 2.1 and the existing test models under tests/. Imagine additional test shapes which will be challenging for stage 2.1. Create these test shapes in tests/ccad/ - see tests/ccad/README.md.
683:qj|- [x] Implement stage 2.1. Make sure --compare passes for all test shapes composed only of planar surfaces.
684:qs|- [x] Understand stage 2.2 and the existing test models under tests/. Imagine additional test shapes which will be challenging for stage 2.2. Create these test shapes in tests/ccad/ - see tests/ccad/README.md.
685:tw|- [x] Implement stage 2.2: Deduce cylindrical hypotheses.
686:ce|- [x] Understand stage 2.3 and the existing test models under tests/. Imagine additional test shapes which will be challenging for stage 2.3. Create these test shapes in tests/ccad/ - see tests/ccad/README.md.
687:bg|- [x] Implement stage 2.3: Deduce spherical hypotheses.
688:lu|- [x] Implement stage 2.6: Select surfaces for reconstruction using per-face priority rule.
689:sw|- [x] Implement the --angular-tolerance flag for cylindrical and spherical surface 
... (truncated, 15303 chars)
```
</details>

Now I have the complete picture. Let me now apply all the edits. I'll do this systematically.

**Todo list:**
- ✅ Read papers/chatgpt conversation
- ✅ Read LEARNINGS.md and README.md
- ✅ Read current DEVELOPMENT_PLAN.md
- ✅ Read current source code structure
- ✅ Design reworked pipeline
- 🔄 Rewrite DEVELOPMENT_PLAN.md
- ⬜ Commit changes

Now I'll apply the edits. Let me do this in several batches:

**Edit 1:** Replace sections 2.4 and 2.5 (ruled surfaces and NURBS TODOs) with conical and toroidal hypothesis descriptions.

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L398-L532">DEVELOPMENT_PLAN.md -10+134</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "398:az,399:cj,400:dd,401:ne,402:dd,403:we,404:ib,405:dd,406:wv,407:dd",
      "content": "#### 2.4 Deduce conical hypotheses\n\nFit conical hypotheses to connected sets of faces that lie on a common cone. A cone is a surface of revolution (SOR) with a linear profile — it generalizes the cylinder (which has a constant-radius profile). Conical surfaces commonly appear as chamfers, tapered bores, and funnel shapes in CAD models.\n\nA conical hypothesis consists of:\n- `apex: [f64; 3]` — Apex (tip) of the cone.\n- `axis_direction: [f64; 3]` — Unit direction vector along the axis (from apex outward toward the open end).\n- `half_angle: f64` — Half-angle of the cone (in radians). The angle between the axis and any generator line on the surface.\n- `convex: bool` — Whether face normals point away from the axis (convex, like the outside of a cone) or toward it (concave, like the inside of a conical hole).\n- `faces: Vec<usize>` — Mesh face indices that fit this hypothesis.\n- `vertices: Vec<usize>` — Mesh vertex indices on this cone.\n- `error_max: f64` — Maximum absolute distance from any vertex to the cone surface.\n- `error_abs_sum: f64` — Sum of absolute vertex-to-surface distances.\n\n**Cone parameterization:**\nA cone with apex $A$, axis direction $\\hat{a}$, and half-angle $\\theta$ satisfies: for any point $p$ on the surface, the angle between $(p - A)$ and $\\hat{a}$ equals $\\theta$. Equivalently, in (h,r) coordinates where $h = (p - A) \\cdot \\hat{a}$ (signed axial distance from apex) and $r = |(p - A) - h\\hat{a}|$ (radial distance from axis), the surface satisfies $r = h \\tan\\theta$.\n\nThe signed distance from a point $p$ to the cone surface (for LM residual) is:\n$$d = r_i \\cos\\theta - h_i \\sin\\theta$$\nwhere $h_i = (p_i - A) \\cdot \\hat{a}$ and $r_i = |(p_i - A) - h_i \\hat{a}|$.\n\n**Initialization via (h,r) profile fitting:**\nThis exploits the key insight from the unified SOR framework (see Algorithm Reference): all surfaces of revolution reduce to a 2D profile fitting problem once the axis is estimated.\n\n1. **Axis estimation** from normal covariance (same as cylinder): compute weighted covariance $M = \\sum w_i n_i n_i^T$ where $n_i$ are face normals and $w_i$ are face areas. The eigenvector with smallest eigenvalue is the axis direction estimate.\n2. **Reduce to (h,r) coordinates**: $h_i = (v_i - \\text{centroid}) \\cdot \\hat{a}$, $r_i = |(v_i - \\text{centroid}) - h_i \\hat{a}|$.\n3. **Fit linear profile** $r = m \\cdot h + b$ via weighted linear regression (weights = vertex area).\n4. **Extract cone parameters**: half-angle $\\theta = \\arctan(m)$, apex at distance $-b/m$ from centroid along axis.\n5. **Reject if cylindrical**: if $|m| < 0.01$ (nearly constant radius), this is a cylinder, not a cone. Also reject if $\\theta > 85°$ (nearly planar).\n\n**Alternative initialization via quadric fitting:**\nA cone is a
... (truncated, 12337 chars)
```
**Output:**
```
{"status":"ok","applied":1,"file":"DEVELOPMENT_PLAN.md","startLine":398,"endLine":531}
```
</details>

**Edit 2:** Update stage 2.6 to include conical and toroidal hypotheses.

<details>
<summary>Reading lines 530-560 of <a href="../DEVELOPMENT_PLAN.md#L530-L561">DEVELOPMENT_PLAN.md</a></summary>

```
*Comment: The medial axis / tube center method is far more robust for fillet-sized torus patches than direct 7-parameter nonlinear fitting. By reducing the problem to (1) estimate one scalar r, (2) fit a 3D circle to the tube centers, it separates the easy part (circle fitting is linear algebra) from the hard part (estimating the minor radius). The spine classification step (Step 3) also provides a natural way to detect degenerate cases where the "torus" is actually a cylinder or sphere — this happens when the fillet radius approaches the edge length or when the surface is nearly spherical.*

#### 2.6 Select surfaces to use for reconstruction
Assign each mesh face to exactly one "selected surface" for reconstruction. Since stages 2.1–2.3 produce hypotheses that may overlap (a face can belong to one planar, one cylindrical, and one spherical hypothesis simultaneously), surface selection must resolve these overlaps.

**Greedy area-based selection:**

The algorithm selects hypotheses greedily by total mesh face area, largest first. This naturally resolves conflicts: real surface hypotheses cover many faces with large total area, while bogus hypotheses (e.g., a near-degenerate r=139mm sphere from 3 nearly-coplanar faces) cover minimal area and lose their faces to correctly-fitted surfaces.

**Algorithm:**
1. Compute the geometric area of each mesh face.
2. Build a candidate list of all hypotheses (planar with face count > 1, cylindrical, spherical). Each candidate tracks its set of member faces and their total area.
3. **Greedy loop**: while unassigned faces remain:
   a. For each candidate, compute the total area of its still-unassigned faces.
   b. Select the candidate with the largest remaining area.
   c. Assign all its still-unassigned faces to it as a selected surface.
   d. Remove those faces from all other candidates' pools.
4. Assign any remaining unassigned faces to their single-face planar hypothesis (fallback for faces on surfaces not yet fitted: conical, toroidal, freeform). Stages 2.4–2.5 will replace some of these with ruled or NURBS surfaces.
5. Build a `Vec<SelectedSurface>`, one per selected hypothesis, containing:
   - `surface_type: SurfaceType` — enum: Planar, Cylindrical, Spherical (later: Ruled, BSpline).
   - `hypothesis_index: usize` — index into the appropriate hypothesis vector in Stage2Output.
   - `faces: Vec<usize>` — mesh face indices assigned to this surface.
   - `vertices: Vec<usize>` — mesh vertex indices on this surface (union of face vertices).
6. Validate: every face is assigned to exactly one selected surface.
7. Print a summary: count of selected surfaces by type, total faces covered.

**Why this works better than a fixed type-priority rule:** A fixed priority (e.g., spherical > cylindrical > multi-face planar) fails when a bogus hypothesis of a high-priority type (3-face r=139mm sphere) competes with a correct hypothesis of a lower-priority type (35-face r=2mm cylinder). Area-based greedy selection naturally picks the correct hypothesis because it covers vastly more area.

**Nerfed tests:**
- `onshape_pipe_elbow_stage26`: Runs without `--compare` because torus surfaces can't be fitted with current primitives (planes, cylinders, spheres). This is a missing surface type issue, not a selection algorithm issue — greedy selection alone won't fix it.

```
</details>

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L533-L535">DEVELOPMENT_PLAN.md -1+2</a>, <a href="../DEVELOPMENT_PLAN.md#L542-L544">-1+2</a>, <a href="../DEVELOPMENT_PLAN.md#L549-L551">-1+2</a>, <a href="../DEVELOPMENT_PLAN.md#L552-L554">-1+2</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "Assign each mesh face to exactly one \"selected surface\" for reconstruction. Since stages 2.1–2.5 produce hypotheses that may overlap (a face can belong to one planar, one cylindrical, one spherical, one conical, and one toroidal hypothesis simultaneously), surface selection must resolve these overlaps.\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "533:cf"
    },
    {
      "content": "2. Build a candidate list of all hypotheses (planar with face count > 1, cylindrical, spherical, conical, toroidal). Each candidate tracks its set of member faces and their total area.\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "541:fb"
    },
    {
      "content": "4. Assign any remaining unassigned faces to their single-face planar hypothesis (fallback for faces on surfaces not yet fitted: ruled, freeform). Stage 2.8 may replace some of these with NURBS surfaces.\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "547:hz"
    },
    {
      "content": "   - `surface_type: SurfaceType` — enum: Planar, Cylindrical, Spherical, Conical, Toroidal (later: BSpline).\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "549:ru"
    }
  ]
}
```
**Output:**
```
{
  "applied": 4,
  "failed": 0,
  "files": [
    {
      "file": "DEVELOPMENT_PLAN.md",
      "startLine": 533,
      "endLine": 553
    }
  ]
}
```
</details>

**Edit 3:** Add section 2.8 (NURBS fallback) after section 2.7, and add an Algorithm Reference section.

<details>
<summary>Reading lines 565-585 of <a href="../DEVELOPMENT_PLAN.md#L565-L586">DEVELOPMENT_PLAN.md</a></summary>

```
- With the `--compare` flag, for each selected surface:
    - Compute face centroids and project them onto the selected surface.
    - Measure distance from each projected centroid to the nearest surface in the reference STEP file.
    - For all hypotheses, report an error if distance exceeds `--surface-tolerance`.

#### 2.7 Surface refitting (optional)

After stage 2.6 selects surfaces, some border faces may have been absorbed by a hypothesis that fits within tolerance but isn't the best fit. This step refines assignments and re-fits surfaces to improve accuracy.

**Algorithm:**
1. For each mesh face, check whether it belongs to more than one hypothesis (i.e., it has valid assignments in multiple hypothesis types — planar, cylindrical, spherical).
2. For each such face, compute the vertex-to-surface distance for each candidate surface (using the selected surfaces from 2.6, not the raw hypotheses).
3. Reassign the face to the selected surface with the smallest maximum vertex distance.
4. After all reassignments, re-fit each affected surface from its updated face/vertex set to remove any bias introduced by the extra vertices.

This step is expected to be most useful at boundaries between surfaces of different types (e.g., where a cylinder meets a plane), where BFS may have greedily absorbed a border face that technically fits within tolerance but belongs more naturally to the adjacent surface. It may be unnecessary if the greedy area-based selection in 2.6 already produces clean boundaries — implement only if stage 3 reconstruction reveals boundary accuracy problems.

---

### Stage 3: Surface Reconstruction

```
</details>

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L575-L577">DEVELOPMENT_PLAN.md -1+2</a>, <a href="../DEVELOPMENT_PLAN.md#L581-L663">-1+82</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "1. For each mesh face, check whether it belongs to more than one hypothesis (i.e., it has valid assignments in multiple hypothesis types — planar, cylindrical, spherical, conical, toroidal).\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "575:cw"
    },
    {
      "content": "This step is expected to be most useful at boundaries between surfaces of different types (e.g., where a cylinder meets a plane), where BFS may have greedily absorbed a border face that technically fits within tolerance but belongs more naturally to the adjacent surface. It may be unnecessary if the greedy area-based selection in 2.6 already produces clean boundaries — implement only if stage 3 reconstruction reveals boundary accuracy problems.\n\n#### 2.8 NURBS fallback\n\nFor groups of adjacent faces that remain as single-face planar hypotheses after stages 2.1–2.5 (i.e., they don't fit any analytic surface), try to fit a NURBS or B-spline surface.\n\n**Algorithm:**\n1. Identify connected components of single-face planar hypothesis faces that are adjacent to each other in the mesh.\n2. For each component with ≥ 4 faces:\n   a. Compute UV parameterization of the face vertices (using discrete harmonic mapping or Floater's mean value coordinates).\n   b. Use OCCT's `GeomAPI_PointsToBSplineSurface` to fit a B-spline through the parameterized points.\n   c. Validate: max vertex-to-surface distance < `--surface-tolerance`.\n   d. If valid, create a NURBS hypothesis; if not, leave as single-face planar.\n3. Add valid NURBS hypotheses to the surface selection.\n\n*Comment: NURBS fitting is complex: parameterization quality strongly affects fit quality, degree/knot selection matters, and thin features may need special handling. Start with OCCT’s built-in B-spline fitter which handles degree selection and parameterization internally. For “freeform” regions that are nearly planar, a plane with small tolerance is preferable to a NURBS that overfits noise.*\n\n---\n\n### Algorithm Reference\n\nThis section documents key algorithmic techniques drawn from computational geometry research that inform the surface fitting approach. These are referenced by the stage descriptions above.\n\n#### Unified Surface-of-Revolution (SOR) Framework\n\nAll surfaces of revolution — cylinder, cone, sphere, torus — share a common structure: they are generated by rotating a 2D profile curve around an axis. This means they can all be fitted using a unified approach:\n\n1. **Estimate the axis of revolution** from the surface normals: for any SOR, surface normals are perpendicular to the axis (cylinder), make a constant angle with the axis (cone), point toward the axis (sphere pole behavior), or some combination. The axis can be estimated as the smallest eigenvector of the area-weighted normal covariance matrix $M = \\sum w_i n_i n_i^T$.\n\n2. **Reduce to (h,r) coordinates**: for each vertex, compute $h = (p - \\text{centroid})
... (truncated, 7535 chars)
```
**Output:**
```
{
  "applied": 2,
  "failed": 0,
  "files": [
    {
      "file": "DEVELOPMENT_PLAN.md",
      "startLine": 575,
      "endLine": 662
    }
  ]
}
```
</details>

**Edit 4:** Update stage 3.1 to include cone and torus surface creation.

Reading lines 660-610 of [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#L660-L611)

<details>
<summary>Reading lines 665-685 of <a href="../DEVELOPMENT_PLAN.md#L665-L686">DEVELOPMENT_PLAN.md</a></summary>

```

### Stage 3: Surface Reconstruction

Build OCCT B-Rep topology (faces, edges, vertices) from the selected surfaces. This is the most complex stage — surface intersections in 3D are numerically delicate, and small perturbations can cause large changes in intersection curves or cause them to disappear.

**Core data structures:**

A vector of `FaceDescriptor` structs, one per selected surface:
- `selected_surface_idx: usize` — Index into the selected surfaces from stage 2.6.
- `surface: OwnedPtr<HandleGeomSurface>` — Infinite OCCT surface, type-erased (created in 3.1).
- `adjacent_faces: Vec<usize>` — Indices of adjacent FaceDescriptors, ordered topologically around this face's boundary. A face may appear multiple times if it shares multiple edges with this face (e.g., a cylindrical face adjacent to itself via a seam edge).
- `edge_indices: Vec<usize>` — ReconEdge indices. `edge_indices[i]` is the edge between this face and `adjacent_faces[i]`.
- `vertex_indices: Vec<usize>` — BRepVertex indices at corners. `vertex_indices[i]` is the vertex between `edge_indices[i]` and `edge_indices[(i+1) % N]`.
- (TODO) `occt_face: Option<TopoDS_Face>` — Populated in 3.4.

A vector of `ReconEdge` structs:
- `face_indices: [usize; 2]` — Indices of the two adjacent FaceDescriptors.
- `vertex_indices: [usize; 2]` — Indices of the two BRepVertices at each end. For closed-loop edges (no vertices), both are `usize::MAX`.
- `curve_3d: Option<OwnedPtr<HandleGeomCurve>>` — 3D intersection curve, trimmed to vertex endpoints (populated in 3.3).
- (TODO) `pcurves: [Handle<Geom2d_Curve>; 2]` — 2D parametric curve on each adjacent face's surface.
- `tangent: bool` — Whether the adjacent surfaces are tangent along this edge (detected in 3.2).
```
</details>

<details>
<summary>Reading lines 688-710 of <a href="../DEVELOPMENT_PLAN.md#L688-L711">DEVELOPMENT_PLAN.md</a></summary>

```
A vector of `BRepVertex` structs:
- `point: [f64; 3]` — 3D position (currently raw mesh vertex position; future: project onto adjacent surfaces and average).
- `adjacent_faces: Vec<usize>` — Indices of FaceDescriptors meeting at this vertex, in topological order.
- `adjacent_edges: Vec<usize>` — Indices of ReconEdges meeting at this vertex, in topological order.
- (TODO) `uv_coords: Vec<(f64, f64)>` — UV coordinates of this vertex on each adjacent face's surface.
- (TODO) `tolerance: f64` — Maximum deviation from any adjacent surface.

#### 3.1 Create OCCT surface objects and build adjacency graph

**Surface creation** (`create_occt_surface`):
- For each selected surface, create the corresponding infinite OCCT surface:
    - Planar → `geom::Plane::new_pnt_dir` with `gp::Pnt` at centroid and `gp::Dir` along normal. Wrapped via `to_handle().to_handle_surface()`.
    - Cylindrical → `geom::CylindricalSurface::new_ax3_real` with `gp::Ax3` from axis origin/direction and radius. Wrapped via `to_handle().to_handle_surface()`.
    - Spherical → `geom::SphericalSurface::new_ax3_real` with `gp::Ax3` from center and Z-axis direction. Wrapped via `to_handle().to_handle_surface()`.
    - (Future: BSpline → `Geom_BSplineSurface` via `GeomAPI_PointsToBSplineSurface`, etc.)
- For consistency with OCCT conventions: planes use gp_Ax3 with Z as normal; cylinders have Z along axis; spheres have poles at Z extremes. This affects pcurve computation in 3.3.

**Adjacency graph construction:**
1. Build face-to-surface map: for each mesh face, record which selected surface it belongs to (`build_face_to_surface_map`).
2. Collect boundary edges: walk all mesh edges; when an edge separates faces belonging to different surfaces, record it as an `UndirectedEdge` keyed by `SurfacePair` (`collect_boundary_edges`).
3. Chain boundary edges: for each surface pair, chain connected boundary edges into ordered vertex sequences (`chain_boundary_edges`). Each chain becomes a `ReconEdge` with the mesh boundary vertices in order.
4. Find corner vertices: mesh vertices where 3+ selected surfaces meet become `BRepVertex` entries (`find_corner_vertices`). Their 3D position is taken from the mesh vertex.
5. Assign vertex indices to edges: for each `ReconEdge`, check if its first/last mesh boundary vertex is a corner vertex and set `vertex_indices` accordingly (or `usize::MAX` for closed-loop edges).
```
</details>

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L699-L706">DEVELOPMENT_PLAN.md -4+7</a>, <a href="../DEVELOPMENT_PLAN.md#L706-L708">-1+2</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "    - Planar → `geom::Plane::new_pnt_dir` with `gp::Pnt` at centroid and `gp::Dir` along normal. Wrapped via `to_handle().to_handle_surface()`.\n    - Cylindrical → `geom::CylindricalSurface::new_ax3_real` with `gp::Ax3` from axis origin/direction and radius. Wrapped via `to_handle().to_handle_surface()`.\n    - Spherical → `geom::SphericalSurface::new_ax3_real` with `gp::Ax3` from center and Z-axis direction. Wrapped via `to_handle().to_handle_surface()`.\n    - Conical → `geom::ConicalSurface::new_ax3_real2` with `gp::Ax3` from apex/direction and half-angle + radius at reference height. Wrapped via `to_handle().to_handle_surface()`. The `Ax3` origin is at the cone apex, Z along axis.\n    - Toroidal → `geom::ToroidalSurface::new_ax3_real2` with `gp::Ax3` from center/axis direction, major radius $R$, and minor radius $r$. Wrapped via `to_handle().to_handle_surface()`. The `Ax3` origin is at the torus center, Z along rotation axis.\n    - (Future: BSpline → `Geom_BSplineSurface` via `GeomAPI_PointsToBSplineSurface`.)\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "699:cs,700:wg,701:zd,702:sp"
    },
    {
      "content": "- For consistency with OCCT conventions: planes use gp_Ax3 with Z as normal; cylinders have Z along axis; spheres have poles at Z extremes; cones have Z along axis from apex; tori have Z along rotation axis. This affects pcurve computation in 3.3.\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "703:wh"
    }
  ]
}
```
**Output:**
```
{
  "applied": 2,
  "failed": 0,
  "files": [
    {
      "file": "DEVELOPMENT_PLAN.md",
      "startLine": 699,
      "endLine": 707
    }
  ]
}
```
</details>

**Edit 5:** Update stage 3.2 tangency detection to include cone and torus normal formulas.

Reading lines 720-640 of [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#L720-L641)

<details>
<summary>Reading lines 720-740 of <a href="../DEVELOPMENT_PLAN.md#L720-L741">DEVELOPMENT_PLAN.md</a></summary>

```
    - For each `ReconEdge`, sample the mesh boundary vertices along the edge. For each sample vertex, compute the distance to the nearest edge/vertex in the reference STEP file using `BRepExtrema_DistShapeShape`. Report an error if any sample exceeds `--vertex-tolerance`.
    - For each `BRepVertex`, compute the distance from the vertex's 3D position to the nearest vertex in the reference STEP file. Report an error if any exceeds `--vertex-tolerance`.
    - These checks validate that the adjacency graph's topological features (edges where surfaces meet, corners where 3+ surfaces meet) correspond to real features in the reference model.

#### 3.2 Detect tangency relationships
For each ReconEdge, determine whether the two adjacent surfaces are tangent along the shared boundary. This matters because `GeomAPI_IntSS` can fail or produce degenerate results for tangent/near-tangent surfaces, requiring special handling in edge curve computation (3.3).

**Detection algorithm (implemented):**
- For each ReconEdge, sample mesh boundary vertices along the shared boundary (every 5th vertex, minimum 3 samples, always including first and last).
- At each sample point, compute the outward-facing surface normal of both adjacent surfaces analytically:
    - Planar: the hypothesis normal (constant).
    - Cylindrical: normalized radial direction from axis to point, negated if concave.
    - Spherical: normalized direction from center to point, negated if concave.
- Compute the dot product of the two normals. If all sampled pairs agree within 2° (dot > cos(2°) ≈ 0.9994), mark the edge as tangent.
- Points too close to a cylinder axis or sphere center (degenerate) cause the edge to be marked non-tangent.

**Decision: do not modify surfaces to enforce tangency.** Modifying analytic surfaces would change the geometry. Instead, tangent edges get special handling in edge curve computation (3.3): construct the edge curve directly rather than relying on surface-surface intersection.

Tangent edges arise from fillets and blends: part_rounded_cube_10_r2 has 8 plane-cylinder tangent edges, and rounded_cube_10_r2 has 46 tangent edges (plane-cylinder + sphere-cylinder).

- With the `--compare` flag:
```
</details>

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L730-L736">DEVELOPMENT_PLAN.md -3+6</a>, <a href="../DEVELOPMENT_PLAN.md#L741-L743">-1+2</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "    - Planar: the hypothesis normal (constant).\n    - Cylindrical: normalized radial direction from axis to point, negated if concave.\n    - Spherical: normalized direction from center to point, negated if concave.\n    - Conical: radial direction from axis at the point's axial position, rotated by the cone's half-angle away from the axis, negated if concave. Specifically: for point $p$ with axial component $h = (p-A)\\cdot\\hat{a}$, the normal is the unit vector in the plane of (radial, axis) at angle $(90° - \\theta)$ from radial.\n    - Toroidal: direction from the nearest tube center point to the surface point, negated if concave. The tube center is $c = C + R \\cdot \\hat{r}$ where $\\hat{r}$ is the radial unit vector from the torus axis to the point.\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "730:xy,731:re,732:tc"
    },
    {
      "content": "Tangent edges arise from fillets and blends: part_rounded_cube_10_r2 has 8 plane-cylinder tangent edges, and rounded_cube_10_r2 has 46 tangent edges (plane-cylinder + sphere-cylinder). Toroidal fillets produce tangent edges at both boundaries (torus-plane, torus-cylinder, torus-torus).\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "738:vu"
    }
  ]
}
```
**Output:**
```
{
  "applied": 2,
  "failed": 0,
  "files": [
    {
      "file": "DEVELOPMENT_PLAN.md",
      "startLine": 730,
      "endLine": 742
    }
  ]
}
```
</details>

> 💭 Good, the tangency section has been updated. Now let me continue with the remaining edits. Let me check the current state of the file to find the next sections to edit.

Let me continue with the remaining edits. Let me read the current state of the file around the sections I still need to update.

**Todo list:**
- ✅ Update stage 3.2 tangency
- 🔄 Update stage 3.3 edge curves
- ⬜ Update stage 3.4 face creation
- ⬜ Rewrite Implementation Phases
- ⬜ Update Test Cases table
- ⬜ Update Future Enhancements
- ⬜ Review and validate full plan

<details>
<summary>Reading lines 743-820 of <a href="../DEVELOPMENT_PLAN.md#L743-L821">DEVELOPMENT_PLAN.md</a></summary>

```

- With the `--compare` flag:
    - For each ReconEdge marked as tangent, verify that the corresponding edge in the reference STEP file is also tangent (i.e., the two adjacent STEP surfaces have matching normals along the STEP edge). Report a warning if tangency is detected in the mesh but not in the STEP reference, or vice versa.
    - This is a consistency check, not a geometry check — stage 3.1's --compare already validates that mesh edge boundary vertices lie on STEP edges. Stage 3.2 adds validation that the tangency classification itself is correct.

#### 3.3 Create edge curves
For each ReconEdge, compute the 3D intersection curve between the two adjacent surfaces, trim it to the vertex endpoints, and store in `edge.curve_3d`.

**Intersection computation (implemented):**
- For non-tangent edges: use `GeomAPI_IntSS` to compute intersection curves with tolerance `vertex_tolerance_mm`. IntSS may return multiple curves (e.g., a plane cutting through a torus); `select_closest_curve()` picks the one closest to the mesh boundary vertices by sampling up to 10 evenly-spaced boundary vertices, projecting each onto each candidate curve via `GeomAPI_ProjectPointOnCurve`, and summing distances.
- For tangent edges: construct the curve analytically rather than using `GeomAPI_IntSS` (which fails or produces degenerate results for tangent/near-tangent surfaces). Dispatches by surface pair type:
  - **Plane-cylinder**: `Geom_Line` parallel to the cylinder axis at the tangent point, computed from the cylinder axis, radius, and the plane normal component perpendicular to the axis.
  - **Cylinder-cylinder**: `Geom_Line` along the first cylinder's axis direction.
  - **Sphere-cylinder**: `Geom_Circle` (great circle arc on the sphere). Center and radius come from the sphere hypothesis (which is accurately fitted, unlike cylinders which may have limited mesh data). The circle plane normal is `normalize(cross(v0 - center, v1 - center))`. Arc selection samples mesh boundary vertices to determine forward vs. reverse arc on the periodic circle.
  - **Fallback**: Construct a line from the two vertex endpoints.
  All tangent edges are trimmed to vertex endpoints using the same parameter projection as non-tangent edges. A `validate_tangent_curve` step checks that boundary vertices lie within `surface_tolerance_mm` of the constructed curve.

**Trimming to vertex endpoints (implemented):**
- For each ReconVertex at an edge endpoint, project the vertex's 3D position onto the intersection curve using `GeomAPI_ProjectPointOnCurve` to get the curve parameter value.
- Trim the curve to the parameter range `[t_start, t_end]` using `Geom_TrimmedCurve`.
- For closed-loop edges (vertex_indices == usize::MAX), use the full intersection curve parameter range.
- **Partial arc reconstruction**: When `IntSS` returns a partial arc for a closed-loop edge (e.g., a semicircle when a sphere-plane intersection passes through the sphere's UV poles), reconstruct the full circle from 3 sampled points on the arc using the circumscribed circle formula. This fixes cases where the intersection plane contains the sphere axis, causing `IntSS` to split the circle into two separate arcs in UV space.
- For non-periodic curves, ensure `t_start < t_end` (swap if needed).
- The resulting `TrimmedCurve` is upcast to `HandleGeomCurve` via `.to_handle().to_handle_curve()` and stored in `edge.curve_3d`.

**Pcurve computation (deferred to stage 3.4):**
- Pcurves will be derived when creating `TopoDS_Edge` objects.
- Options: analytic pcurves for simple cases (line on plane, circle on cylinder), or `ShapeFix_Wire::FixEdgeCurves` for general cases.

**Seam edges (future):**
- A face adjacent to itself (e.g., a full cylinder wrapping around) requires a seam edge at a fixed U or V parameter. Create the seam as an iso-parametric curve on the surface.

**Vertex position consistency (future):**
- OCCT requires all edges at a vertex to share the same `TopoDS_Vertex` (same 3D point). When edges comput
... (truncated, 10697 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
743:dd|
744:wc|- With the `--compare` flag:
745:jx|    - For each ReconEdge marked as tangent, verify that the corresponding edge in the reference STEP file is also tangent (i.e., the two adjacent STEP surfaces have matching normals along the STEP edge). Report a warning if tangency is detected in the mesh but not in the STEP reference, or vice versa.
746:qs|    - This is a consistency check, not a geometry check — stage 3.1's --compare already validates that mesh edge boundary vertices lie on STEP edges. Stage 3.2 adds validation that the tangency classification itself is correct.
747:dd|
748:oy|#### 3.3 Create edge curves
749:nm|For each ReconEdge, compute the 3D intersection curve between the two adjacent surfaces, trim it to the vertex endpoints, and store in `edge.curve_3d`.
750:dd|
751:qw|**Intersection computation (implemented):**
752:vr|- For non-tangent edges: use `GeomAPI_IntSS` to compute intersection curves with tolerance `vertex_tolerance_mm`. IntSS may return multiple curves (e.g., a plane cutting through a torus); `select_closest_curve()` picks the one closest to the mesh boundary vertices by sampling up to 10 evenly-spaced boundary vertices, projecting each onto each candidate curve via `GeomAPI_ProjectPointOnCurve`, and summing distances.
753:dm|- For tangent edges: construct the curve analytically rather than using `GeomAPI_IntSS` (which fails or produces degenerate results for tangent/near-tangent surfaces). Dispatches by surface pair type:
754:lc|  - **Plane-cylinder**: `Geom_Line` parallel to the cylinder axis at the tangent point, computed from the cylinder axis, radius, and the plane normal component perpendicular to the axis.
755:qt|  - **Cylinder-cylinder**: `Geom_Line` along the first cylinder's axis direction.
756:vs|  - **Sphere-cylinder**: `Geom_Circle` (great circle arc on the sphere). Center and radius come from the sphere hypothesis (which is accurately fitted, unlike cylinders which may have limited mesh data). The circle plane normal is `normalize(cross(v0 - center, v1 - center))`. Arc selection samples mesh boundary vertices to determine forward vs. reverse arc on the periodic circle.
757:vi|  - **Fallback**: Construct a line from the two vertex endpoints.
758:go|  All tangent edges are trimmed to vertex endpoints using the same parameter projection as non-tangent edges. A `validate_tangent_curve` step checks that boundary vertices lie within `surface_tolerance_mm` of the constructed curve.
759:dd|
760:iy|**Trimming to vertex endpoints (implemented):**
761:ji|- For each ReconVertex at an edge endpoint, project the vertex's 3D position onto the intersection curve using `GeomAPI_ProjectPointOnCurve` to get the curve parameter value.
762:ps|- Trim the curve to the parameter range `[t_start, t_end]` using `Geom_TrimmedCurve`.
763:oi|- For closed-loop edges (vertex_indices == usize::MAX), use the full intersection curve parameter range.
764:lk|- **Partial arc reconstruction**: When `IntSS` returns a partial arc for a closed-loop edge (e.g., a semicircle when a sphere-plane intersection passes through the sphere's UV poles), reconstruct the full circle from 3 sampled points on the arc using the circumscribed circle formula. This fixes cases where the intersection plane contains the sphere axis, causing `IntSS` to split the circle into two separate arcs in UV space.
765:nt|- For non-periodic curves, ensure `t_start < t_end` (swap if needed).
766:uy|- The resulting `TrimmedCurve` is upcast to `HandleGeomCurve` via `.to_handle().to_handle_curve()` and stored in `edge.curve_3d`.
767:dd|
768:rl|**Pcurve computation (deferred to stage 3.4):**
769:ob|- Pcurves will be derived when creating `TopoDS_Edge` objects.
770:fy|- Options: analytic pcurves for simple cases (line on plane, circle on cylinder), or `ShapeFix_Wire::FixEdgeCurves` for general cases.
771:dd|
772:hd|**Seam edges (future):**
773:pz|- A face adjacent to itself (e.g., a full cylinder wrapping around) requires a seam edge at a fixed U or V pa
... (truncated, 11243 chars)
```
</details>

> 💭 Let me read the rest of the file to see the Implementation Phases section and the Test Cases table.

<details>
<summary>Reading lines 820-920 of <a href="../DEVELOPMENT_PLAN.md#L820-L921">DEVELOPMENT_PLAN.md</a></summary>

```
  - `Solid`, `Compound`, and `Compsolid` sub-shapes are explored recursively for `Shell` children.
- Apply `ShapeFix_Shell::Perform()` (which calls `ShapeFix_Face::Perform()` on each face to re-add pcurves that sewing may have discarded, and `FixFaceOrientation` for edge consistency). Face orientation is NOT post-processed — `FixFaceOrientation` handles edge consistency and `SolidFromShell` (stage 3.6) handles global orientation via `PerformInfinitePoint`. Investigation showed that flipping individual face orientations breaks edge consistency and causes volume computation errors.
- Validate with `ShapeAnalysis_Shell::CheckOrientedShells` and report inconsistencies.
- Report sewing statistics: free edges, multiple edges, contiguous edges.

- With the `--compare` flag:
    - Count shells in the reference STEP file by exploring for `Shell` sub-shapes.
    - Report an error if the sewing produced free edges (edges not shared by any pair of faces), as this indicates incomplete stitching.
    - Report shell count comparison and orientation check results.

#### 3.6 Construct solids
Convert closed shells into `TopoDS_Solid` objects.

**Algorithm:**
- For each shell, use `ShapeFix_Solid::SolidFromShell` which handles orientation automatically.
- Apply `ShapeFix_Shape::Perform()` for comprehensive face/wire/edge/shell/solid fixes.
- Call `BRepLib::SameParameter` with `forced=false` and tight tolerance (`vertex_tolerance_mm`) to ensure pcurve/3D curve agreement without corrupting already-correct pcurves. Using `forced=true` or a large tolerance (e.g. 1.0mm) recomputes all pcurves and can corrupt faces with closely-spaced tangent edges (e.g. sphere faces at sphere-cylinder junctions), causing BRepCheck self-intersection failures.
- Call `BRepLib::UpdateTolerances` and `BRepLib::OrientClosedSolid`.
- Validate with `BRepCheck_Analyzer`. Log warning if validation fails (may indicate upstream face issues from stage 3.4).
- Compute and report volume using `BRepGProp::VolumeProperties`.
- Multi-shell solids (outer shell containing voids) not yet implemented — currently each shell produces one solid.

- With the `--compare` flag:
    - Count solids in the reference STEP file.
    - Compute volumes of both constructed and reference solids. Match by sorted volume. Report warning if relative volume difference exceeds 1%. All current test models achieve volume agreement within ~1e-7 relative difference.
    - Compute `BRepExtrema_DistShapeShape` between each constructed solid and the reference STEP shape. Report error if distance exceeds `--surface-tolerance`. This is the tightest geometric accuracy check.

---

### Stage 4: Output

#### 4.1 Output objects
Write constructed solid(s) to a STEP file.

**Algorithm (implemented):**
- Build a `topo_ds::Compound` containing all solids from stage 3.6.
- Set STEP metadata: AP214 schema, product name "brepper" via `Interface_Static::set_c_val`.
- Create a `STEPControl_Writer`, transfer the compound with `StepModelType::Asis` mode to preserve exact geometry.
- Write the output file. Report error if transfer or write returns non-Done status.
- With the `--compare` flag:
    - Re-read the written STEP file via `STEPControl_Reader` to validate the round-trip.
    - Compare volumes of the output shape vs reference shape using `BRepGProp::VolumeProperties`. Warn on >1% relative difference.
    - Compute maximum distance between output and reference using `BRepExtrema_DistShapeShape`. Hard error if exceeds `--surface-tolerance`.

---

## Source Code Organization

Each stage will be represented by a function which takes a ref to a configuration data structure and consumes an input data structure, and returns an output data structure (or error). That data structure is passed to the next stage:

- main:
  - let config = parse flags, load --compare STEP file
  - out1 = stage1(config).unwrap()
  - if config.stage < 2: exit
  - out2 = stage2(congig, out1).unwrap()
  - if config.stage < 3: exit
  - out3 = stage3(config, o
... (truncated, 10325 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
820:qx|  - `Solid`, `Compound`, and `Compsolid` sub-shapes are explored recursively for `Shell` children.
821:bq|- Apply `ShapeFix_Shell::Perform()` (which calls `ShapeFix_Face::Perform()` on each face to re-add pcurves that sewing may have discarded, and `FixFaceOrientation` for edge consistency). Face orientation is NOT post-processed — `FixFaceOrientation` handles edge consistency and `SolidFromShell` (stage 3.6) handles global orientation via `PerformInfinitePoint`. Investigation showed that flipping individual face orientations breaks edge consistency and causes volume computation errors.
822:jm|- Validate with `ShapeAnalysis_Shell::CheckOrientedShells` and report inconsistencies.
823:oy|- Report sewing statistics: free edges, multiple edges, contiguous edges.
824:dd|
825:wc|- With the `--compare` flag:
826:wu|    - Count shells in the reference STEP file by exploring for `Shell` sub-shapes.
827:gy|    - Report an error if the sewing produced free edges (edges not shared by any pair of faces), as this indicates incomplete stitching.
828:pf|    - Report shell count comparison and orientation check results.
829:dd|
830:nr|#### 3.6 Construct solids
831:ub|Convert closed shells into `TopoDS_Solid` objects.
832:dd|
833:mv|**Algorithm:**
834:rb|- For each shell, use `ShapeFix_Solid::SolidFromShell` which handles orientation automatically.
835:er|- Apply `ShapeFix_Shape::Perform()` for comprehensive face/wire/edge/shell/solid fixes.
836:kn|- Call `BRepLib::SameParameter` with `forced=false` and tight tolerance (`vertex_tolerance_mm`) to ensure pcurve/3D curve agreement without corrupting already-correct pcurves. Using `forced=true` or a large tolerance (e.g. 1.0mm) recomputes all pcurves and can corrupt faces with closely-spaced tangent edges (e.g. sphere faces at sphere-cylinder junctions), causing BRepCheck self-intersection failures.
837:en|- Call `BRepLib::UpdateTolerances` and `BRepLib::OrientClosedSolid`.
838:fo|- Validate with `BRepCheck_Analyzer`. Log warning if validation fails (may indicate upstream face issues from stage 3.4).
839:yf|- Compute and report volume using `BRepGProp::VolumeProperties`.
840:ln|- Multi-shell solids (outer shell containing voids) not yet implemented — currently each shell produces one solid.
841:dd|
842:wc|- With the `--compare` flag:
843:cy|    - Count solids in the reference STEP file.
844:bo|    - Compute volumes of both constructed and reference solids. Match by sorted volume. Report warning if relative volume difference exceeds 1%. All current test models achieve volume agreement within ~1e-7 relative difference.
845:wx|    - Compute `BRepExtrema_DistShapeShape` between each constructed solid and the reference STEP shape. Report error if distance exceeds `--surface-tolerance`. This is the tightest geometric accuracy check.
846:dd|
847:ea|---
848:dd|
849:vd|### Stage 4: Output
850:dd|
851:tv|#### 4.1 Output objects
852:ra|Write constructed solid(s) to a STEP file.
853:dd|
854:py|**Algorithm (implemented):**
855:ce|- Build a `topo_ds::Compound` containing all solids from stage 3.6.
856:ks|- Set STEP metadata: AP214 schema, product name "brepper" via `Interface_Static::set_c_val`.
857:pv|- Create a `STEPControl_Writer`, transfer the compound with `StepModelType::Asis` mode to preserve exact geometry.
858:qy|- Write the output file. Report error if transfer or write returns non-Done status.
859:wc|- With the `--compare` flag:
860:bc|    - Re-read the written STEP file via `STEPControl_Reader` to validate the round-trip.
861:lv|    - Compare volumes of the output shape vs reference shape using `BRepGProp::VolumeProperties`. Warn on >1% relative difference.
862:ln|    - Compute maximum distance between output and reference using `BRepExtrema_DistShapeShape`. Hard error if exceeds `--surface-tolerance`.
863:dd|
864:ea|---
865:dd|
866:yk|## Source Code Organization
867:dd|
868:od|Each stage will be represented by a function which takes a ref to a configuration data structure and consumes an inpu
... (truncated, 11032 chars)
```
</details>

<details>
<summary>Reading lines 920-1000 of <a href="../DEVELOPMENT_PLAN.md#L920-L1001">DEVELOPMENT_PLAN.md</a></summary>

```

### Stage 4: Output
- [x] Implement stage 4.1: Write solids to STEP file using `STEPControl_Writer` with `StepModelType::Asis`. Builds compound from all solids, sets AP214 schema. Compare re-reads written file and validates volume agreement (warning on >1% diff) and `BRepExtrema_DistShapeShape` distance to STEP reference. 15 integration tests: 3 basic output + 11 compare + 1 missing-output-path error.

### Stage 3 refinements
- [x] Revisit stage 3.2: Tangency detection correctly identifies 8 tangent edges on part_rounded_cube_10_r2 (cylinder-plane tangencies at fillet boundaries). Detection algorithm using analytical surface normals at sampled boundary points works well. No changes needed to the 2° threshold.
- [x] Revisit stage 3.3: Implement tangent edge curve computation. For tangent edges, construct curves analytically rather than using `GeomAPI_IntSS` (which fails for tangent surfaces). Plane-cylinder tangencies produce lines parallel to the cylinder axis at the analytically-computed tangent point. Also fix `MakeEdge` in stage 3.4: use `new_handlegeomcurve_vertex2_real2` with explicit parameter values (instead of relying on OCCT's vertex-to-curve projection which fails when mesh vertex tolerance exceeds OCCT's default precision of 1e-7mm), and set vertex tolerance to `vertex_tolerance_mm` via `BRep_Builder::update_vertex_vertex_real`. This unblocks part_rounded_cube_10_r2 — all 10 faces (6 planar + 4 cylindrical) reconstruct correctly with volume matching STEP reference to 2.19e-7 relative difference.
- [x] Implement sphere-cylinder tangent edge curves: great circle arcs on the sphere surface, using sphere center/radius (not cylinder parameters, which may be imprecise). Adaptive per-vertex tolerance in MakeEdge accommodates surface fit imprecision. Unblocks rounded_cube_10_r2_coarse full pipeline — STEP file exports with all 94 faces (76 planar + 10 cylindrical + 8 spherical), though many faces have BRepCheck warnings due to imprecise cylinder fits.
- [x] Implement closed-loop sphere-cylinder tangent edge curves and pole-safe hemisphere face construction for pill/capsule shapes. Closed-loop tangent circles are constructed as full `Geom_Circle` curves. When boundary circles pass within 45° of a sphere pole, the sphere surface is reoriented to align with the cylinder axis, making UV-bounds construction work correctly. Unblocks pill_coarse.stl and pill_fine.stl — STEP output matches reference to <1e-7 relative volume difference.
- [ ] Consider implementing stage 2.7 (surface refitting) if stage 3 reconstruction reveals boundary accuracy problems from incorrect face-to-surface assignments.

### Stage 2 Extensions: Additional Surface Types
- [ ] Understand stage 2.4 (ruled surfaces) and imagine challenging test shapes. Create test models in tests/ccad/.
- [ ] Implement stage 2.4: Deduce ruled surface hypotheses.
- [ ] Understand stage 2.5 (NURBS/B-spline surfaces) and imagine challenging test shapes. Create test models in tests/ccad/.
- [ ] Implement stage 2.5: Deduce NURBS hypotheses.
- [ ] Imagine test shapes requiring extended surface selection (mixed analytic + freeform). Create test models in tests/ccad/.
- [ ] Revisit stage 2.6: Extend surface selection to handle ruled and NURBS surfaces alongside analytic surfaces.

---

## Testing Strategy

The main testing strategy is to process a set of example stl/step pairs, and use the --compare flag to ensure that the pipeline is working correctly for each. 

1. **Unit Tests**: Individual components (readers, fitters, converters)
2. **Integration Tests**: Full pipeline on known geometries
3. **Regression Tests**: Compare output STEP to reference
4. **Validation**: Round-trip test (STEP → mesh → STEP)

### Test Cases

| Test Case | Input | Expected Output | Status |
|-----------|-------|-----------------|--------|
| Cube | 12 triangles | 6 planes, 1 solid | ✓ Stage 4.1 (STEP output) |
| Wedge | 12 triangles | 6 planes (incl. angled) | ✓ Stage 4.1 (STEP output) |
| T-Shape | 28 tr
... (truncated, 7293 chars)
```
</details>

<details>
<summary>Reading lines 1000-1050 of <a href="../DEVELOPMENT_PLAN.md#L1000-L1051">DEVELOPMENT_PLAN.md</a></summary>

```
---

## Future Enhancements

- [ ] Support for OBJ, PLY input formats
- [ ] IGES export option
- [ ] Machine learning for surface type classification
- [ ] Hole detection and filling
- [ ] Feature recognition (holes, pockets, bosses)

```
</details>

> 💭 Now I have a clear picture of the entire file. Let me make the remaining edits:
> 
> 1. Stage 3.3 - Add cone/torus edge curve handling
> 2. Stage 3.4 - Add cone/torus face creation
> 3. Rewrite "Stage 2 Extensions" implementation phase
> 4. Update test cases table
> 5. Update Future Enhancements
> 
> Let me do these edits now.
> 
> First, let me edit stage 3.3 to add cone and torus edge handling. I need to add to the tangent edge dispatch list:
> 
> Current tangent edge pairs (lines 753-757):
> - Plane-cylinder
> 
> 
> - Cylinder-cylinder
> - Sphere-cylinder
> - Fallback
> 
> Need to add:
> - Plane-cone
> - Cylinder-cone
> - Plane-torus
> - Cylinder-torus
> - Torus-torus
> - Sphere-torus
> 
> And for non-tangent edges, add notes about cone/torus intersection curves. I'll also add to the seam edges section about cone/torus seams. For stage 3.4, I need to add entries about cone and torus face creation approaches. Let me make these edits now using hashline_edit for efficiency.

Now I have a clear view of the remaining sections. Let me apply the remaining edits.Now let me apply the remaining edits. I'll do them in batch.

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L753-L765">DEVELOPMENT_PLAN.md -5+12</a>, <a href="../DEVELOPMENT_PLAN.md#L779-L784">-2+5</a>, <a href="../DEVELOPMENT_PLAN.md#L809-L811">-1+2</a>, <a href="../DEVELOPMENT_PLAN.md#L814-L818">-1+4</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "753:dm,754:lc,755:qt,756:vs,757:vi",
      "content": "- For tangent edges: construct the curve analytically rather than using `GeomAPI_IntSS` (which fails or produces degenerate results for tangent/near-tangent surfaces). Dispatches by surface pair type:\n  - **Plane-cylinder**: `Geom_Line` parallel to the cylinder axis at the tangent point, computed from the cylinder axis, radius, and the plane normal component perpendicular to the axis.\n  - **Cylinder-cylinder**: `Geom_Line` along the first cylinder's axis direction.\n  - **Sphere-cylinder**: `Geom_Circle` (great circle arc on the sphere). Center and radius come from the sphere hypothesis. The circle plane normal is `normalize(cross(v0 - center, v1 - center))`. Arc selection samples mesh boundary vertices to determine forward vs. reverse arc on the periodic circle.\n  - **Plane-cone** (TODO): `Geom_Line` along the cone's generator at the tangent azimuth, from the tangent point toward (or away from) the apex.\n  - **Cylinder-cone** (TODO): `Geom_Circle` at the axial position where the cone radius equals the cylinder radius (when coaxial), or `IntSS`-based (non-coaxial).\n  - **Plane-torus** (TODO): `Geom_Circle` — the torus-plane tangent curve is a circle on the torus at the minor boundary (inner or outer equator). Center on the major circle, radius = R ± r.\n  - **Cylinder-torus** (TODO): `Geom_Circle` at the junction where the torus minor circle meets the cylinder. Commonly occurs at fillet-to-cylinder transitions.\n  - **Torus-torus** (TODO): `Geom_Circle` where two fillet torus patches meet (e.g., at rounded corners where 3 fillets converge). The shared circle lies on both tori.\n  - **Sphere-torus** (TODO): `Geom_Circle` — analogous to sphere-cylinder but the junction is a circle on the sphere.\n  - **Fallback**: Construct a line from the two vertex endpoints.\n"
    },
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "772:hd,773:pz",
      "content": "**Seam edges (future):**\n- A face adjacent to itself (e.g., a full cylinder wrapping around) requires a seam edge at a fixed U or V parameter. Create the seam as an iso-parametric curve on the surface.\n- Conical faces: full-revolution cones require a seam edge along one generator line (iso-U curve from apex to base).\n- Toroidal faces: full-revolution tori (360° around major axis) require a seam at one meridian (iso-U); full-tube tori (360° around minor circle) also need a seam at one parallel (iso-V). A complete torus needs both.\n"
    },
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "799:vs",
      "content": "- **Cylindrical, spherical, conical, and toroidal faces** use three approaches:\n"
    },
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "803:vb",
      "content": "  - **Partial revoluti
... (truncated, 4080 chars)
```
**Output:**
```
{
  "applied": 4,
  "failed": 0,
  "files": [
    {
      "file": "DEVELOPMENT_PLAN.md",
      "startLine": 753,
      "endLine": 817
    }
  ]
}
```
</details>

**Todo list:**
- ✅ Update stage 3.2 tangency
- ✅ Update stage 3.3 edge curves
- ✅ Update stage 3.4 face creation
- 🔄 Rewrite Implementation Phases
- ⬜ Update Test Cases table
- ⬜ Update Future Enhancements
- ⬜ Review and validate full plan

Now let me rewrite the "Stage 2 Extensions" implementation phases section. Let me find the exact current line numbers:

<details>
<summary>Reading lines 940-960 of <a href="../DEVELOPMENT_PLAN.md#L940-L961">DEVELOPMENT_PLAN.md</a></summary>

```
- [x] Revisit stage 3.3: Implement tangent edge curve computation. For tangent edges, construct curves analytically rather than using `GeomAPI_IntSS` (which fails for tangent surfaces). Plane-cylinder tangencies produce lines parallel to the cylinder axis at the analytically-computed tangent point. Also fix `MakeEdge` in stage 3.4: use `new_handlegeomcurve_vertex2_real2` with explicit parameter values (instead of relying on OCCT's vertex-to-curve projection which fails when mesh vertex tolerance exceeds OCCT's default precision of 1e-7mm), and set vertex tolerance to `vertex_tolerance_mm` via `BRep_Builder::update_vertex_vertex_real`. This unblocks part_rounded_cube_10_r2 — all 10 faces (6 planar + 4 cylindrical) reconstruct correctly with volume matching STEP reference to 2.19e-7 relative difference.
- [x] Implement sphere-cylinder tangent edge curves: great circle arcs on the sphere surface, using sphere center/radius (not cylinder parameters, which may be imprecise). Adaptive per-vertex tolerance in MakeEdge accommodates surface fit imprecision. Unblocks rounded_cube_10_r2_coarse full pipeline — STEP file exports with all 94 faces (76 planar + 10 cylindrical + 8 spherical), though many faces have BRepCheck warnings due to imprecise cylinder fits.
- [x] Implement closed-loop sphere-cylinder tangent edge curves and pole-safe hemisphere face construction for pill/capsule shapes. Closed-loop tangent circles are constructed as full `Geom_Circle` curves. When boundary circles pass within 45° of a sphere pole, the sphere surface is reoriented to align with the cylinder axis, making UV-bounds construction work correctly. Unblocks pill_coarse.stl and pill_fine.stl — STEP output matches reference to <1e-7 relative volume difference.
- [ ] Consider implementing stage 2.7 (surface refitting) if stage 3 reconstruction reveals boundary accuracy problems from incorrect face-to-surface assignments.

### Stage 2 Extensions: Additional Surface Types
- [ ] Understand stage 2.4 (ruled surfaces) and imagine challenging test shapes. Create test models in tests/ccad/.
- [ ] Implement stage 2.4: Deduce ruled surface hypotheses.
- [ ] Understand stage 2.5 (NURBS/B-spline surfaces) and imagine challenging test shapes. Create test models in tests/ccad/.
- [ ] Implement stage 2.5: Deduce NURBS hypotheses.
- [ ] Imagine test shapes requiring extended surface selection (mixed analytic + freeform). Create test models in tests/ccad/.
- [ ] Revisit stage 2.6: Extend surface selection to handle ruled and NURBS surfaces alongside analytic surfaces.

---

## Testing Strategy

The main testing strategy is to process a set of example stl/step pairs, and use the --compare flag to ensure that the pipeline is working correctly for each. 

1. **Unit Tests**: Individual components (readers, fitters, converters)
2. **Integration Tests**: Full pipeline on known geometries
```
</details>

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L945-L995">DEVELOPMENT_PLAN.md -7+50</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "### Stage 2 Extensions: Conical Surfaces\n- [ ] Create ccad test models for conical surfaces: `simple_cone.lua` (truncated cone), `block_with_conical_hole.lua` (block with conical bore), `cone_cylinder.lua` (cone joined to cylinder). Export STL+STEP pairs.\n- [ ] Implement `ConicalHypothesis` data structure in stage 2 (apex, axis, half-angle, convex, faces, vertices, errors).\n- [ ] Implement cone fitting: axis estimation from normal covariance, (h,r) profile fitting via linear regression to get half-angle and apex position. See Architecture section 2.4 for details.\n- [ ] Implement cone BFS region growing with vertex-to-cone distance validation and angular coverage check.\n- [ ] Extend stage 2.6 surface selection to include conical candidates.\n- [ ] Distinguish cones from cylinders: when both fit well, compare RMS residuals and prefer simpler cylinder unless cone is significantly better.\n- [ ] Unit tests: cone fitting on synthetic point sets; BFS growing on test meshes.\n- [ ] Full pipeline tests: simple_cone and block_with_conical_hole pass `--compare` through stage 4.1.\n- [ ] Unblock existing `cone_15x20_medium` (onshape) test.\n\n### Stage 2 Extensions: Toroidal Surfaces\n- [ ] Create ccad test models for toroidal surfaces: `filleted_block.lua` (block with `fillet_all()`), `quarter_torus.lua` (quarter-torus section), `pipe_elbow_ccad.lua` (90° pipe elbow with inner/outer torus + planes).\n- [ ] Implement `ToroidalHypothesis` data structure in stage 2 (center, axis, major_radius, minor_radius, convex, faces, vertices, errors).\n- [ ] Implement torus fitting via medial axis / tube center method: estimate minor radius from normal-line intersections, compute tube centers k_i = p_i + r*n_i, fit 3D circle to tube centers for major circle parameters. See Architecture section 2.5 for details.\n- [ ] Implement torus BFS region growing with vertex-to-torus distance validation.\n- [ ] Extend stage 2.6 surface selection to include toroidal candidates.\n- [ ] Unit tests: torus fitting on synthetic point sets; BFS growing on fillet meshes.\n- [ ] Full pipeline tests: filleted_block passes `--compare` through stage 4.1.\n- [ ] Unblock existing `pipe_elbow_10_fine` (onshape) test.\n\n### Stage 3 Extensions for Cone and Torus\n- [ ] Stage 3.1: Add `Geom_ConicalSurface` and `Geom_ToroidalSurface` creation from hypothesis parameters.\n- [ ] Stage 3.2: Add cone and torus analytical normal formulas for tangency detection.\n- [ ] Stage 3.3: Implement tangent edge curve computation for plane-cone, cylinder-cone, plane-torus, cylinder-torus, and torus-torus pairs.\n- [ ] Stage 3.3: Handle non-tangent cone and torus edge intersections via `GeomAPI_IntSS` with curve selection.\n- [ ] Stage 3.4: Implement conical face construction (UV-bounds for full revolution, wire+pcurves for partial, apex singularity handling).\n- [ ] Stage 3.4: Implement toroidal face construction (UV-bounds for full revolution, wire+pcurves for partial fi
... (truncated, 5318 chars)
```
**Output:**
```
{"status":"ok","applied":1,"file":"DEVELOPMENT_PLAN.md","startLine":945,"endLine":994}
```
</details>

Now update the test cases table. Let me find the current line numbers:

<details>
<summary>Reading lines 995-1050 of <a href="../DEVELOPMENT_PLAN.md#L995-L1051">DEVELOPMENT_PLAN.md</a></summary>

```

---

## Testing Strategy

The main testing strategy is to process a set of example stl/step pairs, and use the --compare flag to ensure that the pipeline is working correctly for each. 

1. **Unit Tests**: Individual components (readers, fitters, converters)
2. **Integration Tests**: Full pipeline on known geometries
3. **Regression Tests**: Compare output STEP to reference
4. **Validation**: Round-trip test (STEP → mesh → STEP)

### Test Cases

| Test Case | Input | Expected Output | Status |
|-----------|-------|-----------------|--------|
| Cube | 12 triangles | 6 planes, 1 solid | ✓ Stage 4.1 (STEP output) |
| Wedge | 12 triangles | 6 planes (incl. angled) | ✓ Stage 4.1 (STEP output) |
| T-Shape | 28 triangles | 10 planes | ✓ Stage 4.1 (STEP output) |
| Staircase | 48 triangles | 12 planes | ✓ Stage 4.1 (STEP output) |
| Chamfered Cube (onshape) | 44 triangles | 26 planes | ✓ Stage 4.1 (STEP output) |
| Stepped Block (onshape) | 52 triangles | 16 planes | ✓ Stage 4.1 (STEP output) |
| L Bracket (onshape) | 492 triangles | 10 planes | ✓ Stage 4.1 (STEP output) |
| Cylinder (onshape) | 232 triangles | 1 cylinder + 2 planes | ✓ Stage 4.1 (STEP output) |
| Simple Cylinder (ccad) | 124 triangles | 1 cylinder + 2 planes | ✓ Stage 4.1 (STEP output) |
| Block with Hole (ccad) | 132 triangles | 6 planes + 1 concave cylinder | ✓ Stage 4.1 (STEP output) |
| Pipe (ccad) | 244 triangles | 2 cylinders (in/out) + 2 annular planes | ✓ Stage 4.1 (STEP output) |
| Stepped Cylinder (ccad) | 240 triangles | 2 cylinders + 3 planes | ✓ Stage 4.1 (STEP output) |
| Two Holes (ccad) | 252 triangles | 6 planes + 2 concave cylinders | ✓ Stage 4.1 (STEP output) |
| Block Corner Hole (ccad) | 44 triangles | 6 planes + 1 concave cylinder | ✓ Stage 4.1 (STEP output) |
| Simple Sphere (ccad) | 974 triangles | 1 sphere | ✓ Stage 4.1 (STEP output) |
| Hemisphere (ccad) | 518 triangles | 1 sphere + 1 plane | ✓ Stage 4.1 (STEP output) |
| Spherical Pocket (ccad) | 486 triangles | 6 planes + 1 concave sphere | ✓ Stage 4.1 (STEP output) |
| Ball on Cylinder (ccad) | 764 triangles | 1 sphere + 1 cylinder + 1 plane | ✓ Stage 4.1 (STEP output) |
| Sphere (onshape) | 20448 triangles | 1 sphere | ✓ Stage 4.1 (STEP output) |
| Dome Hemisphere (onshape) | 10368 triangles | 1 sphere + 1 plane | ✓ Stage 4.1 (STEP output) |
| Pill coarse (onshape) | 752 triangles | 1 cylinder + 2 spheres | ✓ Stage 4.1 (STEP output) |
| Pill fine (onshape) | 20736 triangles | 1 cylinder + 2 spheres | ✓ Stage 4.1 (STEP output) |
| Plate with Hole (onshape) | 136 triangles | 6 planes + 1 cylinder | ✓ Stage 4.1 (STEP output) |
| Plate with Hole low (fusion) | 112 triangles | 6 planes + 1 cylinder | ✓ Stage 4.1 (STEP output) |
| Plate with Hole med (fusion) | 168 triangles | 6 planes + 1 cylinder | ✓ Stage 4.1 (STEP output) |
| Plate with Hole high (fusion) | 284 triangles | 6 planes + 1 cylinder | ✓ Stage 4.1 (STEP output) |
| Part Rounded Cube coarse (onshape) | 140 triangles | 6 planes + 4 cylinders | ✓ Stage 4.1 (STEP output) |
| Part Rounded Cube fine (onshape) | 588 triangles | 6 planes + 4 cylinders | ✓ Stage 4.1 (STEP output) |
| Rounded Cube coarse (onshape) | 820 triangles | 6 planes + 12 cylinders + 8 spheres | ✓ Stage 4.1 (needs --angular-tolerance 20) |
| Rounded Cube medium (onshape) | 3548 triangles | 6 planes + 12 cylinders + 8 spheres | ✓ Stage 4.1 (STEP output) |
| Rounded Cube fine (onshape) | 21466 triangles | 6 planes + 12 cylinders + 8 spheres | ✓ Stage 4.1 (STEP output) |
| Pipe Elbow (onshape) | 11232 triangles | Cylinders + torus | ✗ Stage 3.1 (missing torus surface type) |
| Cone (onshape) | 116 triangles | 1 cone + 1 plane | ✗ Stage 3.1 (missing cone surface type) |
| Fillet | Blended edge | Planes + fillet surface | |
| Complex part | Real CAD export | Matching topology | |

---

## Performance Targets

```
</details>

**Todo list:**
- ✅ Update stage 3.2 tangency
- ✅ Update stage 3.3 edge curves
- ✅ Update stage 3.4 face creation
- ✅ Rewrite Implementation Phases
- 🔄 Update Test Cases table
- ⬜ Update Future Enhancements
- ⬜ Review and validate full plan

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L1042-L1053">DEVELOPMENT_PLAN.md -4+11</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "| Pipe Elbow (onshape) | 11232 triangles | Cylinders + torus | ✗ Stage 3.1 (missing torus surface type) |\n| Cone (onshape) | 116 triangles | 1 cone + 1 plane | ✗ Stage 3.1 (missing cone surface type) |\n| Simple Cone (ccad, TODO) | ~120 triangles | 1 truncated cone + 2 planes | Planned: conical surfaces phase |\n| Block with Conical Hole (ccad, TODO) | ~200 triangles | 6 planes + 1 concave cone | Planned: conical surfaces phase |\n| Cone-Cylinder (ccad, TODO) | ~300 triangles | 1 cone + 1 cylinder + 2 planes | Planned: conical surfaces phase |\n| Filleted Block (ccad, TODO) | ~400 triangles | 6 planes + 12 torus fillets | Planned: toroidal surfaces phase |\n| Quarter Torus (ccad, TODO) | ~200 triangles | 1 torus + 2 planes | Planned: toroidal surfaces phase |\n| Pipe Elbow ccad (ccad, TODO) | ~400 triangles | 2 tori (in/out) + 2 annular planes | Planned: toroidal surfaces phase |\n| Fillet + Cone (ccad, TODO) | ~500 triangles | Planes + cone + torus fillets | Planned: mixed cone+torus test |\n| Complex part | Real CAD export | Matching topology | |\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "1042:dz,1043:bx,1044:re,1045:sq"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"DEVELOPMENT_PLAN.md","startLine":1042,"endLine":1052}
```
</details>

Now update Future Enhancements:

<details>
<summary>Reading lines 1055-1075 of <a href="../DEVELOPMENT_PLAN.md#L1055-L1076">DEVELOPMENT_PLAN.md</a></summary>

```

## Performance Targets

| Mesh Size | Target Time |
|-----------|-------------|
| < 10K triangles | < 5 seconds |
| 10K - 100K triangles | < 30 seconds |
| 100K - 1M triangles | < 5 minutes |

---

## Future Enhancements

- [ ] Support for OBJ, PLY input formats
- [ ] IGES export option
- [ ] Machine learning for surface type classification
- [ ] Hole detection and filling
- [ ] Feature recognition (holes, pockets, bosses)

```
</details>

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L1068-L1076">DEVELOPMENT_PLAN.md -5+8</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "- [ ] Support for OBJ, PLY input formats\n- [ ] IGES export option\n- [ ] Hole detection and filling\n- [ ] Feature recognition (holes, pockets, bosses)\n- [ ] Curvature-based surface detection: use per-vertex principal curvatures (Rusinkiewicz estimator) as an alternative seeding signal. Principal curvature signatures: plane (k1≈k2≈0), sphere (k1≈k2, constant), cylinder (k1=const, k2≈0), cone (k1 varies, k2≈0), torus (k1=const, k2 varies).\n- [ ] Quadric pre-classification: fit 10-coefficient quadric via SVD as a fast O(N) surface classifier before expensive nonlinear fitting. Eigenvalue pattern of the 3×3 quadric matrix directly classifies plane/sphere/cylinder/cone.\n- [ ] Adaptive mesh density handling: current BFS tolerance assumes roughly uniform tessellation. For meshes with varying triangle density (e.g., adaptive refinement near features), weight BFS decisions by triangle area.\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "1068:ey,1069:de,1070:bs,1071:bu,1072:vw"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"DEVELOPMENT_PLAN.md","startLine":1068,"endLine":1075}
```
</details>

**Todo list:**
- ✅ Update stage 3.2 tangency
- ✅ Update stage 3.3 edge curves
- ✅ Update stage 3.4 face creation
- ✅ Rewrite Implementation Phases
- ✅ Update Test Cases table
- ✅ Update Future Enhancements
- 🔄 Review and validate full plan

Let me do a final review by reading the key new/changed sections to make sure everything is coherent:

<details>
<summary>Reading lines 395-550 of <a href="../DEVELOPMENT_PLAN.md#L395-L551">DEVELOPMENT_PLAN.md</a></summary>

```

*Comment: Spheres are 4 DOF (center + radius). Minimum 4 non-coplanar points for a unique fit. For "negative curvature" (concave spherical patch), track normal orientation relative to center rather than using negative radius. ~~Key challenge: partial spherical patches are hard to distinguish from cylinders or even planes if the patch is small relative to the radius. Consider requiring a minimum angular extent or using curvature analysis to disambiguate.~~ Addressed: solid-angle coverage validation prevents 1D strip growth (cylinder fillets), surface-tolerance validation in BFS prevents growth along non-spherical surfaces, and max_sphere_radius prevents degenerate large-radius fits on flat faces. Also consider toroidal surfaces (donuts, fillets)—they're common in CAD and combine characteristics of cylinders and spheres. Note: torus faces locally fit spheres, producing sphere hypotheses on torus surfaces (e.g., pipe_elbow). Stage 2.6 will need torus hypothesis support to resolve this.*

#### 2.4 Deduce conical hypotheses

Fit conical hypotheses to connected sets of faces that lie on a common cone. A cone is a surface of revolution (SOR) with a linear profile — it generalizes the cylinder (which has a constant-radius profile). Conical surfaces commonly appear as chamfers, tapered bores, and funnel shapes in CAD models.

A conical hypothesis consists of:
- `apex: [f64; 3]` — Apex (tip) of the cone.
- `axis_direction: [f64; 3]` — Unit direction vector along the axis (from apex outward toward the open end).
- `half_angle: f64` — Half-angle of the cone (in radians). The angle between the axis and any generator line on the surface.
- `convex: bool` — Whether face normals point away from the axis (convex, like the outside of a cone) or toward it (concave, like the inside of a conical hole).
- `faces: Vec<usize>` — Mesh face indices that fit this hypothesis.
- `vertices: Vec<usize>` — Mesh vertex indices on this cone.
- `error_max: f64` — Maximum absolute distance from any vertex to the cone surface.
- `error_abs_sum: f64` — Sum of absolute vertex-to-surface distances.

**Cone parameterization:**
A cone with apex $A$, axis direction $\hat{a}$, and half-angle $\theta$ satisfies: for any point $p$ on the surface, the angle between $(p - A)$ and $\hat{a}$ equals $\theta$. Equivalently, in (h,r) coordinates where $h = (p - A) \cdot \hat{a}$ (signed axial distance from apex) and $r = |(p - A) - h\hat{a}|$ (radial distance from axis), the surface satisfies $r = h \tan\theta$.

The signed distance from a point $p$ to the cone surface (for LM residual) is:
$$d = r_i \cos\theta - h_i \sin\theta$$
where $h_i = (p_i - A) \cdot \hat{a}$ and $r_i = |(p_i - A) - h_i \hat{a}|$.

**Initialization via (h,r) profile fitting:**
This exploits the key insight from the unified SOR framework (see Algorithm Reference): all surfaces of revolution reduce to a 2D profile fitting problem once the axis is estimated.

1. **Axis estimation** from normal covariance (same as cylinder): compute weighted covariance $M = \sum w_i n_i n_i^T$ where $n_i$ are face normals and $w_i$ are face areas. The eigenvector with smallest eigenvalue is the axis direction estimate.
2. **Reduce to (h,r) coordinates**: $h_i = (v_i - \text{centroid}) \cdot \hat{a}$, $r_i = |(v_i - \text{centroid}) - h_i \hat{a}|$.
3. **Fit linear profile** $r = m \cdot h + b$ via weighted linear regression (weights = vertex area).
4. **Extract cone parameters**: half-angle $\theta = \arctan(m)$, apex at distance $-b/m$ from centroid along axis.
5. **Reject if cylindrical**: if $|m| < 0.01$ (nearly constant radius), this is a cylinder, not a cone. Also reject if $\theta > 85°$ (nearly planar).

**Alternative initialization via quadric fitting:**
A cone is a quadric surface: $x^T A x + b^T x + c = 0$ with specific eigenvalue structure. This approach is useful as a cross-check or when the axis is poorly estimated.

1. Build the 10-coefficient quadric feature matrix: for each vertex $p_i = (x,y,z)$, construct 
... (truncated, 14403 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
395:dd|
396:nw|*Comment: Spheres are 4 DOF (center + radius). Minimum 4 non-coplanar points for a unique fit. For "negative curvature" (concave spherical patch), track normal orientation relative to center rather than using negative radius. ~~Key challenge: partial spherical patches are hard to distinguish from cylinders or even planes if the patch is small relative to the radius. Consider requiring a minimum angular extent or using curvature analysis to disambiguate.~~ Addressed: solid-angle coverage validation prevents 1D strip growth (cylinder fillets), surface-tolerance validation in BFS prevents growth along non-spherical surfaces, and max_sphere_radius prevents degenerate large-radius fits on flat faces. Also consider toroidal surfaces (donuts, fillets)—they're common in CAD and combine characteristics of cylinders and spheres. Note: torus faces locally fit spheres, producing sphere hypotheses on torus surfaces (e.g., pipe_elbow). Stage 2.6 will need torus hypothesis support to resolve this.*
397:dd|
398:ea|#### 2.4 Deduce conical hypotheses
399:dd|
400:dw|Fit conical hypotheses to connected sets of faces that lie on a common cone. A cone is a surface of revolution (SOR) with a linear profile — it generalizes the cylinder (which has a constant-radius profile). Conical surfaces commonly appear as chamfers, tapered bores, and funnel shapes in CAD models.
401:dd|
402:iy|A conical hypothesis consists of:
403:nm|- `apex: [f64; 3]` — Apex (tip) of the cone.
404:ew|- `axis_direction: [f64; 3]` — Unit direction vector along the axis (from apex outward toward the open end).
405:cz|- `half_angle: f64` — Half-angle of the cone (in radians). The angle between the axis and any generator line on the surface.
406:ld|- `convex: bool` — Whether face normals point away from the axis (convex, like the outside of a cone) or toward it (concave, like the inside of a conical hole).
407:lp|- `faces: Vec<usize>` — Mesh face indices that fit this hypothesis.
408:vr|- `vertices: Vec<usize>` — Mesh vertex indices on this cone.
409:lp|- `error_max: f64` — Maximum absolute distance from any vertex to the cone surface.
410:ov|- `error_abs_sum: f64` — Sum of absolute vertex-to-surface distances.
411:dd|
412:rb|**Cone parameterization:**
413:hi|A cone with apex $A$, axis direction $\hat{a}$, and half-angle $\theta$ satisfies: for any point $p$ on the surface, the angle between $(p - A)$ and $\hat{a}$ equals $\theta$. Equivalently, in (h,r) coordinates where $h = (p - A) \cdot \hat{a}$ (signed axial distance from apex) and $r = |(p - A) - h\hat{a}|$ (radial distance from axis), the surface satisfies $r = h \tan\theta$.
414:dd|
415:ym|The signed distance from a point $p$ to the cone surface (for LM residual) is:
416:ub|$$d = r_i \cos\theta - h_i \sin\theta$$
417:yg|where $h_i = (p_i - A) \cdot \hat{a}$ and $r_i = |(p_i - A) - h_i \hat{a}|$.
418:dd|
419:gs|**Initialization via (h,r) profile fitting:**
420:tr|This exploits the key insight from the unified SOR framework (see Algorithm Reference): all surfaces of revolution reduce to a 2D profile fitting problem once the axis is estimated.
421:dd|
422:kw|1. **Axis estimation** from normal covariance (same as cylinder): compute weighted covariance $M = \sum w_i n_i n_i^T$ where $n_i$ are face normals and $w_i$ are face areas. The eigenvector with smallest eigenvalue is the axis direction estimate.
423:hm|2. **Reduce to (h,r) coordinates**: $h_i = (v_i - \text{centroid}) \cdot \hat{a}$, $r_i = |(v_i - \text{centroid}) - h_i \hat{a}|$.
424:za|3. **Fit linear profile** $r = m \cdot h + b$ via weighted linear regression (weights = vertex area).
425:hx|4. **Extract cone parameters**: half-angle $\theta = \arctan(m)$, apex at distance $-b/m$ from centroid along axis.
426:hk|5. **Reject if cylindrical**: if $|m| < 0.01$ (nearly constant radius), this is a cylinder, not a cone. Also reject if $\theta > 85°$ (nearly planar).
427:dd|
428:cn|**Alternative initialization via quadric fitting:**
429:bm|A cone is a quadric s
... (truncated, 15495 chars)
```
</details>

> 💭 Good, sections 2.4 and 2.5 look great. Let me also read sections 2.6-2.8 and the Algorithm Reference to check for coherence.

Good - sections 2.4 and 2.5 are well-written. Let me quickly check the Algorithm Reference and section 2.8:

<details>
<summary>Reading lines 560-700 of <a href="../DEVELOPMENT_PLAN.md#L560-L701">DEVELOPMENT_PLAN.md</a></summary>

```
**Why this works better than a fixed type-priority rule:** A fixed priority (e.g., spherical > cylindrical > multi-face planar) fails when a bogus hypothesis of a high-priority type (3-face r=139mm sphere) competes with a correct hypothesis of a lower-priority type (35-face r=2mm cylinder). Area-based greedy selection naturally picks the correct hypothesis because it covers vastly more area.

**Nerfed tests:**
- `onshape_pipe_elbow_stage26`: Runs without `--compare` because torus surfaces can't be fitted with current primitives (planes, cylinders, spheres). This is a missing surface type issue, not a selection algorithm issue — greedy selection alone won't fix it.

- With the `--compare` flag, for each selected surface:
    - Compute face centroids and project them onto the selected surface.
    - Measure distance from each projected centroid to the nearest surface in the reference STEP file.
    - For all hypotheses, report an error if distance exceeds `--surface-tolerance`.

#### 2.7 Surface refitting (optional)

After stage 2.6 selects surfaces, some border faces may have been absorbed by a hypothesis that fits within tolerance but isn't the best fit. This step refines assignments and re-fits surfaces to improve accuracy.

**Algorithm:**
1. For each mesh face, check whether it belongs to more than one hypothesis (i.e., it has valid assignments in multiple hypothesis types — planar, cylindrical, spherical, conical, toroidal).

2. For each such face, compute the vertex-to-surface distance for each candidate surface (using the selected surfaces from 2.6, not the raw hypotheses).
3. Reassign the face to the selected surface with the smallest maximum vertex distance.
4. After all reassignments, re-fit each affected surface from its updated face/vertex set to remove any bias introduced by the extra vertices.

This step is expected to be most useful at boundaries between surfaces of different types (e.g., where a cylinder meets a plane), where BFS may have greedily absorbed a border face that technically fits within tolerance but belongs more naturally to the adjacent surface. It may be unnecessary if the greedy area-based selection in 2.6 already produces clean boundaries — implement only if stage 3 reconstruction reveals boundary accuracy problems.

#### 2.8 NURBS fallback

For groups of adjacent faces that remain as single-face planar hypotheses after stages 2.1–2.5 (i.e., they don't fit any analytic surface), try to fit a NURBS or B-spline surface.

**Algorithm:**
1. Identify connected components of single-face planar hypothesis faces that are adjacent to each other in the mesh.
2. For each component with ≥ 4 faces:
   a. Compute UV parameterization of the face vertices (using discrete harmonic mapping or Floater's mean value coordinates).
   b. Use OCCT's `GeomAPI_PointsToBSplineSurface` to fit a B-spline through the parameterized points.
   c. Validate: max vertex-to-surface distance < `--surface-tolerance`.
   d. If valid, create a NURBS hypothesis; if not, leave as single-face planar.
3. Add valid NURBS hypotheses to the surface selection.

*Comment: NURBS fitting is complex: parameterization quality strongly affects fit quality, degree/knot selection matters, and thin features may need special handling. Start with OCCT’s built-in B-spline fitter which handles degree selection and parameterization internally. For “freeform” regions that are nearly planar, a plane with small tolerance is preferable to a NURBS that overfits noise.*

---

### Algorithm Reference

This section documents key algorithmic techniques drawn from computational geometry research that inform the surface fitting approach. These are referenced by the stage descriptions above.

#### Unified Surface-of-Revolution (SOR) Framework

All surfaces of revolution — cylinder, cone, sphere, torus — share a common structure: they are generated by rotating a 2D profile curve around an axis. This means they can all be fitted using a unified approach:

1. **Estimate t
... (truncated, 11656 chars)
```
</details>

<details>
<summary>Reading all lines of <a href="../DEVELOPMENT_PLAN.md">DEVELOPMENT_PLAN.md</a></summary>

```
560:zt|**Why this works better than a fixed type-priority rule:** A fixed priority (e.g., spherical > cylindrical > multi-face planar) fails when a bogus hypothesis of a high-priority type (3-face r=139mm sphere) competes with a correct hypothesis of a lower-priority type (35-face r=2mm cylinder). Area-based greedy selection naturally picks the correct hypothesis because it covers vastly more area.
561:dd|
562:oy|**Nerfed tests:**
563:xv|- `onshape_pipe_elbow_stage26`: Runs without `--compare` because torus surfaces can't be fitted with current primitives (planes, cylinders, spheres). This is a missing surface type issue, not a selection algorithm issue — greedy selection alone won't fix it.
564:dd|
565:cn|- With the `--compare` flag, for each selected surface:
566:gs|    - Compute face centroids and project them onto the selected surface.
567:iy|    - Measure distance from each projected centroid to the nearest surface in the reference STEP file.
568:rj|    - For all hypotheses, report an error if distance exceeds `--surface-tolerance`.
569:dd|
570:cl|#### 2.7 Surface refitting (optional)
571:dd|
572:bj|After stage 2.6 selects surfaces, some border faces may have been absorbed by a hypothesis that fits within tolerance but isn't the best fit. This step refines assignments and re-fits surfaces to improve accuracy.
573:dd|
574:mv|**Algorithm:**
575:va|1. For each mesh face, check whether it belongs to more than one hypothesis (i.e., it has valid assignments in multiple hypothesis types — planar, cylindrical, spherical, conical, toroidal).
576:dd|
577:jo|2. For each such face, compute the vertex-to-surface distance for each candidate surface (using the selected surfaces from 2.6, not the raw hypotheses).
578:du|3. Reassign the face to the selected surface with the smallest maximum vertex distance.
579:fs|4. After all reassignments, re-fit each affected surface from its updated face/vertex set to remove any bias introduced by the extra vertices.
580:dd|
581:hr|This step is expected to be most useful at boundaries between surfaces of different types (e.g., where a cylinder meets a plane), where BFS may have greedily absorbed a border face that technically fits within tolerance but belongs more naturally to the adjacent surface. It may be unnecessary if the greedy area-based selection in 2.6 already produces clean boundaries — implement only if stage 3 reconstruction reveals boundary accuracy problems.
582:dd|
583:ln|#### 2.8 NURBS fallback
584:dd|
585:aa|For groups of adjacent faces that remain as single-face planar hypotheses after stages 2.1–2.5 (i.e., they don't fit any analytic surface), try to fit a NURBS or B-spline surface.
586:dd|
587:mv|**Algorithm:**
588:oi|1. Identify connected components of single-face planar hypothesis faces that are adjacent to each other in the mesh.
589:xw|2. For each component with ≥ 4 faces:
590:xu|   a. Compute UV parameterization of the face vertices (using discrete harmonic mapping or Floater's mean value coordinates).
591:jt|   b. Use OCCT's `GeomAPI_PointsToBSplineSurface` to fit a B-spline through the parameterized points.
592:gi|   c. Validate: max vertex-to-surface distance < `--surface-tolerance`.
593:zw|   d. If valid, create a NURBS hypothesis; if not, leave as single-face planar.
594:ek|3. Add valid NURBS hypotheses to the surface selection.
595:dd|
596:ge|*Comment: NURBS fitting is complex: parameterization quality strongly affects fit quality, degree/knot selection matters, and thin features may need special handling. Start with OCCT’s built-in B-spline fitter which handles degree selection and parameterization internally. For “freeform” regions that are nearly planar, a plane with small tolerance is preferable to a NURBS that overfits noise.*
597:dd|
598:ea|---
599:dd|
600:ow|### Algorithm Reference
601:dd|
602:vq|This section documents key algorithmic techniques drawn from computational geometry research that inform the surface fitting approach. These are referenced by the stage d
... (truncated, 12643 chars)
```
</details>

<details>
<summary>Reading lines 700-730 of <a href="../DEVELOPMENT_PLAN.md#L700-L731">DEVELOPMENT_PLAN.md</a></summary>

```
    - Cylindrical → `geom::CylindricalSurface::new_ax3_real` with `gp::Ax3` from axis origin/direction and radius. Wrapped via `to_handle().to_handle_surface()`.
    - Spherical → `geom::SphericalSurface::new_ax3_real` with `gp::Ax3` from center and Z-axis direction. Wrapped via `to_handle().to_handle_surface()`.
    - Conical → `geom::ConicalSurface::new_ax3_real2` with `gp::Ax3` from apex/direction and half-angle + radius at reference height. Wrapped via `to_handle().to_handle_surface()`. The `Ax3` origin is at the cone apex, Z along axis.
    - Toroidal → `geom::ToroidalSurface::new_ax3_real2` with `gp::Ax3` from center/axis direction, major radius $R$, and minor radius $r$. Wrapped via `to_handle().to_handle_surface()`. The `Ax3` origin is at the torus center, Z along rotation axis.
    - (Future: BSpline → `Geom_BSplineSurface` via `GeomAPI_PointsToBSplineSurface`.)

- For consistency with OCCT conventions: planes use gp_Ax3 with Z as normal; cylinders have Z along axis; spheres have poles at Z extremes; cones have Z along axis from apex; tori have Z along rotation axis. This affects pcurve computation in 3.3.


**Adjacency graph construction:**
1. Build face-to-surface map: for each mesh face, record which selected surface it belongs to (`build_face_to_surface_map`).
2. Collect boundary edges: walk all mesh edges; when an edge separates faces belonging to different surfaces, record it as an `UndirectedEdge` keyed by `SurfacePair` (`collect_boundary_edges`).
3. Chain boundary edges: for each surface pair, chain connected boundary edges into ordered vertex sequences (`chain_boundary_edges`). Each chain becomes a `ReconEdge` with the mesh boundary vertices in order.
4. Find corner vertices: mesh vertices where 3+ selected surfaces meet become `BRepVertex` entries (`find_corner_vertices`). Their 3D position is taken from the mesh vertex.
5. Assign vertex indices to edges: for each `ReconEdge`, check if its first/last mesh boundary vertex is a corner vertex and set `vertex_indices` accordingly (or `usize::MAX` for closed-loop edges).
6. Build face descriptors: for each selected surface, create a `FaceDescriptor` with the OCCT surface handle. Walk the boundary edges, ordering them topologically around the face boundary (`walk_edges_around_face`). This uses a rotation-order algorithm: at each vertex, pick the next edge that rotates counter-clockwise around the face. This populates `adjacent_faces`, `edge_indices`, and `vertex_indices` for each face.
7. Build vertex adjacency: for each `BRepVertex`, walk the edges meeting at that vertex in topological order, recording `adjacent_faces` and `adjacent_edges`.
8. Validate topology: check Euler's formula V-E+F=2 for genus-0 models.

- With the `--compare` flag:
    - For each `ReconEdge`, sample the mesh boundary vertices along the edge. For each sample vertex, compute the distance to the nearest edge/vertex in the reference STEP file using `BRepExtrema_DistShapeShape`. Report an error if any sample exceeds `--vertex-tolerance`.
    - For each `BRepVertex`, compute the distance from the vertex's 3D position to the nearest vertex in the reference STEP file. Report an error if any exceeds `--vertex-tolerance`.
    - These checks validate that the adjacency graph's topological features (edges where surfaces meet, corners where 3+ surfaces meet) correspond to real features in the reference model.

#### 3.2 Detect tangency relationships
For each ReconEdge, determine whether the two adjacent surfaces are tangent along the shared boundary. This matters because `GeomAPI_IntSS` can fail or produce degenerate results for tangent/near-tangent surfaces, requiring special handling in edge curve computation (3.3).

**Detection algorithm (implemented):**
- For each ReconEdge, sample mesh boundary vertices along the shared boundary (every 5th vertex, minimum 3 samples, always including first and last).
- At each sample point, compute the outward-facing surface normal of both adjacent surfaces analyt
... (truncated, 4055 chars)
```
</details>

The existing content all looks coherent. Let me also quickly check the implementation phases section I just wrote:

<details>
<summary>Reading lines 945-995 of <a href="../DEVELOPMENT_PLAN.md#L945-L996">DEVELOPMENT_PLAN.md</a></summary>

```
### Stage 2 Extensions: Conical Surfaces
- [ ] Create ccad test models for conical surfaces: `simple_cone.lua` (truncated cone), `block_with_conical_hole.lua` (block with conical bore), `cone_cylinder.lua` (cone joined to cylinder). Export STL+STEP pairs.
- [ ] Implement `ConicalHypothesis` data structure in stage 2 (apex, axis, half-angle, convex, faces, vertices, errors).
- [ ] Implement cone fitting: axis estimation from normal covariance, (h,r) profile fitting via linear regression to get half-angle and apex position. See Architecture section 2.4 for details.
- [ ] Implement cone BFS region growing with vertex-to-cone distance validation and angular coverage check.
- [ ] Extend stage 2.6 surface selection to include conical candidates.
- [ ] Distinguish cones from cylinders: when both fit well, compare RMS residuals and prefer simpler cylinder unless cone is significantly better.
- [ ] Unit tests: cone fitting on synthetic point sets; BFS growing on test meshes.
- [ ] Full pipeline tests: simple_cone and block_with_conical_hole pass `--compare` through stage 4.1.
- [ ] Unblock existing `cone_15x20_medium` (onshape) test.

### Stage 2 Extensions: Toroidal Surfaces
- [ ] Create ccad test models for toroidal surfaces: `filleted_block.lua` (block with `fillet_all()`), `quarter_torus.lua` (quarter-torus section), `pipe_elbow_ccad.lua` (90° pipe elbow with inner/outer torus + planes).
- [ ] Implement `ToroidalHypothesis` data structure in stage 2 (center, axis, major_radius, minor_radius, convex, faces, vertices, errors).
- [ ] Implement torus fitting via medial axis / tube center method: estimate minor radius from normal-line intersections, compute tube centers k_i = p_i + r*n_i, fit 3D circle to tube centers for major circle parameters. See Architecture section 2.5 for details.
- [ ] Implement torus BFS region growing with vertex-to-torus distance validation.
- [ ] Extend stage 2.6 surface selection to include toroidal candidates.
- [ ] Unit tests: torus fitting on synthetic point sets; BFS growing on fillet meshes.
- [ ] Full pipeline tests: filleted_block passes `--compare` through stage 4.1.
- [ ] Unblock existing `pipe_elbow_10_fine` (onshape) test.

### Stage 3 Extensions for Cone and Torus
- [ ] Stage 3.1: Add `Geom_ConicalSurface` and `Geom_ToroidalSurface` creation from hypothesis parameters.
- [ ] Stage 3.2: Add cone and torus analytical normal formulas for tangency detection.
- [ ] Stage 3.3: Implement tangent edge curve computation for plane-cone, cylinder-cone, plane-torus, cylinder-torus, and torus-torus pairs.
- [ ] Stage 3.3: Handle non-tangent cone and torus edge intersections via `GeomAPI_IntSS` with curve selection.
- [ ] Stage 3.4: Implement conical face construction (UV-bounds for full revolution, wire+pcurves for partial, apex singularity handling).
- [ ] Stage 3.4: Implement toroidal face construction (UV-bounds for full revolution, wire+pcurves for partial fillet patches, doubly-periodic UV handling).
- [ ] Full pipeline tests: all cone and torus test models pass --compare through stage 4.1.

### Unified Surface-of-Revolution Framework (refactor)
- [ ] Extract shared SOR axis estimation (normal covariance smallest eigenvector) into a common utility function.
- [ ] Implement (h,r) coordinate reduction: for a set of vertices and an axis, compute axial and radial coordinates.
- [ ] Implement unified profile classifer: given (h,r) points, fit horizontal line (cylinder), sloped line (cone), circle centered on axis (sphere), and offset circle (torus). Return all fits with RMS residuals.
- [ ] Refactor cylinder fitting to use (h,r) reduction + profile classification as an alternative initialization.
- [ ] Refactor sphere fitting to use (h,r) reduction when axis is estimated from normal covariance.
- [ ] Integrate unified SOR into BFS: after initial seed, try all profile types and pick best before growing.
- [ ] Regression tests: ensure all existing test models still pass after refactor.

### Gaussian Map Pre-Cl
... (truncated, 5061 chars)
```
</details>

Everything looks coherent. Let me do a final check on the test cases table to make sure it rendered correctly:

<details>
<summary>Reading lines 1038-1055 of <a href="../DEVELOPMENT_PLAN.md#L1038-L1056">DEVELOPMENT_PLAN.md</a></summary>

```
| Part Rounded Cube fine (onshape) | 588 triangles | 6 planes + 4 cylinders | ✓ Stage 4.1 (STEP output) |
| Rounded Cube coarse (onshape) | 820 triangles | 6 planes + 12 cylinders + 8 spheres | ✓ Stage 4.1 (needs --angular-tolerance 20) |
| Rounded Cube medium (onshape) | 3548 triangles | 6 planes + 12 cylinders + 8 spheres | ✓ Stage 4.1 (STEP output) |
| Rounded Cube fine (onshape) | 21466 triangles | 6 planes + 12 cylinders + 8 spheres | ✓ Stage 4.1 (STEP output) |
| Pipe Elbow (onshape) | 11232 triangles | Cylinders + torus | ✗ Stage 3.1 (missing torus surface type) |
| Cone (onshape) | 116 triangles | 1 cone + 1 plane | ✗ Stage 3.1 (missing cone surface type) |
| Simple Cone (ccad, TODO) | ~120 triangles | 1 truncated cone + 2 planes | Planned: conical surfaces phase |
| Block with Conical Hole (ccad, TODO) | ~200 triangles | 6 planes + 1 concave cone | Planned: conical surfaces phase |
| Cone-Cylinder (ccad, TODO) | ~300 triangles | 1 cone + 1 cylinder + 2 planes | Planned: conical surfaces phase |
| Filleted Block (ccad, TODO) | ~400 triangles | 6 planes + 12 torus fillets | Planned: toroidal surfaces phase |
| Quarter Torus (ccad, TODO) | ~200 triangles | 1 torus + 2 planes | Planned: toroidal surfaces phase |
| Pipe Elbow ccad (ccad, TODO) | ~400 triangles | 2 tori (in/out) + 2 annular planes | Planned: toroidal surfaces phase |
| Fillet + Cone (ccad, TODO) | ~500 triangles | Planes + cone + torus fillets | Planned: mixed cone+torus test |
| Complex part | Real CAD export | Matching topology | |


---

```
</details>

Good, everything looks clean. Let me mark the review as complete:

**Todo list:**
- ✅ Update stage 3.2 tangency
- ✅ Update stage 3.3 edge curves
- ✅ Update stage 3.4 face creation
- ✅ Rewrite Implementation Phases
- ✅ Update Test Cases table
- ✅ Update Future Enhancements
- ✅ Review and validate full plan

Now let me follow the `AGENTS.md` workflow — run clippy and commit:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo clippy --all-targets 2>&1 | tail -5
```
```
    Checking brepper v0.1.0 (/Users/sethml/src/brepper)
    Finished `dev` profile [optimized + debuginfo] target(s) in 3.77s
```

Clean. Now let me commit:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git add DEVELOPMENT_PLAN.md && git diff --cached --stat
```
```
 DEVELOPMENT_PLAN.md | 335 ++++++++++++++++++++++++++++++++++++++++++++++++----
 1 file changed, 310 insertions(+), 25 deletions(-)
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git commit -m "dev plan: add conical, toroidal, unified SOR, and NURBS pipeline

Rework development plan based on surface detection techniques from
papers/chatgpt-3d-fitting-conversation.md:

Architecture additions:
- Section 2.4: Conical hypothesis deduction via (h,r) profile fitting
  and quadric initialization, with BFS region growing
- Section 2.5: Toroidal hypothesis deduction via medial axis / tube
  center method (estimate r from normal intersections, fit 3D circle
  to tube centers for major circle parameters)
- Section 2.8: NURBS fallback via GeomAPI_PointsToBSplineSurface
- Algorithm Reference section documenting unified SOR framework,
  Gaussian map classification, medial axis method, quadric fitting

Stage 3 updates:
- 3.1: Add Geom_ConicalSurface and Geom_ToroidalSurface creation
- 3.2: Add cone/torus analytical normal formulas for tangency
- 3.3: Add tangent edge dispatch for plane-cone, cylinder-cone,
  plane-torus, cylinder-torus, torus-torus, sphere-torus pairs
- 3.3: Add cone/torus seam edge notes
- 3.4: Add conical and toroidal face construction approaches

Implementation phases:
- Conical surfaces phase with ccad test models
- Toroidal surfaces phase with ccad test models
- Stage 3 extensions for cone and torus
- Unified SOR framework refactor
- Gaussian map pre-classification
- NURBS fallback

Test cases: 7 new planned models (3 cone, 3 torus, 1 mixed)
Future enhancements: curvature detection, quadric pre-classification"
```
```
[main b5f7421] dev plan: add conical, toroidal, unified SOR, and NURBS pipeline
 1 file changed, 310 insertions(+), 25 deletions(-)
```

The development plan rewrite is complete and committed. Here's a summary of what was done:

**Architecture additions to `DEVELOPMENT_PLAN.md`:**

- **Section 2.4 — Conical hypothesis deduction**: Uses the (h,r) profile fitting approach from the SOR framework (axis estimation → reduce to (h,r) → linear regression for sloped-line profile → extract half-angle and apex). Quadric fitting as alternative initialization. BFS region growing with cone-specific distance validation.

- **Section 2.5 — Toroidal hypothesis deduction**: Uses the medial axis / tube center method — estimate minor radius $r$ from normal-line intersections between adjacent faces, compute tube centers $k_i = p_i + r \cdot n_i$, fit 3D circle to tube centers for major circle parameters. Edge-based seeding targets fillet faces between fitted surfaces.

- **Section 2.8 — NURBS fallback**: For remaining unclassified face groups, using OCCT's `GeomAPI_PointsToBSplineSurface`.

- **Algorithm Reference**: Documents the unified SOR framework, Gaussian map classification, medial axis/tube center method, and quadric fitting.

- **Stage 3 updates**: Cone/torus support in surface creation (3.1), tangency detection (3.2), edge curves (3.3, including 6 new tangent edge pair types), seam edges, and face construction (3.4).

**Implementation phases** (6 incremental phases, each testable):
1. Conical surfaces (3 ccad test models + onshape cone)
2. Toroidal surfaces (3 ccad test models + pipe_elbow unblock)  
3. Stage 3 extensions for cone/torus
4. Unified SOR framework refactor
5. Gaussian map pre-classification
6. NURBS fallback

**7 new test cases** planned (simple_cone, block_with_conical_hole, cone_cylinder, filleted_block, quarter_torus, pipe_elbow_ccad, fillet+cone).

*2026-03-11 16:34 · 1001s · Words: 20,148 in · 12,389 out · 67,157 ctx · 73 rounds*

---
