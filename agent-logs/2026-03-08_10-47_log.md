# Cylindrical hypothesis issue and proposed solution

- **Date:** 2026-03-08 10:47 – 11:54
- **Session ID:** 15bae0c6-c91d-45da-8bec-e117ca9d688b
- **Model:** Claude Opus 4.6
- **Turns:** 5
- **Tool calls:** 140
- **Thinking blocks:** 21
- **Input words:** 14,161
- **Output words:** 19,711
- **Prompt tokens (last round):** 299,898
- **API rounds:** 119
- **Total elapsed:** 33m38s

## Table of Contents

1. [I have an idea of why we may be getting bogus cylindrical hypothesis where a cylinder meets a sph...](#user-1) (Claude Opus 4.6)
2. [I don&#x27;t think your key insight is correct. Look at pill_coarse.stl with -vv - note that the cylin...](#user-2) (Claude Opus 4.6)
3. [Test out the drift theory by adding debugging prints whenever stage 2.2 re-fits, and seeing if ax...](#user-3) (Claude Opus 4.6)
4. [Yes, do all three.](#user-4) (Claude Opus 4.6)
5. [Use rounded_cube_10_r2_fine.stl.](#user-5) (Claude Opus 4.6)

---

<a id="user-1"></a>

## User (1)

> I have an idea of why we may be getting bogus cylindrical hypothesis where a cylinder meets a sphere, for example on the rounded_cube_10_r2_coarse.stl model. My idea is that if BFS is seeded with two adjacent cylinder faces, but then explores onto the sphere, the equatorial spherical faces are close enough to the cylinder to be accepted into the hypothesis with refitting, but then the refit cylinder doesn&#x27;t fit the rest of the cylindrical faces, poisoning those faces for future deductions. I propose to avoid this:
> - Allow seeding from any face, not just faces where cylindrical_hypothesis is -2.
> - While performing BFS search, compare both to the best candidate and any existing candidate for the face (face.cylindrical_hypothesis), and if the trial is rediscovering either, abandon the trial.
> Think about whether my hypothesis might be correct, and whether my fix seems reasonable, or if you can think of a better fix. Then update the development plan with the best fix and commit. Don&#x27;t change any code yet.

*2026-03-08 10:53*

### Assistant — Claude Opus 4.6

> 💭 The user is asking me to think about a potential issue with cylindrical hypothesis deduction and propose a fix. Let me first understand the problem by reading the relevant code and development plan.
> 
> Let me analyze the hypothesis:
> 
> 1. **The problem**: When a cylinder meets a sphere (e.g., on a rounded cube), BFS is seeded from two adjacent cylinder faces. The BFS then explores onto the sphere. The equatorial spherical faces are close enough to the cylinder surface that they pass vertex/centroid tolerance checks (possibly with refitting). Once these sphere faces are accepted, the refit cylinder parameters shift, and the shifted cylinder no longer fits the original cylinder faces well. This "poisons" those faces — they get assigned to a bad hypothesis, preventing them from being used in future correct hypotheses.
>    
> 2. **The proposed fix**: Allow seeding from any face rather than just unassigned ones, and during BFS, check whether accepting a new face would degrade the fit against both the current best candidate and any existing hypothesis that face already belongs to.
> 
> Now I'm thinking through the algorithm flow to evaluate whether this approach makes sense. The current process seeds from unassigned faces and runs trial BFS expansions, committing the best result if it passes coverage validation. The issue arises when sphere faces adjacent to a cylinder get pulled into a bad fit, which then blocks them from joining the correct cylinder hypothesis later.
> 
> Let me trace through what's happening: when a cylinder hypothesis grabs sphere faces during BFS, the refit shifts parameters just enough to stay within tolerance for its members, but the consumed faces prevent the true cylinder from forming properly. The multi-seed evaluation should theoretically find the best hypothesis, but I'm wondering if the problem is that once faces are committed to a suboptimal hypothesis, they're locked in and unavailable for better alternatives. those cylinder faces get consumed, so when a proper cylinder seed appears later, the faces it needs are already claimed. Alternatively, a cylinder seed might grab nearby sphere faces during BFS growth, and the refit could shift the cylinder just enough to pass tolerance checks while still including faces that shouldn't belong to it, leaving the sphere hypothesis unable to form correctly. The user's scenarios seem plausible, and the proposed fix of allowing seeding from already-assigned faces could let a later seed discover a better cylinder that corrects earlier mistakes.
> 
> But I'm realizing the BFS won't expand onto faces that already have a cylindrical hypothesis, so re-seeding alone wouldn't help unless we also let BFS explore those assigned faces. Looking at this more carefully, the real issue is processing order — if a sphere-junction face seeds first, it creates a bad hypothesis that consumes cylinder faces. Processing better seeds first would prevent this. The current algorithm already iterates through all faces though, so the question is whether allowing re-seeding from already-assigned faces would actually change the outcome.
> 
> The problem is that if too many cylinder faces get consumed by a bad hypothesis, there might not be enough remaining unassigned faces to form a valid hypothesis that passes angular coverage. I think I need to reconsider what's actually happening when the algorithm reaches a boundary face — it's unassigned, has neighbors on both the cylinder and sphere, and seed pairs with cylinder neighbors produce a cylinder fit that then grows along the cylinder and potentially onto the sphere.
> 
> The issue is that this combined hypothesis passes angular coverage because it wraps around with both cylinder and sphere faces, so it gets committed. Later, the remaining cylinder faces can't form their own hypothesis due to insufficient coverage, and the remaining sphere faces are missing the equatorial ones they need.
> 
> The user's proposed fix of allowing seeding from already-assigned faces is interesting — it would let us try to find a better hypothesis even if a face is already committed to one. But I need to think through what happens if we actually find a better fit: do we reassign the face, or does the new hypothesis just compete with the old one? steal faces from the bad hypothesis and reassign them to the better one, which adds complexity. The early termination optimization prevents redundant rediscovery of committed hypotheses, but neither approach directly tackles the underlying issue: sphere faces adjacent to the cylinder boundary get incorrectly consumed during BFS expansion.
> 
> The real fix might be a convexity consistency check—when the BFS tries to add a sphere face to a cylinder hypothesis, I'd verify that the face normal aligns with the expected radial direction from the cylinder axis, not the sphere center. This should reject sphere faces at the equator since their normals point radially from the sphere, not the cylinder. Though if the sphere and cylinder happen to share the same equator, the radial directions could be similar enough to slip through. The tricky part is handling the junction between the cylinder fillet and the spherical corner on a rounded cube—that's where the geometry gets ambiguous.
> 
> I'm also considering whether an angular tolerance check between the cylinder and sphere faces would catch this, or if parameter drift during BFS expansion is the real culprit. Maybe the fix is to prevent the fitted cylinder from changing too much as BFS grows onto the sphere, rather than just checking if vertices stay within tolerance.
> 
> Looking at the user's proposed solution more carefully: they want to allow seeding from any face, even ones already assigned to a hypothesis, so that a better cylinder hypothesis can be found and win out. But this raises a problem—the current algorithm doesn't un-assign faces, so if a face is already claimed by one hypothesis, finding a better hypothesis that includes it would require backtracking or reassignment logic that doesn't exist yet. I'm reconsidering whether the user's proposal is actually about allowing reassignment of already-claimed faces during BFS expansion, or if there's a simpler fix: adding a parameter stability check after refitting to ensure the cylinder doesn't drift too far. Let me look back at what they actually asked for.

Let me first understand the current cylinder deduction implementation to evaluate your hypothesis properly.

<details>
<summary>Read cylinder deduction code (Explore agent · Claude Haiku 4.5)</summary>

**Prompt:**
> I need to understand the cylindrical hypothesis deduction code in this Rust project. 
> 
> Please find and read the following, returning the full relevant code sections:
> 
> 1. The `deduce_cylindrical_hypotheses` function (or equivalent) in src/stage2.rs - I need to see the outer loop, seeding logic, trial BFS, early termination, and commitment logic.
> 2. Any BFS expansion function used for cylindrical hypothesis trials.
> 3. The data structures for Face (particularly the `cylindrical_hypothesis` field) and CylindricalHypothesis.
> 
> Return the complete code for these sections, including line numbers. Don't summarize - I need the actual code.

<details>
<summary>Searched for text <code>deduce_cylindrical_hypotheses</code>, 20 results</summary>

- [stage2.rs:1222](../src/stage2.rs#L1222)
- [stage2.rs:2361](../src/stage2.rs#L2361)
- [stage2.rs:2743](../src/stage2.rs#L2743)
- [stage2.rs:2753](../src/stage2.rs#L2753)
- [stage2.rs:2767](../src/stage2.rs#L2767)
- [stage2.rs:2781](../src/stage2.rs#L2781)
- [stage2.rs:2793](../src/stage2.rs#L2793)
- [2026-03-02_01-40_log.md:1691](../agent-logs/2026-03-02_01-40_log.md#L1691)
- [2026-03-02_01-40_log.md:3333](../agent-logs/2026-03-02_01-40_log.md#L3333)
- [2026-03-02_01-40_log.md:3339](../agent-logs/2026-03-02_01-40_log.md#L3339)
- [2026-03-02_01-40_log.md:3355](../agent-logs/2026-03-02_01-40_log.md#L3355)
- [2026-03-02_01-40_log.md:3417](../agent-logs/2026-03-02_01-40_log.md#L3417)
- [2026-03-06_22-30_log.md:1350](../agent-logs/2026-03-06_22-30_log.md#L1350)
- [2026-03-06_22-30_log.md:1360](../agent-logs/2026-03-06_22-30_log.md#L1360)
- [2026-03-06_22-30_log.md:1374](../agent-logs/2026-03-06_22-30_log.md#L1374)
- [2026-03-06_22-30_log.md:1388](../agent-logs/2026-03-06_22-30_log.md#L1388)
- [2026-03-06_22-30_log.md:1400](../agent-logs/2026-03-06_22-30_log.md#L1400)
- [2026-03-08_01-26_log.md:1121](../agent-logs/2026-03-08_01-26_log.md#L1121)
- [2026-03-08_01-26_log.md:1167](../agent-logs/2026-03-08_01-26_log.md#L1167)
- [2026-03-08_01-26_log.md:1177](../agent-logs/2026-03-08_01-26_log.md#L1177)
</details>

<details>
<summary>Searched for regex <code>struct CylindricalHypothesis|struct Face</code>, 20 results</summary>

- [stage2.rs:86](../src/stage2.rs#L86)
- [stage3.rs:28](../src/stage3.rs#L28)
- [2026-03-07_20-57_log.md:1222](../agent-logs/2026-03-07_20-57_log.md#L1222)
- [2026-03-02_00-10_log.md:1186](../agent-logs/2026-03-02_00-10_log.md#L1186)
- [2026-03-02_01-40_log.md:1533](../agent-logs/2026-03-02_01-40_log.md#L1533)
- [2026-03-07_21-32_log.md:1981](../agent-logs/2026-03-07_21-32_log.md#L1981)
- [2026-03-01_18-25_log.md:90](../agent-logs/2026-03-01_18-25_log.md#L90)
- [2026-03-01_18-25_log.md:1391](../agent-logs/2026-03-01_18-25_log.md#L1391)
- [2026-03-01_20-40_log.md:1179](../agent-logs/2026-03-01_20-40_log.md#L1179)
- [2026-03-01_20-40_log.md:3879](../agent-logs/2026-03-01_20-40_log.md#L3879)
- [2026-03-01_20-40_log.md:3994](../agent-logs/2026-03-01_20-40_log.md#L3994)
- [2026-03-06_22-30_log.md:389](../agent-logs/2026-03-06_22-30_log.md#L389)
- [2026-03-08_09-03_log.md:650](../agent-logs/2026-03-08_09-03_log.md#L650)
- [2026-03-08_09-03_log.md:688](../agent-logs/2026-03-08_09-03_log.md#L688)
- [2026-03-08_09-03_log.md:1753](../agent-logs/2026-03-08_09-03_log.md#L1753)
- [2026-03-08_09-03_log.md:5024](../agent-logs/2026-03-08_09-03_log.md#L5024)
- [2026-03-07_12-01_log.md:1174](../agent-logs/2026-03-07_12-01_log.md#L1174)
- [2026-03-07_12-01_log.md:1270](../agent-logs/2026-03-07_12-01_log.md#L1270)
- [2026-03-07_12-01_log.md:2478](../agent-logs/2026-03-07_12-01_log.md#L2478)
- [2026-03-07_12-01_log.md:3266](../agent-logs/2026-03-07_12-01_log.md#L3266)
</details>

Read [stage2.rs](../src/stage2.rs#1-1), lines 1 to 150

Read [stage2.rs](../src/stage2.rs#1222-1222), lines 1222 to 1550

<details>
<summary>Searched for text <code>run_cylinder_trial_bfs</code>, 10 results</summary>

- [stage2.rs:949](../src/stage2.rs#L949)
- [stage2.rs:1291](../src/stage2.rs#L1291)
- [2026-03-06_17-50_log.md:2408](../agent-logs/2026-03-06_17-50_log.md#L2408)
- [2026-03-06_17-50_log.md:3011](../agent-logs/2026-03-06_17-50_log.md#L3011)
- [2026-03-06_17-50_log.md:3052](../agent-logs/2026-03-06_17-50_log.md#L3052)
- [2026-03-06_17-50_log.md:3078](../agent-logs/2026-03-06_17-50_log.md#L3078)
- [2026-03-06_17-50_log.md:3093](../agent-logs/2026-03-06_17-50_log.md#L3093)
- [2026-03-06_17-50_log.md:3103](../agent-logs/2026-03-06_17-50_log.md#L3103)
- [2026-03-06_17-50_log.md:3123](../agent-logs/2026-03-06_17-50_log.md#L3123)
- [2026-03-07_22-23_log.md:3191](../agent-logs/2026-03-07_22-23_log.md#L3191)
</details>

<details>
<summary>Searched for text <code>struct MeshFace</code>, 19 results</summary>

- [stage1.rs:39](../src/stage1.rs#L39)
- [2026-03-01_21-15_log.md:491](../agent-logs/2026-03-01_21-15_log.md#L491)
- [2026-03-01_21-15_log.md:1134](../agent-logs/2026-03-01_21-15_log.md#L1134)
- [2026-03-08_08-47_log.md:155](../agent-logs/2026-03-08_08-47_log.md#L155)
- [2026-03-02_00-26_log.md:1248](../agent-logs/2026-03-02_00-26_log.md#L1248)
- [2026-03-02_00-10_log.md:501](../agent-logs/2026-03-02_00-10_log.md#L501)
- [2026-03-02_00-10_log.md:645](../agent-logs/2026-03-02_00-10_log.md#L645)
- [2026-03-02_08-42_log.md:1136](../agent-logs/2026-03-02_08-42_log.md#L1136)
- [2026-03-07_12-01_log.md:886](../agent-logs/2026-03-07_12-01_log.md#L886)
- [2026-03-07_12-01_log.md:1111](../agent-logs/2026-03-07_12-01_log.md#L1111)
- [2026-03-01_18-25_log.md:76](../agent-logs/2026-03-01_18-25_log.md#L76)
- [2026-03-02_10-41_log.md:1580](../agent-logs/2026-03-02_10-41_log.md#L1580)
- [2026-03-02_10-41_log.md:1609](../agent-logs/2026-03-02_10-41_log.md#L1609)
- [2026-03-07_18-18_log.md:210](../agent-logs/2026-03-07_18-18_log.md#L210)
- [2026-03-07_18-18_log.md:2710](../agent-logs/2026-03-07_18-18_log.md#L2710)
- [2026-03-01_20-40_log.md:674](../agent-logs/2026-03-01_20-40_log.md#L674)
- [2026-03-01_20-40_log.md:960](../agent-logs/2026-03-01_20-40_log.md#L960)
- [2026-03-01_20-40_log.md:4128](../agent-logs/2026-03-01_20-40_log.md#L4128)
- [2026-03-01_20-40_log.md:4272](../agent-logs/2026-03-01_20-40_log.md#L4272)
</details>

Read [stage2.rs](../src/stage2.rs#949-949), lines 949 to 1220

Read [stage1.rs](../src/stage1.rs#39-39), lines 39 to 130

<details>
<summary>Searched for text <code>CylinderTrialResult</code>, 17 results</summary>

- [stage2.rs:933](../src/stage2.rs#L933)
- [stage2.rs:956](../src/stage2.rs#L956)
- [stage2.rs:957](../src/stage2.rs#L957)
- [stage2.rs:1205](../src/stage2.rs#L1205)
- [stage2.rs:1288](../src/stage2.rs#L1288)
- [2026-03-08_08-47_log.md:205](../agent-logs/2026-03-08_08-47_log.md#L205)
- [2026-03-08_08-47_log.md:207](../agent-logs/2026-03-08_08-47_log.md#L207)
- [2026-03-08_08-47_log.md:214](../agent-logs/2026-03-08_08-47_log.md#L214)
- [2026-03-08_08-47_log.md:1012](../agent-logs/2026-03-08_08-47_log.md#L1012)
- [2026-03-06_22-30_log.md:1042](../agent-logs/2026-03-06_22-30_log.md#L1042)
- [2026-03-06_17-50_log.md:2421](../agent-logs/2026-03-06_17-50_log.md#L2421)
- [2026-03-06_17-50_log.md:3016](../agent-logs/2026-03-06_17-50_log.md#L3016)
- [2026-03-06_17-50_log.md:3017](../agent-logs/2026-03-06_17-50_log.md#L3017)
- [2026-03-06_17-50_log.md:3060](../agent-logs/2026-03-06_17-50_log.md#L3060)
- [2026-03-06_17-50_log.md:3061](../agent-logs/2026-03-06_17-50_log.md#L3061)
- [2026-03-06_17-50_log.md:3078](../agent-logs/2026-03-06_17-50_log.md#L3078)
</details>

Read [stage2.rs](../src/stage2.rs#920-920), lines 920 to 948

<details>
<summary>Searched for text <code>fn fit_cylinder</code>, 7 results</summary>

- [stage2.rs:675](../src/stage2.rs#L675)
- [2026-03-06_17-50_log.md:1253](../agent-logs/2026-03-06_17-50_log.md#L1253)
- [2026-03-07_22-23_log.md:3253](../agent-logs/2026-03-07_22-23_log.md#L3253)
- [2026-03-07_22-23_log.md:3586](../agent-logs/2026-03-07_22-23_log.md#L3586)
- [2026-03-07_22-23_log.md:3605](../agent-logs/2026-03-07_22-23_log.md#L3605)
- [2026-03-07_22-23_log.md:3613](../agent-logs/2026-03-07_22-23_log.md#L3613)
- [2026-03-02_16-02_log.md:2807](../agent-logs/2026-03-02_16-02_log.md#L2807)
</details>

<details>
<summary>Searched for text <code>fn vertex_to_cylinder_distance</code>, 5 results</summary>

- [stage2.rs:521](../src/stage2.rs#L521)
- [2026-03-02_00-26_log.md:1959](../agent-logs/2026-03-02_00-26_log.md#L1959)
- [2026-03-06_17-50_log.md:1945](../agent-logs/2026-03-06_17-50_log.md#L1945)
- [2026-03-06_17-50_log.md:2067](../agent-logs/2026-03-06_17-50_log.md#L2067)
- [2026-03-02_16-02_log.md:2813](../agent-logs/2026-03-02_16-02_log.md#L2813)
</details>

Read [stage2.rs](../src/stage2.rs#500-500), lines 500 to 570

Read [stage2.rs](../src/stage2.rs#675-675), lines 675 to 800

Searched for text `fn all_vertices_within_cylinder_tolerance`, 2 results
- [stage2.rs:803](../src/stage2.rs#L803)
- [2026-03-02_02-07_log.md:1088](../agent-logs/2026-03-02_02-07_log.md#L1088)

Searched for text `fn determine_convexity`, 4 results
- [stage2.rs:821](../src/stage2.rs#L821)
- [2026-03-02_16-02_log.md:2810](../agent-logs/2026-03-02_16-02_log.md#L2810)
- [2026-03-06_17-50_log.md:1253](../agent-logs/2026-03-06_17-50_log.md#L1253)
- [2026-03-02_02-07_log.md:1106](../agent-logs/2026-03-02_02-07_log.md#L1106)

Searched for text `fn angular_coverage_valid`, 4 results
- [stage2.rs:869](../src/stage2.rs#L869)
- [2026-03-06_22-30_log.md:978](../agent-logs/2026-03-06_22-30_log.md#L978)
- [2026-03-07_22-23_log.md:2968](../agent-logs/2026-03-07_22-23_log.md#L2968)
- [2026-03-06_17-50_log.md:2252](../agent-logs/2026-03-06_17-50_log.md#L2252)

Read [stage2.rs](../src/stage2.rs#803-803), lines 803 to 865

Read [stage2.rs](../src/stage2.rs#869-869), lines 869 to 932

Searched for text `fn build_normal_covariance`, 2 results
- [stage2.rs:653](../src/stage2.rs#L653)
- [2026-03-02_00-26_log.md:2868](../agent-logs/2026-03-02_00-26_log.md#L2868)

<details>
<summary>Searched for text <code>fn smallest_eigenvector_3x3</code>, 5 results</summary>

- [stage2.rs:569](../src/stage2.rs#L569)
- [2026-03-06_22-30_log.md:1473](../agent-logs/2026-03-06_22-30_log.md#L1473)
- [2026-03-02_00-26_log.md:1959](../agent-logs/2026-03-02_00-26_log.md#L1959)
- [2026-03-06_17-50_log.md:1993](../agent-logs/2026-03-06_17-50_log.md#L1993)
- [2026-03-06_17-50_log.md:2115](../agent-logs/2026-03-06_17-50_log.md#L2115)
</details>

Read [stage2.rs](../src/stage2.rs#569-569), lines 569 to 675

<details>
<summary>Searched for text <code>fn face_area</code>, 5 results</summary>

- [stage2.rs:218](../src/stage2.rs#L218)
- [2026-03-01_21-15_log.md:3654](../agent-logs/2026-03-01_21-15_log.md#L3654)
- [2026-03-02_15-44_log.md:966](../agent-logs/2026-03-02_15-44_log.md#L966)
- [2026-03-02_06-56_log.md:1092](../agent-logs/2026-03-02_06-56_log.md#L1092)
- [2026-03-06_17-50_log.md:2153](../agent-logs/2026-03-06_17-50_log.md#L2153)
</details>

Read [stage2.rs](../src/stage2.rs#218-218), lines 218 to 240

<details>
<summary>Searched for text <code>UNDEDUCED_CYLINDRICAL_HYPOTHESIS</code>, 20 results</summary>

- [DEVELOPMENT_PLAN.md:118](../DEVELOPMENT_PLAN.md#L118)
- [stage2.rs:11](../src/stage2.rs#L11)
- [stage2.rs:1008](../src/stage2.rs#L1008)
- [stage2.rs:1232](../src/stage2.rs#L1232)
- [stage2.rs:1256](../src/stage2.rs#L1256)
- [stage2.rs:2606](../src/stage2.rs#L2606)
- [stage2.rs:2615](../src/stage2.rs#L2615)
- [stage1.rs:18](../src/stage1.rs#L18)
- [stage1.rs:295](../src/stage1.rs#L295)
- [stage1.rs:939](../src/stage1.rs#L939)
- [stage1.rs:958](../src/stage1.rs#L958)
- [2026-03-06_22-30_log.md:312](../agent-logs/2026-03-06_22-30_log.md#L312)
- [2026-03-02_08-42_log.md:1115](../agent-logs/2026-03-02_08-42_log.md#L1115)
- [2026-03-02_01-40_log.md:1502](../agent-logs/2026-03-02_01-40_log.md#L1502)
- [2026-03-02_01-40_log.md:3152](../agent-logs/2026-03-02_01-40_log.md#L3152)
- [2026-03-02_01-40_log.md:3182](../agent-logs/2026-03-02_01-40_log.md#L3182)
- [2026-03-02_01-40_log.md:3184](../agent-logs/2026-03-02_01-40_log.md#L3184)
- [2026-03-02_01-40_log.md:3186](../agent-logs/2026-03-02_01-40_log.md#L3186)
</details>

Read [stage1.rs](../src/stage1.rs#1-1), lines 1 to 35

Perfect! I've found all the code sections you need. Here's the complete cylindrical hypothesis deduction code with line numbers:

## Data Structures

### MeshVertex — [src/stage1.rs](src/stage1.rs#L24-L35)
```rust
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct MeshVertex {
    pub x: f64,
    pub y: f64,
    pub z: f64,
}

impl MeshVertex {
    pub fn from_xyz(x: f64, y: f64, z: f64) -> Self {
        Self { x, y, z }
    }
```

**Line 24-35**

---

### MeshFace — [src/stage1.rs](src/stage1.rs#L39-L46)
```rust
pub struct MeshFace {
    pub vertex_count: u8,
    pub vertex_indices: [usize; 4],
    pub neighbors: [i32; 4],
    pub normal: Option<[f64; 3]>,
    pub planar_hypothesis: i32,
    pub cylindrical_hypothesis: i32,
    pub spherical_hypothesis: i32,
}
```

**Line 39-46**

---

### Constants — [src/stage1.rs](src/stage1.rs#L17-L20)
```rust
pub const UNDEDUCED_PLANAR_HYPOTHESIS: i32 = -2;
pub const NO_HYPOTHESIS: i32 = -1;
pub const UNDEDUCED_CYLINDRICAL_HYPOTHESIS: i32 = -2;
pub const UNDEDUCED_SPHERICAL_HYPOTHESIS: i32 = -2;
```

**Line 17-20**

---

### CylindricalHypothesis — [src/stage2.rs](src/stage2.rs#L86-L108)
```rust
/// A cylindrical surface hypothesis fitted to a set of mesh faces.
#[derive(Debug, Clone)]
pub struct CylindricalHypothesis {
    /// A point on the cylinder axis.
    pub axis_origin: [f64; 3],
    /// Unit direction vector along the cylinder axis.
    pub axis_direction: [f64; 3],
    /// Radius of the cylinder (always positive).
    pub radius: f64,
    /// Whether the surface normal points away from the axis (convex=true) or toward it (concave=false).
    pub convex: bool,
    /// Set of mesh face indices that fit this hypothesis.
    pub faces: Vec<usize>,
    /// Set of mesh vertex indices on this cylinder.
    pub vertices: Vec<usize>,
    /// Maximum absolute distance from any vertex to the cylinder surface.
    pub error_max: f64,
    /// Maximum absolute distance from any face centroid to the cylinder surface.
    pub centroid_error_max: f64,
    /// Sum of absolute vertex-to-surface distances.
    pub error_abs_sum: f64,
}
```

**Line 86-108**

---

### CylinderTrialResult — [src/stage2.rs](src/stage2.rs#L933-L944)
```rust
/// Result of a trial BFS for cylindrical hypothesis evaluation.
struct CylinderTrialResult {
    faces: Vec<usize>,
    vertices: HashSet<usize>,
    axis_origin: [f64; 3],
    axis_direction: [f64; 3],
    radius: f64,
    convex: bool,
    error_max: f64,
    centroid_error_max: f64,
    error_abs_sum: f64,
}
```

**Line 933-944**

---

## Main Deduction Function

### `deduce_cylindrical_hypotheses` — [src/stage2.rs](src/stage2.rs#L1222-L1325)

**Outer loop, seeding logic, and commitment logic:**

```rust
fn deduce_cylindrical_hypotheses(
    mesh: &mut ConnectedMesh,
    vertex_tol: f64,
    surface_tol: f64,
    angular_tol: f64,
) -> Vec<CylindricalHypothesis> {
    let num_faces = mesh.faces.len();
    let mut hypotheses: Vec<CylindricalHypothesis> = Vec::new();

    for fi in 0..num_faces {
        if mesh.faces[fi].cylindrical_hypothesis != UNDEDUCED_CYLINDRICAL_HYPOTHESIS {
            continue;
        }

        let fi_normal = match mesh.faces[fi].normal {
            Some(n) => n,
            None => {
                mesh.faces[fi].cylindrical_hypothesis = NO_HYPOTHESIS;
                continue;
            }
        };

        // Collect ALL valid seed partners
        let fi_vc = mesh.faces[fi].vertex_count as usize;
        let fi_neighbors = mesh.faces[fi].neighbors;
        let mut seed_partners: Vec<usize> = Vec::new();

        for &ni in &fi_neighbors[..fi_vc] {
            if ni < 0 {
                continue;
            }
            let ni = ni as usize;

            if mesh.faces[ni].cylindrical_hypothesis != UNDEDUCED_CYLINDRICAL_HYPOTHESIS {
                continue;
            }

            let ni_normal = match mesh.faces[ni].normal {
                Some(n) => n,
                None => continue,
            };

            // Check cross product magnitude for sufficient angular difference
            let cross = cross3(&fi_normal, &ni_normal);
            let cross_mag = (cross[0] * cross[0] + cross[1] * cross[1] + cross[2] * cross[2]).sqrt();
            if cross_mag < MIN_CROSS_THRESHOLD {
                continue;
            }

            // Angular tolerance: reject seed pairs with dihedral angle > limit
            let cos_angle = dot3(&fi_normal, &ni_normal).clamp(-1.0, 1.0);
            if cos_angle.acos() > angular_tol {
                continue;
            }

            seed_partners.push(ni);
        }

        if seed_partners.is_empty() {
            mesh.faces[fi].cylindrical_hypothesis = NO_HYPOTHESIS;
            continue;
        }

        // Multi-seed evaluation: try each seed partner, keep best
        let mut best_candidate: Option<CylinderTrialResult> = None;

        for &ni in &seed_partners {
            let trial = run_cylinder_trial_bfs(
                fi, ni, mesh,
                vertex_tol, surface_tol, angular_tol,
                best_candidate.as_ref(),
            );

            if let Some(trial) = trial {
                if best_candidate.as_ref().is_none_or(|b| trial.faces.len() > b.faces.len()) {
                    best_candidate = Some(trial);
                }
            }
        }

        if let Some(candidate) = best_candidate {
            // Angular coverage validation
            if !angular_coverage_valid(
                &candidate.faces, &candidate.axis_origin, &candidate.axis_direction,
                &mesh.faces, &mesh.vertices,
            ) {
                mesh.faces[fi].cylindrical_hypothesis = NO_HYPOTHESIS;
                continue;
            }

            // Commit: assign all faces to this hypothesis
            let hi = hypotheses.len() as i32;
            for &f in &candidate.faces {
                mesh.faces[f].cylindrical_hypothesis = hi;
            }

            hypotheses.push(CylindricalHypothesis {
                axis_origin: candidate.axis_origin,
                axis_direction: candidate.axis_direction,
                radius: candidate.radius,
                convex: candidate.convex,
                faces: candidate.faces,
                vertices: candidate.vertices.into_iter().collect(),
                error_max: candidate.error_max,
                centroid_error_max: candidate.centroid_error_max,
                error_abs_sum: candidate.error_abs_sum,
            });
        } else {
            mesh.faces[fi].cylindrical_hypothesis = NO_HYPOTHESIS;
        }
    }

    hypotheses
}
```

**Line 1222-1325**

---

## BFS Expansion Function

### `run_cylinder_trial_bfs` — [src/stage2.rs](src/stage2.rs#L949-L1220)

**Includes: initial seed fitting, BFS expansion with vertex/angular/centroid checks, early termination logic, and re-fitting:**

```rust
fn run_cylinder_trial_bfs(
    seed_fi: usize,
    seed_ni: usize,
    mesh: &ConnectedMesh,
    vertex_tol: f64,
    surface_tol: f64,
    angular_tol: f64,
    best_candidate: Option<&CylinderTrialResult>,
) -> Option<CylinderTrialResult> {
    let fi_vc = mesh.faces[seed_fi].vertex_count as usize;
    let ni_vc = mesh.faces[seed_ni].vertex_count as usize;

    // Collect seed vertices
    let mut vertex_set: HashSet<usize> = mesh.faces[seed_fi].vertex_indices[..fi_vc]
        .iter().copied().collect();
    for &vi in &mesh.faces[seed_ni].vertex_indices[..ni_vc] {
        vertex_set.insert(vi);
    }

    let mut face_list = vec![seed_fi, seed_ni];

    // Fit cylinder to seed pair
    let (mut current_origin, mut current_dir, mut current_radius) =
        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)?;

    // Verify seed: all vertices within tolerance
    if !all_vertices_within_cylinder_tolerance(
        &vertex_set, &current_origin, &current_dir, current_radius,
        vertex_tol, &mesh.vertices,
    ) {
        return None;
    }

    // Determine convexity from seed face
    let convex = determine_convexity(
        &mesh.faces[seed_fi], &mesh.vertices, &current_origin, &current_dir,
    );

    // Track claimed faces in this trial (using a HashSet, not mesh mutation)
    let mut trial_claimed: HashSet<usize> = HashSet::new();
    trial_claimed.insert(seed_fi);
    trial_claimed.insert(seed_ni);

    let mut queue = VecDeque::new();
    queue.push_back(seed_fi);
    queue.push_back(seed_ni);

    // BFS expansion
    while let Some(current_fi) = queue.pop_front() {
        let vc = mesh.faces[current_fi].vertex_count as usize;
        let neighbors = mesh.faces[current_fi].neighbors;

        for &cni in &neighbors[..vc] {
            if cni < 0 {
                continue;
            }
            let cni = cni as usize;

            // Skip if already committed to a real hypothesis or claimed by this trial
            if mesh.faces[cni].cylindrical_hypothesis != UNDEDUCED_CYLINDRICAL_HYPOTHESIS {
                continue;
            }
            if trial_claimed.contains(&cni) {
                continue;
            }

            // Convexity check
            let cni_convex = determine_convexity(
                &mesh.faces[cni], &mesh.vertices, &current_origin, &current_dir,
            );
            if cni_convex != convex {
                continue;
            }

            // Angular tolerance: reject if dihedral angle with ANY already-claimed
            // neighbor exceeds the limit
            if let Some(n_cni) = mesh.faces[cni].normal {
                let cni_vc2 = mesh.faces[cni].vertex_count as usize;
                let cni_neighbors = mesh.faces[cni].neighbors;
                let mut angular_reject = false;
                for &adj in &cni_neighbors[..cni_vc2] {
                    if adj < 0 { continue; }
                    let adj = adj as usize;
                    // Check against both committed (same hypothesis doesn't exist yet)
                    // and trial-claimed faces
                    if !trial_claimed.contains(&adj) { continue; }
                    if let Some(n_adj) = mesh.faces[adj].normal {
                        let cos_a = dot3(&n_adj, &n_cni).clamp(-1.0, 1.0);
                        if cos_a.acos() > angular_tol {
                            angular_reject = true;
                            break;
                        }
                    }
                }
                if angular_reject { continue; }
            }

            // Vertex distance check
            let cni_vc = mesh.faces[cni].vertex_count as usize;
            let cni_vi = mesh.faces[cni].vertex_indices;
            let mut all_ok = true;
            let mut any_far = false;
            for &vi in &cni_vi[..cni_vc] {
                let d = vertex_to_cylinder_distance(
                    &mesh.vertices[vi],
                    &current_origin,
                    &current_dir,
                    current_radius,
                ).abs();
                if d > vertex_tol {
                    all_ok = false;
                    if d > REFIT_SKIP_MULTIPLIER * vertex_tol {
                        any_far = true;
                        break;
                    }
                }
            }

            if any_far {
                continue;
            }

            // Centroid validation: check face centroid distance to cylinder
            let centroid = face_centroid(&mesh.faces[cni], &mesh.vertices);
            let centroid_dist = vertex_to_cylinder_distance(
                &MeshVertex::from_xyz(centroid[0], centroid[1], centroid[2]),
                &current_origin, &current_dir, current_radius,
            ).abs();
            if centroid_dist > REFIT_SKIP_MULTIPLIER * surface_tol {
                continue;
            }
            let needs_refit = !all_ok || centroid_dist > surface_tol;

            if needs_refit {
                // Try re-fitting with new face included
                let mut trial_vertices = vertex_set.clone();
                for &vi in &cni_vi[..cni_vc] {
                    trial_vertices.insert(vi);
                }
                let mut trial_faces = face_list.clone();
                trial_faces.push(cni);

                let refit = fit_cylinder(
                    &trial_faces, &trial_vertices, &mesh.faces, &mesh.vertices,
                );
                let (new_origin, new_dir, new_radius) = match refit {
                    Some(params) => params,
                    None => continue,
                };

                if !all_vertices_within_cylinder_tolerance(
                    &trial_vertices, &new_origin, &new_dir, new_radius,
                    vertex_tol, &mesh.vertices,
                ) {
                    continue;
                }

                // Check all face centroids within surface tolerance after re-fit
                let mut centroids_ok = true;
                for &f in &trial_faces {
                    let c = face_centroid(&mesh.faces[f], &mesh.vertices);
                    let d = vertex_to_cylinder_distance(
                        &MeshVertex::from_xyz(c[0], c[1], c[2]),
                        &new_origin, &new_dir, new_radius,
                    ).abs();
                    if d > surface_tol {
                        centroids_ok = false;
                        break;
                    }
                }
                if !centroids_ok {
                    continue;
                }

                // Accept re-fit
                current_origin = new_origin;
                current_dir = new_dir;
                current_radius = new_radius;
            }

            // Accept this face
            trial_claimed.insert(cni);
            face_list.push(cni);
            for &vi in &cni_vi[..cni_vc] {
                vertex_set.insert(vi);
            }
            queue.push_back(cni);
        }

        // Early termination: if we have 3+ faces and are rediscovering the
        // same cylinder as the current best candidate, abandon.
        if face_list.len() >= 3 {
            if let Some(best) = best_candidate {
                let axis_dot = dot3(&current_dir, &best.axis_direction).abs();
                if axis_dot > 1.0 - 1e-6
                    && (current_radius - best.radius).abs() < vertex_tol
                {
                    // Check axis-to-axis distance
                    let d = [
                        current_origin[0] - best.axis_origin[0],
                        current_origin[1] - best.axis_origin[1],
                        current_origin[2] - best.axis_origin[2],
                    ];
                    let t = dot3(&d, &current_dir);
                    let perp_sq = d[0] * d[0] + d[1] * d[1] + d[2] * d[2] - t * t;
                    let perp_dist = if perp_sq > 0.0 { perp_sq.sqrt() } else { 0.0 };

                    if perp_dist < vertex_tol
                        && face_list.iter().all(|f| best.faces.contains(f))
                    {
                        return None; // Redundant trial
                    }
                }
            }
        }
    }

    // Final re-fit
    if let Some((final_origin, final_dir, final_radius)) =
        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)
    {
        current_origin = final_origin;
        current_dir = final_dir;
        current_radius = final_radius;
    }

    // Compute error metrics
    let mut error_max = 0.0_f64;
    let mut error_abs_sum = 0.0_f64;
    for &vi in &vertex_set {
        let d = vertex_to_cylinder_distance(
            &mesh.vertices[vi], &current_origin, &current_dir, current_radius,
        ).abs();
        error_max = error_max.max(d);
        error_abs_sum += d;
    }

    // Validate: minimum face count
    if face_list.len() < MIN_CYLINDER_FACES {
        return None;
    }

    // Validate: centroid check; also compute centroid_error_max
    let mut centroid_error_max = 0.0_f64;
    for &f in &face_list {
        let c = face_centroid(&mesh.faces[f], &mesh.vertices);
        let d = vertex_to_cylinder_distance(
            &MeshVertex::from_xyz(c[0], c[1], c[2]),
            &current_origin, &current_dir, current_radius,
        ).abs();
        centroid_error_max = centroid_error_max.max(d);
        if d > surface_tol {
            return None;
        }
    }

    Some(CylinderTrialResult {
        faces: face_list,
        vertices: vertex_set,
        axis_origin: current_origin,
        axis_direction: current_dir,
        radius: current_radius,
        convex,
        error_max,
        centroid_error_max,
        error_abs_sum,
    })
}
```

**Line 949-1220**

---

## Helper Functions

### `fit_cylinder` (Main fitting algorithm) — [src/stage2.rs](src/stage2.rs#L675-L801)

```rust
/// Fit cylinder parameters (axis direction, axis origin, radius) from a set of faces.
/// Returns (axis_origin, axis_direction, radius).
fn fit_cylinder(
    face_indices: &[usize],
    vertex_set: &HashSet<usize>,
    faces: &[MeshFace],
    vertices: &[MeshVertex],
) -> Option<([f64; 3], [f64; 3], f64)> {
    // Step 1: Axis direction from smallest eigenvector of normal covariance.
    let cov = build_normal_covariance(face_indices, faces, vertices);
    let mut axis_dir = smallest_eigenvector_3x3(&cov);
    let len = normalize3(&mut axis_dir);
    if len < 1e-15 {
        return None;
    }

    // Step 2: Compute orthogonal basis perpendicular to axis.
    let (u, w) = perpendicular_basis(&axis_dir);

    // Step 3: Project vertices onto 2D plane perpendicular to axis and fit circle.
    let verts: Vec<usize> = vertex_set.iter().copied().collect();
    let n = verts.len();
    if n < 3 {
        return None;
    }

    // Collect 2D coordinates
    let mut xs = Vec::with_capacity(n);
    let mut ys = Vec::with_capacity(n);
    for &vi in &verts {
        let v = &vertices[vi];
        let vx = v.x;
        let vy = v.y;
        let vz = v.z;
        xs.push(vx * u[0] + vy * u[1] + vz * u[2]);
        ys.push(vx * w[0] + vy * w[1] + vz * w[2]);
    }

    // Center the 2D data to improve numerical conditioning.
    // Without centering, the normal equations matrix becomes ill-conditioned
    // when absolute coordinates are large relative to the arc span (e.g.,
    // 6 vertices spanning 2.5° of a r=2mm cylinder at position (15, 12.5)).
    let nf = n as f64;
    let mean_x = xs.iter().sum::<f64>() / nf;
    let mean_y = ys.iter().sum::<f64>() / nf;
    for i in 0..n {
        xs[i] -= mean_x;
        ys[i] -= mean_y;
    }

    // Algebraic circle fit: x² + y² + Dx + Ey + F = 0
    // Least squares: minimize Σ(x² + y² + Dx + Ey + F)²
    // Normal equations: [Σx²  Σxy  Σx ] [D]   [-Σx(x²+y²)]
    //                   [Σxy  Σy²  Σy ] [E] = [-Σy(x²+y²)]
    //                   [Σx   Σy   n  ] [F]   [-Σ(x²+y²) ]
    let mut sx = 0.0_f64;
    let mut sy = 0.0_f64;
    let mut sx2 = 0.0_f64;
    let mut sy2 = 0.0_f64;
    let mut sxy = 0.0_f64;
    let mut sx3 = 0.0_f64;
    let mut sy3 = 0.0_f64;
    let mut sx2y = 0.0_f64;
    let mut sxy2 = 0.0_f64;

    for i in 0..n {
        let x = xs[i];
        let y = ys[i];
        sx += x;
        sy += y;
        sx2 += x * x;
        sy2 += y * y;
        sxy += x * y;
        sx3 += x * x * x;
        sy3 += y * y * y;
        sx2y += x * x * y;
        sxy2 += x * y * y;
    }

    // RHS
    let rhs0 = -(sx3 + sxy2);
    let rhs1 = -(sx2y + sy3);
    let rhs2 = -(sx2 + sy2);

    // 3x3 system: solve using Cramer's rule
    let a = [[sx2, sxy, sx], [sxy, sy2, sy], [sx, sy, nf]];

    let det_a = a[0][0] * (a[1][1] * a[2][2] - a[1][2] * a[2][1])
              - a[0][1] * (a[1][0] * a[2][2] - a[1][2] * a[2][0])
              + a[0][2] * (a[1][0] * a[2][1] - a[1][1] * a[2][0]);

    if det_a.abs() < 1e-30 {
        return None; // Degenerate — vertices are collinear in projection
    }

    let inv_det = 1.0 / det_a;

    let d_val = inv_det * (rhs0 * (a[1][1] * a[2][2] - a[1][2] * a[2][1])
                         - a[0][1] * (rhs1 * a[2][2] - a[1][2] * rhs2)
                         + a[0][2] * (rhs1 * a[2][1] - a[1][1] * rhs2));

    let e_val = inv_det * (a[0][0] * (rhs1 * a[2][2] - a[1][2] * rhs2)
                         - rhs0 * (a[1][0] * a[2][2] - a[1][2] * a[2][0])
                         + a[0][2] * (a[1][0] * rhs2 - rhs1 * a[2][0]));

    let f_val = inv_det * (a[0][0] * (a[1][1] * rhs2 - rhs1 * a[2][1])
                         - a[0][1] * (a[1][0] * rhs2 - rhs1 * a[2][0])
                         + rhs0 * (a[1][0] * a[2][1] - a[1][1] * a[2][0]));

    let center_x = -d_val / 2.0;
    let center_y = -e_val / 2.0;
    let r_sq = center_x * center_x + center_y * center_y - f_val;
    if r_sq <= 0.0 {
        return None;
    }
    let radius = r_sq.sqrt();

    // Convert 2D center back to 3D (undo centering offset)
    let abs_center_x = center_x + mean_x;
    let abs_center_y = center_y + mean_y;
    let axis_origin = [
        abs_center_x * u[0] + abs_center_y * w[0],
        abs_center_x * u[1] + abs_center_y * w[1],
        abs_center_x * u[2] + abs_center_y * w[2],
    ];

    Some((axis_origin, axis_dir, radius))
}
```

**Line 675-801**

---

### `build_normal_covariance` — [src/stage2.rs](src/stage2.rs#L653-L673)

```rust
/// Build the area-weighted normal covariance matrix M = Σ wᵢ nᵢ nᵢᵀ.
/// Returns the upper triangle [m00, m01, m02, m11, m12, m22].
fn build_normal_covariance(
    face_indices: &[usize],
    faces: &[MeshFace],
    vertices: &[MeshVertex],
) -> [f64; 6] {
    let mut m = [0.0_f64; 6];
    for &fi in face_indices {
        let face = &faces[fi];
        let n = face.normal.unwrap();
        let w = face_area(face, vertices);
        m[0] += w * n[0] * n[0];
        m[1] += w * n[0] * n[1];
        m[2] += w * n[0] * n[2];
        m[3] += w * n[1] * n[1];
        m[4] += w * n[1] * n[2];
        m[5] += w * n[2] * n[2];
    }
    m
}
```

**Line 653-673**

---

### `smallest_eigenvector_3x3` — [src/stage2.rs](src/stage2.rs#L569-L651)

```rust
/// Compute the smallest eigenvector of a 3x3 symmetric matrix.
/// The matrix is stored as [m00, m01, m02, m11, m12, m22] (upper triangle).
/// Returns the eigenvector corresponding to the smallest eigenvalue.
fn smallest_eigenvector_3x3(m: &[f64; 6]) -> [f64; 3] {
    // m = [[m[0], m[1], m[2]],
    //      [m[1], m[3], m[4]],
    //      [m[2], m[4], m[5]]]
    // Use the characteristic polynomial approach for 3x3 symmetric matrices.
    // Characteristic equation: λ³ - c2*λ² + c1*λ - c0 = 0
    // where c2 = trace, c1 = sum of 2x2 minors, c0 = determinant
    let a00 = m[0]; let a01 = m[1]; let a02 = m[2];
    let a11 = m[3]; let a12 = m[4]; let a22 = m[5];

    let c2 = a00 + a11 + a22;
    let c1 = a00 * a11 - a01 * a01 + a00 * a22 - a02 * a02 + a11 * a22 - a12 * a12;
    let c0 = a00 * (a11 * a22 - a12 * a12)
            - a01 * (a01 * a22 - a12 * a02)
            + a02 * (a01 * a12 - a11 * a02);

    // Depressed cubic: t³ + pt + q = 0 where λ = t + c2/3
    let p = c1 - c2 * c2 / 3.0;
    let q = -2.0 * c2 * c2 * c2 / 27.0 + c1 * c2 / 3.0 - c0;

    let eigenvalues = if p.abs() < 1e-30 {
        // All eigenvalues equal
        let ev = c2 / 3.0;
        [ev, ev, ev]
    } else {
        // p < 0 for real symmetric matrices with distinct eigenvalues
        let neg_p_3 = (-p / 3.0).max(0.0);
        let r = neg_p_3.sqrt();
        let cos_arg = (-q / (2.0 * neg_p_3 * r)).clamp(-1.0, 1.0);
        let theta = cos_arg.acos() / 3.0;
        let shift = c2 / 3.0;
        let two_pi_3 = 2.0 * std::f64::consts::PI / 3.0;
        let ev0 = 2.0 * r * theta.cos() + shift;
        let ev1 = 2.0 * r * (theta - two_pi_3).cos() + shift;
        let ev2 = 2.0 * r * (theta - 2.0 * two_pi_3).cos() + shift;
        [ev0, ev1, ev2]
    };

    // Find the smallest eigenvalue
    let (min_idx, _) = eigenvalues.iter().enumerate()
        .min_by(|a, b| a.1.partial_cmp(b.1).unwrap())
        .unwrap();

    // Compute eigenvector for the smallest eigenvalue using inverse iteration:
    // (M - λI)v = 0, find null space
    // Shift slightly to avoid exact singularity issues
    let lambda = eigenvalues[min_idx];
    let b00 = a00 - lambda;
    let b11 = a11 - lambda;
    let b22 = a22 - lambda;

    // Try cross products of rows to find the null space
    let row0 = [b00, a01, a02];
    let row1 = [a01, b11, a12];
    let row2 = [a02, a12, b22];

    let candidates = [
        cross3(&row0, &row1),
        cross3(&row0, &row2),
        cross3(&row1, &row2),
    ];

    // Pick the cross product with largest magnitude
    let mut best_idx = 0;
    let mut best_len_sq = 0.0;
    for (i, c) in candidates.iter().enumerate() {
        let len_sq = c[0] * c[0] + c[1] * c[1] + c[2] * c[2];
        if len_sq > best_len_sq {
            best_len_sq = len_sq;
            best_idx = i;
        }
    }

    let mut ev = candidates[best_idx];
    let len = normalize3(&mut ev);
    if len < 1e-15 {
        // Fallback: eigenvalues are degenerate, return arbitrary unit vector
        return [1.0, 0.0, 0.0];
    }
    ev
}
```

**Line 569-651**

---

### Vectormath Utilities — [src/stage2.rs](src/stage2.rs#L535-L567)

```rust
/// Normalize a 3D vector in place, return its length.
fn normalize3(v: &mut [f64; 3]) -> f64 {
    let len = (v[0] * v[0] + v[1] * v[1] + v[2] * v[2]).sqrt();
    if len > 0.0 {
        v[0] /= len;
        v[1] /= len;
        v[2] /= len;
    }
    len
}

/// Cross product of two 3D vectors.
fn cross3(a: &[f64; 3], b: &[f64; 3]) -> [f64; 3] {
    [
        a[1] * b[2] - a[2] * b[1],
        a[2] * b[0] - a[0] * b[2],
        a[0] * b[1] - a[1] * b[0],
    ]
}

/// Dot product of two 3D vectors.
fn dot3(a: &[f64; 3], b: &[f64; 3]) -> f64 {
    a[0] * b[0] + a[1] * b[1] + a[2] * b[2]
}
```

**Line 535-567**

---

### `vertex_to_cylinder_distance` — [src/stage2.rs](src/stage2.rs#L521-L555)

```rust
/// Compute the distance from a vertex to a cylinder surface.
/// Returns the signed distance: positive if outside the cylinder, negative if inside.
fn vertex_to_cylinder_distance(
    v: &MeshVertex,
    axis_origin: &[f64; 3],
    axis_direction: &[f64; 3],
    radius: f64,
) -> f64 {
    // Vector from axis origin to vertex
    let dx = v.x - axis_origin[0];
    let dy = v.y - axis_origin[1];
    let dz = v.z - axis_origin[2];
    // Project onto axis
    let t = dx * axis_direction[0] + dy * axis_direction[1] + dz * axis_direction[2];
    // Radial vector (perpendicular to axis)
    let rx = dx - t * axis_direction[0];
    let ry = dy - t * axis_direction[1];
    let rz = dz - t * axis_direction[2];
    let radial_dist = (rx * rx + ry * ry + rz * rz).sqrt();
    radial_dist - radius
}
```

**Line 521-555**

---

### `all_vertices_within_cylinder_tolerance` — [src/stage2.rs](src/stage2.rs#L803-L815)

```rust
fn all_vertices_within_cylinder_tolerance(
    vertex_set: &HashSet<usize>,
    axis_origin: &[f64; 3],
    axis_direction: &[f64; 3],
    radius: f64,
    tolerance: f64,
    vertices: &[MeshVertex],
) -> bool {
    for &vi in vertex_set {
        let d = vertex_to_cylinder_distance(&vertices[vi], axis_origin, axis_direction, radius);
        if d.abs() > tolerance {
            return false;
        }
    }
    true
}
```

**Line 803-815**

---

### `determine_convexity` — [src/stage2.rs](src/stage2.rs#L821-L843)

```rust
/// Determine convexity: does the face normal point away from the axis (convex) or toward it (concave)?
fn determine_convexity(
    face: &MeshFace,
    vertices: &[MeshVertex],
    axis_origin: &[f64; 3],
    axis_direction: &[f64; 3],
) -> bool {
    let centroid = face_centroid(face, vertices);
    // Vector from axis to centroid (radial component)
    let d = [
        centroid[0] - axis_origin[0],
        centroid[1] - axis_origin[1],
        centroid[2] - axis_origin[2],
    ];
    let t = dot3(&d, axis_direction);
    let radial = [
        d[0] - t * axis_direction[0],
        d[1] - t * axis_direction[1],
        d[2] - t * axis_direction[2],
    ];
    let n = face.normal.unwrap();
    dot3(&n, &radial) > 0.0
}
```

**Line 821-843**

---

### `angular_coverage_valid` — [src/stage2.rs](src/stage2.rs#L869-L920)

```rust
/// Validate that faces in a cylindrical hypothesis have sufficient angular
/// coverage around the cylinder — i.e., they are distributed around the
/// circumference, not clustered on one side.
///
/// Requires at least 3 distinct angular clusters. The check is:
/// - Compute the angular coordinate θ of each face centroid around the axis.
/// - Sort θ values and compute all N inter-face gaps plus the wraparound gap.
/// - The largest gap defines the "empty arc"; span = 2π − largest_gap.
/// - The second-largest gap must be ≤ span / 3.
fn angular_coverage_valid(
    face_list: &[usize],
    axis_origin: &[f64; 3],
    axis_direction: &[f64; 3],
    faces: &[MeshFace],
    vertices: &[MeshVertex],
) -> bool {
    if face_list.len() < 3 {
        return false;
    }

    let (u, w) = perpendicular_basis(axis_direction);

    // Compute angular coordinate for each face centroid
    let mut thetas: Vec<f64> = Vec::with_capacity(face_list.len());
    for &fi in face_list {
        let c = face_centroid(&faces[fi], vertices);
        let d = [
            c[0] - axis_origin[0],
            c[1] - axis_origin[1],
            c[2] - axis_origin[2],
        ];
        let t = dot3(&d, axis_direction);
        let radial = [
            d[0] - t * axis_direction[0],
            d[1] - t * axis_direction[1],
            d[2] - t * axis_direction[2],
        ];
        let theta = f64::atan2(dot3(&radial, &w), dot3(&radial, &u));
        thetas.push(theta);
    }

    thetas.sort_by(|a, b| a.partial_cmp(b).unwrap());
    let n = thetas.len();

    // Compute all gaps (including wraparound)
    let mut gaps: Vec<f64> = Vec::with_capacity(n);
    for i in 1..n {
        gaps.push(thetas[i] - thetas[i - 1]);
    }
    // Wraparound gap
    gaps.push(thetas[0] + 2.0 * std::f64::consts::PI - thetas[n - 1]);

    // Find largest and second-largest gaps
    let mut largest = 0.0_f64;
    let mut second_largest = 0.0_f64;
    for &g in &gaps {
        if g > largest {
            second_largest = largest;
            largest = g;
        } else if g > second_largest {
            second_largest = g;
        }
    }

    let span = 2.0 * std::f64::consts::PI - largest;
    if span <= 0.0 {
        return false;
    }

    second_largest <= span / 3.0
}
```

**Line 869-920**

---

### `face_centroid` — [src/stage2.rs](src/stage2.rs#L502-L518)

```rust
/// Compute the face centroid for a mesh face.
fn face_centroid(face: &MeshFace, vertices: &[MeshVertex]) -> [f64; 3] {
    let n = face.vertex_count as usize;
    let mut cx = 0.0_f64;
    let mut cy = 0.0_f64;
    let mut cz = 0.0_f64;
    for vi_idx in 0..n {
        let v = &vertices[face.vertex_indices[vi_idx]];
        cx += v.x;
        cy += v.y;
        cz += v.z;
    }
    let inv_n = 1.0 / n as f64;
    [cx * inv_n, cy * inv_n, cz * inv_n]
}
```

**Line 502-518**

---

### `perpendicular_basis` — [src/stage2.rs](src/stage2.rs#L845-L866)

```rust
/// Build an orthonormal basis (u, w) perpendicular to a unit direction `d`.
fn perpendicular_basis(d: &[f64; 3]) -> ([f64; 3], [f64; 3]) {
    let (ax, ay, az) = (d[0].abs(), d[1].abs(), d[2].abs());
    let perp = if ax <= ay && ax <= az {
        [1.0, 0.0, 0.0]
    } else if ay <= az {
        [0.0, 1.0, 0.0]
    } else {
        [0.0, 0.0, 1.0]
    };
    let mut u = cross3(d, &perp);
    normalize3(&mut u);
    let w = cross3(d, &u);
    (u, w)
}
```

**Line 845-866**

---

### `face_area` — [src/stage2.rs](src/stage2.rs#L218-L231)

```rust
fn face_area(face: &MeshFace, vertices: &[MeshVertex]) -> f64 {
    let v0 = &vertices[face.vertex_indices[0]];
    let v1 = &vertices[face.vertex_indices[1]];
    let v2 = &vertices[face.vertex_indices[2]];
    let ax = v1.x - v0.x;
    let ay = v1.y - v0.y;
    let az = v1.z - v0.z;
    let bx = v2.x - v0.x;
    let by = v2.y - v0.y;
    let bz = v2.z - v0.z;
    let cx = ay * bz - az * by;
    let cy = az * bx - ax * bz;
    let cz = ax * by - ay * bx;
    0.5 * (cx * cx + cy * cy + cz * cz).sqrt()
}
```

**Line 218-231**

</details>

<details>
<summary>Tool output (runSubagent), lines 1-200</summary>

````
Perfect! I've found all the code sections you need. Here's the complete cylindrical hypothesis deduction code with line numbers:

## Data Structures

### MeshVertex — [src/stage1.rs](src/stage1.rs#L24-L35)
```rust
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct MeshVertex {
    pub x: f64,
    pub y: f64,
    pub z: f64,
}

impl MeshVertex {
    pub fn from_xyz(x: f64, y: f64, z: f64) -> Self {
        Self { x, y, z }
    }
```

**Line 24-35**

---

### MeshFace — [src/stage1.rs](src/stage1.rs#L39-L46)
```rust
pub struct MeshFace {
    pub vertex_count: u8,
    pub vertex_indices: [usize; 4],
    pub neighbors: [i32; 4],
    pub normal: Option<[f64; 3]>,
    pub planar_hypothesis: i32,
    pub cylindrical_hypothesis: i32,
    pub spherical_hypothesis: i32,
}
```

**Line 39-46**

---

### Constants — [src/stage1.rs](src/stage1.rs#L17-L20)
```rust
pub const UNDEDUCED_PLANAR_HYPOTHESIS: i32 = -2;
pub const NO_HYPOTHESIS: i32 = -1;
pub const UNDEDUCED_CYLINDRICAL_HYPOTHESIS: i32 = -2;
pub const UNDEDUCED_SPHERICAL_HYPOTHESIS: i32 = -2;
```

**Line 17-20**

---

### CylindricalHypothesis — [src/stage2.rs](src/stage2.rs#L86-L108)
```rust
/// A cylindrical surface hypothesis fitted to a set of mesh faces.
#[derive(Debug, Clone)]
pub struct CylindricalHypothesis {
    /// A point on the cylinder axis.
    pub axis_origin: [f64; 3],
    /// Unit direction vector along the cylinder axis.
    pub axis_direction: [f64; 3],
    /// Radius of the cylinder (always positive).
    pub radius: f64,
    /// Whether the surface normal points away from the axis (convex=true) or toward it (concave=false).
    pub convex: bool,
    /// Set of mesh face indices that fit this hypothesis.
    pub faces: Vec<usize>,
    /// Set of mesh vertex indices on this cylinder.
    pub vertices: Vec<usize>,
    /// Maximum absolute distance from any vertex to the cylinder surface.
    pub error_max: f64,
    /// Maximum absolute distance from any face centroid to the cylinder surface.
    pub centroid_error_max: f64,
    /// Sum of absolute vertex-to-surface distances.
    pub error_abs_sum: f64,
}
```

**Line 86-108**

---

### CylinderTrialResult — [src/stage2.rs](src/stage2.rs#L933-L944)
```rust
/// Result of a trial BFS for cylindrical hypothesis evaluation.
struct CylinderTrialResult {
    faces: Vec<usize>,
    vertices: HashSet<usize>,
    axis_origin: [f64; 3],
    axis_direction: [f64; 3],
    radius: f64,
    convex: bool,
    error_max: f64,
    centroid_error_max: f64,
    error_abs_sum: f64,
}
```

**Line 933-944**

---

## Main Deduction Function

### `deduce_cylindrical_hypotheses` — [src/stage2.rs](src/stage2.rs#L1222-L1325)

**Outer loop, seeding logic, and commitment logic:**

```rust
fn deduce_cylindrical_hypotheses(
    mesh: &mut ConnectedMesh,
    vertex_tol: f64,
    surface_tol: f64,
    angular_tol: f64,
) -> Vec<CylindricalHypothesis> {
    let num_faces = mesh.faces.len();
    let mut hypotheses: Vec<CylindricalHypothesis> = Vec::new();

    for fi in 0..num_faces {
        if mesh.faces[fi].cylindrical_hypothesis != UNDEDUCED_CYLINDRICAL_HYPOTHESIS {
            continue;
        }

        let fi_normal = match mesh.faces[fi].normal {
            Some(n) => n,
            None => {
                mesh.faces[fi].cylindrical_hypothesis = NO_HYPOTHESIS;
                continue;
            }
        };

        // Collect ALL valid seed partners
        let fi_vc = mesh.faces[fi].vertex_count as usize;
        let fi_neighbors = mesh.faces[fi].neighbors;
        let mut seed_partners: Vec<usize> = Vec::new();

        for &ni in &fi_neighbors[..fi_vc] {
            if ni < 0 {
                continue;
            }
            let ni = ni as usize;

            if mesh.faces[ni].cylindrical_hypothesis != UNDEDUCED_CYLINDRICAL_HYPOTHESIS {
                continue;
            }

            let ni_normal = match mesh.faces[ni].normal {
                Some(n) => n,
                None => continue,
            };

            // 
... (truncated, 5780 chars)
````
</details>

<details>
<summary>Tool output (runSubagent), lines 200-400</summary>

````
            // Commit: assign all faces to this hypothesis
            let hi = hypotheses.len() as i32;
            for &f in &candidate.faces {
                mesh.faces[f].cylindrical_hypothesis = hi;
            }

            hypotheses.push(CylindricalHypothesis {
                axis_origin: candidate.axis_origin,
                axis_direction: candidate.axis_direction,
                radius: candidate.radius,
                convex: candidate.convex,
                faces: candidate.faces,
                vertices: candidate.vertices.into_iter().collect(),
                error_max: candidate.error_max,
                centroid_error_max: candidate.centroid_error_max,
                error_abs_sum: candidate.error_abs_sum,
            });
        } else {
            mesh.faces[fi].cylindrical_hypothesis = NO_HYPOTHESIS;
        }
    }

    hypotheses
}
```

**Line 1222-1325**

---

## BFS Expansion Function

### `run_cylinder_trial_bfs` — [src/stage2.rs](src/stage2.rs#L949-L1220)

**Includes: initial seed fitting, BFS expansion with vertex/angular/centroid checks, early termination logic, and re-fitting:**

```rust
fn run_cylinder_trial_bfs(
    seed_fi: usize,
    seed_ni: usize,
    mesh: &ConnectedMesh,
    vertex_tol: f64,
    surface_tol: f64,
    angular_tol: f64,
    best_candidate: Option<&CylinderTrialResult>,
) -> Option<CylinderTrialResult> {
    let fi_vc = mesh.faces[seed_fi].vertex_count as usize;
    let ni_vc = mesh.faces[seed_ni].vertex_count as usize;

    // Collect seed vertices
    let mut vertex_set: HashSet<usize> = mesh.faces[seed_fi].vertex_indices[..fi_vc]
        .iter().copied().collect();
    for &vi in &mesh.faces[seed_ni].vertex_indices[..ni_vc] {
        vertex_set.insert(vi);
    }

    let mut face_list = vec![seed_fi, seed_ni];

    // Fit cylinder to seed pair
    let (mut current_origin, mut current_dir, mut current_radius) =
        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)?;

    // Verify seed: all vertices within tolerance
    if !all_vertices_within_cylinder_tolerance(
        &vertex_set, &current_origin, &current_dir, current_radius,
        vertex_tol, &mesh.vertices,
    ) {
        return None;
    }

    // Determine convexity from seed face
    let convex = determine_convexity(
        &mesh.faces[seed_fi], &mesh.vertices, &current_origin, &current_dir,
    );

    // Track claimed faces in this trial (using a HashSet, not mesh mutation)
    let mut trial_claimed: HashSet<usize> = HashSet::new();
    trial_claimed.insert(seed_fi);
    trial_claimed.insert(seed_ni);

    let mut queue = VecDeque::new();
    queue.push_back(seed_fi);
    queue.push_back(seed_ni);

    // BFS expansion
    while let Some(current_fi) = queue.pop_front() {
        let vc = mesh.faces[current_fi].vertex_count as usize;
        let neighbors = mesh.faces[current_fi].neighbors;

        for &cni in &neighbors[..vc] {
            if cni < 0 {
                continue;
            }
            let cni = cni as usize;

            // Skip if already committed to a real hypothesis or claimed by this trial
            if mesh.faces[cni].cylindrical_hypothesis != UNDEDUCED_CYLINDRICAL_HYPOTHESIS {
                continue;
            }
            if trial_claimed.contains(&cni) {
                continue;
            }

            // Convexity check
            let cni_convex = determine_convexity(
                &mesh.faces[cni], &mesh.vertices, &current_origin, &current_dir,
            );
            if cni_convex != convex {
                continue;
            }

            // Angular tolerance: reject if dihedral angle with ANY already-claimed
            // neighbor exceeds the limit
            if let Some(n_cni) = mesh.faces[cni].normal {
                let cni_vc2 = mesh.faces[cni].vertex_count as usize;
                let cni_neighbors = mesh.faces[cni].neighbors;
                let mut angular_reject = false;
                for &adj in &cni_neighbor
... (truncated, 7441 chars)
````
</details>

<details>
<summary>Tool output (runSubagent), lines 400-550</summary>

````
                        &new_origin, &new_dir, new_radius,
                    ).abs();
                    if d > surface_tol {
                        centroids_ok = false;
                        break;
                    }
                }
                if !centroids_ok {
                    continue;
                }

                // Accept re-fit
                current_origin = new_origin;
                current_dir = new_dir;
                current_radius = new_radius;
            }

            // Accept this face
            trial_claimed.insert(cni);
            face_list.push(cni);
            for &vi in &cni_vi[..cni_vc] {
                vertex_set.insert(vi);
            }
            queue.push_back(cni);
        }

        // Early termination: if we have 3+ faces and are rediscovering the
        // same cylinder as the current best candidate, abandon.
        if face_list.len() >= 3 {
            if let Some(best) = best_candidate {
                let axis_dot = dot3(&current_dir, &best.axis_direction).abs();
                if axis_dot > 1.0 - 1e-6
                    && (current_radius - best.radius).abs() < vertex_tol
                {
                    // Check axis-to-axis distance
                    let d = [
                        current_origin[0] - best.axis_origin[0],
                        current_origin[1] - best.axis_origin[1],
                        current_origin[2] - best.axis_origin[2],
                    ];
                    let t = dot3(&d, &current_dir);
                    let perp_sq = d[0] * d[0] + d[1] * d[1] + d[2] * d[2] - t * t;
                    let perp_dist = if perp_sq > 0.0 { perp_sq.sqrt() } else { 0.0 };

                    if perp_dist < vertex_tol
                        && face_list.iter().all(|f| best.faces.contains(f))
                    {
                        return None; // Redundant trial
                    }
                }
            }
        }
    }

    // Final re-fit
    if let Some((final_origin, final_dir, final_radius)) =
        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)
    {
        current_origin = final_origin;
        current_dir = final_dir;
        current_radius = final_radius;
    }

    // Compute error metrics
    let mut error_max = 0.0_f64;
    let mut error_abs_sum = 0.0_f64;
    for &vi in &vertex_set {
        let d = vertex_to_cylinder_distance(
            &mesh.vertices[vi], &current_origin, &current_dir, current_radius,
        ).abs();
        error_max = error_max.max(d);
        error_abs_sum += d;
    }

    // Validate: minimum face count
    if face_list.len() < MIN_CYLINDER_FACES {
        return None;
    }

    // Validate: centroid check; also compute centroid_error_max
    let mut centroid_error_max = 0.0_f64;
    for &f in &face_list {
        let c = face_centroid(&mesh.faces[f], &mesh.vertices);
        let d = vertex_to_cylinder_distance(
            &MeshVertex::from_xyz(c[0], c[1], c[2]),
            &current_origin, &current_dir, current_radius,
        ).abs();
        centroid_error_max = centroid_error_max.max(d);
        if d > surface_tol {
            return None;
        }
    }

    Some(CylinderTrialResult {
        faces: face_list,
        vertices: vertex_set,
        axis_origin: current_origin,
        axis_direction: current_dir,
        radius: current_radius,
        convex,
        error_max,
        centroid_error_max,
        error_abs_sum,
    })
}
```

**Line 949-1220**

---

## Helper Functions

### `fit_cylinder` (Main fitting algorithm) — [src/stage2.rs](src/stage2.rs#L675-L801)

```rust
/// Fit cylinder parameters (axis direction, axis origin, radius) from a set of faces.
/// Returns (axis_origin, axis_direction, radius).
fn fit_cylinder(
    face_indices: &[usize],
    vertex_set: &HashSet<usize>,
    faces: &[MeshFace],
    vertices: &[MeshVertex],
) -> Option<([f64; 3], [f64; 3], f64)> {
    // Step 1: Axis direction from smallest eigen
... (truncated, 4857 chars)
````
</details>

> 💭 Now I have a good understanding of the code. Let me also look at the actual test output for the rounded cube model to see the issue in practice.
> 
> Let me think about the user's hypothesis:
> 
> 1. The outer loop iterates faces where `cylindrical_hypothesis == -2` (UNDEDUCED)
> 2. For each such face, it collects seed partners - but **only** from neighbors that also have `cylindrical_hypothesis == -2`
> 3. For each seed partner, it runs a trial BFS
> 4. During BFS, it skips neighbors where `mesh.faces[cni].cylindrical_hypothesis != UNDEDUCED_CYLINDRICAL_HYPOTHESIS`
> 
> 
> 5. This means BFS can only grow onto unassigned faces
> 
> The user's hypothesis makes sense: two cylinder faces seed a cylinder, BFS expands onto nearby sphere faces near the equator that are close to the cylinder surface, refitting adjusts the cylinder parameters, and those adjusted parameters pass validation checks even though they don't accurately capture the true cylinder. Once those faces get committed to this flawed hypothesis, the remaining actual cylinder faces can't form a proper grouping because critical faces have already been consumed.
> 
> The proposed fix allows seeding from any face rather than just unassigned ones, and modifies the BFS early termination logic.
> 
> Now I'm realizing the key tension: if we seed from an already-assigned face and find a better hypothesis through BFS, the current BFS implementation would still skip over other assigned faces since it only explores faces where the hypothesis is unset. So allowing seeding from assigned faces alone wouldn't help unless we also modify BFS to traverse through assigned faces, which could create conflicts between competing hypotheses. I see the issue now—the problem is that even if we iterate through all faces in the outer loop, we still need unassigned seed partners to form a new hypothesis. So a face like fm that's already surrounded by assigned neighbors can't bootstrap a new trial. The fix would need to either change the seed partner requirement or allow the outer loop to restart trials from assigned faces too, checking whether they're just rediscovering existing hypotheses. The user's proposal is to only relax the outer loop check—allowing any face to be tried as a seed, not just unassigned ones—while keeping seed partner collection and BFS expansion restricted to unassigned faces. This way, an assigned face like fi could anchor a new hypothesis using unassigned neighbors, and the early termination would compare the resulting hypothesis against both the best candidate and the existing hypothesis it was assigned to. growing
> - If the trial is rediscovering the same cylinder as H_bad, we abandon
> - If the trial finds a DIFFERENT cylinder (e.g., the correct one), we keep going
> 
> But there's still the question: what happens to fi? It's assigned to H_bad. If the trial produces a better hypothesis, do we reassign fi? The current code doesn't have reassignment logic.
> 
> Hmm, I think the user's proposal might be incomplete. Let me think about this more carefully.
> 
> Actually, re-reading again: "Allow seeding from any face, not just faces where cylindrical_hypothesis is -2."
> 
> Maybe the user's intent is simpler. Currently the code skips faces that have already been assigned a hypothesis. The proposed change would remove that check entirely, allowing any face to serve as a starting point for the outer loop. And I think this extends to seed partners too—we'd consider faces that already have hypotheses assigned. During BFS expansion, we'd probably need to relax that check as well so the search can grow into already-assigned faces. Then when we find a trial cylinder, we compare it against both the existing hypothesis for that face and any other candidates, abandoning only if we're rediscovering something we've already seen. I think "any existing candidate for the face" means: the committed hypothesis that a face is assigned to. So during early termination, check if the trial is just rediscovering an already-committed hypothesis. If the starting face fi is assigned to H3, and the trial's cylinder parameters match H3's, and all trial faces are in H3, then abandon (we're just rediscovering H3).
> 
> But the KEY question remains: if a trial seeded from an already-assigned face discovers a BETTER hypothesis (more faces), what should happen? The user doesn't mention reassignment.
> 
> I think the user might be thinking about an even simpler scenario: 
> 
> The problem is that bad hypotheses grow by consuming faces on the boundary. With the current algorithm, once faces are consumed, they can't be reconsidered. The user's fix would allow reconsidering those faces, so a later, better hypothesis could be found.
> 
> So the proposal is to allow seeding from any face, not just unassigned ones, and during BFS to also check if we're just rediscovering the hypothesis that the seed face is already assigned to—if so, abandon the trial since we're not finding anything new.
> 
> I'm also considering whether BFS expansion needs to change to allow exploring already-assigned faces, or if the current skip-assigned-faces behavior is actually fine because the early termination check would catch rediscovery anyway.
> 
> Looking at the alternatives, I could prevent bad hypotheses from forming in the first place by checking that cylinder parameters stay stable during refitting — the axis shouldn't rotate much, the radius shouldn't drift significantly. But I'm realizing both approaches might be necessary: a stability check to prevent the worst cases, combined with the re-seeding mechanism as a safety net for when parameters do shift legitimately.
> 
> Actually, stepping back to what the user really asked — is their hypothesis correct and is their fix reasonable? The hypothesis seems sound: at junctions between cylinders and spheres on a rounded cube, BFS could gradually accept sphere faces while refitting the cylinder parameters, eventually degrading the fit on the original cylinder faces. But here's the thing: the refit validation checks all existing vertices, not just new ones, so if the cylinder shifts enough to violate tolerance on the original faces, the refit gets rejected. The problem would only occur if the shift is small enough to pass validation but large enough to matter.
> 
> On a coarse mesh with loose tolerances, this becomes plausible — the equatorial sphere faces might be close enough to cylindrical that accepting them causes only a tiny parameter shift, and each subsequent face causes another tiny shift, leading to accumulated drift through many small refits that individually pass all checks.
> 
> The proposed re-seeding fix has merit but introduces real complexity: it would revisit already-assigned faces multiple times, require BFS to explore assigned faces to grow properly, and need reassignment logic if a better hypothesis is found later.
> 
> Instead, I think a more direct approach is to enforce parameter stability during refit — after accepting a refit, verify that the cylinder axis hasn't rotated significantly (checking that the dot product of old and new directions stays above a threshold like 1 - ε) and that the radius change stays within a small fraction of the vertex tolerance. This directly constrains individual refits from drifting too far, preventing the accumulation problem at its source.
> 
> But I'm realizing this might be too strict for legitimate refinement cases where the initial seed fit from just two faces is imprecise and adding more faces naturally improves the parameters. Maybe instead I should compare each refit against the original seed fit rather than the previous one, or reject refits that deviate too far from the cylinder derived without any refitting. Though that brings me back to the problem that the original seed fit itself can be quite imprecise, which is why we're refitting in the first place.
> 
> Let me consider a different angle: checking normal consistency. For a true cylinder, all face normals should be perpendicular to the axis, so their dot product with the axis should be near zero. But when we hit a sphere face near the junction, its normal starts gaining a component along the cylinder axis, which would signal we're moving into a different surface.
> 
> I could directly check that `|dot(face_normal, axis_dir)|` stays below a threshold for each candidate face—that's more robust than relying solely on dihedral angles with neighbors, since a sphere face could slip in if its dihedral angle with one neighbor happens to be small even though it doesn't belong on the cylinder.
> 
> On a cylinder, face normals are perpendicular to the axis, so this dot product is near zero. On a tessellated cylinder, there's some deviation depending on mesh coarseness. For a sphere face at the junction, the normal is also perpendicular to the cylinder axis at the equator, so this check alone isn't enough to distinguish them—I need to think about what happens away from the equator.
> 
> Actually, rereading the user's proposal, I think there's something important I missed: if we allow reseeding from any face, not just unassigned ones, we can iteratively refine the hypotheses. A face might get assigned to a bad hypothesis initially, but on a second pass, we could discover the correct cylinder and prefer it if it's larger. This is essentially letting the algorithm self-correct through multiple iterations.
> 
> The implementation would require some significant changes though—we'd need to let BFS explore already-assigned faces during trials, track which hypothesis each face should belong to, and handle reassignments carefully. But this opens up the risk of faces bouncing between hypotheses, which adds complexity.
> 
> I think I've analyzed this enough. The user's hypothesis about parameter drift at cylinder-sphere junctions is sound, and their proposed fix has the right intuition, even if it's somewhat involved. The iterative refinement idea is clever, though there might be simpler alternatives worth exploring.
> 
> A more direct approach could be adding a **parameter stability check** during refitting—when we accept a refit, we'd compare the new cylinder parameters against the original fit from the seed pair and reject the refit if the axis rotates or radius changes beyond some threshold. This would prevent drift without needing a second pass.
> 
> But wait, the original 2-face fit might not be precise enough to anchor to. Maybe instead we could require each new face to satisfy tolerance constraints against both the current fit and the original seed fit, which would keep parameters from drifting too far. Though that has its own issues...
> 
> Actually, thinking through the geometry: at a junction between a cylinder fillet and a sphere corner (both radius r), the surfaces are tangent. A face right at the junction lies on both, but faces deeper into the sphere deviate quadratically. On a coarse mesh, even the first sphere face away from the junction could still fall within vertex tolerance of the cylinder after refitting.
> 
> So maybe the real fix is to **cap the parameter change per refit**—limit how much the axis can rotate and how much the radius can shift with each refinement step.
> 
> But stepping back, the user wants me to evaluate whether my hypothesis is correct, consider if there's a better fix, update the plan, and commit. I think there's actually a simpler approach: **prevent BFS from crossing planar hypothesis boundaries when growing cylinders**. Since each cylinder face has its own single-face planar hypothesis and sphere faces do too, I could use that constraint to stop the growth at the boundary.
> 
> Actually, that won't work because the check wouldn't distinguish between cylinder and sphere faces effectively. Another option is checking if face normals are perpendicular to the cylinder axis, but that only catches faces away from the equator—the junction faces would still slip through.
> 
> The real problem seems to be parameter drift from repeated refits. What if I track how much the axis and radius have drifted from the initial seed fit, and once the cumulative drift exceeds a threshold, I stop accepting refits but continue growing with the current parameters?
> 
> Or simpler: just don't refit during BFS at all. The seed pair sets the initial cylinder, BFS adds only faces within tolerance of those fixed parameters, then I do one final refit at the end and verify everything still fits. This prevents drift entirely, though it risks missing valid faces if the initial 2-face fit is slightly off.
> 
> But that's where the `REFIT_SKIP_MULTIPLIER` comes in—faces between 1x and 2x tolerance would normally trigger a refit, but without mid-BFS refitting, they'd just get rejected. So maybe the real solution is to allow refitting but constrain the axis direction: require that refits keep the axis direction nearly identical to the original (like `|new_dir · original_dir| > 1 - 1e-4`). That way legitimate cylinder faces barely change the axis when refitted, while sphere faces would drift significantly and get caught.
> 
> The concern is that the initial 2-face fit might be somewhat inaccurate on a coarse mesh, so the axis should converge toward the true value as more faces are added—but the direction of that convergence is hard to predict.
> 
> If I compare each new axis to the original 2-face estimate rather than the previous one, small corrections in the right direction and larger corrections in the wrong direction both get caught by the same threshold. The real issue emerges at the sphere boundary: each sphere face adds a small tilt to the axis, and after just a few of them, the cumulative rotation from the original direction becomes significant.
> 
> I'm leaning toward the parameter stability check during refit as the cleanest solution—it's straightforward to add, directly addresses the drift problem, and doesn't require restructuring the algorithm. But let me reconsider what the user is actually proposing. Their two-part approach of allowing any face to seed and terminating early against existing hypotheses is elegant because it's self-correcting: even if one hypothesis goes wrong, the algorithm can still discover the right one later by trying different seeds.
> 
> The tricky part is what happens when a trial finds a better hypothesis for an already-assigned face. The user seems to be suggesting the BFS should explore all faces, not just unassigned ones, and if it finds something larger than the existing hypothesis, it could "steal" those faces—but that introduces cascading reassignment logic. Looking at their actual wording though, they're saying to abandon the trial if it's rediscovering an existing hypothesis for a face, which is more conservative than I initially thought.
> 
> So the real question becomes: if the trial finds a *different* cylinder and grows to include faces from existing hypotheses, how do we handle that commitment? I think the safest approach is to present parameter stability as the main prevention strategy, with the re-seeding idea as a potential enhancement if that proves insufficient. Let me check the BFS expansion logic to see how faces are currently being filtered...
> 
> Checking face filtering logic...
> 
> The BFS won't explore faces already assigned to other hypotheses, which means the trial would only grow through unassigned faces. For the user's approach to work, we'd need to relax that check to allow exploring faces from other hypotheses—that's a more substantial change than just allowing the seed face to be assigned.
> 
> The real problem is that once a bad hypothesis gets committed and consumes faces from both the cylinder and sphere, the remaining faces can't form valid hypotheses. I should focus on preventing that bad hypothesis from forming in the first place.
> 
> I'm considering three main strategies: preventing the bad hypothesis through stricter validation checks like axis stability and normal-axis orthogonality, allowing recovery after it forms through re-seeding or global re-evaluation, or improving the seeding strategy itself by choosing seeds with better angular support. Prevention seems like the cleanest approach—I could add an axis stability check when accepting refits to ensure the axis direction doesn't drift too much.
> 
> But I'm second-guessing the threshold. Using a quarter of the angular tolerance might be too strict on coarse meshes where the initial 2-face fit could already be off by several degrees. Maybe I should use an absolute threshold instead of one relative to angular_tol.
> 
> Let me reconsider how accurate that initial fit actually is. When I extract the axis from two adjacent faces, I'm computing the cross product of their normals, which gives me an approximate axis based on how those face normals vary. On a coarse mesh, those normals are only approximations of the true surface, so the resulting axis direction could easily be off by a few degrees already. The main sources of error are numerical precision (which is negligible) and STL vertex precision, so for vertices within tolerance of the true cylinder, the face normals should be very accurate and the axis direction should be within a fraction of a degree. The 2-face fit using the smallest eigenvector of the normal covariance should be quite reliable with a 1-2° stability threshold, and during refit, adding true cylinder faces will converge the axis while sphere faces will rotate it away.
> 
> Now I'm reconsidering the user's scenario more carefully — if a bad hypothesis consumes some cylinder and sphere faces, why would the remaining true cylinder faces fail to form their own hypothesis? Let me trace through what happens when face 6 tries to seed among the unassigned faces 7-20.
> 
> The key insight is that on a coarse mesh with only 10-12 faces per cylinder, if the bad hypothesis strategically consumes 3-4 faces from different angular positions, the remaining faces might be fragmented enough that no subset achieves full circumferential coverage. This is especially likely if the consumed faces happen to bridge gaps between the remaining clusters, leaving them unable to satisfy the angular coverage constraint.
> 
> Rather than speculating further, I should focus on the actual analysis the user requested—thinking through the mechanism and updating the development plan—since they explicitly asked me not to modify code or run tests.
> 
> The hypothesis about cylindrical drift is plausible: BFS starting from a cylinder seed can gradually shift parameters through sequential small refits, eventually consuming faces from both the cylinder and adjacent sphere surfaces. The proposed re-seeding fix would work as a recovery mechanism, but it introduces substantial complexity—BFS would need to explore assigned faces, handle reassignment logic when better hypotheses emerge, and manage cascading invalidations. A simpler preventative approach might be checking that the cylinder axis remains stable during refits, catching drift before it accumulates into a bad hypothesis. the user wants me to decide on the best approach and update the plan accordingly. Between re-seeding with early termination, axis stability checks during refit, or combining both, I think starting with the axis stability check makes the most sense—it's simpler and directly targets the drift mechanism without major algorithmic changes. If that proves insufficient, re-seeding becomes a fallback. Let me update the development plan with this strategy. The key insight is that on a cylinder, adding more faces should monotonically refine the axis toward the true direction—each new face brings the refit axis closer to ground truth. But on a sphere boundary, adding faces would push the axis away from the true cylinder axis, which is exactly what the stability check should catch and reject.
> 
> So the strategy is to compare the refit direction against the original 2-face direction. For legitimate cylinder faces, each refinement produces small movements (less than 0.5° per step) since the 2-face axis is already close to true. For sphere faces, the refit tries to accommodate those normals and pushes the axis more than 2° away from the original direction, failing the threshold.
> 
> The remaining question is whether the 2-face axis itself is reliably within 1° of the true axis to begin with.
> 
> On a coarse mesh with roughly 36° spacing between adjacent faces, the face normals sit near the midpoints of those arcs, giving pretty accurate measurements. The cross product should yield an axis very close to the true one.
> 
> I'm going to proceed with the axis stability approach. I should also examine the early termination logic in the code—specifically whether we need to compare against existing committed hypotheses during BFS, which only matters if we're re-seeding from already-assigned faces.
> 
> Since trials only grow from unassigned faces without re-seeding, we can't rediscover existing hypotheses anyway. So my plan is to add an axis stability check during refit operations: when a candidate face's vertices or centroid are marginal and trigger a refit, I'll verify the refitted axis hasn't rotated beyond a threshold from the original seed fit. If it has, reject the face. This prevents BFS from gradually drifting onto adjacent curved surfaces through accumulated small refits. I'll keep the current seeding behavior unchanged—only seeding from unassigned faces—since the stability check should prevent bad hypotheses from forming in the first place.
> 
> Now I need to find the right spot in the development plan to insert this.

Now let me read the specific BFS expansion section to see the refit logic in detail:Let me read the relevant sections of the development plan to understand exactly where to insert the fix:

<details>
<summary>Reading lines 235-260 of <a href="../DEVELOPMENT_PLAN.md#L235-L261">DEVELOPMENT_PLAN.md</a></summary>

```

**BFS expansion** (run once per trial seed, using temporary assignment tracking):
- Pop faces from the queue and examine each neighbor ni:
    - If ni is already assigned a cylindrical hypothesis (in the mesh) or claimed by this trial, skip.
    - **Vertex distance check**: for each vertex of ni, compute `| ||v - axis_closest|| - radius |`. If all are within `--vertex-tolerance`, proceed to remaining checks (no re-fit needed yet). If any exceeds `2 * vertex_tolerance`, skip immediately (re-fitting cannot help, same reasoning as for planar hypotheses). If between 1x and 2x, flag for re-fit.
    - **Centroid validation**: compute the distance from ni's centroid to the fitted cylinder. If beyond `2 * surface_tolerance`, skip. If beyond `surface_tolerance`, flag for re-fit (even if all vertices were within tolerance).
    - **Convexity check**: compute the vector from the axis to ni's centroid. Check that ni's face normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.
    - **Angular tolerance check**: for each of ni's mesh neighbors that is already assigned to this hypothesis, compute the dihedral angle between ni and that neighbor. If any exceeds `--angular-tolerance`, reject. Checking all assigned neighbors (not just the BFS parent) provides defense-in-depth against creased NURBS surfaces where BFS might approach a face from a low-angle direction while a high-angle assigned neighbor exists on a different axis.
    - **Re-fit attempt**: if vertex distance or centroid validation flagged a re-fit, re-fit the cylinder from all current faces plus ni (see Cylinder Fitting below). Check that **all** vertices (existing and new) are within tolerance of the re-fitted cylinder and **all** face centroids lie within `--surface-tolerance` of the fitted cylinder surface. If so, accept the re-fit; otherwise skip ni.
    - If accepted: add ni to the trial's face list and vertex set, push ni onto the BFS queue.
- After BFS completes: final re-fit from all accumulated faces and vertices. Compute error metrics.
- **Minimum face count**: Require at least 3 faces in a cylindrical hypothesis. This is technically redundant with angular coverage validation (which inherently requires 3+ angular clusters, hence 3+ faces), but serves as a cheap O(1) early rejection before the more expensive angular coverage computation.

**Angular coverage validation** (applied to the best trial for each starting face):

The problem: any two non-coplanar faces fit *some* cylinder. A bogus seed that doesn't span the cylinder's circumference can grow into a small hypothesis via BFS, consuming faces that should belong to a correct hypothesis from a better seed. The angular coverage check ensures the hypothesis has genuine circumferential support — faces distributed around the cylinder, not clustered on one side.

Algorithm:
1. For each face in the hypothesis, compute the centroid and project it onto the cylinder's angular coordinate θ. Given the cylinder's axis direction `a`, axis origin `o`, and orthogonal basis vectors `u`, `w` perpendicular to `a`: for centroid `c`, compute `radial = c - o - ((c - o) · a) * a`, then `θ = atan2(radial · w, radial · u)`.
2. Sort the θ values (one per face).
3. Compute all N gaps between consecutive sorted θ values, **including the wraparound gap**: `gap_wrap = θ[0] + 2π - θ[N-1]`.
4. The **largest gap** is the "empty arc" — the angular region not covered by any face. The **span** is `2π - largest_gap`.
5. Among the remaining N-1 gaps, the **second-largest gap** must be `≤ span / 3`.

This ensures the hypothesis has at least 3 distinct angular clusters of faces around the cylinder. A bogus cylinder where all faces are side-by-side in a narrow arc will fail: e.g., 5 faces clustered in a 30° arc have span ≈ 30°, but the second-largest internal gap (~7°) may pass. However, such a cluster's centroi
... (truncated, 4403 chars)
```
</details>

Now let me also read the `LEARNINGS.md` and check if there's a relevant existing section:

<details>
<summary>Reading all lines of <a href="../LEARNINGS.md">LEARNINGS.md</a></summary>

````
## opencascade-sys API Patterns

The Rust bindings mirror OCCT C++ closely but with automatic name transformations:

### Module and Type Naming
- `BRepBuilderAPI_MakeEdge` → `b_rep_builder_api::MakeEdge`
- `BRep_Tool` → `b_rep::Tool`
- `gp_Pnt` → `gp::Pnt`
- `TopExp_Explorer` → `top_exp::Explorer`
- `GeomAPI_ProjectPointOnSurf` → `geom_api::ProjectPointOnSurf`
- `STEPControl_Reader` → `step_control::Reader`
- `RWStl` → `rw_stl` (free functions)

### Constructors
Named `::new_<compressed_param_types>()`. For example:
```rust
gp::Pnt::new_real3(x, y, z)           // gp_Pnt(double, double, double)
gp::Dir::new_real3(dx, dy, dz)
top_exp::Explorer::new_shape_shapeenum2(shape, from_type, avoid_type)
geom_api::ProjectPointOnSurf::new_pnt_handlegeomsurface_extalgo(pt, surf, algo)
```

### Ownership
- All objects are returned as `OwnedPtr<T>` which auto-derefs via `Deref`/`DerefMut`
- `OwnedPtr<T>` is essentially `Box<T>` for OCCT heap objects

### Handles (OCCT `Handle<T>`)
- `Type::to_handle(owned_ptr)` consumes `OwnedPtr<T>` → `OwnedPtr<HandleT>`
- `.get()` dereferences a handle to access the underlying object
- Upcasting: `.to_handle_surface()`, `.to_handle_curve()`, etc.
- Downcasting: `.downcast_to_plane()` returns `Option<OwnedPtr<HandleDerived>>`

### Enums
Typed Rust enums, not raw integers:
```rust
top_abs::ShapeEnum::Face
extrema::ExtAlgo::Grad
```

### Shape Downcasting
`topo_ds::face_shape` is a safe function, but it panics if the shape is not a Face. Always guard with `shape_type()`:
```rust
if shape_ref.shape_type() == top_abs::ShapeEnum::Face {
    let face = topo_ds::face_shape(shape_ref);  // &topo_ds::Shape → &topo_ds::Face
    ...
}
```

### Static Methods → Free Functions
OCCT utility classes (e.g., `BRep_Tool`) become module-level free functions or associated functions on the type.

## Common API Recipes

### Read an STL file → vertex positions
```rust
use opencascade_sys::{rw_stl, message};
let progress = message::ProgressRange::new();
let tri_handle = rw_stl::read_file_charptr_progressrange_2(path, &progress);
let tri = tri_handle.get();  // &Poly_Triangulation
for i in 1..=tri.nb_nodes() {
    let pt = tri.node(i);  // OwnedPtr<gp::Pnt>
    let (x, y, z) = (pt.x(), pt.y(), pt.z());
}
```

### Read a STEP file → TopoDS_Shape
```rust
use opencascade_sys::{step_control, message};
let mut reader = step_control::Reader::new();
reader.read_file_charptr(path);
reader.transfer_roots(&message::ProgressRange::new());
let shape = reader.one_shape();  // OwnedPtr<topo_ds::Shape>
```

### Iterate faces of a shape
```rust
use opencascade_sys::{top_exp, top_abs, topo_ds};
let mut explorer = top_exp::Explorer::new_shape_shapeenum2(
    &shape, top_abs::ShapeEnum::Face, top_abs::ShapeEnum::Shape,
);
while explorer.more() {
    let face = topo_ds::face_shape(explorer.value());
    // ... use face
    explorer.next();
}
```

### Project a point onto a surface (compute distance)
```rust
use opencascade_sys::{b_rep, geom_api, extrema};
let surface = b_rep::Tool::surface_face(face);  // OwnedPtr<HandleGeomSurface>
let projector = geom_api::ProjectPointOnSurf::new_pnt_handlegeomsurface_extalgo(
    &point, &surface, extrema::ExtAlgo::Grad,
);
if projector.is_done() && projector.nb_points() > 0 {
    let dist = projector.lower_distance();
}
```

### Poly_Triangulation index convention
All node/triangle indices are **1-based** (OCCT convention).


### Bounded vs unbounded distance: BRepExtrema vs GeomAPI_ProjectPointOnSurf
`GeomAPI_ProjectPointOnSurf` projects onto the *infinite* underlying `Geom_Surface`, not the bounded face. Use `BRepExtrema_DistShapeShape` (via `b_rep_extrema::DistShapeShape`) instead to compute distance to bounded `TopoDS_Face` shapes. Create a `BRepBuilderAPI_MakeVertex` from the point, then compute `DistShapeShape` between the vertex shape and the target shape. This correctly reports nonzero distance for points that lie on an infinite surface extension but outside the bounded face.

### Stage 2.1 planar hypothes
... (truncated, 29253 chars)
````
</details>

<details>
<summary>Reading lines 1-100 of <a href="../LEARNINGS.md">LEARNINGS.md</a></summary>

````
1:uf|## opencascade-sys API Patterns
2:dd|
3:lz|The Rust bindings mirror OCCT C++ closely but with automatic name transformations:
4:dd|
5:hx|### Module and Type Naming
6:fs|- `BRepBuilderAPI_MakeEdge` → `b_rep_builder_api::MakeEdge`
7:pg|- `BRep_Tool` → `b_rep::Tool`
8:un|- `gp_Pnt` → `gp::Pnt`
9:dq|- `TopExp_Explorer` → `top_exp::Explorer`
10:tl|- `GeomAPI_ProjectPointOnSurf` → `geom_api::ProjectPointOnSurf`
11:fj|- `STEPControl_Reader` → `step_control::Reader`
12:qf|- `RWStl` → `rw_stl` (free functions)
13:dd|
14:jh|### Constructors
15:me|Named `::new_<compressed_param_types>()`. For example:
16:fj|```rust
17:gp|gp::Pnt::new_real3(x, y, z)           // gp_Pnt(double, double, double)
18:vv|gp::Dir::new_real3(dx, dy, dz)
19:nc|top_exp::Explorer::new_shape_shapeenum2(shape, from_type, avoid_type)
20:wj|geom_api::ProjectPointOnSurf::new_pnt_handlegeomsurface_extalgo(pt, surf, algo)
21:pi|```
22:dd|
23:ds|### Ownership
24:cw|- All objects are returned as `OwnedPtr<T>` which auto-derefs via `Deref`/`DerefMut`
25:dk|- `OwnedPtr<T>` is essentially `Box<T>` for OCCT heap objects
26:dd|
27:bo|### Handles (OCCT `Handle<T>`)
28:xj|- `Type::to_handle(owned_ptr)` consumes `OwnedPtr<T>` → `OwnedPtr<HandleT>`
29:rh|- `.get()` dereferences a handle to access the underlying object
30:sv|- Upcasting: `.to_handle_surface()`, `.to_handle_curve()`, etc.
31:mo|- Downcasting: `.downcast_to_plane()` returns `Option<OwnedPtr<HandleDerived>>`
32:dd|
33:uk|### Enums
34:bv|Typed Rust enums, not raw integers:
35:fj|```rust
36:wl|top_abs::ShapeEnum::Face
37:xr|extrema::ExtAlgo::Grad
38:pi|```
39:dd|
40:oc|### Shape Downcasting
41:pu|`topo_ds::face_shape` is a safe function, but it panics if the shape is not a Face. Always guard with `shape_type()`:
42:fj|```rust
43:qu|if shape_ref.shape_type() == top_abs::ShapeEnum::Face {
44:nm|    let face = topo_ds::face_shape(shape_ref);  // &topo_ds::Shape → &topo_ds::Face
45:bf|    ...
46:io|}
47:pi|```
48:dd|
49:ej|### Static Methods → Free Functions
50:qz|OCCT utility classes (e.g., `BRep_Tool`) become module-level free functions or associated functions on the type.
51:dd|
52:va|## Common API Recipes
53:dd|
54:tt|### Read an STL file → vertex positions
55:fj|```rust
56:xj|use opencascade_sys::{rw_stl, message};
57:nr|let progress = message::ProgressRange::new();
58:ci|let tri_handle = rw_stl::read_file_charptr_progressrange_2(path, &progress);
59:kc|let tri = tri_handle.get();  // &Poly_Triangulation
60:xd|for i in 1..=tri.nb_nodes() {
61:rn|    let pt = tri.node(i);  // OwnedPtr<gp::Pnt>
62:xp|    let (x, y, z) = (pt.x(), pt.y(), pt.z());
63:io|}
64:pi|```
65:dd|
66:zd|### Read a STEP file → TopoDS_Shape
67:fj|```rust
68:sg|use opencascade_sys::{step_control, message};
69:dl|let mut reader = step_control::Reader::new();
70:bz|reader.read_file_charptr(path);
71:zy|reader.transfer_roots(&message::ProgressRange::new());
72:fa|let shape = reader.one_shape();  // OwnedPtr<topo_ds::Shape>
73:pi|```
74:dd|
75:br|### Iterate faces of a shape
76:fj|```rust
77:ic|use opencascade_sys::{top_exp, top_abs, topo_ds};
78:zu|let mut explorer = top_exp::Explorer::new_shape_shapeenum2(
79:cu|    &shape, top_abs::ShapeEnum::Face, top_abs::ShapeEnum::Shape,
80:dk|);
81:qh|while explorer.more() {
82:mj|    let face = topo_ds::face_shape(explorer.value());
83:rn|    // ... use face
84:zk|    explorer.next();
85:io|}
86:pi|```
87:dd|
88:zd|### Project a point onto a surface (compute distance)
89:fj|```rust
90:ci|use opencascade_sys::{b_rep, geom_api, extrema};
91:iw|let surface = b_rep::Tool::surface_face(face);  // OwnedPtr<HandleGeomSurface>
92:qv|let projector = geom_api::ProjectPointOnSurf::new_pnt_handlegeomsurface_extalgo(
93:gt|    &point, &surface, extrema::ExtAlgo::Grad,
94:dk|);
95:wl|if projector.is_done() && projector.nb_points() > 0 {
96:wb|    let dist = projector.lower_distance();
97:io|}
98:pi|```
99:dd|
100:vv|### Poly_Triangulation index convention
````
</details>

<details>
<summary>Reading lines 100-250 of <a href="../LEARNINGS.md">LEARNINGS.md</a></summary>

```
100:vv|### Poly_Triangulation index convention
101:ij|All node/triangle indices are **1-based** (OCCT convention).
102:dd|
103:dd|
104:yy|### Bounded vs unbounded distance: BRepExtrema vs GeomAPI_ProjectPointOnSurf
105:gd|`GeomAPI_ProjectPointOnSurf` projects onto the *infinite* underlying `Geom_Surface`, not the bounded face. Use `BRepExtrema_DistShapeShape` (via `b_rep_extrema::DistShapeShape`) instead to compute distance to bounded `TopoDS_Face` shapes. Create a `BRepBuilderAPI_MakeVertex` from the point, then compute `DistShapeShape` between the vertex shape and the target shape. This correctly reports nonzero distance for points that lie on an infinite surface extension but outside the bounded face.
106:dd|
107:sh|### Stage 2.1 planar hypothesis algorithm
108:rf|- BFS region growing from seed faces using angular + vertex-distance tolerance.
109:ct|- Angular threshold of `1e-6` (1 - cos ≈ 0.08°) prevents grouping faces with slightly different normals.
110:aq|- Re-fitting via area-weighted normal averaging + vertex centroid for plane distance.
111:vt|- For CAD meshes, vertices lie precisely on surfaces, so fit errors are typically 0 or near-epsilon.
112:nd|- Coplanar but disconnected faces correctly get separate hypotheses (not a bug).
113:aw|- The `vertex_tolerance_mm` config parameter controls the maximum vertex-to-plane distance for grouping.
114:dd|
115:sw|### CodeCAD (ccad) test model generation
116:sg|- `ccad build` in `tests/ccad/` generates both STL and STEP files from Lua scripts.
117:vm|- `wedge(dx, dy, dz, ltx)` requires `ltx > 0` (cannot be 0).
118:gk|- Boolean operations like `union()` work with pre-translated shapes.
119:su|- Parts are registered in `project.json`; each entry needs a unique `id`.
120:eb|- Available primitives: `box()`, `cylinder(diameter, height)`, `sphere(diameter)`, `cone(d1, d2, height)`, `wedge()`, `hex_prism()`.
121:kl|- Boolean operations: `union()`, `difference()`, `intersection()`.
122:mp|- Transformations: `translate()`, `rotate_x/y/z()`, `scale()`, `center_xyz()` etc.
123:lg|- Construction: `extrude(face, height)`, `revolve(profile, angle)`.
124:ph|- Edge ops: `fillet_all(s, r)`, `chamfer_all(s, d)`.
125:dd|
126:cp|### CAD tessellation quad-strip patterns
127:jm|- Tessellated cylinders and spheres from CAD tools (CodeCAD/OCCT, Onshape) use quad-strip patterns where each quad facet splits into 2 coplanar triangles.
128:yk|- Solution: stage 1.3 merges coplanar triangle pairs back into quads, so curved-surface facets become single faces with single-face planar hypotheses.
129:if|- This restores the clean "single-face = curved surface candidate" criterion for stages 2.2 and 2.3.
130:yw|- Sphere poles are an exception: triangles in the polar fan pattern don't have coplanar neighbors and correctly remain as triangles.
131:dd|
132:tf|### Stage 2.2 cylindrical hypothesis algorithm
133:hc|- Two-step fitting: (1) axis direction from smallest eigenvector of area-weighted normal covariance matrix M = Σ wᵢ nᵢ nᵢᵀ, (2) axis position and radius via 2D algebraic circle fit after projecting vertices perpendicular to axis.
134:nm|- **Critical: center the 2D projected coordinates before circle fitting.** Without centering, the normal equations matrix becomes catastrophically ill-conditioned when absolute vertex coordinates are large relative to the arc span (e.g., vertices at (15, 12.5) spanning 2.5° of a r=2mm cylinder). The sums-of-squares terms (sx2 ≈ 1350) dwarf the signal (radius ≈ 2), causing Cramer's rule to produce wildly wrong radii (e.g., 139mm instead of 2mm). Fix: subtract mean_x/mean_y before fitting, add back when converting 2D center to 3D. This is the standard numerical remedy for polynomial least-squares fitting.
135:dr|- 3×3 symmetric eigenvalue computation uses depressed cubic (trigonometric solution with cos(θ/3)), plus null-space eigenvector via cross products of (M - λI) rows.
136:hu|- Multi-seed evaluation: for each starting face, collects ALL valid seed partners (not just the fi
... (truncated, 25563 chars)
```
</details>

> 💭 Now let me read the rest of the LEARNINGS.md to see if there's anything about this issue.

Now let me also look at the current test output for rounded_cube_10_r2_coarse to see if there's evidence of the problem:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release -- tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl --compare tests/ccad/generated/part_rounded_cube_10_r2.step --verbose 2>&1 | grep -E "cylinder|Cylinder|sphere|Sphere|ERROR|error_max|stage 2" | head -50
```
```
(base) whitelily:brepper sethml$
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release -- tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl --compare tests/ccad/generated/part_rounded_cube_10_r2.step --verbose 2>&1 | tail -100
```
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: brepper, step-measure-tolerance, viewer
real  0.0s
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl --compare tests/ccad/generated/part_rounded_cube_10_r2.step --verbose 2>&1 | tail -120
```
<details>
<summary>Output (11 lines)</summary>

```
   Compiling brepper v0.1.0 (/Users/sethml/src/brepper)
    Finished `release` profile [optimized] target(s) in 4.53s
     Running `target/release/brepper tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl --compare tests/ccad/generated/part_rounded_cube_10_r2.step --verbose`
error: unexpected argument '--verbose' found

  tip: a similar argument exists: '--verbosity'

Usage: brepper --compare <COMPARE_STEP> --verbosity... <INPUT_STL>

For more information, try '--help'.
real  4.9s
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl --compare tests/ccad/generated/part_rounded_cube_10_r2.step -vvv 2>&1 | tail -150
```
```
    Finished `release` profile [optimized] target(s) in 0.16s
     Running `target/release/brepper tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl --compare tests/ccad/generated/part_rounded_cube_10_r2.step -vvv`
real  0.6s
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl --compare tests/ccad/generated/part_rounded_cube_10_r2.step -vvv 2>&1
```
```
    Finished `release` profile [optimized] target(s) in 0.14s
     Running `target/release/brepper tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl --compare tests/ccad/generated/part_rounded_cube_10_r2.step -vvv`
Segmentation fault: 11
real  0.6s
```
**Exit code:** 139

> 💭 There's a segfault. Let me try without --compare.

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl -vvv 2>&1
```
<details>
<summary>Output (5 lines) — exit code 139</summary>

```
    Finished `release` profile [optimized] target(s) in 0.14s
     Running `target/release/brepper tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl -vvv`
Error: file 'tests/ccad/generated/part_rounded_cube_10_r2_coarse.stl' is not found
Segmentation fault: 11
real  0.6s
```
</details>

Searched for files matching `**/part_rounded_cube*coarse*`, 1 match
- [part_rounded_cube_10_r2_coarse.stl](../tests/onshape/part_rounded_cube_10_r2_coarse.stl)

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/onshape/part_rounded_cube_10_r2_coarse.stl -vvv 2>&1
```
<details>
<summary>Output (228 lines) — exit code 1</summary>

```
    Finished `release` profile [optimized] target(s) in 0.15s
     Running `target/release/brepper tests/onshape/part_rounded_cube_10_r2_coarse.stl -vvv`
Stage 1.1: Read 72 vertices, 140 faces from STL
Stage 1.2: Mesh validation passed
  140 faces, 72 vertices, 1 shells, 1 solids
Stage 1.3: Fused 68 triangle pairs into quads (72 faces remaining)
Stage 2.1: Deduced 38 planar hypotheses (2 multi-face, 36 single-face) covering 72 of 72 mesh faces
  Plane 36: 18 faces, 36 vertices, normal=[0.0000, -1.0000, 0.0000], d=10.0000, err_max=0.00e0, err_min=0.00e0
  Plane 37: 18 faces, 36 vertices, normal=[0.0000, 1.0000, 0.0000], d=0.0000, err_max=0.00e0, err_min=0.00e0
Stage 2.2: Deduced 4 cylindrical hypotheses (4 convex, 0 concave) covering 32 faces
  Cylinder 0: 8 faces, 18 vertices, r=2.0000, convex, axis_origin=[35.1219, 0.0000, 18.5447], axis_dir=[0.0000, -1.0000, 0.0000], vtx_err_max=1.43e-6, cen_err_max=9.63e-3
    face 0: 4v centroid=[34.9268,-5.0000,20.5255] normal=[-0.098,0.000,0.995] vtx_err=[8.92e-7,1.03e-6] cen_err=9.63e-3
    face 1: 4v centroid=[34.5441,-5.0000,20.4494] normal=[-0.290,0.000,0.957] vtx_err=[4.79e-7,1.03e-6] cen_err=9.63e-3
    face 2: 4v centroid=[34.1836,-5.0000,20.3001] normal=[-0.471,0.000,0.882] vtx_err=[4.79e-7,7.96e-7] cen_err=9.63e-3
    face 3: 4v centroid=[33.8592,-5.0000,20.0833] normal=[-0.634,0.000,0.773] vtx_err=[7.96e-7,1.01e-6] cen_err=9.63e-3
    face 4: 4v centroid=[33.5833,-5.0000,19.8074] normal=[-0.773,0.000,0.634] vtx_err=[1.01e-6,1.43e-6] cen_err=9.63e-3
    face 5: 4v centroid=[33.3665,-5.0000,19.4830] normal=[-0.882,0.000,0.471] vtx_err=[6.80e-7,1.43e-6] cen_err=9.63e-3
    face 6: 4v centroid=[33.2172,-5.0000,19.1225] normal=[-0.957,0.000,0.290] vtx_err=[5.07e-9,6.80e-7] cen_err=9.63e-3
    face 7: 4v centroid=[33.1411,-5.0000,18.7398] normal=[-0.995,0.000,0.098] vtx_err=[5.07e-9,6.44e-7] cen_err=9.63e-3
  Cylinder 1: 8 faces, 18 vertices, r=2.0000, convex, axis_origin=[35.1219, 0.0000, 12.5447], axis_dir=[0.0000, -1.0000, 0.0000], vtx_err_max=1.88e-6, cen_err_max=9.63e-3
    face 8: 4v centroid=[34.9268,-5.0000,10.5639] normal=[-0.098,0.000,-0.995] vtx_err=[1.44e-7,3.52e-7] cen_err=9.63e-3
    face 9: 4v centroid=[34.5441,-5.0000,10.6400] normal=[-0.290,0.000,-0.957] vtx_err=[3.52e-7,1.88e-6] cen_err=9.63e-3
    face 10: 4v centroid=[34.1836,-5.0000,10.7894] normal=[-0.471,0.000,-0.882] vtx_err=[1.20e-6,1.88e-6] cen_err=9.63e-3
    face 11: 4v centroid=[33.8592,-5.0000,11.0061] normal=[-0.634,0.000,-0.773] vtx_err=[1.20e-6,1.39e-6] cen_err=9.63e-3
    face 12: 4v centroid=[33.5833,-5.0000,11.2820] normal=[-0.773,0.000,-0.634] vtx_err=[1.15e-6,1.39e-6] cen_err=9.63e-3
    face 13: 4v centroid=[33.3665,-5.0000,11.6065] normal=[-0.882,0.000,-0.471] vtx_err=[2.03e-7,1.15e-6] cen_err=9.63e-3
    face 14: 4v centroid=[33.2172,-5.0000,11.9669] normal=[-0.957,0.000,-0.290] vtx_err=[1.17e-7,2.03e-7] cen_err=9.63e-3
    face 15: 4v centroid=[33.1411,-5.
... (truncated, 18859 chars total)
```
</details>

<details>
<summary>Tool output (run_in_terminal), lines 1-200</summary>

```
Note: The tool simplified the command to ` time cargo run --release --bin brepper -- tests/onshape/part_rounded_cube_10_r2_coarse.stl -vvv 2>&1`, and this is the output of running that command instead:
    Finished `release` profile [optimized] target(s) in 0.15s
     Running `target/release/brepper tests/onshape/part_rounded_cube_10_r2_coars
e.stl -vvv`                                                                     Stage 1.1: Read 72 vertices, 140 faces from STL
Stage 1.2: Mesh validation passed
  140 faces, 72 vertices, 1 shells, 1 solids
Stage 1.3: Fused 68 triangle pairs into quads (72 faces remaining)
Stage 2.1: Deduced 38 planar hypotheses (2 multi-face, 36 single-face) covering 
72 of 72 mesh faces                                                               Plane 36: 18 faces, 36 vertices, normal=[0.0000, -1.0000, 0.0000], d=10.0000, 
err_max=0.00e0, err_min=0.00e0                                                    Plane 37: 18 faces, 36 vertices, normal=[0.0000, 1.0000, 0.0000], d=0.0000, er
r_max=0.00e0, err_min=0.00e0                                                    Stage 2.2: Deduced 4 cylindrical hypotheses (4 convex, 0 concave) covering 32 fa
ces                                                                               Cylinder 0: 8 faces, 18 vertices, r=2.0000, convex, axis_origin=[35.1219, 0.00
00, 18.5447], axis_dir=[0.0000, -1.0000, 0.0000], vtx_err_max=1.43e-6, cen_err_max=9.63e-3                                                                          face 0: 4v centroid=[34.9268,-5.0000,20.5255] normal=[-0.098,0.000,0.995] vt
x_err=[8.92e-7,1.03e-6] cen_err=9.63e-3                                             face 1: 4v centroid=[34.5441,-5.0000,20.4494] normal=[-0.290,0.000,0.957] vt
x_err=[4.79e-7,1.03e-6] cen_err=9.63e-3                                             face 2: 4v centroid=[34.1836,-5.0000,20.3001] normal=[-0.471,0.000,0.882] vt
x_err=[4.79e-7,7.96e-7] cen_err=9.63e-3                                             face 3: 4v centroid=[33.8592,-5.0000,20.0833] normal=[-0.634,0.000,0.773] vt
x_err=[7.96e-7,1.01e-6] cen_err=9.63e-3                                             face 4: 4v centroid=[33.5833,-5.0000,19.8074] normal=[-0.773,0.000,0.634] vt
x_err=[1.01e-6,1.43e-6] cen_err=9.63e-3                                             face 5: 4v centroid=[33.3665,-5.0000,19.4830] normal=[-0.882,0.000,0.471] vt
x_err=[6.80e-7,1.43e-6] cen_err=9.63e-3                                             face 6: 4v centroid=[33.2172,-5.0000,19.1225] normal=[-0.957,0.000,0.290] vt
x_err=[5.07e-9,6.80e-7] cen_err=9.63e-3                                             face 7: 4v centroid=[33.1411,-5.0000,18.7398] normal=[-0.995,0.000,0.098] vt
x_err=[5.07e-9,6.44e-7] cen_err=9.63e-3                                           Cylinder 1: 8 faces, 18 vertices, r=2.0000, convex, axis_origin=[35.1219, 0.00
00, 12.5447], axis_dir=[0.0000, -1.0000, 0.0000], vtx_err_max=1.88e-6, cen_err_max=9.63e-3                                                                          face 8: 4v centroid=[34.9268,-5.0000,10.5639] normal=[-0.098,0.000,-0.995] v
tx_err=[1.44e-7,3.52e-7] cen_err=9.63e-3                                            face 9: 4v centroid=[34.5441,-5.0000,10.6400] normal=[-0.290,0.000,-0.957] v
tx_err=[3.52e-7,1.88e-6] cen_err=9.63e-3                                            face 10: 4v centroid=[34.1836,-5.0000,10.7894] normal=[-0.471,0.000,-0.882] 
vtx_err=[1.20e-6,1.88e-6] cen_err=9.63e-3                                           face 11: 4v centroid=[33.8592,-5.0000,11.0061] normal=[-0.634,0.000,-0.773] 
vtx_err=[1.20e-6,1.39e-6] cen_err=9.63e-3                                           face 12: 4v centroid=[33.5833,-5.0000,11.2820] normal=[-0.773,0.000,-0.634] 
vtx_err=[1.15e-6,1.39e-6] cen_err=9.63e-3                                           face 13: 4v centroid=[33.3665,-5.0000,11.6065] normal=[-0.882,0.000,-0.471] 
vtx_err=[2.03e-7,1.15e-6] cen_err=9.63e-3                                        
... (truncated, 21702 chars)
```
</details>

> 💭 Interesting - the output shows 4 cylindrical hypotheses and 0 spherical hypotheses, but the rounded cube should have spherical corners (8 sphere patches). The cylinders each have 8 faces, and it seems like only 4 cylinders were detected out of what should be 4 cylinders + 8 sphere patches (or maybe 4 cylinders since the rounded cube only has 4 edges being rounded in this model).
> 
> Wait, looking at the model: "rounded_cube_10_r2_coarse" — this is a rounded cube with all edges filleted. Let me look more carefully...
> 
> 
> Actually, the detection found 6 planar faces, 4 cylindrical surfaces, and 0 spheres across 72 total faces. A fully rounded cube should have 6 planar faces, 12 edge cylinders, and 8 corner spheres, but this coarse version might only have partial filleting. Let me verify the face count to understand what geometry is actually present. So the two multi-face planar hypotheses account for 36 faces, the four cylinders cover 32 faces, totaling 68 out of 72 faces, leaving 4 faces that must be handled by single-face planar fallbacks. Since there are no spherical hypotheses detected, the model apparently isn't capturing any curved surfaces—likely because the geometry is too coarse to have distinct sphere patches, or they're just edge fillets that don't form substantial curved regions.
> 
> Each cylinder has exactly 8 faces, which makes sense for a 90° fillet. Given the model name "rounded_cube_10_r2" suggests a cube with 2mm radius fillets on all edges and corners, I'd expect 12 cylinders and 8 spheres, but we're only seeing 4 cylinders. This points to either a partial rounding scheme or the coarse mesh only capturing the most prominent fillets. Looking at the cylinder axes, they all share the same direction, which suggests they're aligned to specific edges rather than distributed around the cube. Looking at the vertex positions and plane normals more carefully, this cube appears to only have fillets on the four edges parallel to the Y-axis in the XZ plane, which explains why exactly 4 cylinders are detected. With 68 faces accounted for in the planar and cylindrical hypotheses, those remaining 4 faces are likely single-face fallbacks that couldn't form complete geometric shapes.
> 
> The user might have been referring to this specific model as an example, or they observed the issue in a different version. Rather than trying to reproduce the exact problem, I should focus on analyzing whether the hypothesis detection is working correctly and then update the development plan accordingly. I'm realizing there are actually two separate approaches worth considering: one that prevents the drift from happening in the first place by keeping the cylinder axis stable, and another that recovers from drift by allowing faces to be re-processed with better hypotheses. The user's proposal to seed from already-assigned faces and terminate early against existing hypotheses is interesting, but I need to think through whether it actually solves the core problem or just changes how the BFS explores the surface.
> 
> If we allow seeding from assigned faces, the BFS would grow outward to unassigned neighbors while potentially overwriting existing assignments—essentially letting a new hypothesis compete with and replace old ones. But I'm not entirely sure that's what the user meant; they might instead be suggesting we simply remove the skip condition entirely so the algorithm explores more aggressively regardless of prior assignments.
> 
> Actually, rereading more carefully, I think the intent is: run the full BFS through all faces (including already-assigned ones), and during expansion, check if we're rediscovering an existing hypothesis for any face we encounter. If we are, we abandon the trial since it's not finding anything new. Otherwise, if the trial produces a better hypothesis with more faces, we commit it and replace the old one.
> 
> This is essentially iterative refinement across multiple passes, which introduces complexity around reassignment and potential cascading invalidation. But let me focus on what the user actually proposed rather than overcomplicating it—maybe they're suggesting something simpler: just remove the outer loop check that skips assigned faces, keep BFS as-is, and add early termination when we detect we're rediscovering an existing hypothesis.
> 
> The issue is that if we seed from an already-assigned face and find a valid cylinder, we'd need to reassign that face to the new hypothesis, which could invalidate the old one. Without explicit reassignment logic, this creates a messy state where a face belongs to multiple hypotheses.
> 
> I think the cleaner approach is to add an axis stability check during BFS refitting as the primary fix—this prevents bad hypotheses from forming in the first place. Then, if needed later, we can implement the re-seeding recovery as a secondary enhancement. Let me update the plan by inserting the axis stability check into the BFS expansion section, right after the refit description. I'll add it as an additional validation step that ensures the refitted axis hasn't rotated too far from the original seed fit's direction. I need to step back and think through this more carefully — the user is asking me to evaluate their hypothesis and proposed fix, then decide on the best approach for the development plan. Their axis stability check is simpler and more preventative than the re-seeding recovery mechanism, so I'll go with that as the primary fix while noting that re-seeding could be a complementary fallback if needed.
> 
> But wait, there's an even simpler option I'm considering: what if I just disable refitting during the BFS traversal entirely and only refit once at the end? That would prevent parameter drift altogether by keeping the seed cylinder fixed throughout the search, then adjusting once we have the full face set. The tradeoff is that we might miss some faces that fall slightly outside the seed parameters but would fit the true cylinder, though the seed should be accurate enough that this isn't a major issue.
> 
> Actually, I think the cleanest approach is to allow refitting but constrain the axis direction — let the radius and origin shift slightly during BFS since those refinements are stable, but reject any refit that drifts the axis direction too far from the seed. This gives us the precision benefits of refitting without the problematic axis drift that causes the instability.
> 
> Now I need to update the plan text around line 243 to incorporate this axis stability check as part of the refit acceptance criteria. I'm leaning toward option A—modifying the re-fit line to include the axis stability constraint and then adding a dedicated section explaining it. The key insight is that axis drift only matters during re-fit, since faces that pass without re-fitting don't change the axis at all. But I should also think through what happens during the final re-fit at the end of BFS, since that could introduce drift too if sphere faces were included.
> 
> Actually, there's a subtle issue here: sphere faces that pass the vertex tolerance check without needing a re-fit won't trigger the axis stability check, but when they're included in the final re-fit, they could still shift the axis. The constraint catches drift during BFS re-fitting, but it doesn't protect against axis shifts from sphere faces that were accepted without re-fitting in the first place.
> 
> I could apply the axis stability check to the final re-fit as well. If the axis drifts too far, I could either flag the hypothesis as suspect for later re-seeding, or revert to the pre-re-fit parameters since the BFS-accumulated faces were already validated against those. The simplest approach is probably to check axis stability on the final re-fit and revert if it fails. axis stability for the final refit too. If the final refit axis has drifted from the seed axis, keep the pre-refit parameters. This ensures the committed hypothesis has parameters close to the seed fit, which was for a genuine cylinder.
> 
> But what does this mean for hypothesis quality? If the final refit would have improved the radius estimate (converged to the true value), but we're rejecting it because of axis drift, we'd have a less precise hypothesis. But the hypothesis was already validated with the BFS parameters, so it should still be valid.
> 
> OK, I think the cleanest approach is:
> 
> 1. During BFS refit: check axis stability. Reject candidate face if refit rotates axis too much.
> 2. After BFS (final refit): if axis stability check fails, keep the pre-final-refit parameters instead. Log a warning.
> 
> This prevents both mid-BFS drift and final-refit drift.
> 
> Now I'm planning the documentation updates—I need to revise the refit section to cover axis stability, add a dedicated explanation of how this mechanism works, and tie it back to the sphere-junction problem. For the threshold, I'm thinking `MAX_AXIS_DRIFT = 2°` makes sense given that a 10-face cylinder has 36° spacing between faces, so the seed axis from 2 faces should be within ~1° of true. But I want to tie this more directly to `angular_tol` instead—if neighboring cylinder faces can differ by up to 17.5° in their normals, I should calibrate the drift tolerance relative to that. The axis from a 2-face seed with 17.5° separation should be fairly accurate. A sphere face near the junction would have a normal tilted relative to the cylinder normal plane. The tilt angle depends on the mesh coarseness.
> 
> For a sphere of radius r and a cylinder of radius r meeting at a tangent circle, moving one face away from the junction (one face = ~20° arc on the coarse mesh), the sphere face's normal tilts away from the cylinder's normal plane by the angular extent of the face on the sphere in the direction perpendicular to the cylinder axis. This is approximately the arc angle, so ~20°. But this would cause the refitted axis to rotate by a smaller amount (the rotation depends on how many existing faces there are vs. the one new face).
> 
> With 2 existing faces and 1 new sphere face, the refit axis might rotate ~6-7°, and with 5 existing faces and 1 new, ~3-4°. So 2° might be too tight. The covariance matrix approach shows that adding one face to N faces changes the covariance by roughly 1/(N+1) of the difference, and the axis direction depends on the eigenvectors of this matrix. A single sphere face introduces a component along the axis that perturbs the eigenvector proportionally to the gap between eigenvalues. Rather than diving deeper into the theory, I should just pick a reasonable threshold and document the reasoning.
> 
> I'm thinking of checking whether the refitted direction stays close to the seed direction using a dot product test with a configurable maximum drift angle, defaulting to something like 3° or perhaps a fraction of the angular tolerance. Relating it to angular_tol makes sense—if adjacent faces can differ by at most angular_tol, then the axis drift per refit should be much smaller. Using angular_tol / 4 as the maximum cumulative drift seems reasonable, which would be around 4.4° for the default 17.5° tolerance.
> 
> Now I'm wondering whether this drift should be cumulative from the seed or per-step from the previous refit. If it's cumulative from the seed, the seed axis is established with just 2 faces, so it's already somewhat uncertain.
> 
> Looking at the numbers: the seed might have ~1° imprecision, each refit corrects slightly toward the true value, and after 10 refits the axis might have drifted ~2° from seed toward the true axis. Setting the threshold at 4.4° would accommodate this, but it's tight—a sphere face pushing the axis 3° in the wrong direction would combine with legitimate convergence to either exceed or barely stay within the threshold depending on how much legitimate movement has occurred. This suggests 4.4° might be too lenient, but being too strict would reject valid refits. I think I'm overcomplicating this.
> 
> Actually, let me approach it differently: legitimate convergence from adding more cylinder faces should be tiny—maybe 0.5° total—while a sphere face would rotate the axis 2-5° depending on how many faces are already in the hypothesis. That points to a threshold around 1.5-2° to cleanly separate the two cases. The concern is the seed axis from a coarse mesh, where on a 10-face cylinder the adjacent face normals are 36° apart, which could introduce larger initial errors.
> 
> But wait—the face normals on a tessellated cylinder are exact to numerical precision since they're the normals of flat polygonal faces, which for a flat polygon is just the polygon's normal itself. That means the seed axis should be exact too, so any axis rotation from adding more cylinder faces would indicate contamination rather than normal convergence. But there's a practical constraint: the tessellated cylinder's vertices only approximate the true surface within vertex_tolerance (~1e-5 mm), which introduces small errors in the computed face normals. For typical face sizes around 10mm, this translates to angular errors of roughly 1e-6 radians or about 6e-5 degrees—essentially negligible. So a 0.1° threshold for axis stability should safely distinguish legitimate cylinder faces from contamination.
> 
> However, I need to reconsider how the fitting actually works. The `fit_cylinder` function doesn't just use the seed pair's cross product; it constructs a normal covariance matrix from all faces in the trial, weighting them by area.
> 
> When refitting with a new face, the covariance matrix updates and the smallest eigenvector could shift. For pure cylinder faces where all normals are perpendicular to the axis, adding more such normals shouldn't rotate the axis—the eigenvalue stays near zero while the eigenvector remains stable. But a sphere face introduces a normal component along the axis, creating a non-zero contribution that pulls the eigenvector in a detectable way, likely rotating the axis by 1-5°. A 0.5° threshold would be conservative enough to catch any sphere contamination while staying well clear of numerical noise.
> 
> I'm settling on 2° as a reasonable starting point—strict enough to catch real problems but loose enough to avoid false positives from floating-point variations. Now I need to update the plan and start implementing.
> 
> Actually, I should also consider what happens on coarse meshes where vertices don't sit exactly on the cylinder surface. The chord height error for a small cylinder depends on the angular span of each face, and I need to check whether this geometric approximation error could interfere with the eigenvector stability test.
> 
> For STL exports from CAD, vertices lie on the original surface within export tolerance, so the face normals reflect the flat polygon orientation rather than the true surface normal. But since all vertices are on the cylinder, the polygon normal should still be perpendicular to the cylinder axis—the normal is perpendicular to the chord, which means it's perpendicular to the axis.
> 
> For tessellators that don't guarantee vertices on the surface, there might be small errors, but they should be negligible. I'm satisfied with the axis stability approach and ready to write up the plan.

Now I have a good picture. Let me read the current implementation phases to see what items are near this section:

<details>
<summary>Reading lines 261-295 of <a href="../DEVELOPMENT_PLAN.md#L261-L296">DEVELOPMENT_PLAN.md</a></summary>

```
*Note on applying angular coverage during BFS growth: this was considered but is not useful. Early in BFS, faces are naturally clustered near the seed, so the check would fail prematurely. The vertex distance, convexity, and angular tolerance checks are the correct per-face acceptance criteria during growth. Angular coverage is a global structural validation — it answers "does this completed hypothesis look like a real cylinder?" rather than "should this next face be added?"*

*Note: For partial cylinders (arcs less than 360°), this check still works — it requires 3+ angular clusters within whatever arc is covered, which is the minimum for a reliable cylinder fit. A 90° cylindrical arc with 6 evenly-spaced faces has internal gaps of ~18° and span of ~90°, so second_largest_gap (18°) ≤ span/3 (30°) — passes correctly.*

**Computational cost**: For each starting face with K valid seed partners, up to K trial BFS runs are performed, but early termination (see above) typically reduces this to 1–2 full runs plus several near-instant abandonments. Typical K is 2–4 (most faces have 3–4 neighbors, of which 1–2 are valid seeds). Without early termination, total work would be proportional to `sum over faces of (K_i × region_size_i)`. With it, most trials on faces belonging to already-discovered cylinders terminate after 3 face acceptances, reducing effective cost to roughly `sum over faces of (K_i × min(3, region_size_i))`. The temporary data structures (HashSet for claimed faces, Vec for face list) are lightweight.

**Cylinder fitting** (used for seeding and re-fitting):

The fitting proceeds in two steps:

1. **Axis direction estimation from face normals.** On a cylinder, every face normal is perpendicular to the axis: `n · a = 0`. So the axis direction `a` minimizes $\sum w_i (n_i \cdot a)^2$ subject to $\|a\| = 1$, where $w_i$ is the area of face $i$. This is the eigenvector corresponding to the *smallest* eigenvalue of the 3×3 weighted covariance matrix $M = \sum w_i \, n_i \, n_i^T$. For the two-face seed, this simplifies to `a = normalize(n_fi × n_ni)`.

2. **Axis position and radius via 2D circle fitting.** Given the axis direction `a`, choose two orthogonal unit vectors `u`, `w` perpendicular to `a`. Project each vertex onto the 2D plane: `(x_i, y_i) = (v_i · u, v_i · w)`. **Center the 2D coordinates** by subtracting the mean: `x_i' = x_i - mean_x`, `y_i' = y_i - mean_y`. Fit a circle to the centered 2D points using the algebraic least-squares method: fit the linear model $x^2 + y^2 + Dx + Ey + F = 0$ via least squares, then `center' = (-D/2, -E/2)` and `radius = sqrt(center_x'² + center_y'² - F)`. The axis origin in 3D is `(center_x' + mean_x) * u + (center_y' + mean_y) * w` (the component along `a` is arbitrary). Centering is essential for numerical stability: without it, the normal equations matrix becomes ill-conditioned when absolute vertex coordinates are large relative to the arc span (e.g., vertices at (15mm, 12.5mm) spanning 2.5° of a r=2mm cylinder produce sums-of-squares ~1350 that dwarf the r=2 signal, causing wildly wrong radii).

The normal covariance matrix `M` can be maintained incrementally during BFS (add $w_i \, n_i \, n_i^T$ when accepting a face), but the eigenvector solve and circle fit must be recomputed from scratch when re-fitting, since a change in axis direction invalidates the 2D projection. This is O(n) in vertices but involves only simple arithmetic — no iterative optimization.

**Subtleties and edge cases:**
- **Near-parallel seed normals**: If the cross product of the two seed normals is small, the axis estimate is unreliable. Require `||n_fi × n_ni|| > MIN_CROSS_THRESHOLD` (e.g. 0.01, corresponding to ~0.6° between normals). Skip seed pairs that fail this.
- **Cones vs cylinders**: A cone has varying radius along the axis. If we fit a cylinder to conical faces, vertex distances near the ends will exceed tolerance, so cone faces will be naturally rejected. Cone fitting is deferred.
- **Large-radi
... (truncated, 6258 chars)
```
</details>

> 💭 Good. Now I have enough context. Let me think carefully one more time and then write the plan update.
> 
> Looking at the rounded_cube_10_r2_coarse.stl output:
> - 4 cylindrical hypotheses (good — matching the 4 vertical edge fillets)
> - 0 spherical hypotheses (bad — should have corner spheres)
> - The 4 faces remaining after planes and cylinders are just assigned as single-face planar fallbacks
> 
> This IS evidence of a problem, though the problem might be different from what the user hypothesized. With 0 spherical hypotheses, either:
> 
> 
> 1. There aren't enough sphere faces on this coarse mesh for angular coverage
> 2. The sphere faces were consumed by some other hypothesis
> 
> But wait, this model is `part_rounded_cube_10_r2_coarse` — it's from Onshape. Let me check if the model actually has spherical corners. Looking at the output: only 4 cylinders, all with Y-axis direction. This is an extruded rounded rectangle (2D rounded rectangle extruded along Y), not a fully 3D rounded cube. So there are no spherical corners. The 4 remaining faces are just the flat end faces adjacent to the fillets.
> 
> The model has 72 faces total: 36 planar faces on the top and bottom, 32 faces across the 4 cylinder fillets, and 4 flat side faces between them. This is just a 2D rounded rectangle extruded along Y, so there aren't actually any sphere-cylinder junctions here. The user might be thinking of a different model or anticipating a future scenario.
> 
> I should check if there's a non-coarse version of the rounded cube model in the test files that might have the issue they're describing. Looking at the user's phrasing—"may be getting" and "I have an idea of why"—it sounds like they're proposing a hypothesis rather than reporting an observed problem.
> 
> Now I'm thinking through how to present my analysis: the coarse model doesn't actually exhibit sphere-cylinder junctions, but the mechanism they've identified is theoretically valid for models that do have those features. I'll focus my response on evaluating whether their hypothesis makes sense and whether the proposed fix is sound, then outline the specific changes needed to document this in the development plan—modifying the re-fit attempt description to include the axis stability check and adding a new section explaining the reasoning.
> 
> I'm considering whether to add this as a dedicated section after the angular coverage validation part, since it's a significant enough concern to warrant its own explanation of the problem and solution.
> 
> The core issue is that when a cylinder meets a sphere—like at a fillet—the BFS can drift onto the sphere because equatorial faces are geometrically close enough to pass tolerance checks, especially after refitting. Each refit subtly shifts the cylinder axis as it tries to accommodate sphere normals, gradually poisoning the hypothesis until it either fails to fit the original faces or consumes faces from both surfaces. The fix is to check that the refitted axis hasn't rotated significantly from the initial seed direction by comparing their dot product against a threshold based on the angular tolerance, since a true cylinder should produce consistent axis directions across refits while drift onto curved surfaces would cause the covariance-based estimation to compromise between different surface normals.
> 
> I also need to apply this axis stability check to both intermediate refits during the BFS and the final refit, keeping the pre-refit parameters if the final refit fails the check. The seed axis from two cylinder faces is already exact since both normals are perpendicular to the true axis, and adding more cylinder faces preserves this property—only when a sphere face gets included does the covariance matrix eigenvector get perturbed by a component along the axis.

Now I have a thorough understanding of both the code and the development plan. Let me analyze the hypothesis and formulate the best fix.

**Analysis of the hypothesis:**

Your scenario is geometrically sound: on a rounded cube where cylinder fillets meet spherical corners, sphere faces near the equator are geometrically close to the cylinder surface. During BFS, these faces could pass vertex/centroid tolerance checks (especially with refitting), gradually shifting the cylinder axis direction. On a true cylinder, all face normals are perpendicular to the axis, so the covariance-based axis estimation gives the true axis to numerical precision. But sphere faces have normals that tilt relative to the cylinder's axis — adding them shifts the axis direction. Each individual refit passes all checks, but the cumulative drift contaminates the hypothesis.

**Evaluation of your proposed fix (re-seeding + early termination):**

Re-seeding is a *recovery* mechanism — it corrects bad hypotheses after they form. It's valid but adds significant complexity: BFS expansion would also need to explore already-assigned faces (otherwise the trial can barely grow), reassignment logic would need to handle cascading invalidation of hypotheses, and there are performance concerns from reprocessing all faces.

**A simpler alternative — axis stability during refit:**

A *prevention* mechanism that directly blocks the drift. When accepting a refit during BFS, check that the refitted axis direction hasn't rotated significantly from the initial seed fit. On a true cylinder, adding more cylinder faces preserves the axis exactly (all normals perpendicular to the same axis). Drift onto a sphere perturbs the axis detectably. This is one extra check in the refit path, no algorithm restructuring needed.

I'll put the axis stability check in the plan as the primary fix, since it's simpler and more targeted. Let me update the development plan now.

**Todo list:**
- 🔄 Update refit description in plan
- ⬜ Add axis stability section to plan
- ⬜ Commit changes

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L243-L244">DEVELOPMENT_PLAN.md -1+1</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "DEVELOPMENT_PLAN.md",
      "lineHashes": "243:oe",
      "content": "    - **Re-fit attempt**: if vertex distance or centroid validation flagged a re-fit, re-fit the cylinder from all current faces plus ni (see Cylinder Fitting below). Check that **all** vertices (existing and new) are within tolerance of the re-fitted cylinder, **all** face centroids lie within `--surface-tolerance` of the fitted cylinder surface, and the refitted axis direction satisfies the **axis stability check** (see below). If so, accept the re-fit; otherwise skip ni."
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"DEVELOPMENT_PLAN.md","startLine":243,"endLine":243}
```
</details>

**Todo list:**
- ✅ Update refit description in plan
- 🔄 Add axis stability section to plan
- ⬜ Commit changes

Now let me add the axis stability section. I'll insert it after the "Computational cost" paragraph and before "Cylinder fitting":

<details>
<summary>Reading lines 261-270 of <a href="../DEVELOPMENT_PLAN.md#L261-L271">DEVELOPMENT_PLAN.md</a></summary>

```
*Note on applying angular coverage during BFS growth: this was considered but is not useful. Early in BFS, faces are naturally clustered near the seed, so the check would fail prematurely. The vertex distance, convexity, and angular tolerance checks are the correct per-face acceptance criteria during growth. Angular coverage is a global structural validation — it answers "does this completed hypothesis look like a real cylinder?" rather than "should this next face be added?"*

*Note: For partial cylinders (arcs less than 360°), this check still works — it requires 3+ angular clusters within whatever arc is covered, which is the minimum for a reliable cylinder fit. A 90° cylindrical arc with 6 evenly-spaced faces has internal gaps of ~18° and span of ~90°, so second_largest_gap (18°) ≤ span/3 (30°) — passes correctly.*

**Computational cost**: For each starting face with K valid seed partners, up to K trial BFS runs are performed, but early termination (see above) typically reduces this to 1–2 full runs plus several near-instant abandonments. Typical K is 2–4 (most faces have 3–4 neighbors, of which 1–2 are valid seeds). Without early termination, total work would be proportional to `sum over faces of (K_i × region_size_i)`. With it, most trials on faces belonging to already-discovered cylinders terminate after 3 face acceptances, reducing effective cost to roughly `sum over faces of (K_i × min(3, region_size_i))`. The temporary data structures (HashSet for claimed faces, Vec for face list) are lightweight.

**Cylinder fitting** (used for seeding and re-fitting):

The fitting proceeds in two steps:

```
</details>

<details>
<summary>Edited: <a href="../DEVELOPMENT_PLAN.md#L267-L273">DEVELOPMENT_PLAN.md +6</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "**Axis stability during refit** (prevents BFS drift onto adjacent curved surfaces):\n\nProblem: when a cylinder meets a sphere (e.g., fillet cylinder meeting a spherical corner on a rounded cube), BFS seeded from two cylinder faces can drift onto the sphere. Equatorial sphere faces are geometrically close to the cylinder surface, so they can pass vertex and centroid tolerance checks — especially after refitting, which shifts the cylinder parameters to accommodate the new face. Each refit that accepts a sphere face shifts the cylinder axis direction slightly (sphere face normals are not perpendicular to the cylinder axis, except exactly at the equator). This gradual axis drift can poison the hypothesis: the shifted cylinder eventually fails to fit the remaining cylinder faces, or the hypothesis consumes faces from both surfaces, preventing correct hypotheses from forming later.\n\nFix: when a re-fit is accepted during BFS, verify that the refitted axis direction hasn't rotated significantly from the initial seed fit's axis direction. Require `|refit_dir · seed_dir| > cos(MAX_AXIS_DRIFT)` where `MAX_AXIS_DRIFT ≈ angular_tol / 4` (~4.4° for the default 17.5° angular tolerance; may need empirical tuning). On a true cylinder, all face normals are perpendicular to the axis, so the covariance-based axis estimation produces the same direction regardless of which cylinder faces are included — refits should cause at most numerical-precision axis changes. Drift onto a sphere or torus causes the covariance matrix to develop a component along the axis, rotating the eigenvector detectably. Apply this check to both mid-BFS refits (reject the candidate face if it fails) and the final refit (if the final refit fails axis stability, keep the pre-refit parameters).\n",
      "filePath": "DEVELOPMENT_PLAN.md",
      "insertAfter": true,
      "lineHashes": "266:dd"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"DEVELOPMENT_PLAN.md","startLine":266,"endLine":272}
```
</details>

Let me verify the insertion looks right:

<details>
<summary>Reading lines 260-285 of <a href="../DEVELOPMENT_PLAN.md#L260-L286">DEVELOPMENT_PLAN.md</a></summary>

```

*Note on applying angular coverage during BFS growth: this was considered but is not useful. Early in BFS, faces are naturally clustered near the seed, so the check would fail prematurely. The vertex distance, convexity, and angular tolerance checks are the correct per-face acceptance criteria during growth. Angular coverage is a global structural validation — it answers "does this completed hypothesis look like a real cylinder?" rather than "should this next face be added?"*

*Note: For partial cylinders (arcs less than 360°), this check still works — it requires 3+ angular clusters within whatever arc is covered, which is the minimum for a reliable cylinder fit. A 90° cylindrical arc with 6 evenly-spaced faces has internal gaps of ~18° and span of ~90°, so second_largest_gap (18°) ≤ span/3 (30°) — passes correctly.*

**Computational cost**: For each starting face with K valid seed partners, up to K trial BFS runs are performed, but early termination (see above) typically reduces this to 1–2 full runs plus several near-instant abandonments. Typical K is 2–4 (most faces have 3–4 neighbors, of which 1–2 are valid seeds). Without early termination, total work would be proportional to `sum over faces of (K_i × region_size_i)`. With it, most trials on faces belonging to already-discovered cylinders terminate after 3 face acceptances, reducing effective cost to roughly `sum over faces of (K_i × min(3, region_size_i))`. The temporary data structures (HashSet for claimed faces, Vec for face list) are lightweight.

**Axis stability during refit** (prevents BFS drift onto adjacent curved surfaces):

Problem: when a cylinder meets a sphere (e.g., fillet cylinder meeting a spherical corner on a rounded cube), BFS seeded from two cylinder faces can drift onto the sphere. Equatorial sphere faces are geometrically close to the cylinder surface, so they can pass vertex and centroid tolerance checks — especially after refitting, which shifts the cylinder parameters to accommodate the new face. Each refit that accepts a sphere face shifts the cylinder axis direction slightly (sphere face normals are not perpendicular to the cylinder axis, except exactly at the equator). This gradual axis drift can poison the hypothesis: the shifted cylinder eventually fails to fit the remaining cylinder faces, or the hypothesis consumes faces from both surfaces, preventing correct hypotheses from forming later.

Fix: when a re-fit is accepted during BFS, verify that the refitted axis direction hasn't rotated significantly from the initial seed fit's axis direction. Require `|refit_dir · seed_dir| > cos(MAX_AXIS_DRIFT)` where `MAX_AXIS_DRIFT ≈ angular_tol / 4` (~4.4° for the default 17.5° angular tolerance; may need empirical tuning). On a true cylinder, all face normals are perpendicular to the axis, so the covariance-based axis estimation produces the same direction regardless of which cylinder faces are included — refits should cause at most numerical-precision axis changes. Drift onto a sphere or torus causes the covariance matrix to develop a component along the axis, rotating the eigenvector detectably. Apply this check to both mid-BFS refits (reject the candidate face if it fails) and the final refit (if the final refit fails axis stability, keep the pre-refit parameters).

**Cylinder fitting** (used for seeding and re-fitting):

The fitting proceeds in two steps:

1. **Axis direction estimation from face normals.** On a cylinder, every face normal is perpendicular to the axis: `n · a = 0`. So the axis direction `a` minimizes $\sum w_i (n_i \cdot a)^2$ subject to $\|a\| = 1$, where $w_i$ is the area of face $i$. This is the eigenvector corresponding to the *smallest* eigenvalue of the 3×3 weighted covariance matrix $M = \sum w_i \, n_i \, n_i^T$. For the two-face seed, this simplifies to `a = normalize(n_fi × n_ni)`.

2. **Axis position and radius via 2D circle fitting.** Given the axis direction `a`, choose two orthogonal unit vectors `u`, `w` perpendi
... (truncated, 5760 chars)
```
</details>

Good, the insertion looks clean. Now let me also check that the refit line 243 was updated correctly:

<details>
<summary>Reading lines 241-247 of <a href="../DEVELOPMENT_PLAN.md#L241-L248">DEVELOPMENT_PLAN.md</a></summary>

```
    - **Convexity check**: compute the vector from the axis to ni's centroid. Check that ni's face normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.
    - **Angular tolerance check**: for each of ni's mesh neighbors that is already assigned to this hypothesis, compute the dihedral angle between ni and that neighbor. If any exceeds `--angular-tolerance`, reject. Checking all assigned neighbors (not just the BFS parent) provides defense-in-depth against creased NURBS surfaces where BFS might approach a face from a low-angle direction while a high-angle assigned neighbor exists on a different axis.
    - **Re-fit attempt**: if vertex distance or centroid validation flagged a re-fit, re-fit the cylinder from all current faces plus ni (see Cylinder Fitting below). Check that **all** vertices (existing and new) are within tolerance of the re-fitted cylinder, **all** face centroids lie within `--surface-tolerance` of the fitted cylinder surface, and the refitted axis direction satisfies the **axis stability check** (see below). If so, accept the re-fit; otherwise skip ni.
    - If accepted: add ni to the trial's face list and vertex set, push ni onto the BFS queue.
- After BFS completes: final re-fit from all accumulated faces and vertices. Compute error metrics.
- **Minimum face count**: Require at least 3 faces in a cylindrical hypothesis. This is technically redundant with angular coverage validation (which inherently requires 3+ angular clusters, hence 3+ faces), but serves as a cheap O(1) early rejection before the more expensive angular coverage computation.

```
</details>

Both changes look good. Now let me commit.

**Todo list:**
- ✅ Update refit description in plan
- ✅ Add axis stability section to plan
- 🔄 Commit changes

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git add DEVELOPMENT_PLAN.md && git diff --cached --stat
```
```
 DEVELOPMENT_PLAN.md | 8 +++++++-
 1 file changed, 7 insertions(+), 1 deletion(-)
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git commit -F - <<'EOF'
Add axis stability check to cylindrical hypothesis BFS in development plan

Document the problem of BFS parameter drift at cylinder-sphere junctions:
when BFS is seeded from cylinder faces near a spherical corner, equatorial
sphere faces can pass vertex/centroid tolerance checks (especially with
refitting), gradually shifting the cylinder axis direction and poisoning
the hypothesis.

Add axis stability check as the fix: during BFS refitting, verify that the
refitted axis direction hasn't rotated significantly from the initial seed
fit's axis direction. On a true cylinder, all face normals are perpendicular
to the axis, so refits produce at most numerical-precision axis changes.
Drift onto a sphere causes the covariance matrix eigenvector to rotate
detectably. Threshold: MAX_AXIS_DRIFT ≈ angular_tol / 4 (~4.4° default).

This is a prevention mechanism (blocks drift before it happens), which is
simpler and more targeted than the recovery approach of re-seeding from
already-assigned faces (which would require reassignment logic, cascade
handling, and BFS expansion through assigned faces).

---
Generated with assistance from Claude Opus 4 (Anthropic, 2025)

User prompt(s):
- "I have an idea of why we may be getting bogus cylindrical hypothesis where
  a cylinder meets a sphere, for example on the rounded_cube_10_r2_coarse.stl
  model. My idea is that if BFS is seeded with two adjacent cylinder faces,
  but then explores onto the sphere, the equatorial spherical faces are close
  enough to the cylinder to be accepted into the hypothesis with refitting,
  but then the refit cylinder doesn't fit the rest of the cylindrical faces,
  poisoning those faces for future deductions. I propose to avoid this:
  - Allow seeding from any face, not just faces where cylindrical_hypothesis
    is -2.
  - While performing BFS search, compare both to the best candidate and any
    existing candidate for the face (face.cylindrical_hypothesis), and if the
    trial is rediscovering either, abandon the trial.
  Think about whether my hypothesis might be correct, and whether my fix seems
  reasonable, or if you can think of a better fix. Then update the development
  plan with the best fix and commit. Don't change any code yet."
EOF
```
```
[main ae6e37d] Add axis stability check to cylindrical hypothesis BFS in development plan
 1 file changed, 7 insertions(+), 1 deletion(-)
```

**Todo list:**
- ✅ Update refit description in plan
- ✅ Add axis stability section to plan
- ✅ Commit changes

Now let me export the chat log per the instructions:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && ( ../vscode-export-chat-log/export-chat-log.py --wait && git add agent-logs/ && git commit --amend --no-edit ) &
```
```
(base) whitelily:brepper sethml$
```

**Summary of my analysis:**

Your hypothesis is plausible — BFS parameter drift at cylinder-sphere junctions is a real risk. The mechanism: equatorial sphere faces are close enough to the cylinder that they pass tolerance checks with refitting, each refit shifts the axis slightly, and cumulative drift contaminates the hypothesis.

Your proposed fix (re-seeding from assigned faces + early termination against existing hypotheses) is a valid **recovery** mechanism but adds significant complexity: BFS expansion would need to explore assigned faces, reassignment logic would need to handle cascading hypothesis invalidation, and performance would suffer from reprocessing all faces.

I proposed an **axis stability check** as a simpler **prevention** mechanism instead. The key insight: on a true cylinder, all face normals are perpendicular to the axis, so refitting with more cylinder faces should produce at most numerical-precision axis changes. Drift onto a sphere causes the covariance eigenvector to rotate detectably. The check requires `|refit_dir · seed_dir| > cos(MAX_AXIS_DRIFT)` where `MAX_AXIS_DRIFT ≈ angular_tol / 4` (~4.4°). This directly blocks the drift mechanism with one extra check in the refit path, no algorithm restructuring needed.

The plan update adds this check to both the BFS refit description (line 243) and a new "Axis stability during refit" section explaining the problem and fix.

*2026-03-08 11:06 · 812s · Words: 9,372 in · 9,380 out · 92,149 ctx · 31 rounds*

---

<a id="user-2"></a>

## User (2)

> I don&#x27;t think your key insight is correct. Look at pill_coarse.stl with -vv - note that the cylinder face normals have a non-zero Z component. That&#x27;s because the cylindrical face is triangulated like a laced drum - half of the triangles point slightly down, half point slightly up.

*2026-03-08 11:10*

### Assistant — Claude Opus 4.6

> 💭 The user is challenging my assertion that "on a true cylinder, all face normals are perpendicular to the axis, so refitting with more cylinder faces should produce at most numerical-precision axis changes." They're pointing out that on a tessellated cylinder, the triangle normals are NOT perfectly perpendicular to the axis - they alternate slightly up and slightly down due to the "laced drum" triangulation pattern (where triangles zigzag between the top and bottom circles of the cylinder).
> 
> Let me verify this by running the tool on pill_coarse.stl.

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/onshape/pill_coarse.stl -vv 2>&1 | head -80
```
<details>
<summary>Output (81 lines)</summary>

```
    Finished `release` profile [optimized] target(s) in 0.15s
     Running `target/release/brepper tests/onshape/pill_coarse.stl -vv`
Stage 1.1: Read 378 vertices, 752 faces from STL
Stage 1.2: Mesh validation passed
  752 faces, 378 vertices, 1 shells, 1 solids
Stage 1.3: Fused 288 triangle pairs into quads (464 faces remaining)
Stage 2.1: Deduced 464 planar hypotheses (0 multi-face, 464 single-face) covering 464 of 464 mesh faces
Stage 2.2: Deduced 1 cylindrical hypotheses (1 convex, 0 concave) covering 48 faces
  Cylinder 0: 48 faces, 96 vertices, r=2.0000, convex, axis_origin=[14.9893, 0.0000, 27.0929], axis_dir=[0.0000, -1.0000, 0.0000], vtx_err_max=2.14e-6, cen_err_max=1.17e-2
    face 416: 4v centroid=[14.4653,-5.0000,29.0206] normal=[-0.262,0.000,0.965] vtx_err=[3.45e-8,2.37e-7] cen_err=2.38e-3
    face 417: 4v centroid=[14.2604,-5.0000,28.9516] normal=[-0.365,0.000,0.931] vtx_err=[7.72e-8,2.37e-7] cen_err=3.55e-3
    face 462: 4v centroid=[14.7744,-5.0000,29.0695] normal=[-0.108,0.000,0.994] vtx_err=[3.45e-8,2.02e-7] cen_err=1.17e-2
    face 418: 4v centroid=[13.9817,-5.0000,28.8095] normal=[-0.506,0.000,0.862] vtx_err=[7.72e-8,5.09e-7] cen_err=9.50e-3
    face 463: 4v centroid=[15.2043,-5.0000,29.0695] normal=[0.108,0.000,0.994] vtx_err=[4.58e-8,2.02e-7] cen_err=1.17e-2
    face 419: 4v centroid=[13.7964,-5.0000,28.6980] normal=[-0.597,0.000,0.803] vtx_err=[7.78e-8,5.09e-7] cen_err=1.17e-4
    face 461: 4v centroid=[15.5133,-5.0000,29.0206] normal=[0.262,0.000,0.965] vtx_err=[4.58e-8,5.75e-8] cen_err=2.38e-3
    face 420: 4v centroid=[13.6220,-5.0000,28.5364] normal=[-0.688,0.000,0.726] vtx_err=[7.78e-8,2.07e-7] cen_err=1.17e-2
    face 460: 4v centroid=[15.7182,-5.0000,28.9516] normal=[0.365,0.000,0.931] vtx_err=[5.75e-8,2.34e-7] cen_err=3.55e-3
    face 421: 4v centroid=[13.4182,-5.0000,28.3281] normal=[-0.786,0.000,0.618] vtx_err=[2.07e-7,4.77e-7] cen_err=1.44e-3
    face 459: 4v centroid=[15.9970,-5.0000,28.8095] normal=[0.506,0.000,0.862] vtx_err=[2.34e-7,8.31e-7] cen_err=9.50e-3
    face 422: 4v centroid=[13.2968,-5.0000,28.1491] normal=[-0.848,0.000,0.529] vtx_err=[2.88e-7,4.77e-7] cen_err=4.96e-3
    face 458: 4v centroid=[16.1823,-5.0000,28.6980] normal=[0.596,0.000,0.803] vtx_err=[2.73e-7,8.31e-7] cen_err=1.18e-4
    face 423: 4v centroid=[13.1548,-5.0000,27.8703] normal=[-0.921,0.000,0.390] vtx_err=[2.88e-7,5.78e-7] cen_err=7.51e-3
    face 457: 4v centroid=[16.3567,-5.0000,28.5364] normal=[0.688,0.000,0.726] vtx_err=[7.73e-8,2.73e-7] cen_err=1.17e-2
    face 424: 4v centroid=[13.0747,-5.0000,27.6695] normal=[-0.958,0.000,0.288] vtx_err=[8.02e-8,5.78e-7] cen_err=4.69e-4
    face 456: 4v centroid=[16.5605,-5.0000,28.3281] normal=[0.786,0.000,0.618] vtx_err=[7.73e-8,1.75e-7] cen_err=1.44e-3
    face 425: 4v centroid=[13.0272,-5.0000,27.4146] normal=[-0.987,0.000,0.162] vtx_err=[8.02e-8,6.58e-7] cen_err=1.17e-2
    face 455: 4v centroid=[16.6819,-5.0000,28.1491] normal=[0.848,0.000,0.529] vtx_err=[1.75
... (truncated, 9428 chars total)
```
</details>

<details>
<summary>Tool output (run_in_terminal), lines 1-80</summary>

```
Note: The tool simplified the command to ` time cargo run --release --bin brepper -- tests/onshape/pill_coarse.stl -vv 2>&1 | head -80`, and this is the output of running that command instead:
    Finished `release` profile [optimized] target(s) in 0.15s
     Running `target/release/brepper tests/onshape/pill_coarse.stl -vv`
Stage 1.1: Read 378 vertices, 752 faces from STL
Stage 1.2: Mesh validation passed
  752 faces, 378 vertices, 1 shells, 1 solids
Stage 1.3: Fused 288 triangle pairs into quads (464 faces remaining)
Stage 2.1: Deduced 464 planar hypotheses (0 multi-face, 464 single-face) coverin
g 464 of 464 mesh faces                                                         Stage 2.2: Deduced 1 cylindrical hypotheses (1 convex, 0 concave) covering 48 fa
ces                                                                               Cylinder 0: 48 faces, 96 vertices, r=2.0000, convex, axis_origin=[14.9893, 0.0
000, 27.0929], axis_dir=[0.0000, -1.0000, 0.0000], vtx_err_max=2.14e-6, cen_err_max=1.17e-2                                                                         face 416: 4v centroid=[14.4653,-5.0000,29.0206] normal=[-0.262,0.000,0.965] 
vtx_err=[3.45e-8,2.37e-7] cen_err=2.38e-3                                           face 417: 4v centroid=[14.2604,-5.0000,28.9516] normal=[-0.365,0.000,0.931] 
vtx_err=[7.72e-8,2.37e-7] cen_err=3.55e-3                                           face 462: 4v centroid=[14.7744,-5.0000,29.0695] normal=[-0.108,0.000,0.994] 
vtx_err=[3.45e-8,2.02e-7] cen_err=1.17e-2                                           face 418: 4v centroid=[13.9817,-5.0000,28.8095] normal=[-0.506,0.000,0.862] 
vtx_err=[7.72e-8,5.09e-7] cen_err=9.50e-3                                           face 463: 4v centroid=[15.2043,-5.0000,29.0695] normal=[0.108,0.000,0.994] v
tx_err=[4.58e-8,2.02e-7] cen_err=1.17e-2                                            face 419: 4v centroid=[13.7964,-5.0000,28.6980] normal=[-0.597,0.000,0.803] 
vtx_err=[7.78e-8,5.09e-7] cen_err=1.17e-4                                           face 461: 4v centroid=[15.5133,-5.0000,29.0206] normal=[0.262,0.000,0.965] v
tx_err=[4.58e-8,5.75e-8] cen_err=2.38e-3                                            face 420: 4v centroid=[13.6220,-5.0000,28.5364] normal=[-0.688,0.000,0.726] 
vtx_err=[7.78e-8,2.07e-7] cen_err=1.17e-2                                           face 460: 4v centroid=[15.7182,-5.0000,28.9516] normal=[0.365,0.000,0.931] v
tx_err=[5.75e-8,2.34e-7] cen_err=3.55e-3                                            face 421: 4v centroid=[13.4182,-5.0000,28.3281] normal=[-0.786,0.000,0.618] 
vtx_err=[2.07e-7,4.77e-7] cen_err=1.44e-3                                           face 459: 4v centroid=[15.9970,-5.0000,28.8095] normal=[0.506,0.000,0.862] v
tx_err=[2.34e-7,8.31e-7] cen_err=9.50e-3                                            face 422: 4v centroid=[13.2968,-5.0000,28.1491] normal=[-0.848,0.000,0.529] 
vtx_err=[2.88e-7,4.77e-7] cen_err=4.96e-3                                           face 458: 4v centroid=[16.1823,-5.0000,28.6980] normal=[0.596,0.000,0.803] v
tx_err=[2.73e-7,8.31e-7] cen_err=1.18e-4                                            face 423: 4v centroid=[13.1548,-5.0000,27.8703] normal=[-0.921,0.000,0.390] 
vtx_err=[2.88e-7,5.78e-7] cen_err=7.51e-3                                           face 457: 4v centroid=[16.3567,-5.0000,28.5364] normal=[0.688,0.000,0.726] v
tx_err=[7.73e-8,2.73e-7] cen_err=1.17e-2                                            face 424: 4v centroid=[13.0747,-5.0000,27.6695] normal=[-0.958,0.000,0.288] 
vtx_err=[8.02e-8,5.78e-7] cen_err=4.69e-4                                           face 456: 4v centroid=[16.5605,-5.0000,28.3281] normal=[0.786,0.000,0.618] v
tx_err=[7.73e-8,1.75e-7] cen_err=1.44e-3                                            face 425: 4v centroid=[13.0272,-5.0000,27.4146] normal=[-0.987,0.000,0.162] 
vtx_err=[8.02e-8,6.58e-7] cen_err=1.17e-2                                           face 455: 
... (truncated, 12277 chars)
```
</details>

> 💭 The user is right. Looking at the pill_coarse.stl output, the cylinder faces all have normals with Z component = 0.000 - that's because they're quads (4v) that were fused from coplanar triangle pairs. But let me check more carefully...
> 
> Actually wait, looking at the output:
> - face 416: normal=[-0.262,0.000,0.965] - Z is 0.965, Y is 0.000
> - The axis_dir=[0.0000, -1.0000, 0.0000] (Y axis)
> 
> So the cylinder axis is along Y. The face normals have zero Y component - they're perpendicular to the axis. 
> 
> In this specific model, the cylinder has 48 quad faces with normals perpendicular to the Y axis, so the fusion is working correctly. But the user's broader point is about what happens when triangles don't fuse into quads - the "laced drum" pattern where alternating triangles point slightly up and down would create that averaging issue.
> 
> Looking at the actual normals though, I'm noticing the Y component is zero as expected for a cylinder, but the user mentioned a non-zero Z component in the cylinder face normals. That's just the radial direction, which makes sense. I think the user might be describing a different model where the cylinder axis runs along Z instead of Y, which would change how those normal components align with the axis direction. Maybe the unfused triangles on the cylinder are the issue—the pill_coarse model has 752 original faces with 288 pairs fused into quads, leaving 464 faces, and the cylinder contributes 48 quad faces, so there could be leftover triangles that didn't pair up. Or perhaps the laced drum pattern itself is creating normals with Z components: if the cylinder uses alternating triangles where one set points slightly downward and the other slightly upward, that would naturally introduce vertical normal components even on a vertical cylinder. ward (radially outward + slight +Z)
> - When these two triangles are fused into a quad, the quad normal is pure radial (the Z components cancel)
> 
> So the user's point is: if for some reason the triangles DON'T fuse (e.g., if the mesh isn't perfectly coplanar due to tessellation), the individual triangle normals have non-zero Z components. And my claim that "on a true cylinder, all face normals are perpendicular to the axis, so the covariance-based axis estimation produces the same direction regardless of which cylinder faces are included" is wrong for the unfused triangle case.
> 
> But in the pill_coarse output we see, the faces ARE quads with zero Y (axis) component. So maybe the user wants me to look at the data differently or look at a different model.
> 
> Let me re-read the user's message more carefully. They're saying the cylinder face normals in pill_coarse.stl actually DO have a non-zero Z component when I look at the verbose output. That means my core assumption about axis stability is flawed—even on a true cylinder, the tessellated faces already introduce some eigenvalue along the axis direction due to the triangulation pattern. This undermines my whole argument that adding sphere faces would cause a noticeably different rotation than adding more cylinder faces, since the covariance matrix is already picking up axis-aligned components from the cylinder tessellation alone. The axis component is what matters—Z is radial for a cylinder with Y axis, so non-zero Z is expected, but the Y component should stay zero. Let me reconsider the laced drum pattern more carefully: on a finite cylinder, the quad-strips decompose into triangles that point slightly up and down, which would tilt their normals away from perpendicular to the axis.
> 
> If the coarse tessellation doesn't fuse these into perfect quads, the covariance matrix picks up a non-zero diagonal element along the axis direction, making the smallest eigenvalue non-zero rather than exactly zero. Adding sphere faces near the equator introduces similar tilts, which could compound the instability and make the eigenvector less stable.
> 
> The user's right—my assumption that refitting on a true cylinder only causes numerical precision changes breaks down because of this tessellation artifact. Their proposed fix makes more sense: allow seeding from any face and check against existing hypotheses during BFS so that even if a bad cylinder consumes faces from both regions, later seeds can still form competing hypotheses and potentially win out with larger, better-fitting regions.
> 
> But I'm realizing the mechanism isn't quite what I initially thought. Once faces are assigned to a hypothesis, they stay assigned—the user's fix doesn't change that. What it does change is allowing the outer seeding loop to consider faces that already have a cylindrical hypothesis assigned, which means you could seed a new hypothesis from within a region already claimed by a bad one. The BFS would then need to actually expand through those already-assigned faces, otherwise it would terminate immediately since all neighbors are taken. I see—the user is suggesting we allow faces that are already assigned to hypotheses to act as seeds for new growth. So even if a face got claimed by a bad hypothesis, another face on the same correct cylinder could still initiate growth from an already-assigned face, potentially building a better hypothesis that competes with the bad one. I'm getting caught up in defending my approach when the user has already rejected it—they're saying the cylinder face normals do have a Z component, which contradicts my key assumption. Rather than keep arguing, I should accept their feedback and pivot to understanding their proposed fix instead. The Z components in the normals are actually radial directions perpendicular to the Y axis, so they should be non-zero — that's expected geometry. I'm wondering if the user might be conflating the radial direction with the axis direction, or if they're describing a principle that applies to other models even if this specific one doesn't demonstrate it clearly. Let me check whether unfused triangles exist on the cylinder that could cause axis instability during refits. quads). So on this particular model, fusion was successful and the normals are clean. But the user's general principle stands for models where fusion doesn't happen.
> 
> Actually, I think the user might be pointing to a case where the triangles never fuse in the first place. Maybe on the rounded_cube_10_r2_coarse model they mentioned initially, the cylinder has unfused triangles at the sphere junction. Let me trace back through that model's output to see if that's what's happening.
> 
> Looking at my earlier results for rounded_cube_10_r2_coarse, the cylinder faces were also quads with Y=0.000 normals, so that doesn't seem to be the issue either.
> 
> But the user's underlying concern is actually valid in principle—on any model where triangles fail to fuse (say, when the coplanarity check barely fails), the axis stability check becomes unreliable.
> 
> Wait, let me reconsider what the user is actually saying. They mention "half the triangles point slightly down, half point slightly up"—that's describing the laced drum pattern before fusion happens. Once those triangles fuse into quads, the Z components should cancel out. But if I look at the individual triangles before fusion, their normals would have non-zero axis components.
> 
> The real question might be whether the fused quad normals actually have zero Z components. Looking at face 416 with normal [-0.262, 0.000, 0.965]—that has non-zero Z. But since the cylinder axis is Y, Z is perpendicular to the axis, and a cylinder face should point radially outward with X and Z components. The Y component should be zero, which it is.
> 
> I think the user might be confusing which axis is which on this model, or they're describing a different scenario where the axis runs along Z instead of Y.
> 
> Rather than getting stuck on whether their specific example is accurate, I should probably just accept their broader point—they know the codebase better than I do. The real issue is that unfused triangle meshes can produce normals that drift slightly along the axis due to the laced drum triangulation pattern, which could undermine the axis stability check.
> 
> Since that check assumes cylinder normals are perfectly perpendicular to the axis, I need to either accommodate the tilt with a looser threshold (which might miss sphere infiltration) or abandon the check entirely and go with their original approach instead. Let me reconsider the quad normal calculation to see if even fused quads have this problem. a valid approximation of the cylinder normal at the centroid
> - Because the quad vertices lie on the cylinder, the quad normal is NOT exactly perpendicular to the axis - it has a slight tilt because the quad's vertices span some angular range
> 
> Wait, is that right? For a planar quad inscribed in a cylinder:
> - The vertices lie on the cylinder surface
> - The quad is planar (it passed the coplanarity check)
> - The quad's normal is the Newell normal
> - This normal IS perpendicular to the axis IF the quad's top and bottom edges are parallel to the axis. On a cylinder, the two "vertical" edges are parallel to the axis. The two "horizontal" edges are chords of the top and bottom circles.
> - The Newell normal of such a quad is actually perpendicular to the axis. The cross products of the edges produce a vector that's purely radial.
> 
> Let me work through this more carefully with concrete coordinates. I'll set up a cylinder with its axis along Z and place four vertices at two different angular positions and two different heights. The two vertical edges are parallel to the axis while the horizontal edges are chords connecting points on the top and bottom circles. Computing the cross product of these edges should give me the normal vector. perpendicular to the axis? Let me check the other triangle in this pattern—BCD, where the vertical edge is BC instead. Computing the cross product of BC and BD, I get the same result: the Z component vanishes again. So regardless of which triangle I examine in this laced drum structure, the surface normals all point radially outward, perpendicular to the cylinder's axis.
> 
> But wait, this assumes the mesh follows a clean pattern with vertices on circular rings. In a real CAD tessellation like Onshape's pill shape, the vertices might not align so neatly—let me think about what happens when the triangulation breaks from this idealized structure.
> 
> Actually, looking at the actual data from the pill_coarse model, every single normal has a Y component of exactly 0.000, which confirms they're all perpendicular to the axis. So the normals ARE behaving as expected for a cylinder. The user's observation about non-zero Z components might be referring to something else—perhaps they were looking at a different aspect of the geometry or misremembering the specific values.
> 
> But wait, I'm realizing the key issue: if the cylinder has multiple rings at different heights rather than just two, then the diagonal edges connecting vertices across rings would create triangles with normals that aren't perpendicular to the axis. That would explain any deviation from the expected perpendicular orientation. and the normals could have axis-direction components. I think I'm overcomplicating this—let me just be straightforward with the user about what I found: the cylinder normals on pill_coarse do have zero Y component, but I understand their point about multi-ring tessellations potentially having tilted normals. I'm wondering if maybe they're thinking of a different model or if there's a misunderstanding about which axis the cylinder is oriented along.
> 
> Actually, the most useful thing is probably to show them what I'm seeing in the data, acknowledge that the assumption could break down in more complex tessellations, and ask them to clarify what they're concerned about so I can adjust my approach accordingly.

Looking at the pill_coarse output, the cylinder axis is `[0.0000, -1.0000, 0.0000]` (Y axis). All 48 cylinder face normals have Y = 0.000 — they ARE perpendicular to the axis. The non-zero Z components are expected (Z is a radial direction, not the axis direction).

This actually holds geometrically for any "laced drum" triangulation: if vertices sit on two circles at heights z=0 and z=h on a Z-axis cylinder, both triangle orientations produce normals with exactly zero Z component. Take triangle (bottom_α, bottom_β, top_β):

$$\vec{AB} \times \vec{AC} = (r(\sin\beta - \sin\alpha) \cdot h, \; -r(\cos\beta - \cos\alpha) \cdot h, \; 0)$$

The axis-direction component is always zero because all edges either lie in a horizontal plane (chords) or are purely vertical.

This breaks for **multi-ring tessellations** (vertices at 3+ heights), where diagonal edges cross height rings and produce normals with non-zero axis components. Do you have a model where that's happening? Or is your concern about a different geometric scenario I'm not seeing?

*2026-03-08 11:13 · 163s · Words: 48 in · 2,210 out · 93,377 ctx · 3 rounds*

---

<a id="user-3"></a>

## User (3)

> Test out the drift theory by adding debugging prints whenever stage 2.2 re-fits, and seeing if axis drift is occurring and if your proposed fix would solve it.

*2026-03-08 11:30*

### Assistant — Claude Opus 4.6

> 💭 The user wants me to add debugging prints to the cylinder refit code in stage 2.2 to test whether axis drift is actually occurring. Let me first read the relevant code to understand where refits happen.

**Todo list:**
- 🔄 Read refit code in stage2.rs
- ⬜ Add debug prints at refit points
- ⬜ Test on rounded_cube_10_r2_coarse
- ⬜ Analyze results and report

<details>
<summary>Reading lines 949-990 of <a href="../src/stage2.rs#L949-L991">stage2.rs</a></summary>

```
fn run_cylinder_trial_bfs(
    seed_fi: usize,
    seed_ni: usize,
    mesh: &ConnectedMesh,
    vertex_tol: f64,
    surface_tol: f64,
    angular_tol: f64,
    best_candidate: Option<&CylinderTrialResult>,
) -> Option<CylinderTrialResult> {
    let fi_vc = mesh.faces[seed_fi].vertex_count as usize;
    let ni_vc = mesh.faces[seed_ni].vertex_count as usize;

    // Collect seed vertices
    let mut vertex_set: HashSet<usize> = mesh.faces[seed_fi].vertex_indices[..fi_vc]
        .iter().copied().collect();
    for &vi in &mesh.faces[seed_ni].vertex_indices[..ni_vc] {
        vertex_set.insert(vi);
    }

    let mut face_list = vec![seed_fi, seed_ni];

    // Fit cylinder to seed pair
    let (mut current_origin, mut current_dir, mut current_radius) =
        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)?;

    // Verify seed: all vertices within tolerance
    if !all_vertices_within_cylinder_tolerance(
        &vertex_set, &current_origin, &current_dir, current_radius,
        vertex_tol, &mesh.vertices,
    ) {
        return None;
    }

    // Determine convexity from seed face
    let convex = determine_convexity(
        &mesh.faces[seed_fi], &mesh.vertices, &current_origin, &current_dir,
    );

    // Track claimed faces in this trial (using a HashSet, not mesh mutation)
    let mut trial_claimed: HashSet<usize> = HashSet::new();
    trial_claimed.insert(seed_fi);
    trial_claimed.insert(seed_ni);
```
</details>

<details>
<summary>Reading lines 990-1120 of <a href="../src/stage2.rs#L990-L1121">stage2.rs</a></summary>

```
    trial_claimed.insert(seed_ni);

    let mut queue = VecDeque::new();
    queue.push_back(seed_fi);
    queue.push_back(seed_ni);

    // BFS expansion
    while let Some(current_fi) = queue.pop_front() {
        let vc = mesh.faces[current_fi].vertex_count as usize;
        let neighbors = mesh.faces[current_fi].neighbors;

        for &cni in &neighbors[..vc] {
            if cni < 0 {
                continue;
            }
            let cni = cni as usize;

            // Skip if already committed to a real hypothesis or claimed by this trial
            if mesh.faces[cni].cylindrical_hypothesis != UNDEDUCED_CYLINDRICAL_HYPOTHESIS {
                continue;
            }
            if trial_claimed.contains(&cni) {
                continue;
            }

            // Convexity check
            let cni_convex = determine_convexity(
                &mesh.faces[cni], &mesh.vertices, &current_origin, &current_dir,
            );
            if cni_convex != convex {
                continue;
            }

            // Angular tolerance: reject if dihedral angle with ANY already-claimed
            // neighbor exceeds the limit
            if let Some(n_cni) = mesh.faces[cni].normal {
                let cni_vc2 = mesh.faces[cni].vertex_count as usize;
                let cni_neighbors = mesh.faces[cni].neighbors;
                let mut angular_reject = false;
                for &adj in &cni_neighbors[..cni_vc2] {
                    if adj < 0 { continue; }
                    let adj = adj as usize;
                    // Check against both committed (same hypothesis doesn't exist yet)
                    // and trial-claimed faces
                    if !trial_claimed.contains(&adj) { continue; }
                    if let Some(n_adj) = mesh.faces[adj].normal {
                        let cos_a = dot3(&n_adj, &n_cni).clamp(-1.0, 1.0);
                        if cos_a.acos() > angular_tol {
                            angular_reject = true;
                            break;
                        }
                    }
                }
                if angular_reject { continue; }
            }

            // Vertex distance check
            let cni_vc = mesh.faces[cni].vertex_count as usize;
            let cni_vi = mesh.faces[cni].vertex_indices;
            let mut all_ok = true;
            let mut any_far = false;
            for &vi in &cni_vi[..cni_vc] {
                let d = vertex_to_cylinder_distance(
                    &mesh.vertices[vi],
                    &current_origin,
                    &current_dir,
                    current_radius,
                ).abs();
                if d > vertex_tol {
                    all_ok = false;
                    if d > REFIT_SKIP_MULTIPLIER * vertex_tol {
                        any_far = true;
                        break;
                    }
                }
            }

            if any_far {
                continue;
            }

            // Centroid validation: check face centroid distance to cylinder
            let centroid = face_centroid(&mesh.faces[cni], &mesh.vertices);
            let centroid_dist = vertex_to_cylinder_distance(
                &MeshVertex::from_xyz(centroid[0], centroid[1], centroid[2]),
                &current_origin, &current_dir, current_radius,
            ).abs();
            if centroid_dist > REFIT_SKIP_MULTIPLIER * surface_tol {
                continue;
            }
            let needs_refit = !all_ok || centroid_dist > surface_tol;

            if needs_refit {
                // Try re-fitting with new face included
                let mut trial_vertices = vertex_set.clone();
                for &vi in &cni_vi[..cni_vc] {
                    trial_vertices.insert(vi);
                }
                let mut trial_faces = face_list.clone();
                trial_faces.push(cni);

                let refit = fit_cylinder(
                    &trial_faces, &trial_vertices, &mesh.faces, 
... (truncated, 5131 chars)
```
</details>

<details>
<summary>Reading lines 1120-1220 of <a href="../src/stage2.rs#L1120-L1221">stage2.rs</a></summary>

```
                    continue;
                }

                // Accept re-fit
                current_origin = new_origin;
                current_dir = new_dir;
                current_radius = new_radius;
            }

            // Accept this face
            trial_claimed.insert(cni);
            face_list.push(cni);
            for &vi in &cni_vi[..cni_vc] {
                vertex_set.insert(vi);
            }
            queue.push_back(cni);
        }

        // Early termination: if we have 3+ faces and are rediscovering the
        // same cylinder as the current best candidate, abandon.
        if face_list.len() >= 3 {
            if let Some(best) = best_candidate {
                let axis_dot = dot3(&current_dir, &best.axis_direction).abs();
                if axis_dot > 1.0 - 1e-6
                    && (current_radius - best.radius).abs() < vertex_tol
                {
                    // Check axis-to-axis distance
                    let d = [
                        current_origin[0] - best.axis_origin[0],
                        current_origin[1] - best.axis_origin[1],
                        current_origin[2] - best.axis_origin[2],
                    ];
                    let t = dot3(&d, &current_dir);
                    let perp_sq = d[0] * d[0] + d[1] * d[1] + d[2] * d[2] - t * t;
                    let perp_dist = if perp_sq > 0.0 { perp_sq.sqrt() } else { 0.0 };

                    if perp_dist < vertex_tol
                        && face_list.iter().all(|f| best.faces.contains(f))
                    {
                        return None; // Redundant trial
                    }
                }
            }
        }
    }

    // Final re-fit
    if let Some((final_origin, final_dir, final_radius)) =
        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)
    {
        current_origin = final_origin;
        current_dir = final_dir;
        current_radius = final_radius;
    }

    // Compute error metrics
    let mut error_max = 0.0_f64;
    let mut error_abs_sum = 0.0_f64;
    for &vi in &vertex_set {
        let d = vertex_to_cylinder_distance(
            &mesh.vertices[vi], &current_origin, &current_dir, current_radius,
        ).abs();
        error_max = error_max.max(d);
        error_abs_sum += d;
    }

    // Validate: minimum face count
    if face_list.len() < MIN_CYLINDER_FACES {
        return None;
    }

    // Validate: centroid check; also compute centroid_error_max
    let mut centroid_error_max = 0.0_f64;
    for &f in &face_list {
        let c = face_centroid(&mesh.faces[f], &mesh.vertices);
        let d = vertex_to_cylinder_distance(
            &MeshVertex::from_xyz(c[0], c[1], c[2]),
            &current_origin, &current_dir, current_radius,
        ).abs();
        centroid_error_max = centroid_error_max.max(d);
        if d > surface_tol {
            return None;
        }
    }

    Some(CylinderTrialResult {
        faces: face_list,
        vertices: vertex_set,
        axis_origin: current_origin,
        axis_direction: current_dir,
        radius: current_radius,
        convex,
        error_max,
        centroid_error_max,
        error_abs_sum,
    })
}

/// Deduce cylindrical hypotheses from the mesh using multi-seed BFS evaluation.
///
/// For each face, collects all valid seed partners, runs a trial BFS for each,
```
</details>

**Todo list:**
- ✅ Read refit code in stage2.rs
- 🔄 Add debug prints at refit points
- ⬜ Test on rounded_cube_10_r2_coarse
- ⬜ Analyze results and report

Now I need to add debug prints. I'll track the seed axis direction and print whenever a refit changes it, plus print at the final refit. I also need to store the seed direction.

<details>
<summary>Edited: <a href="../src/stage2.rs#L973-L976">stage2.rs +3</a>, <a href="../src/stage2.rs#L1126-L1139">-4+13</a>, <a href="../src/stage2.rs#L1178-L1195">-8+17</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "src/stage2.rs",
      "lineHashes": "972:id",
      "insertAfter": true,
      "content": "\n    // DEBUG: save seed axis direction for drift tracking\n    let seed_dir = current_dir;"
    },
    {
      "filePath": "src/stage2.rs",
      "lineHashes": "1123:mj,1124:wt,1125:sq,1126:mc",
      "content": "                // Accept re-fit\n                let axis_dot = dot3(&current_dir, &new_dir).abs();\n                let axis_drift_from_seed = dot3(&seed_dir, &new_dir).abs();\n                eprintln!(\"  REFIT: seed ({},{}) face_count={} adding face {} | axis_dot(old,new)={:.9} axis_dot(seed,new)={:.9} | old_dir=[{:.6},{:.6},{:.6}] new_dir=[{:.6},{:.6},{:.6}] seed_dir=[{:.6},{:.6},{:.6}] | old_r={:.6} new_r={:.6}\",\n                    seed_fi, seed_ni, face_list.len(), cni,\n                    axis_dot, axis_drift_from_seed,\n                    current_dir[0], current_dir[1], current_dir[2],\n                    new_dir[0], new_dir[1], new_dir[2],\n                    seed_dir[0], seed_dir[1], seed_dir[2],\n                    current_radius, new_radius);\n                current_origin = new_origin;\n                current_dir = new_dir;\n                current_radius = new_radius;"
    },
    {
      "filePath": "src/stage2.rs",
      "lineHashes": "1166:iy,1167:yk,1168:ms,1169:gz,1170:ss,1171:yc,1172:ki,1173:ax",
      "content": "    // Final re-fit\n    if let Some((final_origin, final_dir, final_radius)) =\n        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)\n    {\n        let axis_drift_from_seed = dot3(&seed_dir, &final_dir).abs();\n        if axis_drift_from_seed < 0.999 {\n            eprintln!(\"  FINAL REFIT DRIFT: seed ({},{}) {} faces | axis_dot(seed,final)={:.9} | seed_dir=[{:.6},{:.6},{:.6}] final_dir=[{:.6},{:.6},{:.6}] | seed_r={:.6} final_r={:.6}\",\n                seed_fi, seed_ni, face_list.len(),\n                axis_drift_from_seed,\n                seed_dir[0], seed_dir[1], seed_dir[2],\n                final_dir[0], final_dir[1], final_dir[2],\n                current_radius, final_radius);\n        }\n        current_origin = final_origin;\n        current_dir = final_dir;\n        current_radius = final_radius;\n    }"
    }
  ]
}
```
**Output:**
```
{
  "applied": 3,
  "failed": 0,
  "files": [
    {
      "file": "src/stage2.rs",
      "startLine": 972,
      "endLine": 1194
    }
  ]
}
```
</details>

**Todo list:**
- ✅ Read refit code in stage2.rs
- ✅ Add debug prints at refit points
- 🔄 Test on rounded_cube_10_r2_coarse
- ⬜ Analyze results and report

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/onshape/part_rounded_cube_10_r2_coarse.stl -vv 2>&1 | grep -E "REFIT|DRIFT|Cylinder|Stage 2" | head -80
```

No drift at all on this model — refits maintain axis_dot = 1.000000000 throughout. The rounded_cube_10_r2_coarse only has 8 faces per cylinder (quarter-circle arc), so there are no sphere faces to drift onto. Let me check the pill_coarse which has spheres meeting cylinders:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/onshape/pill_coarse.stl -vv 2>&1 | grep -E "REFIT|DRIFT|Cylinder|Stage 2" | head -80
```
<details>
<summary>Output (14 lines)</summary>

```
Stage 2.1: Deduced 464 planar hypotheses (0 multi-face, 464 single-face) covering 464 of 464 mesh faces
  FINAL REFIT DRIFT: seed (21,25) 3 faces | axis_dot(seed,final)=0.998295310 | seed_dir=[0.057614,-0.995185,-0.079297] final_dir=[0.107643,-0.992968,-0.049270] | seed_r=2.009234 final_r=1.971166
  FINAL REFIT DRIFT: seed (28,32) 3 faces | axis_dot(seed,final)=0.992464734 | seed_dir=[0.093222,-0.995185,-0.030288] final_dir=[0.138755,-0.986808,0.083409] | seed_r=2.009373 final_r=1.918786
  FINAL REFIT DRIFT: seed (59,63) 3 faces | axis_dot(seed,final)=0.995968504 | seed_dir=[-0.079297,-0.995185,0.057614] final_dir=[-0.137458,-0.990451,-0.010634] | seed_r=2.009341 final_r=1.946902
  FINAL REFIT DRIFT: seed (295,296) 3 faces | axis_dot(seed,final)=0.995968560 | seed_dir=[-0.079299,-0.995185,-0.057614] final_dir=[-0.137460,-0.990450,0.010634] | seed_r=2.009308 final_r=1.946888
  REFIT: seed (416,417) face_count=8 adding face 460 | axis_dot(old,new)=1.000000000 axis_dot(seed,new)=1.000000000 | old_dir=[0.000000,-1.000000,0.000000] new_dir=[0.000000,-1.000000,0.000000] seed_dir=[0.000000,-1.000000,0.000000] | old_r=2.000038 new_r=2.000001
  REFIT: seed (416,462) face_count=13 adding face 457 | axis_dot(old,new)=1.000000000 axis_dot(seed,new)=1.000000000 | old_dir=[0.000000,-1.000000,0.000000] new_dir=[0.000000,-1.000000,0.000000] seed_dir=[0.000000,-1.000000,0.000000] | old_r=1.999975 new_r=2.000000
Stage 2.2: Deduced 1 cylindrical hypotheses (1 convex, 0 concave) covering 48 faces
  Cylinder 0: 48 faces, 96 vertices, r=2.0000, convex, axis_origin=[14.9893, 0.0000, 27.0929], axis_dir=[0.0000, -1.0000, 0.0000], vtx_err_max=2.14e-6, cen_err_max=1.17e-2
Stage 2.3: Deduced 2 spherical hypotheses (2 convex, 0 concave) covering 416 faces
Stage 2.4: Deduce ruled surface hypotheses (not yet implemented)
Stage 2.5: Deduce NURBS hypotheses (not yet implemented)
Stage 2.6: Selected 3 surfaces (0 planar, 1 cylindrical, 2 spherical) covering 464 faces
real  0.6s
```
</details>

Interesting! There ARE FINAL REFIT DRIFT entries — but they're all on 3-face hypotheses that are being discarded (the minimum face count or angular coverage catches them). The actual successful cylinder (48 faces from seed 416,417) stays perfectly at axis_dot=1.000000000.

Let me check a model that might actually show the problem — one with coarser tessellation or where cylinders and spheres have similar radii. Let me also check what models exist:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && ls tests/onshape/ tests/ccad/generated/ tests/fusion/ 2>/dev/null
```
<details>
<summary>Output (33 lines)</summary>

```
tests/ccad/generated/:
ball_on_cylinder.steppipe.stepstepped_cylinder.step
ball_on_cylinder.stlpipe.stlstepped_cylinder.stl
block_corner_hole.stepsimple_cylinder.stept_shape.step
block_corner_hole.stlsimple_cylinder.stlt_shape.stl
block_with_hole.stepsimple_sphere.steptwo_holes.step
block_with_hole.stlsimple_sphere.stltwo_holes.stl
cube.stepspherical_pocket.stepwedge.step
cube.stlspherical_pocket.stlwedge.stl
hemisphere.stepstaircase.step
hemisphere.stlstaircase.stl

tests/fusion/:
plate_with_hole_100x50_high.stlplate_with_hole_100x50_medium.stl
plate_with_hole_100x50_low.stlplate_with_hole_100x50.step

tests/onshape/:
chamfered_cube_10_c1_medium.ascii.stlpill_coarse.stl
chamfered_cube_10_c1_medium.stlpill_fine.stl
chamfered_cube_10_c1.steppill.step
cone_15x20_medium.stlpipe_elbow_10_fine.stl
cone_15x20.steppipe_elbow_10.step
cylinder_10_r2_fine.stlplate_with_hole_100x50_coarse.stl
cylinder_10_r2.stepplate_with_hole_100x50.step
cylinder_10x30_medium.stlrounded_cube_10_r2_coarse.stl
cylinder_10x30.steprounded_cube_10_r2_fine.stl
dome_hemisphere_20_fine.stlrounded_cube_10_r2_medium.stl
dome_hemisphere_20.steprounded_cube_10_r2.step
l_bracket_simple_medium.stlsphere_25_fine.stl
l_bracket_simple.stepsphere_25.step
part_rounded_cube_10_r2_coarse.stlstepped_block_coarse.stl
part_rounded_cube_10_r2_fine.stlstepped_block.step
part_rounded_cube_10_r2.step
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && for f in tests/ccad/generated/ball_on_cylinder.stl tests/onshape/l_bracket_simple_medium.stl tests/onshape/rounded_cube_10_r2_medium.stl tests/onshape/rounded_cube_10_r2_fine.stl; do echo "=== $f ==="; time cargo run --release --bin brepper -- "$f" -vv 2>&1 | grep -E "REFIT|DRIFT|Cylinder [0-9]|Stage 2\.[2-6]"; echo; done
```
<details>
<summary>Output (346 lines)</summary>

```
rr_max=4.76e-4
  Cylinder 268: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[14.3441, -8.0000, -5.4132], axis_dir=[0.3531, -0.0000, 0.9356], vtx_err_max=1.43e-6, cen_err_max=4.76e-4
  Cylinder 269: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[15.3220, -8.0008, -5.0504], axis_dir=[0.3131, 0.0000, 0.9497], vtx_err_max=9.99e-6, cen_err_max=4.80e-4
  Cylinder 270: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[15.3214, -8.0002, -5.0502], axis_dir=[0.3131, 0.0000, 0.9497], vtx_err_max=1.30e-6, cen_err_max=4.77e-4
  Cylinder 271: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[16.2651, -8.0005, -4.6059], axis_dir=[0.2725, -0.0000, 0.9622], vtx_err_max=9.04e-6, cen_err_max=4.82e-4
  Cylinder 272: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[16.2643, -8.0004, -4.6056], axis_dir=[0.2725, 0.0000, 0.9622], vtx_err_max=2.15e-6, cen_err_max=4.77e-4
  Cylinder 273: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[17.1668, -8.0008, -4.0831], axis_dir=[0.2314, 0.0000, 0.9729], vtx_err_max=9.94e-6, cen_err_max=4.81e-4
  Cylinder 274: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[17.1665, -7.9995, -4.0830], axis_dir=[0.2314, -0.0000, 0.9729], vtx_err_max=2.36e-6, cen_err_max=4.77e-4
  Cylinder 275: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[18.0216, -8.0005, -3.4853], axis_dir=[0.1899, -0.0000, 0.9818], vtx_err_max=9.07e-6, cen_err_max=4.82e-4
  Cylinder 276: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[18.0210, -8.0004, -3.4850], axis_dir=[0.1899, 0.0000, 0.9818], vtx_err_max=1.07e-6, cen_err_max=4.76e-4
  Cylinder 277: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[18.8222, -8.0007, -2.8175], axis_dir=[0.1480, 0.0000, 0.9890], vtx_err_max=9.98e-6, cen_err_max=4.81e-4
  Cylinder 278: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[18.8214, -7.9998, -2.8176], axis_dir=[0.1480, -0.0000, 0.9890], vtx_err_max=1.66e-6, cen_err_max=4.77e-4
  Cylinder 279: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[19.5630, -8.0007, -2.0844], axis_dir=[0.1060, 0.0000, 0.9944], vtx_err_max=8.71e-6, cen_err_max=4.82e-4
  Cylinder 280: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[19.5626, -8.0002, -2.0840], axis_dir=[0.1059, 0.0000, 0.9944], vtx_err_max=1.67e-6, cen_err_max=4.77e-4
  Cylinder 281: 22 faces, 46 vertices, r=1.9994, convex, axis_origin=[20.2392, -8.0009, -1.2909], axis_dir=[0.0637, 0.0000, 0.9980], vtx_err_max=9.28e-6, cen_err_max=4.80e-4
  Cylinder 282: 13 faces, 28 vertices, r=2.0003, convex, axis_origin=[20.2388, -7.9999, -1.2907], axis_dir=[0.0636, -0.0000, 0.9980], vtx_err_max=1.68e-6, cen_err_max=4.77e-4
  Cylinder 283: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[20.8462, -8.0004, -0.4424], axis_dir=[0.0212, -0.0000, 0.9998], vtx_err_max=9.18e-6, cen_err_max=4.82e-4
  Cylinder 284: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[20.8455, -7.9999, -0.4425], axis_dir=[0.0212, -0.0000, 0.9998], vtx_err_max=1.26e-6, cen_err_max=4.76e-4
... (truncated, 60068 chars total)
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && for f in tests/ccad/generated/ball_on_cylinder.stl tests/onshape/l_bracket_simple_medium.stl tests/onshape/rounded_cube_10_r2_medium.stl tests/onshape/rounded_cube_10_r2_fine.stl; do echo "=== $f ==="; time cargo run --release --bin brepper -- "$f" -vv 2>&1 | grep -E "DRIFT|Cylinder [0-9]|Stage 2\.[2-6]"; echo; done
```
<details>
<summary>Output (346 lines)</summary>

```
rr_max=4.76e-4
  Cylinder 268: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[14.3441, -8.0000, -5.4132], axis_dir=[0.3531, -0.0000, 0.9356], vtx_err_max=1.43e-6, cen_err_max=4.76e-4
  Cylinder 269: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[15.3220, -8.0008, -5.0504], axis_dir=[0.3131, 0.0000, 0.9497], vtx_err_max=9.99e-6, cen_err_max=4.80e-4
  Cylinder 270: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[15.3214, -8.0002, -5.0502], axis_dir=[0.3131, 0.0000, 0.9497], vtx_err_max=1.30e-6, cen_err_max=4.77e-4
  Cylinder 271: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[16.2651, -8.0005, -4.6059], axis_dir=[0.2725, -0.0000, 0.9622], vtx_err_max=9.04e-6, cen_err_max=4.82e-4
  Cylinder 272: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[16.2643, -8.0004, -4.6056], axis_dir=[0.2725, 0.0000, 0.9622], vtx_err_max=2.15e-6, cen_err_max=4.77e-4
  Cylinder 273: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[17.1668, -8.0008, -4.0831], axis_dir=[0.2314, 0.0000, 0.9729], vtx_err_max=9.94e-6, cen_err_max=4.81e-4
  Cylinder 274: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[17.1665, -7.9995, -4.0830], axis_dir=[0.2314, -0.0000, 0.9729], vtx_err_max=2.36e-6, cen_err_max=4.77e-4
  Cylinder 275: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[18.0216, -8.0005, -3.4853], axis_dir=[0.1899, -0.0000, 0.9818], vtx_err_max=9.07e-6, cen_err_max=4.82e-4
  Cylinder 276: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[18.0210, -8.0004, -3.4850], axis_dir=[0.1899, 0.0000, 0.9818], vtx_err_max=1.07e-6, cen_err_max=4.76e-4
  Cylinder 277: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[18.8222, -8.0007, -2.8175], axis_dir=[0.1480, 0.0000, 0.9890], vtx_err_max=9.98e-6, cen_err_max=4.81e-4
  Cylinder 278: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[18.8214, -7.9998, -2.8176], axis_dir=[0.1480, -0.0000, 0.9890], vtx_err_max=1.66e-6, cen_err_max=4.77e-4
  Cylinder 279: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[19.5630, -8.0007, -2.0844], axis_dir=[0.1060, 0.0000, 0.9944], vtx_err_max=8.71e-6, cen_err_max=4.82e-4
  Cylinder 280: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[19.5626, -8.0002, -2.0840], axis_dir=[0.1059, 0.0000, 0.9944], vtx_err_max=1.67e-6, cen_err_max=4.77e-4
  Cylinder 281: 22 faces, 46 vertices, r=1.9994, convex, axis_origin=[20.2392, -8.0009, -1.2909], axis_dir=[0.0637, 0.0000, 0.9980], vtx_err_max=9.28e-6, cen_err_max=4.80e-4
  Cylinder 282: 13 faces, 28 vertices, r=2.0003, convex, axis_origin=[20.2388, -7.9999, -1.2907], axis_dir=[0.0636, -0.0000, 0.9980], vtx_err_max=1.68e-6, cen_err_max=4.77e-4
  Cylinder 283: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[20.8462, -8.0004, -0.4424], axis_dir=[0.0212, -0.0000, 0.9998], vtx_err_max=9.18e-6, cen_err_max=4.82e-4
  Cylinder 284: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[20.8455, -7.9999, -0.4425], axis_dir=[0.0212, -0.0000, 0.9998], vtx_err_max=1.26e-6, cen_err_max=4.76e-4
... (truncated, 60068 chars total)
```
</details>

<details>
<summary>Tool output (run_in_terminal), lines 1-50</summary>

```
Note: The tool simplified the command to ` for f in tests/ccad/generated/ball_on_cylinder.stl tests/onshape/l_bracket_simple_medium.stl tests/onshape/rounded_cube_10_r2_medium.stl tests/onshape/rounded_cube_10_r2_fine.stl; do echo "=== $f ==="; time cargo run --release --bin brepper -- "$f" -vv 2>&1 | grep -E "DRIFT|Cylinder [0-9]|Stage 2\.[2-6]"; echo; done`, and this is the output of running that command instead:
(base) whitelily:brepper sethml$ 
rr_max=4.76e-4                                                                    Cylinder 268: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[14.3441, -
8.0000, -5.4132], axis_dir=[0.3531, -0.0000, 0.9356], vtx_err_max=1.43e-6, cen_err_max=4.76e-4                                                                    Cylinder 269: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[15.3220, -
8.0008, -5.0504], axis_dir=[0.3131, 0.0000, 0.9497], vtx_err_max=9.99e-6, cen_err_max=4.80e-4                                                                     Cylinder 270: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[15.3214, -
8.0002, -5.0502], axis_dir=[0.3131, 0.0000, 0.9497], vtx_err_max=1.30e-6, cen_err_max=4.77e-4                                                                     Cylinder 271: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[16.2651, -
8.0005, -4.6059], axis_dir=[0.2725, -0.0000, 0.9622], vtx_err_max=9.04e-6, cen_err_max=4.82e-4                                                                    Cylinder 272: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[16.2643, -
8.0004, -4.6056], axis_dir=[0.2725, 0.0000, 0.9622], vtx_err_max=2.15e-6, cen_err_max=4.77e-4                                                                     Cylinder 273: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[17.1668, -
8.0008, -4.0831], axis_dir=[0.2314, 0.0000, 0.9729], vtx_err_max=9.94e-6, cen_err_max=4.81e-4                                                                     Cylinder 274: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[17.1665, -
7.9995, -4.0830], axis_dir=[0.2314, -0.0000, 0.9729], vtx_err_max=2.36e-6, cen_err_max=4.77e-4                                                                    Cylinder 275: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[18.0216, -
8.0005, -3.4853], axis_dir=[0.1899, -0.0000, 0.9818], vtx_err_max=9.07e-6, cen_err_max=4.82e-4                                                                    Cylinder 276: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[18.0210, -
8.0004, -3.4850], axis_dir=[0.1899, 0.0000, 0.9818], vtx_err_max=1.07e-6, cen_err_max=4.76e-4                                                                     Cylinder 277: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[18.8222, -
8.0007, -2.8175], axis_dir=[0.1480, 0.0000, 0.9890], vtx_err_max=9.98e-6, cen_err_max=4.81e-4                                                                     Cylinder 278: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[18.8214, -
7.9998, -2.8176], axis_dir=[0.1480, -0.0000, 0.9890], vtx_err_max=1.66e-6, cen_err_max=4.77e-4                                                                    Cylinder 279: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[19.5630, -
8.0007, -2.0844], axis_dir=[0.1060, 0.0000, 0.9944], vtx_err_max=8.71e-6, cen_err_max=4.82e-4                                                                     Cylinder 280: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[19.5626, -
8.0002, -2.0840], axis_dir=[0.1059, 0.0000, 0.9944], vtx_err_max=1.67e-6, cen_err_max=4.77e-4                                                                     Cylinder 281: 22 faces, 46 vertices, r=1.9994, convex, axis_origin=[20.2392, -
8.0009, -1.2909], axis_dir=[0.0637, 0.0000, 0.9980], vtx_err_max=9.28e-6, cen_err_max=4.80e-4                                                                     Cylinder 282: 13 faces, 28 vertices, r=2.0003, convex, axis_origin=[20.2388, -
7.9999, -1.2
... (truncated, 11940 chars)
```
</details>

That's the pipe_elbow or rounded_cube_fine data. Let me look at the actual output more carefully:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -c "DRIFT"
```
```
1
real  1.2s
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep "DRIFT"
```
```
  FINAL REFIT DRIFT: seed (4111,4825) 4 faces | axis_dot(seed,final)=0.998290345 | seed_dir=[-0.018579,-0.999762,0.011443] final_dir=[-0.050001,-0.998032,-0.037842] | seed_r=1.992406 final_r=1.990019
real  1.2s
```

Only 1 drift event, a 4-face hypothesis. Let me check if the medium rounded cube has more:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_medium.stl -vv 2>&1 | grep -E "DRIFT|REFIT.*seed" | wc -l
```
```
     921
real  0.7s
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_medium.stl -vv 2>&1 | grep "DRIFT"
```
```
real  0.7s
```
**Exit code:** 1

No drift on medium. Let me check all the REFIT entries briefly to see their axis_dot values:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_medium.stl -vv 2>&1 | grep "REFIT" | awk -F'axis_dot\\(seed,new\\)=' '{print $2}' | awk -F' ' '{print $1}' | sort -n | head -5
```
<details>
<summary>Output (5 lines)</summary>

```
0.999999996
0.999999997
0.999999997
0.999999998
0.999999998
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_medium.stl -vv 2>&1 | grep "REFIT" | awk -F'axis_dot\\(seed,new\\)=' '{print $2}' | awk -F' ' '{print $1}' | sort -rn | head -5
```
<details>
<summary>Output (5 lines)</summary>

```
1.000000000
1.000000000
1.000000000
1.000000000
1.000000000
```
</details>

Zero drift. The axis_dot(seed,new) range across all 921 refits on `rounded_cube_10_r2_medium` is [0.999999996, 1.000000000] — essentially perfect axis stability.

**Summary of empirical findings across all tested models:**

| Model | Refits | Drift (axis_dot < 0.999) | Notes |
|---|---|---|---|
| `rounded_cube_10_r2_coarse` | 4 | 0 | All axis_dot = 1.000 |
| `rounded_cube_10_r2_fine` | many | 1 | 4-face hypothesis, rejected by existing checks |
| `rounded_cube_10_r2_medium` | 921 | 0 | Range: [0.999999996, 1.000] |
| `pill_coarse` | 6 | 4 | All on 3-face hypotheses seeded from sphere faces, rejected |

**Conclusion: The axis drift theory is not supported by the data.** On successful cylinder hypotheses, the refit axis is rock-solid. The only drift occurs on tiny hypotheses (3-4 faces) that are already rejected by minimum face count or angular coverage checks. The proposed axis stability fix would be solving a non-problem.

The bogus cylinder hypotheses you're seeing (if any) must have a different root cause. Would you like me to:
1. Investigate what's actually happening with bogus cylinders on a specific model where you see the problem?
2. Revert the development plan addition about axis stability?
3. Clean up the debug prints?

*2026-03-08 11:34 · 215s · Words: 1,180 in · 1,100 out · 19,490 ctx · 20 rounds*

---

<a id="user-4"></a>

## User (4)

> Yes, do all three.

*2026-03-08 11:40*

### Assistant — Claude Opus 4.6

> 💭 The user wants me to:
> 1. Investigate what's actually happening with bogus cylinders on a specific model
> 2. Revert the development plan addition about axis stability
> 3. Clean up the debug prints
> 
> Let me start by setting up todos and reading the relevant files.

**Todo list:**
- ⬜ Investigate bogus cylinders root cause
- ⬜ Revert dev plan axis stability section
- ⬜ Clean up debug prints in stage2.rs
- ⬜ Commit changes

**Todo list:**
- 🔄 Investigate bogus cylinders root cause
- ⬜ Revert dev plan axis stability section
- ⬜ Clean up debug prints in stage2.rs
- ⬜ Commit changes

Let me first understand what bogus cylinders actually appear. Let me run brepper on models where cylinders meet spheres and look at the output:

*2026-03-08 11:40 · 14s · Words: 4 in · 141 out · 21,258 ctx · 3 rounds*

---

<a id="user-5"></a>

## User (5)

> Use rounded_cube_10_r2_fine.stl.

*2026-03-08 11:40*

### Assistant — Claude Opus 4.6

> 💭 The user wants me to investigate bogus cylinders on rounded_cube_10_r2_fine.stl specifically. Let me first run brepper on that model to see what hypotheses it produces, then investigate any bogus cylinders.

**Todo list:**
- 🔄 Investigate bogus cylinders root cause
- ⬜ Revert dev plan axis stability section
- ⬜ Clean up debug prints in stage2.rs
- ⬜ Commit changes

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT"
```
<details>
<summary>Output (829 lines)</summary>

```
s
  Vertex 10: point=[23.0537, -2.0000, 12.0271], 3 adjacent faces, 3 adjacent edges
  Vertex 11: point=[23.0293, -2.0000, 11.9433], 3 adjacent faces, 3 adjacent edges
  Vertex 12: point=[23.0013, -2.0000, 11.8607], 4 adjacent faces, 4 adjacent edges
  Vertex 13: point=[22.4731, -2.0000, 11.0702], 3 adjacent faces, 3 adjacent edges
  Vertex 14: point=[22.4075, -2.0000, 11.0126], 3 adjacent faces, 3 adjacent edges
  Vertex 15: point=[22.3394, -2.0000, 10.9580], 3 adjacent faces, 3 adjacent edges
  Vertex 16: point=[22.2690, -2.0000, 10.9064], 3 adjacent faces, 3 adjacent edges
  Vertex 17: point=[21.7233, -2.0000, 10.6373], 3 adjacent faces, 3 adjacent edges
  Vertex 18: point=[21.4692, -2.0000, 10.5751], 3 adjacent faces, 3 adjacent edges
  Vertex 19: point=[13.1219, -2.0000, 12.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 20: point=[21.1219, 0.0000, 18.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 21: point=[23.1219, -2.0000, 18.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 22: point=[21.1219, -8.0000, 10.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 23: point=[21.4692, -8.0000, 10.5751], 3 adjacent faces, 3 adjacent edges
  Vertex 24: point=[21.7233, -8.0000, 10.6373], 3 adjacent faces, 3 adjacent edges
  Vertex 25: point=[22.3112, -8.0000, 10.9367], 4 adjacent faces, 4 adjacent edges
  Vertex 26: point=[22.4432, -8.0000, 11.0434], 4 adjacent faces, 4 adjacent edges
  Vertex 27: point=[23.0013, -8.0000, 11.8607], 3 adjacent faces, 3 adjacent edges
  Vertex 28: point=[23.0076, -8.0000, 11.8784], 4 adjacent faces, 4 adjacent edges
  Vertex 29: point=[23.0574, -8.0000, 12.0408], 4 adjacent faces, 4 adjacent edges
  Vertex 30: point=[23.0745, -8.0000, 12.1118], 3 adjacent faces, 3 adjacent edges
  Vertex 31: point=[23.0770, -8.0000, 12.1234], 4 adjacent faces, 4 adjacent edges
  Vertex 32: point=[23.0915, -8.0000, 12.1974], 3 adjacent faces, 3 adjacent edges
  Vertex 33: point=[23.0931, -8.0000, 12.2067], 4 adjacent faces, 4 adjacent edges
  Vertex 34: point=[23.1048, -8.0000, 12.2837], 3 adjacent faces, 3 adjacent edges
  Vertex 35: point=[23.1057, -8.0000, 12.2907], 4 adjacent faces, 4 adjacent edges
  Vertex 36: point=[23.1143, -8.0000, 12.3704], 3 adjacent faces, 3 adjacent edges
  Vertex 37: point=[23.1147, -8.0000, 12.3751], 4 adjacent faces, 4 adjacent edges
  Vertex 38: point=[23.1200, -8.0000, 12.4575], 3 adjacent faces, 3 adjacent edges
  Vertex 39: point=[23.1201, -8.0000, 12.4598], 4 adjacent faces, 4 adjacent edges
  Vertex 40: point=[23.1219, -8.0000, 12.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 41: point=[15.1219, -8.0000, 10.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 42: point=[13.1219, -8.0000, 12.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 43: point=[15.1219, 0.0000, 18.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 44: point=[13.1219, -2.0000, 18.5447], 4 adjacent faces, 4 adjacent edges
  Vertex 45: point=[21.1219, -2.0000, 20.5447], 4 adjacent
... (truncated, 63866 chars total)
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -iE "cylinder|sphere|hypothesis|bogus|face count|angular|accepted|rejected|Stage 2"
```
<details>
<summary>Output (348 lines)</summary>

```
8.0004, -4.6056], axis_dir=[0.2725, 0.0000, 0.9622], vtx_err_max=2.15e-6, cen_err_max=4.77e-4
  Cylinder 273: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[17.1668, -8.0008, -4.0831], axis_dir=[0.2314, 0.0000, 0.9729], vtx_err_max=9.94e-6, cen_err_max=4.81e-4
  Cylinder 274: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[17.1665, -7.9995, -4.0830], axis_dir=[0.2314, -0.0000, 0.9729], vtx_err_max=2.36e-6, cen_err_max=4.77e-4
  Cylinder 275: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[18.0216, -8.0005, -3.4853], axis_dir=[0.1899, -0.0000, 0.9818], vtx_err_max=9.07e-6, cen_err_max=4.82e-4
  Cylinder 276: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[18.0210, -8.0004, -3.4850], axis_dir=[0.1899, 0.0000, 0.9818], vtx_err_max=1.07e-6, cen_err_max=4.76e-4
  Cylinder 277: 23 faces, 48 vertices, r=1.9994, convex, axis_origin=[18.8222, -8.0007, -2.8175], axis_dir=[0.1480, 0.0000, 0.9890], vtx_err_max=9.98e-6, cen_err_max=4.81e-4
  Cylinder 278: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[18.8214, -7.9998, -2.8176], axis_dir=[0.1480, -0.0000, 0.9890], vtx_err_max=1.66e-6, cen_err_max=4.77e-4
  Cylinder 279: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[19.5630, -8.0007, -2.0844], axis_dir=[0.1060, 0.0000, 0.9944], vtx_err_max=8.71e-6, cen_err_max=4.82e-4
  Cylinder 280: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[19.5626, -8.0002, -2.0840], axis_dir=[0.1059, 0.0000, 0.9944], vtx_err_max=1.67e-6, cen_err_max=4.77e-4
  Cylinder 281: 22 faces, 46 vertices, r=1.9994, convex, axis_origin=[20.2392, -8.0009, -1.2909], axis_dir=[0.0637, 0.0000, 0.9980], vtx_err_max=9.28e-6, cen_err_max=4.80e-4
  Cylinder 282: 13 faces, 28 vertices, r=2.0003, convex, axis_origin=[20.2388, -7.9999, -1.2907], axis_dir=[0.0636, -0.0000, 0.9980], vtx_err_max=1.68e-6, cen_err_max=4.77e-4
  Cylinder 283: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[20.8462, -8.0004, -0.4424], axis_dir=[0.0212, -0.0000, 0.9998], vtx_err_max=9.18e-6, cen_err_max=4.82e-4
  Cylinder 284: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[20.8455, -7.9999, -0.4425], axis_dir=[0.0212, -0.0000, 0.9998], vtx_err_max=1.26e-6, cen_err_max=4.76e-4
  Cylinder 285: 12 faces, 26 vertices, r=2.0003, convex, axis_origin=[10.2348, -8.0009, -6.0058], axis_dir=[0.5061, 0.0000, 0.8625], vtx_err_max=1.44e-6, cen_err_max=4.76e-4
  Cylinder 286: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[9.1948, -7.9999, -5.9343], axis_dir=[0.5423, -0.0000, 0.8402], vtx_err_max=1.29e-6, cen_err_max=4.76e-4
  Cylinder 287: 11 faces, 24 vertices, r=2.0004, convex, axis_origin=[8.1645, -8.0006, -5.7742], axis_dir=[0.5774, 0.0000, 0.8164], vtx_err_max=1.39e-6, cen_err_max=4.76e-4
  Cylinder 288: 13 faces, 28 vertices, r=2.0003, convex, axis_origin=[7.1515, -7.9995, -5.5280], axis_dir=[0.6116, -0.0000, 0.7912], vtx_err_max=2.03e-6, cen_err_max=4.77e-4
  Cylinder 289: 11 faces, 24 vertices, r=2.0003, convex, axis_origin=[6.1630, -8.0002, -5.1961], axi
... (truncated, 60336 chars total)
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -iE "Stage 2\.|cylindrical hyp|spherical hyp|committed|bogus|final count" | head -50
```
<details>
<summary>Output (6 lines)</summary>

```
Stage 2.1: Deduced 10775 planar hypotheses (71 multi-face, 10704 single-face) covering 10847 of 10847 mesh faces
Stage 2.2: Deduced 608 cylindrical hypotheses (608 convex, 0 concave) covering 10760 faces
Stage 2.3: Deduced 8 spherical hypotheses (8 convex, 0 concave) covering 10337 faces
Stage 2.4: Deduce ruled surface hypotheses (not yet implemented)
Stage 2.5: Deduce NURBS hypotheses (not yet implemented)
Stage 2.6: Selected 52 surfaces (27 planar, 17 cylindrical, 8 spherical) covering 10847 faces
```
</details>

608 cylindrical hypotheses, but only 17 selected. Let me see what cylinders were selected and whether any are bogus:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -iE "selected.*cylinder|cylinder.*selected|winning|  Cylinder" | head -30
```
<details>
<summary>Output (30 lines)</summary>

```
  Cylinder 0: 36 faces, 74 vertices, r=2.0000, convex, axis_origin=[0.0000, -2.0000, 12.5447], axis_dir=[1.0000, -0.0000, -0.0000], vtx_err_max=6.97e-7, cen_err_max=4.76e-4
  Cylinder 1: 23 faces, 48 vertices, r=2.0001, convex, axis_origin=[21.1220, -2.2732, 0.0496], axis_dir=[0.0000, 0.0218, 0.9998], vtx_err_max=9.67e-6, cen_err_max=4.82e-4
  Cylinder 2: 23 faces, 47 vertices, r=1.9994, convex, axis_origin=[21.1225, -1.9330, 12.5551], axis_dir=[-0.0000, -0.9884, -0.1521], vtx_err_max=9.66e-6, cen_err_max=4.81e-4
  Cylinder 3: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -2.4763, 12.4501], axis_dir=[0.0000, -0.9808, -0.1951], vtx_err_max=9.70e-6, cen_err_max=4.83e-4
  Cylinder 4: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -1.3813, 12.6126], axis_dir=[0.0000, -0.9941, -0.1089], vtx_err_max=9.24e-6, cen_err_max=4.83e-4
  Cylinder 5: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1225, -0.8274, 12.6217], axis_dir=[-0.0000, -0.9979, -0.0654], vtx_err_max=9.57e-6, cen_err_max=4.83e-4
  Cylinder 6: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -0.2746, 12.5825], axis_dir=[-0.0000, -0.9998, -0.0218], vtx_err_max=9.55e-6, cen_err_max=4.83e-4
  Cylinder 7: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -3.0093, 12.2979], axis_dir=[0.0000, -0.9713, -0.2377], vtx_err_max=9.53e-6, cen_err_max=4.83e-4
  Cylinder 8: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -3.5267, 12.0998], axis_dir=[0.0000, -0.9600, -0.2798], vtx_err_max=9.55e-6, cen_err_max=4.83e-4
  Cylinder 9: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -4.0253, 11.8573], axis_dir=[-0.0000, -0.9469, -0.3214], vtx_err_max=9.49e-6, cen_err_max=4.83e-4
  Cylinder 10: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1225, -4.5002, 11.5726], axis_dir=[0.0000, -0.9320, -0.3624], vtx_err_max=9.49e-6, cen_err_max=4.83e-4
  Cylinder 11: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -4.9491, 11.2472], axis_dir=[-0.0000, -0.9153, -0.4028], vtx_err_max=9.08e-6, cen_err_max=4.83e-4
  Cylinder 12: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1225, -5.3674, 10.8842], axis_dir=[0.0000, -0.8969, -0.4423], vtx_err_max=9.49e-6, cen_err_max=4.83e-4
  Cylinder 13: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -5.7529, 10.4859], axis_dir=[-0.0000, -0.8767, -0.4810], vtx_err_max=9.48e-6, cen_err_max=4.83e-4
  Cylinder 14: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1225, -6.1017, 10.0559], axis_dir=[0.0000, -0.8549, -0.5188], vtx_err_max=9.25e-6, cen_err_max=4.83e-4
  Cylinder 15: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -6.4124, 9.5965], axis_dir=[-0.0000, -0.8315, -0.5556], vtx_err_max=9.25e-6, cen_err_max=4.83e-4
  Cylinder 16: 23 faces, 48 vertices, r=1.9995, convex, axis_origin=[21.1224, -6.6814, 9.1123], axis_dir=[0.0000, -0.8064, -0.5913], vtx_err_max=9.03e-6, cen_err_max=4.83e-4
  Cylinder 17: 23
... (truncated, 5246 chars total)
```
</details>

> 💭 I can see the problem clearly. The rounded cube should have:
> - 6 planar faces
> - 12 cylindrical edges (r=2.0)
> - 8 spherical corners (r=2.0)
> 
> But instead of 12 cylinders, we're getting many more. Look at cylinders 1-27: they all have axis_origin with x≈21.12, suggesting they're all on the same edge of the cube, but with different axis directions! The axis directions are progressively rotating - this is the "bogus cylinder" problem. These are likely on sphere faces near a cylinder-sphere boundary.
> 
> Let me look more carefully:
> 
> 
> - Cylinder 0: axis=[1,0,0], 36 faces - this looks like a real cylinder edge
> - Cylinders 1-27: All have axis_origin x≈21.12, but their axis_dir keeps rotating through different angles. These have 23 faces each with r≈2.0000. The axis directions are sweeping through an arc - they look like strips of a sphere being interpreted as cylinders!
> 
> This is exactly the problem. On a sphere with r=2.0, any great circle strip looks like a cylinder with r=2.0. The triangulation creates bands of faces on the sphere that each locally look like a cylinder, rotating around the sphere.
> 
> The real issue isn't axis drift during refitting—it's that sphere faces genuinely fit cylinder geometry. Each band of triangles on the sphere has consistent normals that align perfectly with a cylindrical surface. Now I need to figure out which of the 17 selected cylinders from stage 2.6 are actually real versus these false positives from the sphere.

There it is — the problem is clear. Cylinders 1-27 all have `axis_origin x≈21.12` but with progressively rotating axis directions. These are strips of the sphere being mistaken for cylinders. Let me see which 17 were actually selected:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -iE "Stage 2\.6|surface selection|selected surface|final surfaces" | head -30
```
```
Stage 2.6: Selected 52 surfaces (27 planar, 17 cylindrical, 8 spherical) covering 10847 faces
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | tail -80
```
<details>
<summary>Output (80 lines)</summary>

```
  Face 1: area = 18.8495 mm², orientation = Forward
  Face 2: area = 18.8496 mm², orientation = Forward
  Face 3: area = 18.8496 mm², orientation = Forward
  Face 4: area = 18.8496 mm², orientation = Forward
  Face 5: area = 18.8495 mm², orientation = Forward
  Face 6: area = 18.8495 mm², orientation = Forward
  Face 7: area = 18.8495 mm², orientation = Forward
  Face 8: area = 18.8496 mm², orientation = Forward
  Face 9: area = 18.8496 mm², orientation = Forward
  Face 10: area = 18.8495 mm², orientation = Forward
  Warning: face 11 (cylindrical) failed BRepCheck: edge/vertex consistency issue
  Face 11: area = 6.0787 mm², orientation = Forward
  Warning: face 12 (cylindrical) failed BRepCheck: edge/vertex consistency issue
  Face 12: area = 3.9248 mm², orientation = Forward
  Warning: face 13 (spherical) failed BRepCheck: edge/vertex consistency issue
  Face 13: area = 15.7947 mm², orientation = Forward
  Warning: face 14 (spherical) failed BRepCheck: edge/vertex consistency issue
  Face 14: area = 7.3863 mm², orientation = Forward
  Warning: face 15 (spherical) failed BRepCheck: edge/vertex consistency issue
  Face 15: area = 2.5529 mm², orientation = Forward
  Warning: face 16 (spherical) failed BRepCheck: edge/vertex consistency issue
  Face 16: area = 5.5694 mm², orientation = Forward
  Warning: face 17 (spherical) failed BRepCheck: edge/vertex consistency issue
  Face 17: area = 1.3744 mm², orientation = Forward
  Warning: face 18 (spherical) failed BRepCheck: edge/vertex consistency issue
  Face 18: area = -3.9279 mm², orientation = Forward
  Warning: face 19 (spherical) failed BRepCheck: edge/vertex consistency issue
  Face 19: area = 4.9881 mm², orientation = Forward
  Warning: face 20 (spherical) failed BRepCheck: edge/vertex consistency issue
  Face 20: area = 2.7576 mm², orientation = Forward
  Warning: face 21 (cylindrical) failed BRepCheck: edge/vertex consistency issue
  Face 21: area = 2.0980 mm², orientation = Forward
  Warning: face 22 (cylindrical) failed BRepCheck: edge/vertex consistency issue
  Face 22: area = 1.5688 mm², orientation = Forward
  Warning: face 23 (cylindrical) failed BRepCheck: edge/vertex consistency issue
  Face 23: area = 1.0062 mm², orientation = Forward
  Warning: face 24 (cylindrical) failed BRepCheck: edge/vertex consistency issue
  Face 24: area = 1.0374 mm², orientation = Forward
  Warning: face 25 (planar) failed BRepCheck: edge/vertex consistency issue
  Face 25: area = 0.0238 mm², orientation = Forward
  Warning: face 26 (planar) failed BRepCheck: edge/vertex consistency issue
  Face 26: area = 0.0185 mm², orientation = Forward
  Warning: face 27 (planar) failed BRepCheck: edge/vertex consistency issue
  Face 27: area = -0.0010 mm², orientation = Forward
  Warning: face 28 (planar) failed BRepCheck: edge/vertex consistency issue
  Face 28: area = 0.2060 mm², orientation = Forward
  Warning: face 29 (planar) failed BRepCheck: edge/vertex consistency is
... (truncated, 4769 chars total)
```
</details>

> 💭 Now I can see the output more clearly. There are 17 cylindrical faces in the output. The rounded cube should have 12 cylinder

> 💭 Now I can see the output more clearly. There are 17 cylindrical faces in the output. The rounded cube should have 12 cylinder edges, so 17 is 5 too many - that's the "bogus" cylinder problem.
> 
> Let me look at the selection/assignment process to see which specific cylinder hypotheses were selected. I need to understand which of the 608 cylinders became the 17 selected ones. Let me look at stage 2.6 output more carefully.

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -E "Assigned|assign|coverage|greedy|Stage 2\.[56]" | head -30
```
```
Stage 2.5: Deduce NURBS hypotheses (not yet implemented)
Stage 2.6: Selected 52 surfaces (27 planar, 17 cylindrical, 8 spherical) covering 10847 faces
```

Let me look at the stage 2.6 code to understand how surfaces are selected, and look at the sphere assignments to understand why sphere faces end up as cylinders:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -n -iE "face.*cylinder\|cylinder.*face\|sphere.*face\|Stage 2\.6\|Stage 3\." | head -20
```
```
(base) whitelily:brepper sethml$
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -E "face.*cylinder|cylinder.*face|sphere.*face|Stage 2.6|Stage 3." | head -20
```
<details>
<summary>Output (7 lines)</summary>

```
Stage 2.6: Selected 52 surfaces (27 planar, 17 cylindrical, 8 spherical) covering 10847 faces
Stage 3.1: Created 52 OCCT surfaces, 105 edges, 55 vertices
Stage 3.2: Tangency detection: 105 of 105 edges are tangent
Stage 3.3: Computed 105/105 edge curves (0 failed)
Stage 3.4: Created 52/52 OCCT faces
Stage 3.5: Constructed 1 shell(s) from 52 faces (52 faces in shells)
Stage 3.6: Constructed 1 solid(s) from 1 shell(s)
```
</details>

Let me look at the stage 2.6 code to understand the selection, and also look at how the selected cylinders correspond to geometry:

Searched for text `Stage 2.6` (`**/src/stage2.rs`), 4 results
- [stage2.rs:156](../src/stage2.rs#L156)
- [stage2.rs:2080](../src/stage2.rs#L2080)
- [stage2.rs:2573](../src/stage2.rs#L2573)
- [stage2.rs:2591](../src/stage2.rs#L2591)

<details>
<summary>Reading lines 2080-2200 of <a href="../src/stage2.rs#L2080-L2201">stage2.rs</a></summary>

```
// Stage 2.6: Select surfaces for reconstruction
// ---------------------------------------------------------------------------

/// Greedy area-based surface selection.
///
/// Selects hypotheses greedily by total mesh face area (largest first).
/// This naturally resolves conflicts: real surface hypotheses cover many faces
/// with large total area, while bogus hypotheses cover minimal area and lose.
///
/// After selection, updates hypothesis face/vertex lists to reflect only the
/// faces actually assigned to each selected surface.
fn select_surfaces(
    mesh: &ConnectedMesh,
    planar_hypotheses: &mut [PlanarHypothesis],
    cylindrical_hypotheses: &mut [CylindricalHypothesis],
    spherical_hypotheses: &mut [SphericalHypothesis],
) -> Vec<SelectedSurface> {
    let num_faces = mesh.faces.len();

    // Step 1: Compute geometric area of each mesh face.
    let face_areas: Vec<f64> = (0..num_faces)
        .map(|fi| face_area(&mesh.faces[fi], &mesh.vertices))
        .collect();

    // Step 2: Build candidate list from multi-face hypotheses.
    // Each candidate tracks its hypothesis type/index and original face set.
    struct Candidate {
        surface_type: u8, // 0=planar, 1=cylindrical, 2=spherical
        hypothesis_index: usize,
        faces: Vec<usize>,
    }

    let mut candidates: Vec<Candidate> = Vec::new();

    for (i, hyp) in planar_hypotheses.iter().enumerate() {
        if hyp.faces.len() > 1 {
            candidates.push(Candidate {
                surface_type: 0,
                hypothesis_index: i,
                faces: hyp.faces.clone(),
            });
        }
    }
    for (i, hyp) in cylindrical_hypotheses.iter().enumerate() {
        candidates.push(Candidate {
            surface_type: 1,
            hypothesis_index: i,
            faces: hyp.faces.clone(),
        });
    }
    for (i, hyp) in spherical_hypotheses.iter().enumerate() {
        candidates.push(Candidate {
            surface_type: 2,
            hypothesis_index: i,
            faces: hyp.faces.clone(),
        });
    }

    // Step 3: Greedy loop — select candidate with largest remaining area.
    let mut assigned = vec![false; num_faces];
    let mut selected: Vec<SelectedSurface> = Vec::new();

    loop {
        let mut best_ci = None;
        let mut best_area = 0.0_f64;

        for (ci, cand) in candidates.iter().enumerate() {
            let area: f64 = cand.faces.iter()
                .filter(|fi| !assigned[**fi])
                .map(|fi| face_areas[*fi])
                .sum();
            if area > best_area {
                best_area = area;
                best_ci = Some(ci);
            }
        }

        let Some(ci) = best_ci else { break };

        // Collect still-unassigned faces for the winning candidate.
        let sel_faces: Vec<usize> = candidates[ci].faces.iter()
            .filter(|fi| !assigned[**fi])
            .copied()
            .collect();

        for &fi in &sel_faces {
            assigned[fi] = true;
        }

        // Build vertex set for the selected faces.
        let sel_vertices: Vec<usize> = {
            let mut vset = HashSet::new();
            for &fi in &sel_faces {
                let f = &mesh.faces[fi];
                for vi in 0..f.vertex_count as usize {
                    vset.insert(f.vertex_indices[vi]);
                }
            }
            let mut v: Vec<usize> = vset.into_iter().collect();
            v.sort_unstable();
            v
        };

        // Update hypothesis face/vertex lists to reflect only the assigned faces,
        // and create the SelectedSurface entry.
        let selected_surface = match candidates[ci].surface_type {
            0 => {
                let idx = candidates[ci].hypothesis_index;
                planar_hypotheses[idx].faces = sel_faces;
                planar_hypotheses[idx].vertices = sel_vertices;
                SelectedSurface::Planar(idx)
            }
            1 => {
                let idx = cand
... (truncated, 4363 chars)
```
</details>

<details>
<summary>Reading lines 2200-2250 of <a href="../src/stage2.rs#L2200-L2251">stage2.rs</a></summary>

```
                spherical_hypotheses[idx].faces = sel_faces;
                spherical_hypotheses[idx].vertices = sel_vertices;
                SelectedSurface::Spherical(idx)
            }
        };
        selected.push(selected_surface);
    }

    // Step 4: Assign remaining faces to their single-face planar hypothesis.
    for (fi, assigned) in assigned.iter().enumerate().take(num_faces) {
        if !assigned {
            let pi = mesh.faces[fi].planar_hypothesis;
            assert!(pi >= 0, "face {fi} has no hypothesis assigned");
            selected.push(SelectedSurface::Planar(pi as usize));
        }
    }

    selected
}

/// Validate selected surfaces against a reference STEP file.
/// For each selected surface, project face centroids onto the fitted surface
/// and check that the projected points are within surface_tolerance of the STEP file.
fn compare_selected_surfaces(
    selected_surfaces: &[SelectedSurface],
    planar_hypotheses: &[PlanarHypothesis],
    cylindrical_hypotheses: &[CylindricalHypothesis],
    spherical_hypotheses: &[SphericalHypothesis],
    mesh: &ConnectedMesh,
    config: &Config,
) -> Result<(), Stage2CompareError> {
    let compare_shape = config.compare_shape.as_ref().unwrap();

    for (si, surface) in selected_surfaces.iter().enumerate() {
        let mut max_dist = 0.0_f64;

        let (hyp_type, face_indices) = match surface {
            SelectedSurface::Planar(i) => ("planar", &planar_hypotheses[*i].faces),
            SelectedSurface::Cylindrical(i) => ("cylindrical", &cylindrical_hypotheses[*i].faces),
            SelectedSurface::Spherical(i) => ("spherical", &spherical_hypotheses[*i].faces),
        };

        for &fi in face_indices {
            let centroid = face_centroid(&mesh.faces[fi], &mesh.vertices);

            let projected = match surface {
                SelectedSurface::Planar(i) => {
                    let hyp = &planar_hypotheses[*i];
                    let dist_to_plane = hyp.normal[0] * centroid[0]
                        + hyp.normal[1] * centroid[1]
                        + hyp.normal[2] * centroid[2]
```
</details>

> 💭 Now I understand the selection process. It's a greedy area-based selection - pick the hypothesis with the largest total area of unassigned faces, then mark its faces as assigned, repeat. 
> 
> The problem is: if a sphere (r=2.0) has many cylinder hypotheses that each cover strips of its surface, and those cylinders get selected BEFORE the sphere (because they have larger area), then the sphere would end up with fewer faces. But actually, the sphere (8 hypotheses covering 10337 faces) should have massive area and win over the individual cylinder strips.
> 
> Wait, but the output says: "17 cylindrical" — a rounded cube should have 12 cylinder edges. So there are 5 extra. Let me think about this...
> 
> 
> A rounded cube should have 6 flat faces, 12 cylindrical edges, and 8 spherical corners for 26 total surfaces, but the selection picked 52 surfaces total with 17 cylinders, which is way more than expected.
> 
> The extra planar surfaces are likely coming from the single-face assignments in step 4 of the selection process. I need to check what the coarse model actually produced, since the conversation summary mentions 4 correct cylinder hypotheses but 0 spheres, which doesn't make sense for a rounded cube.
> 
> Looking at the fine model output more carefully, I see cylinder 0 is the real edge with 36 faces, but cylinders 1-27 are strips of sphere faces being misidentified as cylinders with rotating axes and radius ≈2.0. The key realization is that on a sphere of radius R, any great-circle strip is geometrically an exact cylinder of radius R, not an approximation error—the normals along these strips are consistent with cylindrical geometry.
> 
> The root cause is that sphere faces fit cylinders just as well as spheres because cylinders locally match sphere bands. The BFS grows along these strips, creating many small cylinder hypotheses. The selection phase should prefer the sphere hypothesis (10337 faces) over individual cylinder strips (23 faces each), but the cylinder hypotheses might be getting selected first because they're claiming overlapping faces before the sphere consolidates them.
> 
> Actually, looking at the greedy selection logic more carefully—it picks the candidate with the largest remaining area, so the massive sphere should be selected first, marking all its faces as assigned and leaving the cylinder strips with zero area. But I'm realizing the issue: the cylinder BFS grows from actual cylinder-edge faces onto adjacent sphere-corner faces, so a single cylinder hypothesis can claim both real cylinder faces and sphere faces. When the greedy selection runs, it's not comparing a pure sphere against pure cylinders—it's comparing a sphere against hybrid cylinder hypotheses that have already absorbed some sphere faces.
> 
> Looking at the results, the spheres did win (8 spherical surfaces selected as expected), but I'm getting 52 total surfaces instead of 26. Breaking it down: 27 planar instead of 6, 17 cylindrical instead of 12, and 8 spherical as expected. The extra planar and cylindrical surfaces are single-face hypotheses added in step 4 to cover any remaining unassigned faces—21 extra planar and 5 extra cylindrical.
> 
> Now I need to understand where these 5 extra cylinders are coming from. They're likely covering faces that either weren't claimed by the main hypotheses or were left over after greedy selection. Let me check the actual selected cylinder details to see their final face counts after selection trimmed them down, and look at the face breakdown in the output to understand which faces are being assigned to which surfaces.
> 
> Looking at the face data, I can see the cylindrical faces split into two groups: about 10 proper cylinder faces with reasonable areas around 18.85 mm², and about 7 tiny cylindrical faces with areas under 2 mm² that appear to be remnant strips left on sphere corners or at boundaries. Those tiny ones are the bogus faces I need to account for.
> 
> Now I'm realizing these are BREP faces from the STEP file, not mesh faces—each selected surface becomes one BREP face. The small-area cylindrical faces like 11, 12, and 21-24 are the problematic cylinder strips I should focus on.
> 
> Let me refocus on what the user actually asked: investigate the bogus cylinders on the test file, revert the axis stability section in the development plan, and clean up debug prints. I'll tackle the last two first since they're straightforward, then dig deeper into the cylinder issue.
> 
> The user specifically chose rounded_cube_10_r2_fine.stl as the test case, so I should run it and understand the problem better before tackling all three tasks and committing. Rather than adding more logging, let me check if the existing verbose output already has enough detail to work with.

Let me look at the full stage 2.2 output and the selected cylinder details more carefully:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -E "  Cylinder [0-9]+:" | wc -l
```
```
     608
```

Those are the deduced cylinders. Let me find where the selected surfaces are listed:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep -v "REFIT\|DRIFT" | grep -E "Stage 2\.6" -A 200 | head -60
```
<details>
<summary>Output (60 lines)</summary>

```
Stage 2.6: Selected 52 surfaces (27 planar, 17 cylindrical, 8 spherical) covering 10847 faces
Stage 3.1: Created 52 OCCT surfaces, 105 edges, 55 vertices
  Face 0: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 1: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 2: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 3: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 4: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 5: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 6: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 7: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 8: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 9: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 10: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 11: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 12: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 13: spherical surface, 20 adjacent faces, 20 edges, 20 vertices
  Face 14: spherical surface, 17 adjacent faces, 17 edges, 17 vertices
  Face 15: spherical surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 16: spherical surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 17: spherical surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 18: spherical surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 19: spherical surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 20: spherical surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 21: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 22: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 23: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 24: cylindrical surface, 4 adjacent faces, 4 edges, 4 vertices
  Face 25: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 26: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 27: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 28: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 29: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 30: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 31: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 32: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 33: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 34: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 35: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 36: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 37: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 38: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 39: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 40: planar surface, 3 adjacent faces, 3 edges, 3 vertices
  Face 41: planar surface, 3 adja
... (truncated, 4093 chars total)
```
</details>

> 💭 Now I can see the picture more clearly:
> - Faces 0-12: 13 cylindrical surfaces (all with 4 adj faces, 4 edges)
> - Faces 13-14: 2 spherical surfaces (20 and 17 adj faces = big spheres)
> - Faces 15-20: 6 spherical surfaces (all with 3 adj faces = small spheres)
> - Faces 21-24: 4 cylindrical surfaces (all with 4 adj faces)
> - Faces 25-45: 21 planar surfaces (3 adj faces = tiny single-face)
> 
> 
> - Faces 46-51: 6 planar surfaces (4 adj faces = main flat faces)
> 
> So the 13 plus 4 cylinders gives me 17 total, which matches what I'm seeing. A rounded cube should have 12 cylinder edges, so I've got 5 extra somewhere. The 8 spherical surfaces check out, and the 6 main planar faces are definitely correct—the 21 smaller planar faces are just remainders. Now I need to figure out which 5 of these 17 cylinders are spurious by examining the sphere topology more carefully.
> 
> Looking at the spheres: two of them are large with 20 and 17 adjacent faces respectively, covering most of their corners, while the other six are tiny with only 3 adjacent faces each. This pattern suggests that on six of the eight corners, a cylinder hypothesis grabbed faces that should've belonged to the sphere, leaving behind these small remnants. On the other two corners, the sphere stayed mostly intact. The bogus cylinders are essentially strips of sphere faces that the greedy algorithm claimed before it could process those sphere faces properly.
> 
> But wait—the greedy selection should pick the largest area first, and a sphere hypothesis covers way more area than individual cylinder strips, so why would cylinders get selected first?
> 
> I'm realizing the cylinder hypotheses' faces might not actually overlap with the sphere faces at all. Let me reconsider the geometry here. Looking back at the cylinder details: there's one real cylinder with 36 faces and axis along the x-direction, then 27 bogus cylinders with 23 faces each positioned around the corners. The key question is which of these cylinder faces actually overlap with the sphere faces in the mesh.
> 
> On a rounded cube, the tessellation creates triangles in three regions: flat faces, rounded edges (cylindrical), and rounded corners (spherical). A triangle at the boundary between a cylinder edge and a sphere corner belongs geometrically to one or the other depending on how the tessellation was constructed.
> 
> When the cylinder BFS grows from a seed face on the cylindrical region, it can expand onto sphere-corner faces because locally those faces fit the cylinder hypothesis—a sphere of radius R looks like a cylinder of radius R in that neighborhood. So the final cylinder hypothesis ends up claiming both real cylinder faces and some sphere-corner faces. When the greedy selector picks this cylinder, it takes faces that should belong to the sphere, leaving the sphere selections with fewer faces.
> 
> We do see 8 spheres selected, which is the correct count, but some are tiny with only 3 adjacent faces because the cylinders stole their geometry. The real puzzle is why we're getting 17 cylinders instead of 12—those 5 extra cylinders must be coming from somewhere at the corners where three cylinders meet.
> 
> The issue might be that some sphere faces get claimed by cylinder hypotheses that aren't among the 12 real cylinders. If a cylinder BFS starts at a sphere corner and grows a new hypothesis purely on sphere faces, it would look like a cylinder with a strangely oriented axis since it's fitting a strip of the sphere surface. Looking at the 608 deduced cylinders, many are indeed strips on sphere corners with rotating axes, and if the greedy selection picks a large one of these before the sphere hypothesis can claim those faces, we end up with a bogus cylinder and a reduced sphere.
> 
> But wait — the sphere hypothesis covers 10,337 faces, which is vastly larger than any 23-face cylinder strip. In the greedy algorithm, we compute remaining area for each candidate, pick the one with the largest remaining area, assign its faces, and repeat. The sphere should be selected first since it has the most unassigned faces, which would mark all 10,337 faces as assigned and leave the cylinder strips with zero remaining area. So the bogus cylinders shouldn't actually get selected through the greedy process unless the cylinder hypothesis's face list somehow doesn't overlap with the sphere's faces the way I'm thinking.
> 
> Actually, there are 8 sphere hypotheses and 608 cylinder hypotheses covering different regions of the mesh. The spheres cover 10,337 out of 10,847 total faces — over 95% — which suggests they're growing beyond just the corner regions onto adjacent cylinder and planar surfaces because the tolerance is loose enough to locally fit those shapes too. So the sphere and cylinder hypotheses are competing against each other in the greedy selection, and I need to understand why the cylinders are winning in some cases. The sphere corners on the coarse model don't have enough faces to trigger sphere detection, which explains why zero spheres were found. For the fine model, I need to figure out whether those extra cylinders are actually on sphere-corner faces or if they're artifacts at boundaries. Rather than add more debug logging, let me trace through the deduced cylinder list more carefully—the first batch had origins clustered around x≈21.12 with rotating axes, which points to one corner of the cube.
> 
> Wait, that coordinate doesn't make sense for a 10mm cube with 2mm rounding. Let me just look at the full selected cylinder list and try to identify which ones are the legitimate 12 edges versus the 5 extras.
> 
> Adding logging to the greedy selection...
> 
> Actually, I need to tackle this more systematically. The user wants me to investigate what's causing these bogus cylinders, revert the axis stability section, and clean up the debug output. Let me start by comparing what brepper actually generates against the reference STEP file to pinpoint where the geometry diverges.

So we have 17 cylinders (should be 12) and 8 spheres (correct). Faces 15-20 are tiny sphere remnants (3 adj faces each), suggesting cylinder hypotheses stole faces from sphere corners. Let me look at the reference STEP file and how many mesh faces each sphere gets:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && ls tests/onshape/rounded_cube_10_r2_fine*
```
```
tests/onshape/rounded_cube_10_r2_fine.stl
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && ls tests/onshape/rounded_cube_10_r2*
```
```
tests/onshape/rounded_cube_10_r2_coarse.stl
tests/onshape/rounded_cube_10_r2_fine.stl
tests/onshape/rounded_cube_10_r2_medium.stl
tests/onshape/rounded_cube_10_r2.step
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv --compare tests/onshape/rounded_cube_10_r2.step 2>&1 | grep -v "REFIT\|DRIFT" | grep -iE "compare|error|distance|mismatch|wrong|surface [0-9]|Stage 2\.[23456]" | head -40
```

> 💭 Interesting. So the comparison says all selected surface centroids are within tolerance - meaning even the bogus cylinders are technically valid (their centroids are close to the reference STEP). But vertex 5 is 8.7e-2 mm away, which is a stage 3 issue.
> 
> Let me look at this from the mesh face count perspective. I need to understand which cylinder hypotheses are stealing sphere faces. Let me add some logging to the greedy selection to see the order and which faces get assigned to what.
> 
> Actually, wait. Let me re-read the output. The comparison says "all cylindrical hypothesis centroids within tolerance" and "all selected surface centroids within tolerance." This means the centroid-based comparison doesn't catch the bogus cylinders because the mesh face centroids, even when projected onto a bogus cylinder surface, are still close to the reference STEP.
> 
> 
> This makes sense: on a sphere of radius R, a cylinder of radius R with the right axis puts the projected points very close to the sphere surface. So the comparison can't distinguish them.
> 
> The real problem is that these bogus cylinders are fragmenting the sphere into extra BREP faces and causing BRepCheck errors in the output. The issue is fragmentation, not geometric accuracy. I need to look at how the BFS logic is allowing cylinder hypotheses to grow onto sphere faces in the first place.
> 
> When BFS seeds from a face and fits a cylinder, it grows to adjacent faces that pass the fitness test. Sphere faces near the equator relative to the cylinder axis will pass because the sphere locally looks cylindrical there. The cleanest fix would be to run a post-deduction filter: after deducing sphere hypotheses, eliminate any cylinder hypotheses whose faces are already claimed by a sphere.
> 
> But I'm realizing the greedy selection might be the real culprit—when a cylinder hypothesis covers both actual cylinder faces and sphere-corner faces, it can accumulate enough area to get selected first, then the sphere gets stuck with fewer remaining faces. Let me think through the geometry: a sphere at a cube corner with 2mm radius covers roughly 12.57 mm² per octant, and with 8 octants that's substantial, but if the cylinder is grabbing those corner faces too, it inflates its total area and wins the selection. Looking at the actual counts, the spheres are still being selected but with fewer faces, while those 5 extra cylinders are essentially stealing sphere surface patches through the BFS expansion. The real problem is that cylinder hypotheses are growing beyond their true geometric boundaries during the breadth-first search, creeping onto sphere faces. I could fix this by checking during the BFS whether adding a face would violate the curvature properties of a cylinder — since cylinders have zero Gaussian curvature while spheres don't, I could use that as a boundary condition to prevent the wrong faces from being absorbed.
> 
> Computing Gaussian curvature from the mesh is noisy though, so a cleaner approach would be to trim cylinder face lists after spheres are selected, removing any faces that fit a sphere better. But actually, the greedy selection should already handle this — it picks the largest candidate first, marks those faces as assigned, then remaining candidates can only claim unassigned faces. So if spheres are picked before cylinders, the cylinders shouldn't be able to grab those faces at all. The fact that they are suggests something else is going wrong in the selection logic.
> 
> Let me check which hypotheses are actually winning. The cylinders total around 10,760 faces and spheres around 10,337, but these overlap significantly. In the greedy loop, I need to see which individual hypothesis has the most unassigned area — whether it's a big cylinder or a big sphere. On the fine mesh, both could have substantial coverage, but I'm trying to figure out which one's actually getting picked first and why. 000+ faces per corner, while cylinder hypotheses max out at 36 faces. So the spheres should be selected first. And once they are, the cylinder strips within the sphere region get zeroed out.
> 
> But wait — the sphere hypotheses might be smaller than I think. With 10337 faces across 8 spheres, that's ~1292 faces per sphere. Each face has a small area (fine mesh). The cylinder hypotheses have 23-36 faces each, but each face might be larger.
> 
> Hmm, actually area is what matters, not face count. Let me estimate the surface areas: a sphere octant would be around 6.28 mm², but the sphere hypothesis expands beyond that onto adjacent faces, so with 1292 faces on a fine mesh, the total area could be much larger. A cylinder quarter-section with length ~6mm would be around 9.42 mm², which might actually be comparable or even larger per hypothesis than the sphere.
> 
> I need to instrument the greedy selection to see what's actually winning at each step. Let me add a temporary debug print to the greedy loop to trace the selection order and types.

All surfaces pass centroid comparison, which masks the problem. The bogus cylinders project correctly because a cylinder of radius R touching a sphere of radius R puts points close to the sphere surface. Let me add temporary logging to the greedy selection to understand the ordering:

<details>
<summary>Reading lines 2142-2168 of <a href="../src/stage2.rs#L2142-L2169">stage2.rs</a></summary>

```
    loop {
        let mut best_ci = None;
        let mut best_area = 0.0_f64;

        for (ci, cand) in candidates.iter().enumerate() {
            let area: f64 = cand.faces.iter()
                .filter(|fi| !assigned[**fi])
                .map(|fi| face_areas[*fi])
                .sum();
            if area > best_area {
                best_area = area;
                best_ci = Some(ci);
            }
        }

        let Some(ci) = best_ci else { break };

        // Collect still-unassigned faces for the winning candidate.
        let sel_faces: Vec<usize> = candidates[ci].faces.iter()
            .filter(|fi| !assigned[**fi])
            .copied()
            .collect();

        for &fi in &sel_faces {
            assigned[fi] = true;
        }

```
</details>

<details>
<summary>Edited: <a href="../src/stage2.rs#L2158-L2161">stage2.rs +3</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "src/stage2.rs",
      "lineHashes": "2157:md",
      "insertAfter": true,
      "content": "\n        let type_name = match candidates[ci].surface_type { 0 => \"planar\", 1 => \"cylinder\", _ => \"sphere\" };\n        eprintln!(\"GREEDY: selected {} hyp {} with area {:.4} ({} faces remaining)\", type_name, candidates[ci].hypothesis_index, best_area, sel_faces.len());"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"src/stage2.rs","startLine":2157,"endLine":2160}
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep "GREEDY"
```
```
(base) whitelily:brepper sethml$
```
**Exit code:** 1

Checked [stage2.rs](../src/stage2.rs), 3 problems found

The print references `sel_faces` before it's computed. Let me fix the ordering:

<details>
<summary>Reading lines 2155-2175 of <a href="../src/stage2.rs#L2155-L2176">stage2.rs</a></summary>

```
        }

        let Some(ci) = best_ci else { break };

        let type_name = match candidates[ci].surface_type { 0 => "planar", 1 => "cylinder", _ => "sphere" };
        eprintln!("GREEDY: selected {} hyp {} with area {:.4} ({} faces remaining)", type_name, candidates[ci].hypothesis_index, best_area, sel_faces.len());

        // Collect still-unassigned faces for the winning candidate.
        let sel_faces: Vec<usize> = candidates[ci].faces.iter()
            .filter(|fi| !assigned[**fi])
            .copied()
            .collect();

        for &fi in &sel_faces {
            assigned[fi] = true;
        }

        // Build vertex set for the selected faces.
        let sel_vertices: Vec<usize> = {
            let mut vset = HashSet::new();
            for &fi in &sel_faces {
```
</details>

<details>
<summary>Edited: <a href="../src/stage2.rs#L2159">stage2.rs -2</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "",
      "filePath": "src/stage2.rs",
      "lineHashes": "2159:pp\n2160:id"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"src/stage2.rs","startLine":2159,"endLine":2159}
```
</details>

<details>
<summary>Reading lines 2155-2172 of <a href="../src/stage2.rs#L2155-L2173">stage2.rs</a></summary>

```
        }

        let Some(ci) = best_ci else { break };


        // Collect still-unassigned faces for the winning candidate.
        let sel_faces: Vec<usize> = candidates[ci].faces.iter()
            .filter(|fi| !assigned[**fi])
            .copied()
            .collect();

        for &fi in &sel_faces {
            assigned[fi] = true;
        }

        // Build vertex set for the selected faces.
        let sel_vertices: Vec<usize> = {
            let mut vset = HashSet::new();
```
</details>

<details>
<summary>Edited: <a href="../src/stage2.rs#L2165-L2167">stage2.rs +2</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "        let type_name = match candidates[ci].surface_type { 0 => \"planar\", 1 => \"cylinder\", _ => \"sphere\" };\n        eprintln!(\"GREEDY: selected {} hyp {} with area {:.4} ({} faces remaining)\", type_name, candidates[ci].hypothesis_index, best_area, sel_faces.len());",
      "filePath": "src/stage2.rs",
      "insertAfter": true,
      "lineHashes": "2164:ny"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"src/stage2.rs","startLine":2164,"endLine":2166}
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --release --bin brepper -- tests/onshape/rounded_cube_10_r2_fine.stl -vv 2>&1 | grep "GREEDY"
```
<details>
<summary>Output (25 lines)</summary>

```
GREEDY: selected cylinder hyp 145 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 605 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 152 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 606 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 153 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 603 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 0 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 604 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 602 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 607 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 601 with area 9.4240 (36 faces remaining)
GREEDY: selected cylinder hyp 150 with area 5.8796 (34 faces remaining)
GREEDY: selected cylinder hyp 146 with area 3.8206 (22 faces remaining)
GREEDY: selected sphere hyp 3 with area 3.2582 (1384 faces remaining)
GREEDY: selected sphere hyp 0 with area 3.0786 (1279 faces remaining)
GREEDY: selected sphere hyp 4 with area 3.0786 (1279 faces remaining)
GREEDY: selected sphere hyp 2 with area 3.0786 (1279 faces remaining)
GREEDY: selected sphere hyp 1 with area 3.0786 (1279 faces remaining)
GREEDY: selected sphere hyp 7 with area 3.0786 (1279 faces remaining)
GREEDY: selected sphere hyp 6 with area 3.0786 (1279 faces remaining)
GREEDY: selected sphere hyp 5 with area 3.0786 (1279 faces remaining)
GREEDY: selected cylinder hyp 147 with area 2.0943 (12 faces remaining)
GREEDY: selected cylinder hyp 148 with area 1.5707 (9 faces remaining)
GREEDY: selected cylinder hyp 149 with area 0.7712 (5 faces remaining)
GREEDY: selected cylinder hyp 151 with area 0.7712 (5 faces remaining)
```
</details>

> 💭 Now I can see the problem clearly!
> 
> The selection order:
> 1. First, 11 cylinder hypotheses are selected with area 9.4240 (36 faces each) — these are the real cylinder edges
> 2. Then cylinder hyp 150 with 5.88 area (34 faces) and cylinder hyp 146 with 3.82 area (22 faces) — these are still decent-sized cylinders
> 3. Then ALL 8 sphere hypotheses are selected with areas 3.08-3.26 (1279-1384 faces each)
> 4. THEN 4 more cylinder hypotheses are selected with small areas (2.09, 1.57, 0.77, 0.77) with 12, 9, 5, 5 faces
> 
> 
> 5. The real issue is that cylinders have much higher area-per-surface ratios than sphere octants, so the scoring system is favoring them even when they shouldn't be selected
> 
> . The sphere hypotheses start with around 6.28 mm² of area for a full octant, but after the real cylinders claim their overlapping faces during greedy selection, the remaining sphere area drops to just 3.08 mm² — essentially half of what it should be.
> 
> The problem is that cylinder BFS extends from the actual cylinder edges onto the adjacent sphere corners, creating cylinder hypotheses that include sphere-corner faces. Since these cylinder candidates have larger areas than the sphere octant, they get selected first in the greedy algorithm, and they claim the overlapping boundary faces. This leaves the sphere hypotheses with only their remaining unclaimed faces, while the cylinders effectively steal the sphere-corner regions and create extra candidates in the process.
> 
> Now I'm checking whether the first 11 cylinders are actually stealing sphere faces or if they're legitimately the real cylinder edges. Computing the expected area for a quarter-cylinder on a cube with 10mm sides and 2mm rounding radius: the edge length is 6mm, and with a 2mm radius, the arc length is about 1.57mm, giving an area of roughly 9.42mm². This matches the observed 9.4240 exactly, so these cylinders are the genuine edges. But a cube should have 12 edges, not 11, so one is missing.
> 
> Looking at the remaining hypotheses, I see that hyp 150 has an area of 5.88 and hyp 146 has 3.82—these fragments likely represent the 12th cylinder edge split across multiple selections. The 4 small cylinders appearing after the spheres are probably remnant boundary strips from this splitting. So the real issue is that the sphere octants are losing area because the cylinder BFS is claiming half their faces, reducing them from about 6.28 to 3.08 each.
> 
> Now I'm checking the math: 11 correct cylinders plus 6 remaining ones gives 17 total, but I should only have 12 real cylinder edges. The extra 5 must be artifacts. If the 12th real cylinder is fragmented across those 6 remaining selections, their combined area of 14.90 divided by the expected 9.42 per cylinder gives roughly 1.58, suggesting the 12th cylinder is indeed split but also contaminated with bogus sphere-strip selections.
> 
> I see the root cause now: the BFS expansion in stage 2.2 is growing cylinder hypotheses beyond their actual boundaries onto sphere faces, creating mixed hypotheses that claim both real cylinder and sphere-corner faces. During greedy selection, these mixed hypotheses outcompete pure sphere ones because individual cylinder areas (~9.42) exceed sphere octant areas (~6.28), which steals faces from spheres and leaves fragmented cylinder remnants. The solution is to constrain the cylinder BFS so it doesn't expand onto sphere faces in the first place.
> 
> I'm considering several approaches: I could add curvature checks during the BFS itself, though that's tricky with mesh triangles. Or I could give sphere hypotheses a selection advantage in stage 2.6 by scaling their area or preferring them over cylinders for shared faces. Another option is a post-selection cleanup step that reassigns faces from fragmented cylinders back to their original sphere hypotheses. I could also try a two-pass selection strategy where spheres are selected first, though that risks the opposite problem. The most promising approach seems to be improving the BFS termination condition itself—checking something during the cylinder expansion to prevent it from crossing into sphere territory.
> 
> The key insight is that a real cylinder has zero curvature along its axis direction but non-zero curvature perpendicular to it, while a sphere has curvature in all directions. So I could check the curvature perpendicular to both the face normal and the cylinder axis—it should be near-zero for actual cylinder faces but non-zero for sphere faces. This is subtly different from the axis drift check the user originally proposed; it's about individual face consistency rather than overall axis stability. The real problem is that a cylinder of radius R passing through a sphere of radius R creates a band where the normals align with the cylinder, making sphere faces look like cylinder faces. The distinguishing factor is how the normal varies across the surface. On a sphere, the normal changes in both directions, unlike a cylinder where it stays constant along the axis. The BFS already validates normals against the fitted cylinder, but sphere faces near the equator can pass this check—the real issue is that the algorithm doesn't detect the difference in second-order curvature along the axis. The root cause is that cylinder hypotheses claim larger areas than sphere octants, so they win the greedy selection first.

Now the root cause is crystal clear. The selection order reveals everything:

1. **11 real cylinder edges** selected first (area 9.4240 mm² each, exactly matching the geometry: 6mm × π/2 × 2mm)
2. **2 cylinder fragments** selected next (areas 5.88, 3.82) — these are boundary strips claiming sphere-corner faces
3. **8 sphere octants** selected (area ~3.08 mm² each — only half of the expected ~6.28 mm² because cylinder BFS already claimed half their faces)
4. **4 more tiny cylinder fragments** selected after spheres (areas 2.09, 1.57, 0.77, 0.77)

**Root cause**: It's NOT axis drift during refitting. It's that **cylinder hypotheses have larger geometric area than sphere octants** (~9.42 vs ~6.28 mm²), so they win the greedy selection first. Since BFS grows cylinder hypotheses beyond real cylinder edges onto adjacent sphere-corner faces (which genuinely fit a cylinder of the same radius), they claim sphere faces. The spheres get selected later with reduced area, and leftover boundary strips become bogus extra cylinders.

Now let me do tasks 2 and 3 — revert the dev plan and clean up all debug prints.

**Todo list:**
- ✅ Investigate bogus cylinders root cause
- 🔄 Revert dev plan axis stability section
- ⬜ Clean up debug prints in stage2.rs
- ⬜ Commit changes

<details>
<summary>Reading lines 235-290 of <a href="../DEVELOPMENT_PLAN.md#L235-L291">DEVELOPMENT_PLAN.md</a></summary>

```

**BFS expansion** (run once per trial seed, using temporary assignment tracking):
- Pop faces from the queue and examine each neighbor ni:
    - If ni is already assigned a cylindrical hypothesis (in the mesh) or claimed by this trial, skip.
    - **Vertex distance check**: for each vertex of ni, compute `| ||v - axis_closest|| - radius |`. If all are within `--vertex-tolerance`, proceed to remaining checks (no re-fit needed yet). If any exceeds `2 * vertex_tolerance`, skip immediately (re-fitting cannot help, same reasoning as for planar hypotheses). If between 1x and 2x, flag for re-fit.
    - **Centroid validation**: compute the distance from ni's centroid to the fitted cylinder. If beyond `2 * surface_tolerance`, skip. If beyond `surface_tolerance`, flag for re-fit (even if all vertices were within tolerance).
    - **Convexity check**: compute the vector from the axis to ni's centroid. Check that ni's face normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.
    - **Angular tolerance check**: for each of ni's mesh neighbors that is already assigned to this hypothesis, compute the dihedral angle between ni and that neighbor. If any exceeds `--angular-tolerance`, reject. Checking all assigned neighbors (not just the BFS parent) provides defense-in-depth against creased NURBS surfaces where BFS might approach a face from a low-angle direction while a high-angle assigned neighbor exists on a different axis.
    - **Re-fit attempt**: if vertex distance or centroid validation flagged a re-fit, re-fit the cylinder from all current faces plus ni (see Cylinder Fitting below). Check that **all** vertices (existing and new) are within tolerance of the re-fitted cylinder, **all** face centroids lie within `--surface-tolerance` of the fitted cylinder surface, and the refitted axis direction satisfies the **axis stability check** (see below). If so, accept the re-fit; otherwise skip ni.
    - If accepted: add ni to the trial's face list and vertex set, push ni onto the BFS queue.
- After BFS completes: final re-fit from all accumulated faces and vertices. Compute error metrics.
- **Minimum face count**: Require at least 3 faces in a cylindrical hypothesis. This is technically redundant with angular coverage validation (which inherently requires 3+ angular clusters, hence 3+ faces), but serves as a cheap O(1) early rejection before the more expensive angular coverage computation.

**Angular coverage validation** (applied to the best trial for each starting face):

The problem: any two non-coplanar faces fit *some* cylinder. A bogus seed that doesn't span the cylinder's circumference can grow into a small hypothesis via BFS, consuming faces that should belong to a correct hypothesis from a better seed. The angular coverage check ensures the hypothesis has genuine circumferential support — faces distributed around the cylinder, not clustered on one side.

Algorithm:
1. For each face in the hypothesis, compute the centroid and project it onto the cylinder's angular coordinate θ. Given the cylinder's axis direction `a`, axis origin `o`, and orthogonal basis vectors `u`, `w` perpendicular to `a`: for centroid `c`, compute `radial = c - o - ((c - o) · a) * a`, then `θ = atan2(radial · w, radial · u)`.
2. Sort the θ values (one per face).
3. Compute all N gaps between consecutive sorted θ values, **including the wraparound gap**: `gap_wrap = θ[0] + 2π - θ[N-1]`.
4. The **largest gap** is the "empty arc" — the angular region not covered by any face. The **span** is `2π - largest_gap`.
5. Among the remaining N-1 gaps, the **second-largest gap** must be `≤ span / 3`.

This ensures the hypothesis has at least 3 distinct angular clusters of faces around the cylinder. A bogus cylinder where all faces are side-by-side in a narrow arc will fail: e.g., 5 faces clustered in a 30° arc have span ≈ 30°, but 
... (truncated, 11310 chars)
```
</details>

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#235-235), lines 235 to 290

> 💭 I need to find the axis stability section and revert it. Looking at the text, I see the "Axis stability during refit" section starting around line 267. Let me identify exactly what was added in the previous commit and revert it. Let me also look at the modification to the refit description in the BFS expansion section (line 243).
> 
> Looking at the text:
> 1. Line 243 mentions "the refitted axis direction satisfies the **axis stability check** (see below)" - this was added
> 2. Lines 267-271 (approximately) contain the "Axis stability during refit" paragraph
> 
> Let me read the git diff to see exactly what was changed.

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git log --oneline -5
```
<details>
<summary>Output (5 lines)</summary>

```
d93b8c9 (HEAD -> main) Add axis stability check to cylindrical hypothesis BFS in development plan
600c5ad Handle pill/capsule shapes: closed-loop sphere-cylinder tangent edges and pole-safe hemisphere faces
fef5667 Improve -v debug printing of hypotheses; add -vv per-face detail
cd78cc7 Remove multi-face planar restrictions from cylinder seeding
ea34d5b Skip faces with null triangulation handles in viewer
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git diff d93b8c9^..d93b8c9 -- DEVELOPMENT_PLAN.md
```
<details>
<summary>Output (26 lines)</summary>

```
diff --git a/DEVELOPMENT_PLAN.md b/DEVELOPMENT_PLAN.md
index 0cc4a9c..4d25ee8 100644
--- a/DEVELOPMENT_PLAN.md
+++ b/DEVELOPMENT_PLAN.md
@@ -240,7 +240,7 @@ The algorithm uses BFS region growing, analogous to stage 2.1 but seeded from pa
     - **Centroid validation**: compute the distance from ni's centroid to the fitted cylinder. If beyond `2 * surface_tolerance`, skip. If beyond `surface_tolerance`, flag for re-fit (even if all vertices were within tolerance).
     - **Convexity check**: compute the vector from the axis to ni's centroid. Check that ni's face normal agrees with the hypothesis convexity (dot product with radial vector has the expected sign). Reject if inconsistent — this prevents merging the inner and outer surfaces of a thin-walled cylinder.
     - **Angular tolerance check**: for each of ni's mesh neighbors that is already assigned to this hypothesis, compute the dihedral angle between ni and that neighbor. If any exceeds `--angular-tolerance`, reject. Checking all assigned neighbors (not just the BFS parent) provides defense-in-depth against creased NURBS surfaces where BFS might approach a face from a low-angle direction while a high-angle assigned neighbor exists on a different axis.
-    - **Re-fit attempt**: if vertex distance or centroid validation flagged a re-fit, re-fit the cylinder from all current faces plus ni (see Cylinder Fitting below). Check that **all** vertices (existing and new) are within tolerance of the re-fitted cylinder and **all** face centroids lie within `--surface-tolerance` of the fitted cylinder surface. If so, accept the re-fit; otherwise skip ni.
+    - **Re-fit attempt**: if vertex distance or centroid validation flagged a re-fit, re-fit the cylinder from all current faces plus ni (see Cylinder Fitting below). Check that **all** vertices (existing and new) are within tolerance of the re-fitted cylinder, **all** face centroids lie within `--surface-tolerance` of the fitted cylinder surface, and the refitted axis direction satisfies the **axis stability check** (see below). If so, accept the re-fit; otherwise skip ni.
     - If accepted: add ni to the trial's face list and vertex set, push ni onto the BFS queue.
 - After BFS completes: final re-fit from all accumulated faces and vertices. Compute error metrics.
 - **Minimum face count**: Require at least 3 faces in a cylindrical hypothesis. This is technically redundant with angular coverage validation (which inherently requires 3+ angular clusters, hence 3+ faces), but serves as a cheap O(1) early rejection before the more expensive angular coverage computation.
@@ -264,6 +264,12 @@ This ensures the hypothesis has at least 3 distinct angular clusters of faces ar
 
 **Computational cost**: For each starting face with K valid seed partners, up to K trial BFS runs are performed, but early termination (see above) typically reduces this to 1–2 full runs plus several near-instant abandonments. Typi
... (truncated, 5365 chars total)
```
</details>

Now I can see exactly what to revert. Let me do both the dev plan revert and the debug print cleanup simultaneously. First let me find all the debug prints in stage2.rs:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "REFIT\|DRIFT\|GREEDY\|seed_dir\|axis_dot" src/stage2.rs
```
<details>
<summary>Output (19 lines)</summary>

```
32:/// has a vertex farther than `REFIT_SKIP_MULTIPLIER × vertex_tol` from the
35:const REFIT_SKIP_MULTIPLIER: f64 = 2.0;
356:                        if abs_d > REFIT_SKIP_MULTIPLIER * vertex_tol {
975:    let seed_dir = current_dir;
1063:                    if d > REFIT_SKIP_MULTIPLIER * vertex_tol {
1080:            if centroid_dist > REFIT_SKIP_MULTIPLIER * surface_tol {
1127:                let axis_dot = dot3(&current_dir, &new_dir).abs();
1128:                let axis_drift_from_seed = dot3(&seed_dir, &new_dir).abs();
1129:                eprintln!("  REFIT: seed ({},{}) face_count={} adding face {} | axis_dot(old,new)={:.9} axis_dot(seed,new)={:.9} | old_dir=[{:.6},{:.6},{:.6}] new_dir=[{:.6},{:.6},{:.6}] seed_dir=[{:.6},{:.6},{:.6}] | old_r={:.6} new_r={:.6}",
1131:                    axis_dot, axis_drift_from_seed,
1134:                    seed_dir[0], seed_dir[1], seed_dir[2],
1154:                let axis_dot = dot3(&current_dir, &best.axis_direction).abs();
1155:                if axis_dot > 1.0 - 1e-6
1182:        let axis_drift_from_seed = dot3(&seed_dir, &final_dir).abs();
1184:            eprintln!("  FINAL REFIT DRIFT: seed ({},{}) {} faces | axis_dot(seed,final)={:.9} | seed_dir=[{:.6},{:.6},{:.6}] final_dir=[{:.6},{:.6},{:.6}] | seed_r={:.6} final_r={:.6}",
1187:                seed_dir[0], seed_dir[1], seed_dir[2],
1884:                        if d > REFIT_SKIP_MULTIPLIER * vertex_tol {
1898:                if centroid_dist > REFIT_SKIP_MULTIPLIER * surface_tol {
2166:        eprintln!("GREEDY: selected {} hyp {} with area {:.4} ({} faces remaining)", type_name, candidates[ci].hypothesis_index, best_area, sel_faces.len());
```
</details>

Now let me read the specific debug sections to understand what to remove:

<details>
<summary>Reading lines 970-980 of <a href="../src/stage2.rs#L970-L981">stage2.rs</a></summary>

```
    // Fit cylinder to seed pair
    let (mut current_origin, mut current_dir, mut current_radius) =
        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)?;

    // DEBUG: save seed axis direction for drift tracking
    let seed_dir = current_dir;

    // Verify seed: all vertices within tolerance
    if !all_vertices_within_cylinder_tolerance(
        &vertex_set, &current_origin, &current_dir, current_radius,
        vertex_tol, &mesh.vertices,
```
</details>

<details>
<summary>Reading lines 1120-1145 of <a href="../src/stage2.rs#L1120-L1146">stage2.rs</a></summary>

```
                    }
                }
                if !centroids_ok {
                    continue;
                }

                // Accept re-fit
                let axis_dot = dot3(&current_dir, &new_dir).abs();
                let axis_drift_from_seed = dot3(&seed_dir, &new_dir).abs();
                eprintln!("  REFIT: seed ({},{}) face_count={} adding face {} | axis_dot(old,new)={:.9} axis_dot(seed,new)={:.9} | old_dir=[{:.6},{:.6},{:.6}] new_dir=[{:.6},{:.6},{:.6}] seed_dir=[{:.6},{:.6},{:.6}] | old_r={:.6} new_r={:.6}",
                    seed_fi, seed_ni, face_list.len(), cni,
                    axis_dot, axis_drift_from_seed,
                    current_dir[0], current_dir[1], current_dir[2],
                    new_dir[0], new_dir[1], new_dir[2],
                    seed_dir[0], seed_dir[1], seed_dir[2],
                    current_radius, new_radius);
                current_origin = new_origin;
                current_dir = new_dir;
                current_radius = new_radius;
            }

            // Accept this face
            trial_claimed.insert(cni);
            face_list.push(cni);
            for &vi in &cni_vi[..cni_vc] {
                vertex_set.insert(vi);
```
</details>

<details>
<summary>Reading lines 1170-1200 of <a href="../src/stage2.rs#L1170-L1201">stage2.rs</a></summary>

```
                    {
                        return None; // Redundant trial
                    }
                }
            }
        }
    }

    // Final re-fit
    if let Some((final_origin, final_dir, final_radius)) =
        fit_cylinder(&face_list, &vertex_set, &mesh.faces, &mesh.vertices)
    {
        let axis_drift_from_seed = dot3(&seed_dir, &final_dir).abs();
        if axis_drift_from_seed < 0.999 {
            eprintln!("  FINAL REFIT DRIFT: seed ({},{}) {} faces | axis_dot(seed,final)={:.9} | seed_dir=[{:.6},{:.6},{:.6}] final_dir=[{:.6},{:.6},{:.6}] | seed_r={:.6} final_r={:.6}",
                seed_fi, seed_ni, face_list.len(),
                axis_drift_from_seed,
                seed_dir[0], seed_dir[1], seed_dir[2],
                final_dir[0], final_dir[1], final_dir[2],
                current_radius, final_radius);
        }
        current_origin = final_origin;
        current_dir = final_dir;
        current_radius = final_radius;
    }

    // Compute error metrics
    let mut error_max = 0.0_f64;
    let mut error_abs_sum = 0.0_f64;
    for &vi in &vertex_set {
        let d = vertex_to_cylinder_distance(
```
</details>

<details>
<summary>Reading lines 2160-2175 of <a href="../src/stage2.rs#L2160-L2176">stage2.rs</a></summary>

```
        // Collect still-unassigned faces for the winning candidate.
        let sel_faces: Vec<usize> = candidates[ci].faces.iter()
            .filter(|fi| !assigned[**fi])
            .copied()
            .collect();
        let type_name = match candidates[ci].surface_type { 0 => "planar", 1 => "cylinder", _ => "sphere" };
        eprintln!("GREEDY: selected {} hyp {} with area {:.4} ({} faces remaining)", type_name, candidates[ci].hypothesis_index, best_area, sel_faces.len());

        for &fi in &sel_faces {
            assigned[fi] = true;
        }

        // Build vertex set for the selected faces.
        let sel_vertices: Vec<usize> = {
            let mut vset = HashSet::new();
            for &fi in &sel_faces {
```
</details>

Now I have a clear picture. Let me do all the edits: revert the dev plan AND remove all debug prints from stage2.rs:

<details>
<summary>Edited: <a href="../src/stage2.rs#L974">stage2.rs -2</a>, <a href="../src/stage2.rs#L1125">-9</a>, <a href="../src/stage2.rs#L1171">-9</a>, <a href="../src/stage2.rs#L2145">-2</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "src/stage2.rs",
      "lineHashes": "974:pn\n975:yc",
      "content": ""
    },
    {
      "filePath": "src/stage2.rs",
      "lineHashes": "1127:sb\n1128:nk\n1129:es\n1130:pf\n1131:al\n1132:rz\n1133:cw\n1134:zg\n1135:ki",
      "content": ""
    },
    {
      "filePath": "src/stage2.rs",
      "lineHashes": "1182:rx\n1183:qz\n1184:mk\n1185:ly\n1186:ng\n1187:hh\n1188:cv\n1189:qa\n1190:cy",
      "content": ""
    },
    {
      "filePath": "src/stage2.rs",
      "lineHashes": "2165:pp\n2166:id",
      "content": ""
    }
  ]
}
```
**Output:**
```
{
  "applied": 4,
  "failed": 0,
  "files": [
    {
      "file": "src/stage2.rs",
      "startLine": 974,
      "endLine": 2144
    }
  ]
}
```
</details>

Now revert the `DEVELOPMENT_PLAN.md` changes. I need to revert two changes: the refit description modification and the axis stability section:

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#240-240), lines 240 to 247

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#264-264), lines 264 to 280

Replacing 8 lines with 2 lines in [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md)

```
Now let me verify everything compiles and tests pass:

**Todo list:**
- ✅ Investigate bogus cylinders root cause
- ✅ Revert dev plan axis stability section
- ✅ Clean up debug prints in stage2.rs
- 🔄 Commit changes

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo clippy --all-targets 2>&1
```
<details>
<summary>Output (18 lines)</summary>

```
    Checking brepper v0.1.0 (/Users/sethml/src/brepper)
warning: unused variable: `planar`
    --> src/stage2.rs:2743:13
     |
2743 |         let planar = deduce_planar_hypotheses(&mut mesh, 1e-5);
     |             ^^^^^^ help: if this is intentional, prefix it with an underscore: `_planar`
     |
     = note: `#[warn(unused_variables)]` (part of `#[warn(unused)]`) on by default

warning: unused variable: `planar`
    --> src/stage2.rs:2782:13
     |
2782 |         let planar = deduce_planar_hypotheses(&mut mesh, 1e-5);
     |             ^^^^^^ help: if this is intentional, prefix it with an underscore: `_planar`

warning: `brepper` (lib test) generated 2 warnings (run `cargo clippy --fix --lib -p brepper --tests` to apply 2 suggestions)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.55s
real  1.6s
```
</details>

> 💭 There are 2 clippy warnings about unused variables. These are pre-existing, not from my changes. But the AGENTS.md says "The codebase should have zero clippy warnings at all times." Let me check if these warnings exist before my changes.

Two pre-existing clippy warnings about unused variables. Let me fix those quickly:

<details>
<summary>Reading lines 2740-2790 of <a href="../src/stage2.rs#L2740-L2791">stage2.rs</a></summary>

```
    #[test]
    fn cube_angular_tolerance_rejects_cylinder() {
        let mut mesh = load_stage1("manual/cube.stl");
        let planar = deduce_planar_hypotheses(&mut mesh, 1e-5);
        // 17.5° angular tolerance: cube faces meet at 90°, so no cylinders
        let cyls = deduce_cylindrical_hypotheses(
            &mut mesh, 1e-5, 0.4, 17.5_f64.to_radians(),
        );
        assert_eq!(cyls.len(), 0, "cube should produce 0 cylinders at 17.5° angular tolerance");
    }

    #[test]
    fn cube_angular_tolerance_rejects_sphere() {
        let mut mesh = load_stage1("manual/cube.stl");
        let planar = deduce_planar_hypotheses(&mut mesh, 1e-5);
        let _ = deduce_cylindrical_hypotheses(
            &mut mesh, 1e-5, 0.4, 17.5_f64.to_radians(),
        );
        // 17.5° angular tolerance: cube faces meet at 90°, so no spheres
        let sphs = deduce_spherical_hypotheses(
            &mut mesh, &planar, 1e-5, 0.4, 17.5_f64.to_radians(), 100.0,
        );
        assert_eq!(sphs.len(), 0, "cube should produce 0 spheres at 17.5° angular tolerance");
    }

    #[test]
    fn cube_high_angular_tolerance_allows_sphere() {
        let mut mesh = load_stage1("manual/cube.stl");
        let planar = deduce_planar_hypotheses(&mut mesh, 1e-5);
        let _ = deduce_cylindrical_hypotheses(
            &mut mesh, 1e-5, 0.4, 91.0_f64.to_radians(),
        );
        // 91° angular tolerance: exceeds 90° dihedral angle of cube
        let sphs = deduce_spherical_hypotheses(
            &mut mesh, &planar, 1e-5, 0.4, 91.0_f64.to_radians(), 100.0,
        );
        assert!(!sphs.is_empty(), "cube should produce spheres at 91° angular tolerance");
    }

    #[test]
    fn cylinder_detected_at_default_angular_tolerance() {
        let mut mesh = load_stage1("ccad/generated/simple_cylinder.stl");
        let planar = deduce_planar_hypotheses(&mut mesh, 1e-5);
        let cyls = deduce_cylindrical_hypotheses(
            &mut mesh, 1e-5, 0.4, 17.5_f64.to_radians(),
        );
        assert_eq!(cyls.len(), 1, "simple cylinder should produce 1 cylinder hypothesis");
        assert!(cyls[0].convex);
        assert!((cyls[0].radius - 10.0).abs() < 0.01);
    }

```
</details>

<details>
<summary>Edited: <a href="../src/stage2.rs#L2743-L2744">stage2.rs -1+1</a>, <a href="../src/stage2.rs#L2782-L2783">-1+1</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "        let _planar = deduce_planar_hypotheses(&mut mesh, 1e-5);",
      "filePath": "src/stage2.rs",
      "lineHashes": "2743:uw"
    },
    {
      "content": "        let _planar = deduce_planar_hypotheses(&mut mesh, 1e-5);",
      "filePath": "src/stage2.rs",
      "lineHashes": "2782:uw"
    }
  ]
}
```
**Output:**
```
{
  "applied": 2,
  "failed": 0,
  "files": [
    {
      "file": "src/stage2.rs",
      "startLine": 2743,
      "endLine": 2782
    }
  ]
}
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo clippy --all-targets 2>&1
```
```
    Checking brepper v0.1.0 (/Users/sethml/src/brepper)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.25s
real  1.3s
```

Clean. Let me run the tests:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo test 2>&1
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo test 2>&1 | tail -5
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo test 2>&1 | grep -E "test result|FAILED"
```

All 323 tests pass. Now let me also verify the dev plan looks correct after the revert:

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#264-264), lines 264 to 276

Good, the revert is clean. Let me also verify there's no leftover debug code by checking for any remaining eprintln with our debug markers:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "seed_dir" src/stage2.rs
```
```
(base) whitelily:brepper sethml$
```
**Exit code:** 1

Now let me update `LEARNINGS.md` with the empirical findings, and commit:

Read [LEARNINGS.md](../LEARNINGS.md#1-1), lines 1 to 50

Read [LEARNINGS.md](../LEARNINGS.md#50-50), lines 50 to 100

**Terminal:**
```sh
cd /Users/sethml/src/brepper && wc -l LEARNINGS.md
```
```
     254 LEARNINGS.md
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && tail -30 LEARNINGS.md
```
<details>
<summary>Output (30 lines)</summary>

```
- `BRepBuilderAPI_Sewing`: Create with `Sewing::new_real(tolerance)` where tolerance = `vertex_tolerance_mm`. Add all faces via `.add(face.as_shape())`, then `.perform(&ProgressRange::new())`.
- The sewed shape can contain mixed types: `Shell` (most common), `Face` (single-face models like `simple_sphere`), `Solid`, `Compound`, `Compsolid`. Iterate sub-shapes by type to extract all shells.
- For `Face` results, wrap in a `TopoDS_Shell` manually: `topo_ds::Builder::new()`, `.make_shell(&mut shell)`, `.add(shell.as_shape_mut(), face.as_shape())`.
- `ShapeAnalysis_Shell::check_oriented_shells(shape, false, false)` returns `true` when BAD edges are found (orientation problems), not when the shell is valid. The return value is inverted from what you might expect.
- `ShapeFix_Shell::new_shell(shell)`, then `.fix_face_orientation(shell, false, false)`, `.perform(&ProgressRange::new())`, `.shell()` returns the fixed shell.
- `top_abs::ShapeEnum::Compsolid` (not `CompSolid`) — watch the capitalization.
- `topo_ds::shell_shape(shape)` downcasts `&Shape` to `&Shell` reference; Shell implements `CppCopyable`/`ToOwned` so you can `.to_owned()` to get `OwnedPtr<Shell>`.
- Sewing statistics: `.nb_free_edges()`, `.nb_multiple_edges()`, `.nb_contigous_edges()` — note the OCCT typo "contigous" (not "contiguous").

### Stage 3.6 solid construction
- `shape_fix::Solid::new()`, then `.solid_from_shell(shell)` creates an `OwnedPtr<topo_ds::Solid>` with automatic orientation handling.
- `topo_ds::solid(shape)` downcasts `&Shape` to `&Solid`; `Solid` is `CppCopyable`/`ToOwned`.
- `b_rep_g_prop::volume_properties_shape_gprops_bool3(shape, &mut gprops, only_closed, skip_shared, use_triangulation)` computes volume. `gprops.mass()` returns the signed volume.
- Volume was historically wrong for models with periodic faces (cylinders/spheres) due to pcurve and vertex ordering issues. These are now resolved — all test models produce volumes matching STEP references to within ~1e-7 relative difference.
- Face orientation is handled correctly by the combination of `ShapeFix_Shell::Perform()` (edge consistency + pcurve repair) and `ShapeFix_Solid::SolidFromShell()` (global orientation via `BRepClass3d_SolidClassifier::PerformInfinitePoint`). Do NOT post-process individual face orientations — flipping individual faces breaks edge consistency, which causes both `BRepGProp::VolumeProperties` and `SolidFromShell` to give wrong results. This was extensively tested: 6/8 models produce perfect volume without any post-processing, vs. various failures when individual faces were flipped.
- `b_rep_check::Analyzer::new_shape_bool(shape, true)` + `.is_valid()` validates a solid.

### Stage 4.1 STEP output
- `step_control::Writer::new()` creates a writer. `writer.transfer_shape_stepmodeltype_bool_progressrange(shape, mode, compgraph, &progress)` transfers shapes. `writer.write(path)` writes the file.
- `step_control::StepModelType::Asis` preserves the exact geometry (no conve
... (truncated, 5074 chars total)
```
</details>

Replacing 1 lines with 9 lines in [LEARNINGS.md](../LEARNINGS.md)

Now commit:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git add -A && git diff --cached --stat
```
<details>
<summary>Output (7 lines)</summary>

```
 DEVELOPMENT_PLAN.md |    8 +-
 LEARNINGS.md        |    8 +
 out.step            | 6385 +++++++++++++++++++++++++++++++++++++++++++++++++++
 pill_coarse.step    |  216 ++
 pill_fine.step      |  216 ++
 src/stage2.rs       |    6 +-
 6 files changed, 6830 insertions(+), 9 deletions(-)
```
</details>

> 💭 There are some STEP files in the root that shouldn't be committed (out.step, pill_coarse.step, pill_fine.step). Those look like leftover output files. Let me unstage them.

There are stray STEP files that shouldn't be committed. Let me unstage those:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git reset HEAD out.step pill_coarse.step pill_fine.step && git diff --cached --stat
```
```
 DEVELOPMENT_PLAN.md | 8 +-------
 LEARNINGS.md        | 8 ++++++++
 src/stage2.rs       | 6 ++++--
 3 files changed, 13 insertions(+), 9 deletions(-)
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git commit -F - <<'EOF'
Revert axis stability plan, remove debug prints, document cylinder-sphere root cause

- Revert the "axis stability during refit" section from DEVELOPMENT_PLAN.md.
  Empirical testing showed zero axis drift on any committed cylinder hypothesis
  across all test models (axis_dot ≥ 0.999999996 on 921 refits for
  rounded_cube_10_r2_medium). The fix would solve a non-problem.

- Remove temporary debug eprintln! statements from stage2.rs that were added
  for axis drift investigation (REFIT, DRIFT, GREEDY prints).

- Document the actual root cause in LEARNINGS.md: cylinder BFS extends onto
  sphere-corner faces because a sphere of radius R locally matches a cylinder
  of radius R. Cylinder hypotheses have larger area than sphere octants, so
  they win greedy selection first, fragmenting sphere patches.

- Fix 2 pre-existing clippy warnings (unused variables in tests).

---
Generated with assistance from Claude Opus 4 (Anthropic, 2025)

User prompt(s):
- "Yes, do all three." (referring to: 1. investigate bogus cylinders on a specific model,
  2. revert the development plan axis stability section, 3. clean up debug prints)
- "Use rounded_cube_10_r2_fine.stl."
EOF
```
```
[main 3aec8df] Revert axis stability plan, remove debug prints, document cylinder-sphere root cause
 3 files changed, 13 insertions(+), 9 deletions(-)
```

**Todo list:**
- ✅ Investigate bogus cylinders root cause
- ✅ Revert dev plan axis stability section
- ✅ Clean up debug prints in stage2.rs
- ✅ Commit changes

Now export the chat log:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && ( ../vscode-export-chat-log/export-chat-log.py --wait && git add agent-logs/ && git commit --amend --no-edit ) &
```
```
(base) whitelily:brepper sethml$
```

All three tasks done:

1. **Investigated bogus cylinders** on `rounded_cube_10_r2_fine.stl` — the greedy selection log shows 11 real cylinder edges (area 9.42 each) are selected before all 8 spheres (area ~3.08 each). The root cause: cylinder BFS extends onto sphere faces (a sphere of radius R locally matches a cylinder of radius R), and cylinders have ~50% more area per surface than sphere octants, so they always win selection first, fragmenting sphere patches into 17 cylinders instead of 12.

2. **Reverted** the axis stability section from `DEVELOPMENT_PLAN.md` — the empirical data showed it would solve a non-problem.

3. **Cleaned up** all debug prints (REFIT/DRIFT/GREEDY eprintln! and `seed_dir` variable) from stage2.rs, plus fixed 2 pre-existing clippy warnings.

There once was a drift that we feared,
Would make cylinder axes get weird,
But the data said "nope!"
It's just area and scope—
The spheres lose their turf, as we've cleared.

*2026-03-08 11:54 · 814s · Words: 3,557 in · 6,880 out · 73,624 ctx · 62 rounds*

---
