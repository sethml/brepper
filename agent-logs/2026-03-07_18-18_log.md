# STL Face Orientation Error Handling Enhancement

- **Date:** 2026-03-07 18:18 – 18:30
- **Session ID:** 7ed284b2-30a3-4f9f-9178-e0afdb64ce2a
- **Model:** Claude Opus 4.6
- **Turns:** 1
- **Tool calls:** 103
- **Thinking blocks:** 2
- **Input words:** 4,734
- **Output words:** 3,580
- **Prompt tokens (last round):** 86,619
- **API rounds:** 62
- **Total elapsed:** 684s

## Table of Contents

1. [There have been quite a few confusing issues with face orientation errors leading to incorrect so...](#user-1) (Claude Opus 4.6)

---

<a id="user-1"></a>

## User (1)

> There have been quite a few confusing issues with face orientation errors leading to incorrect solid generation. Add to the --compare handling to validate stl face and deduced surface orientation to the step surface orientation, and let me know where they differ. Then commit.

*2026-03-07 18:19*

### Assistant — Claude Opus 4.6

> 💭 The user wants me to add orientation validation to the --compare mode that

> 💭 The user wants me to add orientation validation to the --compare mode that checks whether the STL face normals and the deduced surface orientations match the STEP surface orientations. Let me start by understanding the current codebase structure.
> 
> Let me read the key files to understand the current implementation.

**Todo list:**
- 🔄 Read LEARNINGS.md and DEVELOPMENT_PLAN.md
- ⬜ Understand current compare mode code
- ⬜ Understand surface orientation in stages
- ⬜ Implement orientation comparison
- ⬜ Test and fix clippy warnings
- ⬜ Commit changes

Read [LEARNINGS.md](../LEARNINGS.md#1-1), lines 1 to 100

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#1-1), lines 1 to 100

Read [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md#100-100), lines 100 to 250

Read [LEARNINGS.md](../LEARNINGS.md#100-100), lines 100 to 200

Read [LEARNINGS.md](../LEARNINGS.md#200-200), lines 200 to 231

**Todo list:**
- ✅ Read LEARNINGS.md and DEVELOPMENT_PLAN.md
- 🔄 Understand current compare mode code
- ⬜ Understand surface orientation in stages
- ⬜ Implement orientation comparison
- ⬜ Test and fix clippy warnings
- ⬜ Commit changes

Now let me understand the current compare mode code and find where orientation is relevant.

<details>
<summary>Research compare mode and orientation (Explore agent · Claude Haiku 4.5)</summary>

**Prompt:**
> Research the brepper codebase to understand:
> 
> 1. How "compare" mode works in stage 3.4 (face creation) and stage 3.6 (solid construction). Look in src/stage3.rs and src/stage4.rs for `--compare` handling.
> 2. How face orientations / normals are determined in the STL mesh (stage 1/2) vs how they're determined from the STEP file surfaces.
> 3. How the current code associates STL mesh faces with STEP surfaces (the hypothesis system).
> 4. What data structures represent hypotheses and how surface orientation (normal direction / convexity) is tracked.
> 
> Read these files thoroughly:
> - src/stage3.rs - especially the compare sections, face creation, shell/solid construction
> - src/stage4.rs - output stage
> - src/config.rs - configuration
> - src/stage2.rs - hypothesis structures and surface fitting (at least the key data structures)
> - src/bin/brepper.rs - main binary, how compare mode is invoked
> 
> Return:
> - The key data structures for hypotheses (planar, cylindrical, spherical) with their orientation-related fields
> - How --compare mode currently validates results at each stage
> - The relevant OCCT APIs used for surface normal evaluation on STEP shapes
> - Where in the code I should add orientation validation logic
> - The approximate line numbers for key sections

Read [stage2.rs](../src/stage2.rs#1-1), lines 1 to 100

Read [brepper.rs](../src/bin/brepper.rs)

Read [config.rs](../src/config.rs#1-1), lines 1 to 80

Read [stage2.rs](../src/stage2.rs#100-100), lines 100 to 300

<details>
<summary>Searched for regex <code>--compare|compare_step|compare_mode</code> (<code>**/src/config.rs</code>), 12 results</summary>

- [config.rs:140](../src/config.rs#L140)
- [config.rs:186](../src/config.rs#L186)
- [config.rs:187](../src/config.rs#L187)
- [config.rs:214](../src/config.rs#L214)
- [config.rs:235](../src/config.rs#L235)
- [config.rs:248](../src/config.rs#L248)
- [config.rs:249](../src/config.rs#L249)
- [config.rs:250](../src/config.rs#L250)
- [config.rs:326](../src/config.rs#L326)
- [config.rs:331](../src/config.rs#L331)
</details>

Searched for regex `--compare|compare_step|compare_mode|COMPARE` (`**/src/bin/brepper.rs`), 1 result
- [brepper.rs:38](../src/bin/brepper.rs#L38)

Read [config.rs](../src/config.rs#80-80), lines 80 to 270

<details>
<summary>Searched for regex <code>--compare|compare|COMPARE|Stage3CompareError</code> (<code>**/src/stage3.rs</code>), 30 results</summary>

- [stage3.rs:140](../src/stage3.rs#L140)
- [stage3.rs:148](../src/stage3.rs#L148)
- [stage3.rs:155](../src/stage3.rs#L155)
- [stage3.rs:156](../src/stage3.rs#L156)
- [stage3.rs:157](../src/stage3.rs#L157)
- [stage3.rs:162](../src/stage3.rs#L162)
- [stage3.rs:170](../src/stage3.rs#L170)
- [stage3.rs:182](../src/stage3.rs#L182)
- [stage3.rs:185](../src/stage3.rs#L185)
- [stage3.rs:190](../src/stage3.rs#L190)
- [stage3.rs:202](../src/stage3.rs#L202)
- [stage3.rs:207](../src/stage3.rs#L207)
- [stage3.rs:223](../src/stage3.rs#L223)
- [stage3.rs:228](../src/stage3.rs#L228)
- [stage3.rs:233](../src/stage3.rs#L233)
- [stage3.rs:234](../src/stage3.rs#L234)
- [stage3.rs:237](../src/stage3.rs#L237)
- [stage3.rs:238](../src/stage3.rs#L238)
- [stage3.rs:266](../src/stage3.rs#L266)
- [stage3.rs:284](../src/stage3.rs#L284)
- [stage3.rs:296](../src/stage3.rs#L296)
- [stage3.rs:843](../src/stage3.rs#L843)
- [stage3.rs:844](../src/stage3.rs#L844)
- [stage3.rs:845](../src/stage3.rs#L845)
- [stage3.rs:928](../src/stage3.rs#L928)
- [stage3.rs:1677](../src/stage3.rs#L1677)
</details>

Searched for regex `--compare|compare|COMPARE` (`**/src/stage4.rs`), no results

Read [stage3.rs](../src/stage3.rs#1-1), lines 1 to 200

Read [stage3.rs](../src/stage3.rs#130-130), lines 130 to 210

Read [stage3.rs](../src/stage3.rs#220-220), lines 220 to 300

Searched for regex `compare_faces_to_step` (`**/src/stage3.rs`), 2 results
- [stage3.rs:1678](../src/stage3.rs#L1678)
- [stage3.rs:1842](../src/stage3.rs#L1842)

Read [stage2.rs](../src/stage2.rs#120-120), lines 120 to 250

Read [stage3.rs](../src/stage3.rs#1670-1670), lines 1670 to 1750

Read [stage3.rs](../src/stage3.rs#1750-1750), lines 1750 to 1850

<details>
<summary>Searched for regex <code>stage 3.5|stage 3.6|construct_shell|construct_solid|orientation</code> (<code>**/src/stage3.rs</code>), 20 results</summary>

- [stage3.rs:111](../src/stage3.rs#L111)
- [stage3.rs:113](../src/stage3.rs#L113)
- [stage3.rs:115](../src/stage3.rs#L115)
- [stage3.rs:855](../src/stage3.rs#L855)
- [stage3.rs:856](../src/stage3.rs#L856)
- [stage3.rs:1625](../src/stage3.rs#L1625)
- [stage3.rs:1629](../src/stage3.rs#L1629)
- [stage3.rs:1653](../src/stage3.rs#L1653)
- [stage3.rs:1662](../src/stage3.rs#L1662)
- [stage3.rs:1663](../src/stage3.rs#L1663)
- [stage3.rs:1850](../src/stage3.rs#L1850)
- [stage3.rs:1857](../src/stage3.rs#L1857)
- [stage3.rs:1948](../src/stage3.rs#L1948)
- [stage3.rs:1951](../src/stage3.rs#L1951)
- [stage3.rs:1952](../src/stage3.rs#L1952)
- [stage3.rs:1956](../src/stage3.rs#L1956)
- [stage3.rs:1990](../src/stage3.rs#L1990)
- [stage3.rs:1996](../src/stage3.rs#L1996)
- [stage3.rs:2012](../src/stage3.rs#L2012)
</details>

Read [stage3.rs](../src/stage3.rs#400-400), lines 400 to 600

Read [stage3.rs](../src/stage3.rs#1850-1850), lines 1850 to 1950

Read [stage3.rs](../src/stage3.rs#1950-1950), lines 1950 to 2050

Read [stage3.rs](../src/stage3.rs#1600-1600), lines 1600 to 1670

Searched for regex `construct_solids|fn stage` (`**/src/stage3.rs`), 3 results
- [stage3.rs:2073](../src/stage3.rs#L2073)
- [stage3.rs:2244](../src/stage3.rs#L2244)
- [stage3.rs:2281](../src/stage3.rs#L2281)

Read [stage3.rs](../src/stage3.rs#2073-2073), lines 2073 to 2140

Read [stage3.rs](../src/stage3.rs#2240-2240), lines 2240 to 2285

<details>
<summary>Searched for regex <code>MeshFace|struct MeshFace|normal</code> (<code>**/src/stage1.rs</code>), 20 results</summary>

- [stage1.rs:4](../src/stage1.rs#L4)
- [stage1.rs:39](../src/stage1.rs#L39)
- [stage1.rs:43](../src/stage1.rs#L43)
- [stage1.rs:63](../src/stage1.rs#L63)
- [stage1.rs:68](../src/stage1.rs#L68)
- [stage1.rs:285](../src/stage1.rs#L285)
- [stage1.rs:289](../src/stage1.rs#L289)
- [stage1.rs:356](../src/stage1.rs#L356)
- [stage1.rs:357](../src/stage1.rs#L357)
- [stage1.rs:358](../src/stage1.rs#L358)
- [stage1.rs:360](../src/stage1.rs#L360)
- [stage1.rs:479](../src/stage1.rs#L479)
- [stage1.rs:485](../src/stage1.rs#L485)
- [stage1.rs:513](../src/stage1.rs#L513)
- [stage1.rs:514](../src/stage1.rs#L514)
- [stage1.rs:528](../src/stage1.rs#L528)
- [stage1.rs:529](../src/stage1.rs#L529)
</details>

Read [stage1.rs](../src/stage1.rs#1-1), lines 1 to 150

Read [stage1.rs](../src/stage1.rs#479-479), lines 479 to 550

Read [stage2.rs](../src/stage2.rs#80-80), lines 80 to 150

<details>
<summary>Searched for regex <code>min_distance_to_shape|b_rep_extrema|bounded_distance</code> (<code>**/src/stage1.rs</code>), 5 results</summary>

- [stage1.rs:7](../src/stage1.rs#L7)
- [stage1.rs:734](../src/stage1.rs#L734)
- [stage1.rs:741](../src/stage1.rs#L741)
- [stage1.rs:764](../src/stage1.rs#L764)
- [stage1.rs:787](../src/stage1.rs#L787)
</details>

Read [stage1.rs](../src/stage1.rs#400-400), lines 400 to 450

Read [stage1.rs](../src/stage1.rs#730-730), lines 730 to 800

Searched for regex `create_occt_surface|create_planar|create_cylindrical|create_spherical` (`**/src/stage3.rs`), 4 results
- [stage3.rs:309](../src/stage3.rs#L309)
- [stage3.rs:575](../src/stage3.rs#L575)
- [stage3.rs:1438](../src/stage3.rs#L1438)
- [stage3.rs:1807](../src/stage3.rs#L1807)

Read [stage3.rs](../src/stage3.rs#1050-1050), lines 1050 to 1200

Read [stage3.rs](../src/stage3.rs#309-309), lines 309 to 380

<details>
<summary>Searched for regex <code>BFS|deduce_|fit_|hypothesis|surface fitting</code> (<code>**/src/stage2.rs</code>), 20 results</summary>

- [stage2.rs:1](../src/stage2.rs#L1)
- [stage2.rs:11](../src/stage2.rs#L11)
- [stage2.rs:31](../src/stage2.rs#L31)
- [stage2.rs:32](../src/stage2.rs#L32)
- [stage2.rs:35](../src/stage2.rs#L35)
- [stage2.rs:37](../src/stage2.rs#L37)
- [stage2.rs:43](../src/stage2.rs#L43)
- [stage2.rs:51](../src/stage2.rs#L51)
- [stage2.rs:56](../src/stage2.rs#L56)
- [stage2.rs:62](../src/stage2.rs#L62)
- [stage2.rs:65](../src/stage2.rs#L65)
- [stage2.rs:67](../src/stage2.rs#L67)
- [stage2.rs:72](../src/stage2.rs#L72)
- [stage2.rs:84](../src/stage2.rs#L84)
- [stage2.rs:86](../src/stage2.rs#L86)
- [stage2.rs:95](../src/stage2.rs#L95)
- [stage2.rs:105](../src/stage2.rs#L105)
</details>

Based on my thorough research of the brepper codebase, here's a comprehensive analysis:

## 1. **Key Data Structures for Hypotheses**

All defined in [src/stage2.rs](src/stage2.rs#L65-L130):

**PlanarHypothesis** ([src/stage2.rs](src/stage2.rs#L67-L85)):
- `normal: [f64; 3]` — Unit outward normal
- `distance: f64` — Signed distance from origin along normal (d in: n·p = d)
- `faces: Vec<usize>` — Mesh face indices
- `vertices: Vec<usize>` — Mesh vertex indices
- `error_max/error_min/error_abs_sum: f64` — Fit quality metrics

**CylindricalHypothesis** ([src/stage2.rs](src/stage2.rs#L86-L105)):
- `axis_origin: [f64; 3]` — Point on cylinder axis
- `axis_direction: [f64; 3]` — Unit direction along axis
- `radius: f64` — Cylinder radius (positive)
- **`convex: bool`** — **Critical orientation field**: `true` if surface normal points away from axis (outward), `false` for inward (holes/pockets)
- `faces: Vec<usize>`, `vertices: Vec<usize>` — Assigned mesh elements
- `error_max/error_abs_sum: f64` — Fit quality

**SphericalHypothesis** ([src/stage2.rs](src/stage2.rs#L107-L122)):
- `center: [f64; 3]` — Sphere center
- `radius: f64` — Sphere radius (positive)
- **`convex: bool`** — **Orientation tracking**: `true` for outward surface (convex), `false` for inward (spherical voids)
- `faces: Vec<usize>`, `vertices: Vec<usize>`
- `error_max/error_abs_sum: f64`

---

## 2. **How --compare Mode Works**

There are **three stages with compare validation**:

### **Stage 3.1 Adjacency Compare** ([src/stage3.rs](src/stage3.rs#L223-L298))
- **What it checks:** Sampled mesh boundary vertices against STEP edges; BRepVertices against STEP vertices
- **Function:** `compare_adjacency_to_step()`
- **Tolerance:** `config.vertex_tolerance_mm` (default 1e-5 mm in STL units)
- **API used:** `BRepExtrema_DistShapeShape` (respects face trimming boundaries)
- **Error type:** `Stage3CompareError { substage: 1, check_type: "edge"/"vertex", ... }`

### **Stage 3.4 Face Compare** ([src/stage3.rs](src/stage3.rs#L1677-L1750))
- **What it checks:** Mesh face centroids sampled from reconstructed faces against STEP reference shape
- **Function:** `compare_faces_to_step()`
- **Tolerance:** `config.surface_tolerance_mm` (default 0.4 mm)
- **Reports:** Counts of faces created vs. reference, max centroid distance
- **Error type:** `Stage3CompareError { substage: 4, check_type: "face", ... }`

### **Stage 3.5 Shell Compare** ([src/stage3.rs](src/stage3.rs#L2012-L2070))
- **What it checks:** Each shell is closed (no free edges); counts shells
- **Function:** `compare_shells_to_step()`
- **Uses:** `ShapeAnalysis_Shell::check_oriented_shells()` and `has_free_edges()`

### **Stage 3.6 Solid Compare** ([src/stage3.rs](src/stage3.rs#L2130-L2180))
- **What it checks:** Volume integrity, count comparison
- **Function:** `compare_solids_to_step()`

**Entry point:** `config.load_compare_step()` in [src/bin/brepper.rs](src/bin/brepper.rs#L38), loads reference STEP into `config.compare_shape: Option<OwnedPtr<topo_ds::Shape>>`

---

## 3. **STL Mesh Normals vs. STEP Surface Normals**

### **STL Mesh Face Normals** ([src/stage1.rs](src/stage1.rs#L479-L508))
- **Computed via:** Newell normal formula (robust for non-planar quads)
- **Stored in:** `MeshFace.normal: Option<[f64; 3]>`
- **Direction:** Determined by winding order (counter-clockwise when viewed from outside = outward)
- **Computed at:** Stage 1.2 validation phase

### **STEP Surface Normals**
- **Planar:** Normal = `gp::Dir` from `Geom_Plane::Direction()`; always points in construction direction
- **Cylindrical:** Normal points **radially away from axis** (outward) — per OCCT convention; tracked via `CylindricalHypothesis.convex` bool
- **Spherical:** Normal points **away from center** — per OCCT convention; tracked via `SphericalHypothesis.convex` bool

**Orientation Handling in Stage 3.4:** ([src/stage3.rs](src/stage3.rs#L1625-L1635))
```rust
let is_concave = match surface {
    SelectedSurface::Cylindrical(idx) => !output.stage2.cylindrical_hypotheses[*idx].convex,
    SelectedSurface::Spherical(idx) => !output.stage2.spherical_hypotheses[*idx].convex,
    _ => false,
};
// If convex=false, face orientation is REVERSED before passing to BRepBuilderAPI_MakeFace
```

---

## 4. **Hypothesis System: Associating STL Faces with STEP Surfaces**

### **BFS Region Growing** ([src/stage2.rs](src/stage2.rs#L250+))
Each hypothesis type (planar, cylindrical, spherical) uses identical BFS strategy:

1. **Seed:** For each unassigned mesh face with no hypothesis yet
2. **Fit surface:** Fit a geometric surface to the seed face
3. **Grow via BFS:** 
   - Add neighboring faces if they satisfy:
     - Normal alignment check (dihedral angle < `angular_tolerance_rad`, default 17.5°)
     - All vertices within `vertex_tolerance_mm` of fitted surface
   - Re-fit iteratively as new faces are added
4. **Store result:** Map mesh face → hypothesis index in `MeshFace.planar/cylindrical/spherical_hypothesis`

### **Selected Surfaces** ([src/stage2.rs](src/stage2.rs#L133-L142))
After all hypotheses deduced in stages 2.1-2.3:
- Stage 2.6 selects **one hypothesis per mesh face** and stores in `Stage2Output.selected_surfaces: Vec<SelectedSurface>`
- `SelectedSurface` enum: `Planar(usize) | Cylindrical(usize) | Spherical(usize)`
- Index refers into `planar_hypotheses`, `cylindrical_hypotheses`, or `spherical_hypotheses` vectors

### **Translation to OCCT** ([src/stage3.rs](src/stage3.rs#L309-L360))
- Build face descriptors with OCCT surface objects via `create_occt_surface()`
- For cylinders/spheres: Hypothesis `convex` bool determines if orientation needs reversal

---

## 5. **Key OCCT APIs Used for Surface Evaluation**

| API | Purpose | Location |
|-----|---------|----------|
| `BRepExtrema_DistShapeShape` | Min distance from point/shape to bounded geometry (respects face trimming) | [stage1.rs](src/stage1.rs#L741) |
| `BRepGProp::SurfaceProperties` | Compute face area and properties | [stage3.rs](src/stage3.rs#L1660) |
| `BRepGProp::VolumeProperties` | Compute solid volume | [stage3.rs](src/stage3.rs#L2104) |
| `BRepCheck_Analyzer` | Validate face/solid topology | [stage3.rs](src/stage3.rs#L1645), [stage3.rs](src/stage3.rs#L2100) |
| `ShapeAnalysis_Shell::check_oriented_shells()` | Check shell orientation consistency and free edges | [stage3.rs](src/stage3.rs#L1973) |
| `TopExp_Explorer` | Iterate faces/edges/vertices in shape hierarchy | Multiple |
| `ShapeFix_Solid::SolidFromShell()` | Convert shell to solid with automatic orientation via `PerformInfinitePoint()` | [stage3.rs](src/stage3.rs#L2082) |
| `Geom_Plane::Direction()`, `Geom_CylindricalSurface`, `Geom_SphericalSurface` | Access surface geometry | [stage3.rs](src/stage3.rs#L309-L360) |

---

## 6. **Where to Add Orientation Validation Logic**

### **Recommended Locations:**

1. **Stage 2.6 (Surface Selection)** — Validate that all selected surfaces have consistent orientation:
   - Check if cylindrical/spherical `convex` flags match mesh normal directions
   - Cross-validate with STEP reference surfaces if `--compare` is enabled

2. **Stage 3.1 (Adjacency Validation)** — Add surface normal vector checking:
   - After building face descriptors, compare reconstructed surface normals with reference STEP surface normals
   - Add new compare function: `compare_surface_normals_to_step()` (~line 295)

3. **Stage 3.4 (Face Creation)** — Enhance existing compare:
   - Current code samples centroids; could extend to:
     - Evaluate surface normals at sample points using `Geom_Surface::Normal()` (not yet bound)
     - Compare normal directions against STEP surface normals
     - Add explicit `Stage3CompareError { check_type: "surface_normal", ... }`

4. **Stage 3.5-3.6 (Shells/Solids)** — Check global orientation:
   - `ShapeAnalysis_Shell::check_oriented_shells()` already validates edge consistency
   - Could add: Orientation check via centroid vs. infinite point test

### **Line Number Summary:**
- Hypothesis structures: [src/stage2.rs](src/stage2.rs#L65-L122) 
- Stage 2.6 selection: [src/stage2.rs](src/stage2.rs#L1000+)
- Stage 3.1 compare: [src/stage3.rs](src/stage3.rs#L223-L298)
- Stage 3.4 face creation & compare: [src/stage3.rs](src/stage3.rs#L1778-L1850)
- Orientation setting: [src/stage3.rs](src/stage3.rs#L1625-L1635)
- Stage 3.5 shells: [src/stage3.rs](src/stage3.rs#L1857-L2010)
- Stage 3.6 solids: [src/stage3.rs](src/stage3.rs#L2073-L2130)

</details>

<details>
<summary>Tool output (runSubagent)</summary>

````
Based on my thorough research of the brepper codebase, here's a comprehensive analysis:

## 1. **Key Data Structures for Hypotheses**

All defined in [src/stage2.rs](src/stage2.rs#L65-L130):

**PlanarHypothesis** ([src/stage2.rs](src/stage2.rs#L67-L85)):
- `normal: [f64; 3]` — Unit outward normal
- `distance: f64` — Signed distance from origin along normal (d in: n·p = d)
- `faces: Vec<usize>` — Mesh face indices
- `vertices: Vec<usize>` — Mesh vertex indices
- `error_max/error_min/error_abs_sum: f64` — Fit quality metrics

**CylindricalHypothesis** ([src/stage2.rs](src/stage2.rs#L86-L105)):
- `axis_origin: [f64; 3]` — Point on cylinder axis
- `axis_direction: [f64; 3]` — Unit direction along axis
- `radius: f64` — Cylinder radius (positive)
- **`convex: bool`** — **Critical orientation field**: `true` if surface normal points away from axis (outward), `false` for inward (holes/pockets)
- `faces: Vec<usize>`, `vertices: Vec<usize>` — Assigned mesh elements
- `error_max/error_abs_sum: f64` — Fit quality

**SphericalHypothesis** ([src/stage2.rs](src/stage2.rs#L107-L122)):
- `center: [f64; 3]` — Sphere center
- `radius: f64` — Sphere radius (positive)
- **`convex: bool`** — **Orientation tracking**: `true` for outward surface (convex), `false` for inward (spherical voids)
- `faces: Vec<usize>`, `vertices: Vec<usize>`
- `error_max/error_abs_sum: f64`

---

## 2. **How --compare Mode Works**

There are **three stages with compare validation**:

### **Stage 3.1 Adjacency Compare** ([src/stage3.rs](src/stage3.rs#L223-L298))
- **What it checks:** Sampled mesh boundary vertices against STEP edges; BRepVertices against STEP vertices
- **Function:** `compare_adjacency_to_step()`
- **Tolerance:** `config.vertex_tolerance_mm` (default 1e-5 mm in STL units)
- **API used:** `BRepExtrema_DistShapeShape` (respects face trimming boundaries)
- **Error type:** `Stage3CompareError { substage: 1, check_type: "edge"/"vertex", ... }`

### **Stage 3.4 Face Compare** ([src/stage3.rs](src/stage3.rs#L1677-L1750))
- **What it checks:** Mesh face centroids sampled from reconstructed faces against STEP reference shape
- **Function:** `compare_faces_to_step()`
- **Tolerance:** `config.surface_tolerance_mm` (default 0.4 mm)
- **Reports:** Counts of faces created vs. reference, max centroid distance
- **Error type:** `Stage3CompareError { substage: 4, check_type: "face", ... }`

### **Stage 3.5 Shell Compare** ([src/stage3.rs](src/stage3.rs#L2012-L2070))
- **What it checks:** Each shell is closed (no free edges); counts shells
- **Function:** `compare_shells_to_step()`
- **Uses:** `ShapeAnalysis_Shell::check_oriented_shells()` and `has_free_edges()`

### **Stage 3.6 Solid Compare** ([src/stage3.rs](src/stage3.rs#L2130-L2180))
- **What it checks:** Volume integrity, count comparison
- **Function:** `compare_solids_to_step()`

**Entry point:** `config.load_compare_step()` in [src/bin/brepper.rs](src/bin/brepper.rs#L38), loads reference STEP into `config.compare_shape: Option<OwnedPtr<topo_ds::Shape>>`

---

## 3. **STL Mesh Normals vs. STEP Surface Normals**

### **STL Mesh Face Normals** ([src/stage1.rs](src/stage1.rs#L479-L508))
- **Computed via:** Newell normal formula (robust for non-planar quads)
- **Stored in:** `MeshFace.normal: Option<[f64; 3]>`
- **Direction:** Determined by winding order (counter-clockwise when viewed from outside = outward)
- **Computed at:** Stage 1.2 validation phase

### **STEP Surface Normals**
- **Planar:** Normal = `gp::Dir` from `Geom_Plane::Direction()`; always points in construction direction
- **Cylindrical:** Normal points **radially away from axis** (outward) — per OCCT convention; tracked via `CylindricalHypothesis.convex` bool
- **Spherical:** Normal points **away from center** — per OCCT convention; tracked via `SphericalHypothesis.convex` bool

**Orientation Handling in Stage 3.4:** ([src/stage3.rs](src/stage3.rs#L1625-L1635))
```rust
let is_concave = match surface {
    SelectedSurface::Cylindrical(idx) => !output.sta
... (truncated, 8433 chars)
````
</details>

**Todo list:**
- ✅ Read LEARNINGS.md and DEVELOPMENT_PLAN.md
- ✅ Understand current compare mode code
- 🔄 Understand surface orientation in stages
- ⬜ Implement orientation comparison
- ⬜ Test and fix clippy warnings
- ⬜ Commit changes

Now let me read the key sections of stage3.rs to understand the compare functions and face creation:Good, hashline tools are loaded. Let me read the relevant sections of stage3.rs:

**Terminal:**
```sh
wc -l /Users/sethml/src/brepper/src/stage3.rs /Users/sethml/src/brepper/src/stage2.rs /Users/sethml/src/brepper/src/stage1.rs
```
```
    2284 /Users/sethml/src/brepper/src/stage3.rs
    2719 /Users/sethml/src/brepper/src/stage2.rs
     973 /Users/sethml/src/brepper/src/stage1.rs
    5976 total
```

Let me read the key sections of stage3.rs - face creation compare, shell compare, and how surfaces are created:

<details>
<summary>Reading lines 1-60 of <a href="../src/stage3.rs#L1-L61">stage3.rs</a></summary>

```
//! Stage 3: Surface Reconstruction
//!
//! 3.1: Create OCCT surface objects from hypotheses and build adjacency graph
//! 3.2: Detect and create tangency relationships
//! 3.3: Create OCCT edge wires (surface-surface intersections)
//! 3.4: Create OCCT faces (surface bounded by wires)
//! 3.5: Construct shells from connected faces
//! 3.6: Construct solids from shells

use crate::config::Config;
use crate::stage1::{ConnectedMesh, MeshVertex};
use crate::stage2::{SelectedSurface, Stage2Output};
use crate::stage1;
use opencascade_sys::{
    b_rep_builder_api, b_rep_check, b_rep_extrema, b_rep_g_prop, extrema,
    geom, geom_api, g_prop, gp, message, shape_analysis, shape_fix,
    top_abs, top_exp, topo_ds, OwnedPtr,
};
use std::collections::{BTreeSet, HashMap, HashSet};
use std::error::Error;
use std::fmt::{Display, Formatter};

// ---------------------------------------------------------------------------
// Data structures — Stage 3 output
// ---------------------------------------------------------------------------

/// Descriptor for a reconstructed B-Rep face, corresponding to one selected surface hypothesis.
pub struct FaceDescriptor {
    /// Index of the selected surface (into Stage2Output.selected_surfaces).
    pub selected_surface_idx: usize,
    /// OCCT surface object (Geom_Plane, Geom_CylindricalSurface, or Geom_SphericalSurface),
    /// type-erased as HandleGeomSurface.
    pub surface: OwnedPtr<geom::HandleGeomSurface>,
    // TODO: OCCT face object (TopoDS_Face) — populated in stage 3.4
    /// Indices of adjacent FaceDescriptors, ordered topologically around this face's boundary.
    /// A face connecting to this one via multiple edges will appear multiple times.
    pub adjacent_faces: Vec<usize>,
    /// ReconEdge indices, one per adjacent face. `edge_indices[i]` is the edge between
    /// this face and `adjacent_faces[i]`.
    pub edge_indices: Vec<usize>,
    /// BRepVertex indices at corners. `vertex_indices[i]` is the vertex between
    /// `edge_indices[i]` and `edge_indices[(i+1) % N]`.
    pub vertex_indices: Vec<usize>,
}

impl std::fmt::Debug for FaceDescriptor {
    fn fmt(&self, f: &mut Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("FaceDescriptor")
            .field("selected_surface_idx", &self.selected_surface_idx)
            .field("surface", &"<HandleGeomSurface>")
            .field("adjacent_faces", &self.adjacent_faces)
            .field("edge_indices", &self.edge_indices)
            .field("vertex_indices", &self.vertex_indices)
            .finish()
    }
}

/// Descriptor for an edge (boundary segment) between two adjacent reconstructed faces.
pub struct ReconEdge {
    /// Indices of the two adjacent FaceDescriptors.
```
</details>

<details>
<summary>Reading lines 300-400 of <a href="../src/stage3.rs#L300-L401">stage3.rs</a></summary>

```

    Ok(())
}

// ---------------------------------------------------------------------------
// Stage 3.1: Create OCCT surfaces and build adjacency graph
// ---------------------------------------------------------------------------

/// Create an OCCT `HandleGeomSurface` for a selected surface hypothesis.
fn create_occt_surface(
    surface: &SelectedSurface,
    output: &Stage2Output,
) -> OwnedPtr<geom::HandleGeomSurface> {
    match surface {
        SelectedSurface::Planar(idx) => {
            let hyp = &output.planar_hypotheses[*idx];
            let origin = gp::Pnt::new_real3(
                hyp.normal[0] * hyp.distance,
                hyp.normal[1] * hyp.distance,
                hyp.normal[2] * hyp.distance,
            );
            let normal = gp::Dir::new_real3(hyp.normal[0], hyp.normal[1], hyp.normal[2]);
            let plane = geom::Plane::new_pnt_dir(&origin, &normal);
            geom::Plane::to_handle(plane).to_handle_surface()
        }
        SelectedSurface::Cylindrical(idx) => {
            let hyp = &output.cylindrical_hypotheses[*idx];
            let origin = gp::Pnt::new_real3(
                hyp.axis_origin[0],
                hyp.axis_origin[1],
                hyp.axis_origin[2],
            );
            let dir = gp::Dir::new_real3(
                hyp.axis_direction[0],
                hyp.axis_direction[1],
                hyp.axis_direction[2],
            );
            let ax3 = gp::Ax3::new_pnt_dir(&origin, &dir);
            let cyl = geom::CylindricalSurface::new_ax3_real(&ax3, hyp.radius);
            geom::CylindricalSurface::to_handle(cyl).to_handle_surface()
        }
        SelectedSurface::Spherical(idx) => {
            let hyp = &output.spherical_hypotheses[*idx];
            let origin = gp::Pnt::new_real3(hyp.center[0], hyp.center[1], hyp.center[2]);
            // Sphere axis direction is arbitrary; use Z-up
            let dir = gp::Dir::new_real3(0.0, 0.0, 1.0);
            let ax3 = gp::Ax3::new_pnt_dir(&origin, &dir);
            let sphere = geom::SphericalSurface::new_ax3_real(&ax3, hyp.radius);
            geom::SphericalSurface::to_handle(sphere).to_handle_surface()
        }
    }
}

/// Get the faces belonging to a selected surface.
fn surface_faces<'a>(surface: &SelectedSurface, output: &'a Stage2Output) -> &'a [usize] {
    match surface {
        SelectedSurface::Planar(idx) => &output.planar_hypotheses[*idx].faces,
        SelectedSurface::Cylindrical(idx) => &output.cylindrical_hypotheses[*idx].faces,
        SelectedSurface::Spherical(idx) => &output.spherical_hypotheses[*idx].faces,
    }
}
/// Canonical (undirected) representation of a mesh edge for hashing.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
struct UndirectedEdge(usize, usize);

impl UndirectedEdge {
    fn new(a: usize, b: usize) -> Self {
        if a <= b {
            UndirectedEdge(a, b)
        } else {
            UndirectedEdge(b, a)
        }
    }
}

/// An ordered pair of selected surface indices (smaller first).
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
struct SurfacePair(usize, usize);

impl SurfacePair {
    fn new(a: usize, b: usize) -> Self {
        if a <= b {
            SurfacePair(a, b)
        } else {
            SurfacePair(b, a)
        }
    }
}

/// Build the mapping from mesh face index to selected surface index.
fn build_face_to_surface_map(output: &Stage2Output) -> Vec<usize> {
    let num_faces = output.mesh.faces.len();
    let mut face_to_surface = vec![usize::MAX; num_faces];
    for (si, surface) in output.selected_surfaces.iter().enumerate() {
        for &fi in surface_faces(surface, output) {
            debug_assert_eq!(
                face_to_surface[fi],
                usize::MAX,
                "face {fi} assigned to multiple selected surfaces"
            );
            face_to_surface[fi] = si;
```
</details>

<details>
<summary>Reading lines 1620-1780 of <a href="../src/stage3.rs#L1620-L1781">stage3.rs</a></summary>

```
            );
            edge_explorer.next();
        }
    }

    // Set face orientation based on surface convexity.
    // OCCT surface normals always point outward (away from axis for cylinders,
    // away from center for spheres). For concave surfaces (holes, pockets),
    // the face normal should point inward (toward the solid interior), so
    // we reverse the face orientation.
    let is_concave = match surface {
        SelectedSurface::Cylindrical(idx) => !output.stage2.cylindrical_hypotheses[*idx].convex,
        SelectedSurface::Spherical(idx) => !output.stage2.spherical_hypotheses[*idx].convex,
        _ => false,
    };

    Ok((make_face, is_concave))
}

/// Validate created faces using BRepCheck_Analyzer.
fn validate_faces(output: &Stage3Output, config: &Config) -> Result<(), Stage3Error> {
    let mut invalid_count = 0;

    for (fi, mf) in output.make_faces.iter().enumerate() {
        let face = mf.face();
        let analyzer = b_rep_check::Analyzer::new_shape(face.as_shape());
        if !analyzer.is_valid() {
            invalid_count += 1;
            if config.verbose {
                eprintln!("  Face {fi}: BRepCheck_Analyzer reports invalid");
            }
        }
        if config.verbose {
            // Report face area and orientation for diagnostics
            let mut gprops = g_prop::GProps::new();
            b_rep_g_prop::surface_properties_shape_gprops_bool2(
                face.as_shape(),
                &mut gprops,
                false, // UseTriangulation
                false, // SkipShared
            );
            let area = gprops.mass();
            let orient = face.as_shape().orientation();
            eprintln!("  Face {fi}: area = {area:.4} mm², orientation = {orient:?}");
        }
    }

    if !config.quiet && invalid_count > 0 {
        eprintln!(
            "  Warning: {invalid_count}/{} faces failed BRepCheck validation",
            output.make_faces.len(),
        );
    }

    Ok(())
}

/// Compare stage 3.4 faces against reference STEP shape.
fn compare_faces_to_step(
    output: &Stage3Output,
    config: &Config,
) -> Result<(), Stage3CompareError> {
    let compare_shape = config.compare_shape.as_ref().unwrap();

    // Count faces in reference STEP
    let mut step_face_count = 0;
    let mut explorer = top_exp::Explorer::new_shape_shapeenum2(
        compare_shape,
        top_abs::ShapeEnum::Face,
        top_abs::ShapeEnum::Shape,
    );
    while explorer.more() {
        step_face_count += 1;
        explorer.next();
    }

    let our_face_count = output.make_faces.len();
    if !config.quiet {
        eprintln!(
            "  Compare 3.4: {} faces created, {} faces in STEP reference",
            our_face_count, step_face_count,
        );
    }

    // For each face, sample a representative point from mesh face centroids
    // and check distance to the reference STEP shape.
    let mut max_dist = 0.0_f64;
    for (fi, fd) in output.face_descriptors.iter().enumerate() {
        let surface = &output.stage2.selected_surfaces[fd.selected_surface_idx];
        let faces = surface_faces(surface, &output.stage2);
        if faces.is_empty() {
            continue;
        }
        // Use the centroid of the first mesh face as a sample point
        let centroid = compute_mesh_face_centroid(faces[0], &output.stage2.mesh);
        let pt = gp::Pnt::new_real3(centroid[0], centroid[1], centroid[2]);
        let d_ref = min_distance_to_shape(&pt, compare_shape);
        max_dist = max_dist.max(d_ref);

        if d_ref > config.surface_tolerance_mm && config.verbose {
            eprintln!(
                "  Face {fi}: centroid distance to STEP: {:.6e} mm (tolerance: {:.6e})",
                d_ref, config.surface_tolerance_mm,
            );
        }
    }

    if !config.quiet {
        eprintln!(
            "  Compare 3.4: max face sample distance to STEP: {:.6e} mm",
            max_dist,
        );
    }

    if max_dist > config.surface_tole
... (truncated, 5752 chars)
```
</details>

Now let me see how the STEP surfaces are explored and how we can get surface normals from them: Now let me see how the STEP surfaces are explored and how we can get surface normals from them:

<details>
<summary>Reading lines 200-300 of <a href="../src/stage3.rs#L200-L301">stage3.rs</a></summary>

```

/// Build a compound containing all vertices from the reference STEP shape.
fn build_vertex_compound(compare_shape: &topo_ds::Shape) -> OwnedPtr<topo_ds::Compound> {
    let builder = topo_ds::Builder::new();
    let mut compound = topo_ds::Compound::new();
    builder.make_compound(&mut compound);
    let mut explorer = top_exp::Explorer::new_shape_shapeenum2(
        compare_shape,
        top_abs::ShapeEnum::Vertex,
        top_abs::ShapeEnum::Shape,
    );
    while explorer.more() {
        builder.add(compound.as_shape_mut(), explorer.value());
        explorer.next();
    }
    compound
}

/// Compute minimum distance from a point to a shape.
fn min_distance_to_shape(pt: &gp::Pnt, shape: &topo_ds::Shape) -> f64 {
    stage1::min_distance_to_shape(pt, shape)
}

/// Compare stage 3.1 adjacency graph against reference STEP shape.
///
/// For each ReconEdge, samples mesh boundary vertices along the edge and checks
/// that each is within vertex_tolerance of a STEP edge.
/// For each BRepVertex, checks that it is within vertex_tolerance of a STEP vertex.
fn compare_adjacency_to_step(
    edges: &[ReconEdge],
    vertices: &[BRepVertex],
    mesh: &ConnectedMesh,
    config: &Config,
) -> Result<(), Stage3CompareError> {
    let compare_shape = config.compare_shape.as_ref().unwrap();

    // Build compounds of edges and vertices from the reference STEP shape
    let edge_compound = build_edge_compound(compare_shape);
    let vertex_compound = build_vertex_compound(compare_shape);

    // Check ReconEdge boundary vertices against STEP edges
    let mut edge_overall_max = 0.0_f64;
    for (ei, edge) in edges.iter().enumerate() {
        let mut max_dist = 0.0_f64;
        let boundary = &edge.mesh_boundary_vertices;

        // Sample mesh boundary vertices: for short edges check all, for longer ones sample
        let step = if boundary.len() <= 10 { 1 } else { boundary.len() / 10 };
        let mut sample_indices: Vec<usize> = (0..boundary.len()).step_by(step).collect();
        // Always include first and last
        if let Some(&last_idx) = sample_indices.last() {
            if last_idx != boundary.len() - 1 {
                sample_indices.push(boundary.len() - 1);
            }
        }

        for &si in &sample_indices {
            let vi = boundary[si];
            let v = &mesh.vertices[vi];
            let pt = gp::Pnt::new_real3(v.x, v.y, v.z);
            let d = min_distance_to_shape(&pt, edge_compound.as_shape());
            max_dist = max_dist.max(d);
        }

        edge_overall_max = edge_overall_max.max(max_dist);
        if max_dist > config.vertex_tolerance_mm {
            return Err(Stage3CompareError {
                substage: 1,
                check_type: "edge",
                element_index: ei,
                max_distance: max_dist,
                tolerance: config.vertex_tolerance_mm,
            });
        }
    }

    // Check BRepVertices against STEP vertices
    let mut vert_overall_max = 0.0_f64;
    for (vi, vert) in vertices.iter().enumerate() {
        let pt = gp::Pnt::new_real3(vert.point[0], vert.point[1], vert.point[2]);
        let d = min_distance_to_shape(&pt, vertex_compound.as_shape());

        vert_overall_max = vert_overall_max.max(d);
        if d > config.vertex_tolerance_mm {
            return Err(Stage3CompareError {
                substage: 1,
                check_type: "vertex",
                element_index: vi,
                max_distance: d,
                tolerance: config.vertex_tolerance_mm,
            });
        }
    }

    if !config.quiet {
        eprintln!(
            "  Compare 3.1: edge boundary max dist {:.6e} mm, vertex max dist {:.6e} mm",
            edge_overall_max, vert_overall_max
        );
    }

```
</details>

<details>
<summary>Reading lines 945-1000 of <a href="../src/stage3.rs#L945-L1001">stage3.rs</a></summary>

```
        if boundary.len() < 2 {
            continue;
        }

        // Choose sample indices: every sample_step-th vertex, at least min_samples,
        // always including first and last.
        let step = if boundary.len() <= min_samples * sample_step {
            (boundary.len() / min_samples).max(1)
        } else {
            sample_step
        };
        let mut sample_indices: Vec<usize> = (0..boundary.len()).step_by(step).collect();
        if let Some(&last) = sample_indices.last() {
            if last != boundary.len() - 1 {
                sample_indices.push(boundary.len() - 1);
            }
        }

        let mut all_tangent = true;
        for &si in &sample_indices {
            let vi = boundary[si];
            let v = &output.stage2.mesh.vertices[vi];
            let pt = [v.x, v.y, v.z];

            let n0 = surface_normal_at_point(surf0, &output.stage2, &pt);
            let n1 = surface_normal_at_point(surf1, &output.stage2, &pt);

            match (n0, n1) {
                (Some(a), Some(b)) => {
                    let dot = a[0] * b[0] + a[1] * b[1] + a[2] * b[2];
                    if dot < tangency_cos {
                        all_tangent = false;
                        break;
                    }
                }
                _ => {
                    // Degenerate point — cannot determine tangency
                    all_tangent = false;
                    break;
                }
            }
        }

        if all_tangent {
            edge.tangent = true;
            tangent_count += 1;
        }
    }

    if !config.quiet {
        eprintln!(
            "Stage 3.2: Tangency detection: {} of {} edges are tangent",
            tangent_count,
            output.edges.len(),
        );
    }
```
</details>

Let me see the `surface_normal_at_point` function and the compare functions for shells and solids:

Searched for text `fn surface_normal_at_point` (`**/src/stage3.rs`), 1 result
- [stage3.rs:870](../src/stage3.rs#L870)

<details>
<summary>Reading lines 870-945 of <a href="../src/stage3.rs#L870-L946">stage3.rs</a></summary>

```
fn surface_normal_at_point(
    surface: &SelectedSurface,
    stage2: &Stage2Output,
    point: &[f64; 3],
) -> Option<[f64; 3]> {
    match surface {
        SelectedSurface::Planar(idx) => {
            let hyp = &stage2.planar_hypotheses[*idx];
            Some(hyp.normal)
        }
        SelectedSurface::Cylindrical(idx) => {
            let hyp = &stage2.cylindrical_hypotheses[*idx];
            // Project point onto the axis to find the closest axis point
            let ax = hyp.axis_direction;
            let ao = hyp.axis_origin;
            let dp = [
                point[0] - ao[0],
                point[1] - ao[1],
                point[2] - ao[2],
            ];
            let t = dp[0] * ax[0] + dp[1] * ax[1] + dp[2] * ax[2];
            let radial = [
                dp[0] - t * ax[0],
                dp[1] - t * ax[1],
                dp[2] - t * ax[2],
            ];
            let len = (radial[0] * radial[0] + radial[1] * radial[1] + radial[2] * radial[2]).sqrt();
            if len < 1e-15 {
                return None; // Point is on the axis
            }
            let mut n = [radial[0] / len, radial[1] / len, radial[2] / len];
            if !hyp.convex {
                n = [-n[0], -n[1], -n[2]];
            }
            Some(n)
        }
        SelectedSurface::Spherical(idx) => {
            let hyp = &stage2.spherical_hypotheses[*idx];
            let dp = [
                point[0] - hyp.center[0],
                point[1] - hyp.center[1],
                point[2] - hyp.center[2],
            ];
            let len = (dp[0] * dp[0] + dp[1] * dp[1] + dp[2] * dp[2]).sqrt();
            if len < 1e-15 {
                return None; // Point is at the center
            }
            let mut n = [dp[0] / len, dp[1] / len, dp[2] / len];
            if !hyp.convex {
                n = [-n[0], -n[1], -n[2]];
            }
            Some(n)
        }
    }
}

/// Detect tangency relationships along edges.
///
/// For each ReconEdge, samples mesh boundary vertices and compares the surface
/// normals of the two adjacent surfaces. If all sampled normals agree within
/// a small angle threshold (2°), the edge is marked as tangent.
fn detect_tangency(mut output: Stage3Output, config: &Config) -> Stage3Output {
    const TANGENCY_ANGLE_DEG: f64 = 2.0;
    let tangency_cos = (TANGENCY_ANGLE_DEG * std::f64::consts::PI / 180.0).cos();
    let min_samples = 3;
    let sample_step = 5; // sample every 5th boundary vertex

    let mut tangent_count = 0;

    for edge in output.edges.iter_mut() {
        let [fi0, fi1] = edge.face_indices;
        let surf0 = &output.stage2.selected_surfaces[output.face_descriptors[fi0].selected_surface_idx];
        let surf1 = &output.stage2.selected_surfaces[output.face_descriptors[fi1].selected_surface_idx];

        let boundary = &edge.mesh_boundary_vertices;
        if boundary.len() < 2 {
```
</details>

<details>
<summary>Reading lines 2010-2284 of <a href="../src/stage3.rs#L2010-L2285">stage3.rs</a></summary>

```
}

/// Compare stage 3.5 shells against reference STEP shape.
fn compare_shells_to_step(
    shells: &[OwnedPtr<topo_ds::Shell>],
    config: &Config,
) -> Result<(), Stage3CompareError> {
    let compare_shape = config.compare_shape.as_ref().unwrap();

    // Count shells in reference STEP
    let mut step_shell_count = 0;
    let mut explorer = top_exp::Explorer::new_shape_shapeenum2(
        compare_shape,
        top_abs::ShapeEnum::Shell,
        top_abs::ShapeEnum::Shape,
    );
    while explorer.more() {
        step_shell_count += 1;
        explorer.next();
    }

    if !config.quiet {
        eprintln!(
            "  Compare 3.5: {} shell(s) created, {} shell(s) in STEP reference",
            shells.len(),
            step_shell_count,
        );
    }

    // Check that each shell is closed (no free edges)
    for (si, shell) in shells.iter().enumerate() {
        let mut sa = shape_analysis::Shell::new();
        sa.check_oriented_shells(shell.as_shape(), true, false);
        if sa.has_free_edges() {
            if !config.quiet {
                eprintln!("  Compare 3.5: shell {si} has free (unclosed) edges");
            }
            return Err(Stage3CompareError {
                substage: 5,
                check_type: "shell",
                element_index: si,
                max_distance: f64::INFINITY,
                tolerance: config.vertex_tolerance_mm,
            });
        }
    }

    // Verify orientation (check_oriented_shells returns true if bad edges found)
    for (si, shell) in shells.iter().enumerate() {
        let mut sa = shape_analysis::Shell::new();
        let has_bad_edges = sa.check_oriented_shells(shell.as_shape(), false, false);
        if has_bad_edges && !config.quiet {
            eprintln!("  Compare 3.5: shell {si} has orientation inconsistencies");
        }
    }

    Ok(())
}

// ---------------------------------------------------------------------------
// Stage 3.6: Construct solids from shells
// ---------------------------------------------------------------------------

fn construct_solids(
    mut output: Stage3Output,
    config: &Config,
) -> Result<Stage3Output, Stage3Error> {
    if output.shells.is_empty() {
        return Err(Stage3Error::AdjacencyError("no shells to make solids from".into()));
    }

    let mut solids: Vec<OwnedPtr<topo_ds::Solid>> = Vec::new();

    for (si, shell) in output.shells.iter().enumerate() {
        // Use ShapeFix_Solid::SolidFromShell which handles orientation automatically
        let mut fixer = shape_fix::Solid::new();
        let solid = fixer.solid_from_shell(shell);

        // Validate with BRepCheck_Analyzer
        let analyzer = b_rep_check::Analyzer::new_shape_bool(solid.as_shape(), true);
        if !analyzer.is_valid() && config.verbose {
            eprintln!("  Warning: solid {si} failed BRepCheck validation");
        }

        // Compute volume for reporting
        let mut gprops = g_prop::GProps::new();
        b_rep_g_prop::volume_properties_shape_gprops_bool3(
            solid.as_shape(),
            &mut gprops,
            true,  // OnlyClosed
            false, // SkipShared
            false, // UseTriangulation
        );
        let volume = gprops.mass();

        if config.verbose {
            eprintln!("  Solid {si}: volume = {volume:.6} mm\u{00b3}");
        }

        solids.push(solid);
    }

    if !config.quiet {
        eprintln!(
            "Stage 3.6: Constructed {} solid(s) from {} shell(s)",
            solids.len(),
            output.shells.len(),
        );
    }

    // Compare against reference STEP if --compare
    if config.compare_shape.is_some() {
        compare_solids_to_step(&solids, config)?;
    }

    output.solids = solids;
    Ok(output)
}

/// Compare stage 3.6 solids against reference STEP shape.
fn compare_solids_to_step(
    solids: &[OwnedPtr<topo_ds::Solid>],
    config: &Config,
) -> Result<(), Stage3CompareError> {
    let compare_shape = config.compare_sh
... (truncated, 8881 chars)
```
</details>

<details>
<summary>Reading lines 2210-2284 of <a href="../src/stage3.rs#L2210-L2285">stage3.rs</a></summary>

```
    // Compute BRepExtrema distance between our solids and STEP shape
    for (si, solid) in solids.iter().enumerate() {
        let progress = message::ProgressRange::new();
        let dist_calc = b_rep_extrema::DistShapeShape::new_shape2_extflag_extalgo_progressrange(
            solid.as_shape(),
            compare_shape,
            0,
            extrema::ExtAlgo::Grad,
            &progress,
        );
        if dist_calc.is_done() && dist_calc.nb_solution() > 0 {
            let dist = dist_calc.value();
            if !config.quiet {
                eprintln!("  Compare 3.6: solid {si} distance to STEP = {dist:.6e} mm");
            }
            if dist > config.surface_tolerance_mm {
                return Err(Stage3CompareError {
                    substage: 6,
                    check_type: "solid",
                    element_index: si,
                    max_distance: dist,
                    tolerance: config.surface_tolerance_mm,
                });
            }
        }
    }

    Ok(())
}

// Stage 3 entry point
// ---------------------------------------------------------------------------

/// Run stage 3: reconstruct B-Rep surfaces, edges, and topology from fitted surfaces.
pub fn stage3(config: &Config, input: Stage2Output) -> Result<Stage3Output, Stage3Error> {
    // Stage 3.1: Create OCCT surface objects and build adjacency graph
    let output = build_surfaces_and_adjacency(input, config)?;

    if !config.stage.at_least(3, 2) {
        return Ok(output);
    }

    // Stage 3.2: Detect tangency relationships along edges
    let output = detect_tangency(output, config);

    if !config.stage.at_least(3, 3) {
        return Ok(output);
    }

    // Stage 3.3: Compute edge curves via surface-surface intersection
    let output = compute_edge_curves_all(output, config)?;

    if !config.stage.at_least(3, 4) {
        return Ok(output);
    }

    // Stage 3.4: Create OCCT faces
    let output = create_occt_faces_all(output, config)?;

    if !config.stage.at_least(3, 5) {
        return Ok(output);
    }

    // Stage 3.5: Construct shells
    let output = construct_shells(output, config)?;

    if !config.stage.at_least(3, 6) {
        return Ok(output);
    }

    // Stage 3.6: Construct solids
    let output = construct_solids(output, config)?;

    Ok(output)
}
```
</details>

Now let me understand how to get the surface normal from STEP reference faces. I need to find what APIs are available to evaluate surface normals on STEP faces. Let me check what's available:

Searched for regex `b_rep_adaptor|BRepAdaptor|GeomLProp|geom_l_prop` (`**/src/**`), 1 result
- [LEARNINGS.md:141](../LEARNINGS.md#L141)

**Todo list:**
- ✅ Read LEARNINGS.md and DEVELOPMENT_PLAN.md
- ✅ Understand current compare mode code
- ✅ Understand surface orientation in stages
- 🔄 Implement orientation comparison
- ⬜ Test and fix clippy warnings
- ⬜ Commit changes

Now I need to understand the approach. For each of our reconstructed faces, I want to:
1. Pick a representative point (mesh face centroid)
2. Compute the surface normal at that point from our deduced surface (already have )
3. Find the closest STEP face and get its surface normal at the closest point
4. Compare the two normals — they should agree in direction

Let me check what OCCT APIs are available for getting surface normals from STEP faces:Now let me look at what OCCT APIs are available for computing surface normals from a face. I need `BRepGProp_Face` or `BRep_Tool::Surface` + UV evaluation. Let me check what's in the bindings:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -ri "normal\|d1\|d2" ../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs 2>/dev/null | head -20
```
<details>
<summary>Output (20 lines)</summary>

```
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    /// **Source:** `BRepAdaptor_CompCurve.hxx`:140 - `BRepAdaptor_CompCurve::D1()`
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    pub fn d1(&self, U: f64, P: &mut crate::gp::Pnt, V: &mut crate::gp::Vec) {
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:            crate::ffi_extern_TKBRep::BRepAdaptor_CompCurve_d1(self as *const Self, U, P, V)
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    /// **Source:** `BRepAdaptor_CompCurve.hxx`:146 - `BRepAdaptor_CompCurve::D2()`
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    pub fn d2(
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:            crate::ffi_extern_TKBRep::BRepAdaptor_CompCurve_d2(self as *const Self, U, P, V1, V2)
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    /// **Source:** `BRepAdaptor_Curve.hxx`:158 - `BRepAdaptor_Curve::D1()`
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    pub fn d1(&self, U: f64, P: &mut crate::gp::Pnt, V: &mut crate::gp::Vec) {
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:            crate::ffi_extern_TKBRep::BRepAdaptor_Curve_d1(self as *const Self, U, P, V)
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    /// **Source:** `BRepAdaptor_Curve.hxx`:164 - `BRepAdaptor_Curve::D2()`
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    pub fn d2(
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:            crate::ffi_extern_TKBRep::BRepAdaptor_Curve_d2(self as *const Self, U, P, V1, V2)
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    /// Inherited: **Source:** `Geom2dAdaptor_Curve.hxx`:136 - `Geom2dAdaptor_Curve::D1()`
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    pub fn d1(&self, U: f64, P: &mut crate::gp::Pnt2d, V: &mut crate::gp::Vec2d) {
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:            crate::ffi_extern_TKBRep::BRepAdaptor_Curve2d_inherited_D1(self as *const Self, U, P, V)
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    /// Inherited: **Source:** `Geom2dAdaptor_Curve.hxx`:142 - `Geom2dAdaptor_Curve::D2()`
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    pub fn d2(
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:            crate::ffi_extern_TKBRep::BRepAdaptor_Curve2d_inherited_D2(
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    /// Tip: use GeomLib::NormEstim() to calculate surface normal at specified (U, V) point.
../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs:    /// **Source:** `BRepAdaptor_Surface.hxx`:176 - `BRepAdaptor_Surface::D1()`
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "pub fn d1\|pub fn d2\|pub fn normal\|pub fn value\|pub fn new_face\|pub fn new_shape" ../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs 2>/dev/null | grep -i "surface"
```
```
(base) whitelily:brepper sethml$
```
**Exit code:** 1

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "pub fn\|pub struct" ../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs 2>/dev/null | grep -i "surface"
```
<details>
<summary>Output (14 lines)</summary>

```
809:    pub fn is_curve_on_surface(&self) -> bool {
827:    pub fn curve_on_surface(&self) -> &crate::adaptor3d::CurveOnSurface {
2330:    pub fn shallow_copy(&self) -> crate::OwnedPtr<crate::ffi_types::HandleAdaptor3dSurface> {
2352:    pub fn surface(&self) -> &crate::geom_adaptor::Surface {
2362:    pub fn change_surface(&mut self) -> &mut crate::geom_adaptor::Surface {
2735:    pub fn get_type(&self) -> crate::geom_abs::SurfaceType {
2844:    pub fn bezier(&self) -> crate::OwnedPtr<crate::ffi_types::HandleGeomBezierSurface> {
2857:    pub fn b_spline(&self) -> crate::OwnedPtr<crate::ffi_types::HandleGeomBSplineSurface> {
2899:    pub fn basis_surface(&self) -> crate::OwnedPtr<crate::ffi_types::HandleAdaptor3dSurface> {
2935:    pub fn as_adaptor3d_surface(&self) -> &crate::adaptor3d::Surface {
2946:    pub fn as_adaptor3d_surface_mut(&mut self) -> &mut crate::adaptor3d::Surface {
3072:    pub fn get(&self) -> &crate::ffi_types::BRepAdaptor_Surface {
3081:    pub fn get_mut(&mut self) -> &mut crate::ffi_types::BRepAdaptor_Surface {
3090:    pub fn to_handle_surface(&self) -> crate::OwnedPtr<crate::ffi_types::HandleAdaptor3dSurface> {
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "pub struct Surface" ../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs 2>/dev/null
```
**Exit code:** 1

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "^pub struct\|^impl " ../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs 2>/dev/null
```
<details>
<summary>Output (10 lines)</summary>

```
38:impl CompCurve {
627:impl HandleBRepAdaptorCompCurve {
696:impl Curve {
1298:impl HandleBRepAdaptorCurve {
1365:impl Curve2d {
1934:impl HandleBRepAdaptorCurve2d {
2002:impl HArray1OfCurve {
2230:impl HandleBRepAdaptorHArray1OfCurve {
2286:impl Surface {
3070:impl HandleBRepAdaptorSurface {
```
</details>

Read [b_rep_adaptor.rs](/Users/sethml/src/opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs)

Now let me look for `D1` or  methods on BRepAdaptor_Surface to evaluate surface position and derivatives:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && sed -n '2560,2750p' ../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs
```
<details>
<summary>Output (190 lines)</summary>

```
        })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:154 - `BRepAdaptor_Surface::IsUPeriodic()`
    pub fn is_u_periodic(&self) -> bool {
        crate::check_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_is_u_periodic(self as *const Self)
        })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:156 - `BRepAdaptor_Surface::UPeriod()`
    pub fn u_period(&self) -> f64 {
        crate::check_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_u_period(self as *const Self)
        })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:158 - `BRepAdaptor_Surface::IsVPeriodic()`
    pub fn is_v_periodic(&self) -> bool {
        crate::check_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_is_v_periodic(self as *const Self)
        })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:160 - `BRepAdaptor_Surface::VPeriod()`
    pub fn v_period(&self) -> f64 {
        crate::check_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_v_period(self as *const Self)
        })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:164 - `BRepAdaptor_Surface::Value()`
    /// Computes the point of parameters U,V on the surface.
    /// Tip: use GeomLib::NormEstim() to calculate surface normal at specified (U, V) point.
    pub fn value(&self, U: f64, V: f64) -> crate::OwnedPtr<crate::gp::Pnt> {
        unsafe {
            crate::OwnedPtr::from_raw(crate::check_result(
                crate::ffi_extern_TKBRep::BRepAdaptor_Surface_value(self as *const Self, U, V),
            ))
        }
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:168 - `BRepAdaptor_Surface::D0()`
    /// Computes the point of parameters U,V on the surface.
    pub fn d0(&self, U: f64, V: f64, P: &mut crate::gp::Pnt) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_d0(self as *const Self, U, V, P)
        })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:176 - `BRepAdaptor_Surface::D1()`
    /// Computes the point  and the first derivatives on the surface.
    /// Raised if the continuity of the current intervals is not C1.
    ///
    /// Tip: use GeomLib::NormEstim() to calculate surface normal at specified (U, V) point.
    pub fn d1(
        &self,
        U: f64,
        V: f64,
        P: &mut crate::gp::Pnt,
        D1U: &mut crate::gp::Vec,
        D1V: &mut crate::gp::Vec,
    ) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_d1(self as *const Self, U, V, P, D1U, D1V)
        })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:186 - `BRepAdaptor_Surface::D2()`
    /// Computes   the point,  the  first  and  second
    /// derivatives on the surface.
    /// Raised  if   the   continuity   of the current
    /// intervals is not C2.
    pub fn d2(
        &self,
        U: f
... (truncated, 7137 chars total)
```
</details>

<details>
<summary>Tool output (run_in_terminal), lines 1-100</summary>

```
Note: The tool simplified the command to ` sed -n '2560,2750p' ../opencascade-rs/crates/opencascade-sys/generated/b_rep_adaptor.rs`, and this is the output of running that command instead:
        })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:154 - `BRepAdaptor_Surface::IsUPer
iodic()`                                                                            pub fn is_u_periodic(&self) -> bool {
        crate::check_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_is_u_periodic(self as 
*const Self)                                                                            })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:156 - `BRepAdaptor_Surface::UPerio
d()`                                                                                pub fn u_period(&self) -> f64 {
        crate::check_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_u_period(self as *cons
t Self)                                                                                 })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:158 - `BRepAdaptor_Surface::IsVPer
iodic()`                                                                            pub fn is_v_periodic(&self) -> bool {
        crate::check_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_is_v_periodic(self as 
*const Self)                                                                            })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:160 - `BRepAdaptor_Surface::VPerio
d()`                                                                                pub fn v_period(&self) -> f64 {
        crate::check_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_v_period(self as *cons
t Self)                                                                                 })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:164 - `BRepAdaptor_Surface::Value(
)`                                                                                  /// Computes the point of parameters U,V on the surface.
    /// Tip: use GeomLib::NormEstim() to calculate surface normal at specified (
U, V) point.                                                                        pub fn value(&self, U: f64, V: f64) -> crate::OwnedPtr<crate::gp::Pnt> {
        unsafe {
            crate::OwnedPtr::from_raw(crate::check_result(
                crate::ffi_extern_TKBRep::BRepAdaptor_Surface_value(self as *con
st Self, U, V),                                                                             ))
        }
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:168 - `BRepAdaptor_Surface::D0()`
    /// Computes the point of parameters U,V on the surface.
    pub fn d0(&self, U: f64, V: f64, P: &mut crate::gp::Pnt) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_d0(self as *const Self
, U, V, P)                                                                              })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:176 - `BRepAdaptor_Surface::D1()`
    /// Computes the point  and the first derivatives on the surface.
    /// Raised if the continuity of the current intervals is not C1.
    ///
    /// Tip: use GeomLib::NormEstim() to calculate surface normal at specified (
U, V) point.                                                                        pub fn d1(
        &self,
        U: f64,
        V: f64,
        P: &mut crate::gp::Pnt,
        D1U: &mut crate::gp::Vec,
        D1V: &mut crate::gp::Vec,
    ) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKBRep::BRepAdaptor_Surface_d1(self as *const Self
, U, V, P, D1U, D1V)                                                                    })
    }

    /// **Source:** `BRepAdaptor_Surface.hxx`:186 - `BRepAdaptor_Surface::D2()`
    /// Computes   the point,  the  first  and  second
    /// derivatives on the surface.
    /// Raised  if   the 
... (truncated, 4667 chars)
```
</details>

I can use  to compute the first derivatives, then cross product D1U × D1V to get the surface normal. But I also need `GeomAPI_ProjectPointOnSurf` to find the UV parameters of my point on the STEP face's surface. Actually, a simpler approach is to use `BRepExtrema_DistShapeShape` to find the closest point on each STEP face, which gives me the point on the nearest face, then I can project onto that face to get UV parameters. 

Actually an even simpler approach: for each of our reconstructed faces, I already know the surface normal from . I need to find the corresponding STEP face and compare normals. The approach:

1. For each of our faces, sample a mesh face centroid
2. Find the closest STEP face using `BRepExtrema_DistShapeShape` with each STEP face individually 
3. For that STEP face, use `GeomAPI_ProjectPointOnSurf` to get UV parameters
4. Use  to get derivatives, cross product for normal
5. Account for face orientation (REVERSED flag flips the normal)
6. Compare with our deduced normal

Let me check if ProjectPointOnSurf is available and also look at how the test code does it:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "ProjectPointOnSurf\|project_point_on_surf" ../opencascade-rs/crates/opencascade-sys/generated/geom_api.rs 2>/dev/null | head -20
```
<details>
<summary>Output (20 lines)</summary>

```
2360:// From GeomAPI_ProjectPointOnSurf.hxx
2363:/// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:34 - `GeomAPI_ProjectPointOnSurf`
2366:pub use crate::ffi_types::GeomAPI_ProjectPointOnSurf as ProjectPointOnSurf;
2368:unsafe impl crate::CppDeletable for ProjectPointOnSurf {
2370:        crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_destructor(ptr);
2374:impl ProjectPointOnSurf {
2375:    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:41 - `GeomAPI_ProjectPointOnSurf::GeomAPI_ProjectPointOnSurf()`
2381:                crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_ctor(),
2386:    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:45 - `GeomAPI_ProjectPointOnSurf::GeomAPI_ProjectPointOnSurf()`
2395:            crate::OwnedPtr::from_raw(crate::check_result(crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_ctor_pnt_handlegeomsurface_extalgo(P, Surface, Algo.into())))
2399:    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:54 - `GeomAPI_ProjectPointOnSurf::GeomAPI_ProjectPointOnSurf()`
2412:            crate::OwnedPtr::from_raw(crate::check_result(crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_ctor_pnt_handlegeomsurface_real_extalgo(P, Surface, Tolerance, Algo.into())))
2416:    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:59 - `GeomAPI_ProjectPointOnSurf::GeomAPI_ProjectPointOnSurf()`
2428:            crate::OwnedPtr::from_raw(crate::check_result(crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_ctor_pnt_handlegeomsurface_real5_extalgo(P, Surface, Umin, Usup, Vmin, Vsup, Tolerance, Algo.into())))
2432:    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:70 - `GeomAPI_ProjectPointOnSurf::GeomAPI_ProjectPointOnSurf()`
2445:            crate::OwnedPtr::from_raw(crate::check_result(crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_ctor_pnt_handlegeomsurface_real4_extalgo(P, Surface, Umin, Usup, Vmin, Vsup, Algo.into())))
2449:    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:78 - `GeomAPI_ProjectPointOnSurf::Init()`
2458:            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_init_pnt_handlegeomsurface_real_extalgo(self as *mut Self, P, Surface, Tolerance, Algo.into())
2462:    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:86 - `GeomAPI_ProjectPointOnSurf::Init()`
2473:            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_init_pnt_handlegeomsurface_extalgo(self as *mut Self, P, Surface, Algo.into())
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "pub fn" ../opencascade-rs/crates/opencascade-sys/generated/geom_api.rs 2>/dev/null | grep -i "project_point_on_surf\|lower_distance\|parameter\|nb_points\|is_done" | head -20
```
<details>
<summary>Output (20 lines)</summary>

```
222:    pub fn parameters(&self, Index: i32, U1: &mut f64, U2: &mut f64) {
277:    pub fn lower_distance_parameters(&self, U1: &mut f64, U2: &mut f64) {
291:    pub fn lower_distance(&self) -> f64 {
333:    pub fn total_lower_distance_parameters(&mut self, U1: &mut f64, U2: &mut f64) -> bool {
347:    pub fn total_lower_distance(&mut self) -> f64 {
519:    pub fn parameters(&self, Index: i32, W: &mut f64, U: &mut f64, V: &mut f64) {
576:    pub fn lower_distance_parameters(&self, W: &mut f64, U: &mut f64, V: &mut f64) {
591:    pub fn lower_distance(&self) -> f64 {
789:    pub fn parameters(&self, Index: i32, U1: &mut f64, V1: &mut f64, U2: &mut f64, V2: &mut f64) {
849:    pub fn lower_distance_parameters(
871:    pub fn lower_distance(&self) -> f64 {
955:    pub fn is_done(&self) -> bool {
965:    pub fn nb_points(&self) -> i32 {
993:    pub fn parameters_int_real3(&self, Index: i32, U: &mut f64, V: &mut f64, W: &mut f64) {
1040:    pub fn parameters_int_real4(
1132:    pub fn is_done(&self) -> bool {
1370:    pub fn is_done(&self) -> bool {
1675:    pub fn is_done(&self) -> bool {
2102:    pub fn is_done(&self) -> bool {
2237:    pub fn nb_points(&self) -> i32 {
```
</details>

Let me check the `ProjectPointOnSurf` methods more precisely:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && sed -n '2470,2550p' ../opencascade-rs/crates/opencascade-sys/generated/geom_api.rs
```
<details>
<summary>Output (81 lines)</summary>

```
        Algo: crate::extrema::ExtAlgo,
    ) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_init_pnt_handlegeomsurface_extalgo(self as *mut Self, P, Surface, Algo.into())
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:90 - `GeomAPI_ProjectPointOnSurf::Init()`
    pub fn init_pnt_handlegeomsurface_real5_extalgo(
        &mut self,
        P: &crate::gp::Pnt,
        Surface: &crate::ffi_types::HandleGeomSurface,
        Umin: f64,
        Usup: f64,
        Vmin: f64,
        Vsup: f64,
        Tolerance: f64,
        Algo: crate::extrema::ExtAlgo,
    ) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_init_pnt_handlegeomsurface_real5_extalgo(self as *mut Self, P, Surface, Umin, Usup, Vmin, Vsup, Tolerance, Algo.into())
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:102 - `GeomAPI_ProjectPointOnSurf::Init()`
    /// Init the projection for many points on a surface
    /// <Surface>. The solutions will be computed in the domain
    /// [Umin,Usup] [Vmin,Vsup] of the surface.
    pub fn init_pnt_handlegeomsurface_real4_extalgo(
        &mut self,
        P: &crate::gp::Pnt,
        Surface: &crate::ffi_types::HandleGeomSurface,
        Umin: f64,
        Usup: f64,
        Vmin: f64,
        Vsup: f64,
        Algo: crate::extrema::ExtAlgo,
    ) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_init_pnt_handlegeomsurface_real4_extalgo(self as *mut Self, P, Surface, Umin, Usup, Vmin, Vsup, Algo.into())
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:110 - `GeomAPI_ProjectPointOnSurf::Init()`
    pub fn init_handlegeomsurface_real5_extalgo(
        &mut self,
        Surface: &crate::ffi_types::HandleGeomSurface,
        Umin: f64,
        Usup: f64,
        Vmin: f64,
        Vsup: f64,
        Tolerance: f64,
        Algo: crate::extrema::ExtAlgo,
    ) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_init_handlegeomsurface_real5_extalgo(self as *mut Self, Surface, Umin, Usup, Vmin, Vsup, Tolerance, Algo.into())
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:118 - `GeomAPI_ProjectPointOnSurf::Init()`
    pub fn init_handlegeomsurface_real4_extalgo(
        &mut self,
        Surface: &crate::ffi_types::HandleGeomSurface,
        Umin: f64,
        Usup: f64,
        Vmin: f64,
        Vsup: f64,
        Algo: crate::extrema::ExtAlgo,
    ) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_init_handlegeomsurface_real4_extalgo(self as *mut Self, Surface, Umin, Usup, Vmin, Vsup, Algo.into())
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:127 - `Geo
... (truncated, 3417 chars total)
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && sed -n '2550,2640p' ../opencascade-rs/crates/opencascade-sys/generated/geom_api.rs
```
<details>
<summary>Output (91 lines)</summary>

```
                self as *mut Self,
                theAlgo.into(),
            )
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:131 - `GeomAPI_ProjectPointOnSurf::SetExtremaFlag()`
    /// Sets the Extrema search flag - MIN or MAX or MINMAX.<br>
    /// By default the Extrema is set to search the MinMax solutions.
    pub fn set_extrema_flag(&mut self, theExtFlag: i32) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_set_extrema_flag(
                self as *mut Self,
                theExtFlag,
            )
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:134 - `GeomAPI_ProjectPointOnSurf::Perform()`
    /// Performs the projection of a point on the current surface.
    pub fn perform(&mut self, P: &crate::gp::Pnt) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_perform(self as *mut Self, P)
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:136 - `GeomAPI_ProjectPointOnSurf::IsDone()`
    pub fn is_done(&self) -> bool {
        crate::check_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_is_done(self as *const Self)
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:140 - `GeomAPI_ProjectPointOnSurf::NbPoints()`
    /// Returns the number of computed orthogonal projection points.
    /// Note: if projection fails, NbPoints returns 0.
    pub fn nb_points(&self) -> i32 {
        crate::check_result(unsafe {
            crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_nb_points(self as *const Self)
        })
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:148 - `GeomAPI_ProjectPointOnSurf::Point()`
    /// Returns the orthogonal projection
    /// on the surface. Index is a number of a computed point.
    /// Exceptions
    /// Standard_OutOfRange if Index is not in the range [ 1,NbPoints ], where
    /// NbPoints is the number of solution points.
    pub fn point(&self, Index: i32) -> crate::OwnedPtr<crate::gp::Pnt> {
        unsafe {
            crate::OwnedPtr::from_raw(crate::check_result(
                crate::ffi_extern_TKGeomAlgo::GeomAPI_ProjectPointOnSurf_point(
                    self as *const Self,
                    Index,
                ),
            ))
        }
    }

    /// **Source:** `GeomAPI_ProjectPointOnSurf.hxx`:156 - `GeomAPI_ProjectPointOnSurf::Parameters()`
    /// Returns the parameters (U,V) on the
    /// surface of the orthogonal projection. Index is a number of a
    /// computed point.
    /// Exceptions
    /// Standard_OutOfRange if Index is not in the range [ 1,NbPoints ], where
    /// NbPoints is the number of solution points.
    pub fn parameters(&self, Index: i32, U: &mut f64, V: &mut f64) {
        crate::check_void_result(unsafe {
            crate::ffi_extern_TK
... (truncated, 3847 chars total)
```
</details>

Excellent! Now I have all the tools I need. Let me also check how face orientation is retrieved - looking at the `orientation()` method and `TopAbs_Orientation`:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "fn orientation\|FORWARD\|REVERSED\|Forward\|Reversed" ../opencascade-rs/crates/opencascade-sys/generated/top_abs.rs 2>/dev/null | head -20
```
<details>
<summary>Output (17 lines)</summary>

```
17:/// \ Or2     FORWARD  REVERSED INTERNAL EXTERNAL
19:/// FORWARD   | FORWARD  REVERSED INTERNAL EXTERNAL
21:/// REVERSED  | REVERSED FORWARD  INTERNAL EXTERNAL
42:/// FORWARD          REVERSED
43:/// REVERSED         FORWARD
61:/// FORWARD          REVERSED
62:/// REVERSED         FORWARD
142:/// @return string identifier from the list FORWARD, REVERSED, INTERNAL, EXTERNAL
158:/// @return shape orientation or TopAbs_FORWARD if string identifier is invalid
175:/// which is oriented FORWARD (say from left to right)
176:/// you can have both a FORWARD and a REVERSED
177:/// edge. The FORWARD edge will be oriented from
178:/// left to right, and the REVERSED edge from right to
193:    Forward = 0,
194:    Reversed = 1,
210:            0 => Ok(Orientation::Forward),
211:            1 => Ok(Orientation::Reversed),
```
</details>

Now I have a clear picture. Let me also check how `b_rep::Tool::surface_face` works (to get the surface from a STEP face):

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "surface_face\|surface.*face" ../opencascade-rs/crates/opencascade-sys/generated/b_rep.rs 2>/dev/null | head -10
```
<details>
<summary>Output (10 lines)</summary>

```
1259:    pub fn surface(&self) -> &crate::ffi_types::HandleGeomSurface {
1344:    pub fn surface2(&self) -> &crate::ffi_types::HandleGeomSurface {
1559:    pub fn surface(&self) -> &crate::ffi_types::HandleGeomSurface {
1568:    pub fn surface2(&self) -> &crate::ffi_types::HandleGeomSurface {
2096:    pub fn surface2(&self) -> &crate::ffi_types::HandleGeomSurface {
2193:    pub fn as_curve_on_surface(&self) -> &CurveOnSurface {
2204:    pub fn as_curve_on_surface_mut(&mut self) -> &mut CurveOnSurface {
2334:    pub fn surface(&self) -> &crate::ffi_types::HandleGeomSurface {
2731:    pub fn is_curve_on_surface_handlegeomsurface_location(
2737:            crate::ffi_extern_TKBRep::BRep_CurveOnSurface_is_curve_on_surface_handlegeomsurface_location(self as *const Self, S, L)
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "pub fn surface" ../opencascade-rs/crates/opencascade-sys/generated/b_rep.rs 2>/dev/null | head -10
```
<details>
<summary>Output (10 lines)</summary>

```
1259:    pub fn surface(&self) -> &crate::ffi_types::HandleGeomSurface {
1344:    pub fn surface2(&self) -> &crate::ffi_types::HandleGeomSurface {
1559:    pub fn surface(&self) -> &crate::ffi_types::HandleGeomSurface {
1568:    pub fn surface2(&self) -> &crate::ffi_types::HandleGeomSurface {
2096:    pub fn surface2(&self) -> &crate::ffi_types::HandleGeomSurface {
2334:    pub fn surface(&self) -> &crate::ffi_types::HandleGeomSurface {
2742:    pub fn surface(&self) -> &crate::ffi_types::HandleGeomSurface {
3094:    pub fn surface2(&self) -> &crate::ffi_types::HandleGeomSurface {
3474:    pub fn surface(&self) -> &crate::ffi_types::HandleGeomSurface {
3631:    pub fn surface2(&self) -> &crate::ffi_types::HandleGeomSurface {
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && grep -n "pub fn surface_face" ../opencascade-rs/crates/opencascade-sys/generated/b_rep.rs 2>/dev/null
```
```
10233:    pub fn surface_face_location(
10245:    pub fn surface_face(
```

Read [b_rep.rs](/Users/sethml/src/opencascade-rs/crates/opencascade-sys/generated/b_rep.rs)

Now I have everything I need. My approach for the surface orientation comparison:

1. For each of our reconstructed faces, get a sample point (mesh face centroid)
2. Compute our surface normal at that point using  
3. Find the closest STEP face by iterating STEP faces and using `BRepExtrema_DistShapeShape`
4. Get the surface from that STEP face via 
5. Project the sample point onto the surface via `GeomAPI_ProjectPointOnSurf` to get UV
6. Evaluate the STEP surface derivatives at UV via  and cross product D1U × D1V
7. Account for face orientation (if `Reversed`, negate the normal)
8. Compare the two normals (dot product should be positive — same direction)

Let me now implement this. Let me first look at the existing `compare_faces_to_step` function location and the area where I'll add the new function:

<details>
<summary>Reading lines 130-170 of <a href="../src/stage3.rs#L130-L171">stage3.rs</a></summary>

```
}

// ---------------------------------------------------------------------------
// Errors
// ---------------------------------------------------------------------------

#[derive(Debug)]
pub enum Stage3Error {
    NotImplemented(String),
    AdjacencyError(String),
    Compare(Stage3CompareError),
}

impl Display for Stage3Error {
    fn fmt(&self, f: &mut Formatter<'_>) -> std::fmt::Result {
        match self {
            Stage3Error::NotImplemented(stage) => write!(f, "stage {stage} not yet implemented"),
            Stage3Error::AdjacencyError(msg) => write!(f, "stage 3.1 adjacency error: {msg}"),
            Stage3Error::Compare(e) => write!(f, "stage 3.{} compare: {e}", e.substage),
        }
    }
}

impl Error for Stage3Error {}

impl From<Stage3CompareError> for Stage3Error {
    fn from(e: Stage3CompareError) -> Self {
        Stage3Error::Compare(e)
    }
}

#[derive(Debug)]
pub struct Stage3CompareError {
    pub substage: u8,
    pub check_type: &'static str,
    pub element_index: usize,
    pub max_distance: f64,
    pub tolerance: f64,
}

impl Display for Stage3CompareError {
```
</details>

Now let me also check `compute_mesh_face_centroid` since we use it:

Searched for text `fn compute_mesh_face_centroid` (`**/src/stage3.rs`), 1 result
- [stage3.rs:1336](../src/stage3.rs#L1336)

<details>
<summary>Reading lines 1336-1370 of <a href="../src/stage3.rs#L1336-L1371">stage3.rs</a></summary>

```
fn compute_mesh_face_centroid(face_idx: usize, mesh: &ConnectedMesh) -> [f64; 3] {
    let face = &mesh.faces[face_idx];
    let vc = face.vertex_count as usize;
    let mut cx = 0.0;
    let mut cy = 0.0;
    let mut cz = 0.0;
    for i in 0..vc {
        let v = &mesh.vertices[face.vertex_indices[i]];
        cx += v.x;
        cy += v.y;
        cz += v.z;
    }
    let n = vc as f64;
    [cx / n, cy / n, cz / n]
}

/// Group a face's edges into separate wire loops based on vertex connectivity.
///
/// Edges sharing BRepVertices are grouped together. Closed-loop edges
/// (both vertex_indices == usize::MAX) each form their own wire.
fn group_edges_into_wires(fd: &FaceDescriptor, edges: &[ReconEdge]) -> Vec<Vec<usize>> {
    if fd.edge_indices.is_empty() {
        return Vec::new();
    }

    let mut groups: Vec<Vec<usize>> = Vec::new();
    // Map from BRepVertex index to which group it belongs to
    let mut vertex_group: HashMap<usize, usize> = HashMap::new();

    for &ei in &fd.edge_indices {
        let edge = &edges[ei];
        let [v0, v1] = edge.vertex_indices;

        if v0 == usize::MAX && v1 == usize::MAX {
            // Closed loop — its own wire
```
</details>

Now I have all the context I need. Let me also check the imports needed — I'll need `b_rep_adaptor` and `b_rep`:

<details>
<summary>Reading lines 14-21 of <a href="../src/stage3.rs#L14-L22">stage3.rs</a></summary>

```
use opencascade_sys::{
    b_rep_builder_api, b_rep_check, b_rep_extrema, b_rep_g_prop, extrema,
    geom, geom_api, g_prop, gp, message, shape_analysis, shape_fix,
    top_abs, top_exp, topo_ds, OwnedPtr,
};
use std::collections::{BTreeSet, HashMap, HashSet};
use std::error::Error;
use std::fmt::{Display, Formatter};
```
</details>

I need to add `b_rep, b_rep_adaptor` to the imports. Now let me implement the orientation comparison function. The plan:

1. Add `b_rep` and `b_rep_adaptor` to imports
2. Add a new `compare_surface_orientations_to_step` function after `compare_faces_to_step` 
3. Call it from the stage 3.4 compare section

Let me look at where `compare_faces_to_step` is called:

<details>
<summary>Reading lines 1800-1850 of <a href="../src/stage3.rs#L1800-L1851">stage3.rs</a></summary>

```
            surface_type,
            SelectedSurface::Cylindrical(_) | SelectedSurface::Spherical(_)
        );

        let (make_face, is_concave) = if is_periodic {
            create_periodic_face(fi, &output, &mut topo_edges, config)?
        } else {
            (create_planar_face(fi, &output, &mut topo_edges, config)?, false)
        };
        *concave_flag = is_concave;

        if config.verbose {
            let stype = match surface_type {
                SelectedSurface::Planar(_) => "planar",
                SelectedSurface::Cylindrical(_) => "cylindrical",
                SelectedSurface::Spherical(_) => "spherical",
            };
            eprintln!(
                "  Face {fi}: {stype} — created successfully ({} edges)",
                output.face_descriptors[fi].edge_indices.len(),
            );
        }

        make_faces.push(make_face);
    }

    if !config.quiet {
        eprintln!(
            "Stage 3.4: Created {}/{} OCCT faces",
            make_faces.len(),
            num_faces,
        );
    }

    output.make_faces = make_faces;
    output.concave_faces = concave_faces;

    // 3. Validate faces
    validate_faces(&output, config)?;

    // 4. Compare against reference STEP if --compare
    if config.compare_shape.is_some() {
        compare_faces_to_step(&output, config)?;
    }

    Ok(output)
}


// ---------------------------------------------------------------------------
// Stage 3.5: Construct shells
```
</details>

Now I have the full picture. Let me implement the orientation comparison. I'll add it after `compare_faces_to_step` and call it from the same place:   Now let me implement the changes. I need to:
1. Add imports for `b_rep` and `b_rep_adaptor`
2. Add the `compare_surface_orientations_to_step` function
3. Call it from the same place as `compare_faces_to_step`

<details>
<summary>Edited: <a href="../src/stage3.rs#L14-L19">stage3.rs -5+5</a></summary>

**Input:**
```
{
  "edits": [
    {
      "filePath": "src/stage3.rs",
      "lineHashes": "14:zw,15:cs,16:mw,17:li,18:vv",
      "content": "use opencascade_sys::{\n    b_rep, b_rep_adaptor, b_rep_builder_api, b_rep_check, b_rep_extrema, b_rep_g_prop, extrema,\n    geom, geom_api, g_prop, gp, message, shape_analysis, shape_fix,\n    top_abs, top_exp, topo_ds, OwnedPtr,\n};"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"src/stage3.rs","startLine":14,"endLine":18}
```
</details>

Now let me add the `compare_surface_orientations_to_step` function after `compare_faces_to_step`. Let me re-read the line hashes:

<details>
<summary>Reading lines 1740-1750 of <a href="../src/stage3.rs#L1740-L1751">stage3.rs</a></summary>

```
            tolerance: config.surface_tolerance_mm,
        });
    }

    Ok(())
}

/// Create OCCT faces for all face descriptors (stage 3.4 main entry).
fn create_occt_faces_all(
    mut output: Stage3Output,
    config: &Config,
```
</details>

<details>
<summary>Edited: <a href="../src/stage3.rs#L1744-L1944">stage3.rs -2+200</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "    Ok(())\n}\n\n/// Compare surface orientations of our deduced surfaces against reference STEP faces.\n///\n/// For each of our reconstructed faces, we:\n/// 1. Sample a mesh face centroid as a representative point\n/// 2. Compute our deduced surface normal at that point\n/// 3. Find the closest STEP face by distance\n/// 4. Evaluate the STEP surface normal at the nearest point (using D1 derivatives)\n/// 5. Account for STEP face orientation (REVERSED flips normal)\n/// 6. Compare the two normals — they should point in the same direction\nfn compare_surface_orientations_to_step(\n    output: &Stage3Output,\n    config: &Config,\n) {\n    let compare_shape = config.compare_shape.as_ref().unwrap();\n\n    // Collect all STEP faces\n    let mut step_faces: Vec<OwnedPtr<topo_ds::Face>> = Vec::new();\n    let mut explorer = top_exp::Explorer::new_shape_shapeenum2(\n        compare_shape,\n        top_abs::ShapeEnum::Face,\n        top_abs::ShapeEnum::Shape,\n    );\n    while explorer.more() {\n        step_faces.push(topo_ds::face_shape(explorer.value()).to_owned());\n        explorer.next();\n    }\n\n    let mut mismatch_count = 0;\n    let mut checked_count = 0;\n\n    for (fi, fd) in output.face_descriptors.iter().enumerate() {\n        let surface = &output.stage2.selected_surfaces[fd.selected_surface_idx];\n        let faces = surface_faces(surface, &output.stage2);\n        if faces.is_empty() {\n            continue;\n        }\n\n        // Use a centroid from one of the mesh faces as sample point\n        let centroid = compute_mesh_face_centroid(faces[0], &output.stage2.mesh);\n        let pt = gp::Pnt::new_real3(centroid[0], centroid[1], centroid[2]);\n\n        // Compute our deduced surface normal at this point\n        let our_normal = match surface_normal_at_point(surface, &output.stage2, &centroid) {\n            Some(n) => n,\n            None => continue, // degenerate point (on axis or center)\n        };\n\n        // Also get the mesh face normal for comparison\n        let mesh_normal = output.stage2.mesh.faces[faces[0]].normal;\n\n        // Find the closest STEP face\n        let vertex_shape = b_rep_builder_api::MakeVertex::new_pnt(&pt);\n        let mut best_face_idx = None;\n        let mut best_dist = f64::MAX;\n        for (si, step_face) in step_faces.iter().enumerate() {\n            let progress = message::ProgressRange::new();\n            let dist_calc = b_rep_extrema::DistShapeShape::new_shape2_extflag_extalgo_progressrange(\n                vertex_shape.shape(),\n                step_face.as_shape(),\n                0,\n                extrema::ExtAlgo::Grad,\n                &progress,\n            );\n            if dist_calc.is_done() && dist_calc.nb_solution() > 0 {\n                let d = dist_calc.value();\n                if d < best_dist {\n                    best_dist = d;\n                    best_face_idx = Some(si);\n                }\n            }\n        
... (truncated, 7940 chars)
```
**Output:**
```
{"status":"ok","applied":1,"file":"src/stage3.rs","startLine":1744,"endLine":1943}
```
</details>

Now I need to call this new function from the compare section in `create_occt_faces_all`. Let me re-read that section:

<details>
<summary>Reading lines 2030-2050 of <a href="../src/stage3.rs#L2030-L2051">stage3.rs</a></summary>

```
    }

    output.make_faces = make_faces;
    output.concave_faces = concave_faces;

    // 3. Validate faces
    validate_faces(&output, config)?;

    // 4. Compare against reference STEP if --compare
    if config.compare_shape.is_some() {
        compare_faces_to_step(&output, config)?;
    }

    Ok(output)
}


// ---------------------------------------------------------------------------
// Stage 3.5: Construct shells
// ---------------------------------------------------------------------------

```
</details>

<details>
<summary>Edited: <a href="../src/stage3.rs#L2038-L2043">stage3.rs -4+5</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "    // 4. Compare against reference STEP if --compare\n    if config.compare_shape.is_some() {\n        compare_faces_to_step(&output, config)?;\n        compare_surface_orientations_to_step(&output, config);\n    }",
      "filePath": "src/stage3.rs",
      "lineHashes": "2038:cy,2039:gi,2040:gg,2041:ax"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"src/stage3.rs","startLine":2038,"endLine":2042}
```
</details>

Now let's build and test:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo build 2>&1
```
<details>
<summary>Output (16 lines) — exit code 101</summary>

```
    Blocking waiting for file lock on build directory
   Compiling brepper v0.1.0 (/Users/sethml/src/brepper)
error[E0596]: cannot borrow `vertex_shape` as mutable, as it is not declared as mutable
    --> src/stage3.rs:1804:17
     |
1804 |                 vertex_shape.shape(),
     |                 ^^^^^^^^^^^^ cannot borrow as mutable
     |
help: consider changing this to be mutable
     |
1798 |         let mut vertex_shape = b_rep_builder_api::MakeVertex::new_pnt(&pt);
     |             +++

For more information about this error, try `rustc --explain E0596`.
error: could not compile `brepper` (lib) due to 1 previous error
real  13.6s
```
</details>

<details>
<summary>Reading lines 1795-1800 of <a href="../src/stage3.rs#L1795-L1801">stage3.rs</a></summary>

```
        let mesh_normal = output.stage2.mesh.faces[faces[0]].normal;

        // Find the closest STEP face
        let vertex_shape = b_rep_builder_api::MakeVertex::new_pnt(&pt);
        let mut best_face_idx = None;
        let mut best_dist = f64::MAX;
```
</details>

<details>
<summary>Edited: <a href="../src/stage3.rs#L1798-L1799">stage3.rs -1+1</a></summary>

**Input:**
```
{
  "edits": [
    {
      "content": "        let mut vertex_shape = b_rep_builder_api::MakeVertex::new_pnt(&pt);",
      "filePath": "src/stage3.rs",
      "lineHashes": "1798:dw"
    }
  ]
}
```
**Output:**
```
{"status":"ok","applied":1,"file":"src/stage3.rs","startLine":1798,"endLine":1798}
```
</details>

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo build 2>&1
```
```
    Blocking waiting for file lock on build directory
   Compiling brepper v0.1.0 (/Users/sethml/src/brepper)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 18.33s
real  18.4s
```

Let's run clippy:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo clippy 2>&1
```

Zero clippy warnings. Now let's run the tests:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && time cargo test 2>&1
```

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo test 2>&1 | tail -20
```

All 289 tests pass (I can see 82 from the last test suite — let me check total):

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo test 2>&1 | grep "^test result:"
```

All 289 tests pass (17+59+131+82). Now let me try it live to see the orientation output:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run -- --stage 3.4 --compare tests/ccad/generated/block_with_hole.step tests/ccad/generated/block_with_hole.stl 2>&1 | grep -i "orient\|Compare 3.4"
```
```
(base) whitelily:brepper sethml$
```
**Exit code:** 1

Hmm, the output is empty. Maybe the message is going to stdout not stderr, or the grep isn't matching. Let me try without the grep:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run -- --stage 3.4 --compare tests/ccad/generated/block_with_hole.step tests/ccad/generated/block_with_hole.stl 2>&1
```
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: brepper, step-measure-tolerance, viewer
```
**Exit code:** 101

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --bin brepper -- --stage 3.4 --compare tests/ccad/generated/block_with_hole.step tests/ccad/generated/block_with_hole.stl 2>&1
```
<details>
<summary>Output (27 lines)</summary>

```
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.16s
     Running `target/debug/brepper --stage 3.4 --compare tests/ccad/generated/block_with_hole.step tests/ccad/generated/block_with_hole.stl`
Loaded 7 faces from comparison STEP file
Stage 1.1: Read 66 vertices, 132 faces from STL
Stage 1.2: Mesh validation passed
  132 faces, 66 vertices, 1 shells, 1 solids
Stage 1.3: Fused 41 triangle pairs into quads (91 faces remaining)
  Compare: vertex max dist 4.900509e-7 mm, centroid max dist 3.517257e-2 mm
Stage 2.1: Deduced 35 planar hypotheses (2 multi-face, 33 single-face) covering 91 of 91 mesh faces
  Compare: all planar hypothesis centroids within tolerance
Stage 2.2: Deduced 1 cylindrical hypotheses (0 convex, 1 concave) covering 29 faces
  Compare: all cylindrical hypothesis centroids within tolerance
Stage 2.3: Deduced 0 spherical hypotheses (0 convex, 0 concave) covering 0 faces
  Compare: all spherical hypothesis centroids within tolerance
Stage 2.4: Deduce ruled surface hypotheses (not yet implemented)
Stage 2.5: Deduce NURBS hypotheses (not yet implemented)
Stage 2.6: Selected 7 surfaces (6 planar, 1 cylindrical, 0 spherical) covering 91 faces
  Compare: all selected surface centroids within tolerance
Stage 3.1: Created 7 OCCT surfaces, 14 edges, 8 vertices
  Compare 3.1: edge boundary max dist 4.900509e-7 mm, vertex max dist 0.000000e0 mm
Stage 3.2: Tangency detection: 0 of 14 edges are tangent
Stage 3.3: Computed 14/14 edge curves (0 failed)
Stage 3.4: Created 7/7 OCCT faces
  Warning: 2/7 faces failed BRepCheck validation
  Compare 3.4: 7 faces created, 7 faces in STEP reference
  Compare 3.4: max face sample distance to STEP: 3.517240e-2 mm
  Compare 3.4 orientation: 7 faces checked, 0 orientation mismatch(es)
```
</details>

All 7 faces match orientation. Let me try a more complex model:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --bin brepper -- --stage 3.4 --compare tests/ccad/generated/spherical_pocket.step tests/ccad/generated/spherical_pocket.stl 2>&1 | grep -i "orient\|Compare 3.4"
```
```
  Compare 3.4: 7 faces created, 7 faces in STEP reference
  Compare 3.4: max face sample distance to STEP: 6.542896e-2 mm
  Compare 3.4 orientation: 7 faces checked, 0 orientation mismatch(es)
```

Let me test on several more models to make sure we're properly detecting mismatches:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && for f in tests/ccad/generated/*.stl tests/manual/*.stl tests/onshape/*.stl; do step="${f%.stl}.step"; if [ -f "$step" ]; then echo "=== $(basename $f) ==="; cargo run --bin brepper -- --stage 3.4 --compare "$step" "$f" 2>&1 | grep "orientation:"; fi; done
```

Interesting! The `tests/manual/cube.stl` has ALL 6 faces with orientation mismatches (dot=-1.0000). This means the manual cube STL has normals pointing inward (or the STEP file has reversed normals relative to the STL). Note that both mesh_dot and deduced dot are -1.0, so the mesh normals agree with the deduced normals — the issue is consistent direction disagreement compared to STEP.

Let me investigate the manual cube briefly to understand whether this is expected:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && cargo run --bin brepper -- --stage 3.4 --compare tests/manual/cube.step tests/manual/cube.stl -v 2>&1 | grep -i "orient"
```
<details>
<summary>Output (13 lines)</summary>

```
  Face 0: area = 1.0000 mm², orientation = Forward
  Face 1: area = 1.0000 mm², orientation = Forward
  Face 2: area = 1.0000 mm², orientation = Forward
  Face 3: area = 1.0000 mm², orientation = Forward
  Face 4: area = 1.0000 mm², orientation = Forward
  Face 5: area = 1.0000 mm², orientation = Forward
  Compare 3.4 orientation: face 0 (planar): deduced normal DISAGREES with STEP (dot=-1.0000, mesh_dot=-1.0000, step_face=4 REVERSED, dist=0.00e0)
  Compare 3.4 orientation: face 1 (planar): deduced normal DISAGREES with STEP (dot=-1.0000, mesh_dot=-1.0000, step_face=5 FORWARD, dist=0.00e0)
  Compare 3.4 orientation: face 2 (planar): deduced normal DISAGREES with STEP (dot=-1.0000, mesh_dot=-1.0000, step_face=2 REVERSED, dist=0.00e0)
  Compare 3.4 orientation: face 3 (planar): deduced normal DISAGREES with STEP (dot=-1.0000, mesh_dot=-1.0000, step_face=1 FORWARD, dist=0.00e0)
  Compare 3.4 orientation: face 4 (planar): deduced normal DISAGREES with STEP (dot=-1.0000, mesh_dot=-1.0000, step_face=3 FORWARD, dist=0.00e0)
  Compare 3.4 orientation: face 5 (planar): deduced normal DISAGREES with STEP (dot=-1.0000, mesh_dot=-1.0000, step_face=0 REVERSED, dist=0.00e0)
  Compare 3.4 orientation: 6 faces checked, 6 orientation mismatch(es)
```
</details>

The manual cube STL has inverted normals compared to the STEP reference. This is actually valuable diagnostic info — the tool is working correctly! All 6 faces consistently have dot=-1.0, meaning the STL was created with normals pointing inward. The ccad-generated test models all pass because they have consistent outward normals.

Now let me update the todo and commit:

**Todo list:**
- ✅ Read LEARNINGS.md and DEVELOPMENT_PLAN.md
- ✅ Understand current compare mode code
- ✅ Understand surface orientation in stages
- ✅ Implement orientation comparison
- ✅ Test and fix clippy warnings
- 🔄 Commit changes

**Terminal:**
```sh
cd /Users/sethml/src/brepper && git add -A && git commit -F - <<'EOF'
Add surface orientation validation to --compare mode (stage 3.4)

New compare_surface_orientations_to_step() function in stage 3.4:
- For each reconstructed face, samples a mesh face centroid
- Computes our deduced surface normal at that point
- Finds the closest STEP face using BRepExtrema_DistShapeShape
- Projects the point onto the STEP face's surface to get UV parameters
- Evaluates STEP surface derivatives via BRepAdaptor_Surface::D1, cross
  product D1U × D1V for the surface normal
- Accounts for STEP face orientation (REVERSED flips normal)
- Compares deduced normal vs STEP normal (dot product should be positive)
- Also reports mesh face normal vs STEP for cross-validation

Reports mismatches with surface type, convexity, STEP face orientation,
and distance. Verbose mode shows the actual normal vectors.

Results on test suite: all ccad and onshape models show 0 mismatches.
The manual cube.stl shows 6/6 mismatches (dot=-1.0) indicating its STL
normals point inward relative to the STEP reference — this is expected
diagnostic output.

All 289 tests pass (17+59+131+82), zero clippy warnings.

---
Generated with assistance from Claude Opus 4 (Anthropic, 2025)

User prompt(s):
- "There have been quite a few confusing issues with face orientation errors
  leading to incorrect solid generation. Add to the --compare handling to
  validate stl face and deduced surface orientation to the step surface
  orientation, and let me know where they differ. Then commit."
EOF
```
```
[main 08aab68] Add surface orientation validation to --compare mode (stage 3.4)
 2 files changed, 202 insertions(+), 3 deletions(-)
```

Now export the chat log:

**Terminal:**
```sh
cd /Users/sethml/src/brepper && ( ../vscode-export-chat-log/export-chat-log.py --wait && git add agent-logs/ && git commit --amend --no-edit ) &
```
```
(base) whitelily:brepper sethml$ Extracting session from: 7ed284b2-30a3-4f9f-917
```

**Todo list:**
- ✅ Read LEARNINGS.md and DEVELOPMENT_PLAN.md
- ✅ Understand current compare mode code
- ✅ Understand surface orientation in stages
- ✅ Implement orientation comparison
- ✅ Test and fix clippy warnings
- ✅ Commit changes

Done. Here's what was added:

**New `compare_surface_orientations_to_step()` function** in stage 3.4 `--compare` mode that:

1. For each reconstructed face, samples a mesh face centroid
2. Computes the deduced surface normal using the existing  
3. Finds the closest STEP face via `BRepExtrema_DistShapeShape`
4. Gets UV parameters via `GeomAPI_ProjectPointOnSurf`, then evaluates  to compute the STEP surface normal (D1U × D1V cross product)
5. Accounts for STEP face orientation (`REVERSED` flips the normal)
6. Compares both the **deduced** surface normal and **mesh** face normal against the STEP normal

**Output format** for mismatches:
```
Compare 3.4 orientation: face 0 (planar): deduced normal DISAGREES with STEP 
  (dot=-1.0000, mesh_dot=-1.0000, step_face=4 REVERSED, dist=0.00e0)
```

**Test results**: All ccad/onshape models show 0 mismatches. The manual `cube.stl` shows 6/6 mismatches (dot=-1.0) — its STL normals consistently point inward relative to the STEP convention. Use `-v` for detailed normal vectors.

*2026-03-07 18:30 · 684s · Words: 4,734 in · 3,580 out · 86,619 ctx · 62 rounds*

---
